# Pendências conhecidas

> Divergências e dívidas **já registradas**. A skill `auditar-docs` lê este arquivo e **não reporta
> nada daqui como achado novo**. Toda linha tem gatilho — pendência sem prazo vira mentira permanente
> (lição 13). Revisado a cada `/fechar-sprint`.
>
> Isto NÃO é backlog de produto — item de código/feature vai para `docs/superpowers/backlog.md`.
> Aqui mora só o que faz um doc ou um mecanismo divergir da realidade.


| ID | Pendência | Por que está aberta | Gatilho de expiração |
|---|---|---|---|
| P-02 | ADR-08 (pruning/retenção da auditoria) segue **aberto** | Política de retenção nunca decidida; `audits` cresce sem poda | Antes de subir para produção |
| P-03 | Compose por worktree não existe | Bloco de backend não pode usar `using-git-worktrees` — o stack monta o main tree e o teste rodaria contra o código errado. **6a (Sprint 3) rodou em main-tree sem atrito — abordagem confirmada**. Gatilho anterior ("se a concorrência passar a doer") era não verificável e escapou do grep de prova do doc-sync 2026-07-30 por diferença de redação — trocado por condição observável na revisão do mesmo dia (Q-6) | Fecha na primeira sprint que precisar de **dois blocos de backend em paralelo** (condição verificável em `state.md`: mais de um `active_work_item` de backend), ou em **2026-10-31**, o que vier primeiro |
| P-04 | Leis invioláveis (§5) são instrução, não guardrail — **parcialmente resolvida em 2026-08-03** | O bloco `hardening-estrutural-pre-sprint-4` entregou a substância para duas frentes, por outro meio que o previsto: `backend/tests/Feature/Shared/DomainDependencyTest.php` (superfície pública + arestas declaradas entre domínios) em PHPUnit puro, **não** Pest Arch — Pest não está instalado e trocá-lo custaria o runner dos 75 arquivos da suíte; e `no-restricted-imports` nativo no `frontend/eslint.config.js` para a lei §5.6, **não** `eslint-boundaries` — são 3 fronteiras, não uma hierarquia. **Seguem sem mecanismo:** §5.1 (DDD-lite, sem Repository sobre Eloquent) e §5.2 (auditoria só na aplicação, nunca em trigger de banco) | Reavaliar em **2026-08-15** (gatilho inalterado), agora só para §5.1 e §5.2 |
| P-05 | Migrations "adicionais" não consolidadas nas originais | Decisão do João no Bloco 2 — evitar inchaço do folder | Antes de subir para produção |
| P-08 | RF-CUR-04 promete template de Manual POR CURSO; implementado Blade única padronizada | Bloco 6d (2026-07-21, spec D6, respaldo em `modulo-operacao.md`): o manual de classe é uma Blade única (`operation/manual-turma`) renderizada com os dados atuais → Gotenberg, não materializado. Schema não tem `course_manual_templates`. YAGNI: ~10 usuários, um formato padrão basta | Se o contratante pedir manual personalizado por curso |
| P-09 | Protótipo Figma mostra **4 tipos** de documento de turma (Manual, Pruebas/evaluaciones, Lista de asistencia, Acta de cierre); implementado são **3** (`MANUAL`/`PRUEBAS`/`EVALUACION_REDATOR`) | Bloco 6-frontend (2026-07-21, brainstorming, decisão D6 da spec `specs/archive/2026-07-21-bloco6-frontend-operacao-design.md`): a taxonomia de RN-16 tem peso legal (define quando a turma habilita) e não se muda no escuro — o front renderiza os 3 do backend; os rótulos extras do Figma eram exploratórios. Mudar para 4 = alterar enum `TurmaDocumentType` + testes do 6d (bloco entregue). **Ficou mais barato em 2026-08-10** (`turma-habilitacao-listagem`): a lista canônica dos tipos obrigatórios saiu de dois pontos de uso para `TurmaDocumentType::values()`, consumido pela relação `Turma::documentacaoObrigatoria()` e pelo `TurmaHabilitacaoService` — o service **não** precisa mais mudar, e o custo do enum virou uma linha. A decisão de negócio segue com a Lotus | Se a Lotus confirmar que quer os 4 tipos |
| P-10 | Coluna **CLIENTE** da tabela de alunos (aba Alumnos, Exec 2) foi **omitida** | Bloco 6-frontend (2026-07-22, Exec 2): `EnrollmentData` não expõe campo cliente e o cliente da turma é único (já aparece no cabeçalho da página de detalhe). Implementação consciente seguindo spec §3 Operação. YAGNI para ~10 usuários com alunos de 1 cliente por turma | Se a Lotus pedir alunos de múltiplos clientes na mesma turma, expor `client_name` em `EnrollmentData` |
| P-13 | Protótipo Figma mostra coluna **CÓDIGO** da turma com identificador próprio (`TR-45`…`TR-42`); implementado renderiza `quote_code` (`Scap 3 - Cot 1`) | Bloco 6b (spec D7): turma se identifica por relacionamento. **Gatilho anterior venceu no bloco visual (2026-07-27) e produziu decisão D8:** a coluna já existia (`TurmasTable.tsx:52-53`, monospace, e a busca filtra por `quote_code`/`budget_code`), remover seria perda funcional, e criar código próprio exige coluna + sequência ADR-17 + DTO + regeneração de tipos — backend com peso legal dentro de um bloco de refino visual. O bloco só trocou o `text-sky-600` hardcoded por variável do tema. Divergência permanece, agora com decisão registrada em vez de indefinição | Se a Lotus pedir identificador próprio de turma (aí vira task de backend, não de UI) |
| P-15 | Protótipo do módulo de alunos mostra coluna `CERTIFICADOS` na listagem e card `CERTIFICADOS EMITIDOS` no detalhe; implementado não tem nenhum dos dois | Bloco alunos (2026-07-27, spec D10): `app/Domains/Certification/` é pasta vazia e não existe migration de `certificates` — é o Bloco 7. Card vazio foi rejeitado explicitamente: afirmar "sem certificados" quando a verdade é "o módulo não existe" é falha silenciosa, e aqui o dado tem peso legal. **Proveniência de D10 ratificada pelo João no doc-sync 2026-07-30** **Gatilho venceu em 2026-08-07:** o Bloco 7 entregou `certificates` e a vertical até a API pública. **Gatilho venceu de novo em 2026-08-08:** o bloco `certificacao-frontend` entregou o módulo próprio `/certificados` (Emisión + Historial) e **não tocou o módulo de alunos** — o escopo aprovado no brainstorming (4 frentes) nunca incluiu a listagem/detalhe do aluno, então a decisão que esta pendência espera segue não tomada. Os dados agora existem de ponta a ponta; expor coluna/card no módulo de alunos é composição de frontend sobre API pronta | Fecha quando o João decidir expor (ou não) certificados na listagem e no detalhe do aluno, ou se a Lotus pedir. Revisar em **2026-09-30** se nada acontecer antes |
| P-16 | Protótipo põe `Alumnos` como primeira aba de `PeoplePage`; implementado mantém `Redactores` primeiro | Bloco alunos (2026-07-27, spec D11): divergência aceita por decisão do João no mesmo dia — a ordem atual fica, a aba `Alumnos` só trocou o empty state fixo pelo conteúdo real | Se a Lotus pedir `Alumnos` como aba padrão |
| P-18 | Página de fechamento do Notion (`Fechamento técnico de sprint`, id `f88bc9603dfa8253b40981686f8ae023`) tem `Descrição: "Fechamento — Sprint 3"` mas a propriedade `Sprint` real é `Sprint 2 · Comercial` | Doc-sync 2026-07-30 (achado E3-05): mislabel dentro do próprio Notion, fora do escopo de escrita autorizado pelo D11 da spec deste bloco (só cobre critério de aceite de H.1.3.1 e status de task) — só reportado, não corrigido | Fecha quando o João corrigir a propriedade manualmente no Notion |
| P-20 | Nenhum ADR cobre a escolha de `openspout/openspout` para leitura de planilha (import de alunos) | Achado na re-auditoria do doc-sync 2026-07-30 (Task 14): `backend/composer.json` declara `openspout/openspout ^5.3`, usado em `SpreadsheetRowReader.php` (Bloco 6c, import xlsx/csv). Decisão real de biblioteca em produção, sem registro em `docs/adrs.md` — é decisão de arquitetura, não fato a corrigir sozinho; espera o João decidir se vira ADR-20 ou nota em ADR-02 | Registrar como ADR (ou nota) no próximo bloco que tocar `docs/adrs.md`, ou quando o João decidir o formato |
| P-23 | `docs/superpowers/progress.md` perdeu a coluna `Contexto` que o `progress-archive.md` mantém — a linha do bloco não aponta para o packet na própria coluna, só dentro do texto de "Referências" | Achado da re-auditoria de fechamento de 2026-07-30 (`progress.md:7` `Data \| Entrega \| Status \| Resultado \| Referências` vs `progress-archive.md:6` `... \| Contexto \| Plano \| Spec`). É mudança de formato, não erro de fato: ou a coluna volta, ou o cabeçalho do doc declara que o formato mudou de propósito — decisão do João, não do agente. Ficou de fora do doc-sync 2026-07-30 por escolha explícita dele no gate de fechamento | Fecha na próxima vez que o João decidir o formato do `progress.md` (restaurar coluna ou declarar a mudança no cabeçalho); revisar em **2026-09-30** se nada acontecer antes |
| P-22 | A task H.1.3.1 existe **duas vezes dentro da base Notion canônica** (`collection://e64b7d57-d000-4433-b652-a410e75193cc`): `3a2bc9603dfa803b94bbf27c075b27d6` (`Sprint 4 · Certificação`, `Critério de aceite` preenchido pelo write da Task 12) e `3a2bc9603dfa8021b69ee399cd8fd915` (`Sprint 3 · Acadêmico`, critério ainda **vazio**) | Achado da revisão do doc-sync 2026-07-30 (Q-1): o relatório usou os dois IDs como se fossem a mesma página — a seção 4 (E3-04) cita a cópia Sprint 3 e as seções 8/10 escrevem na Sprint 4 — e nunca notou que são duas linhas. A duplicata é o mesmo risco de proveniência que gerou os 12 falsos positivos, um nível abaixo: dentro da base certa. Qual cópia é a canônica é decisão do João (a Sprint da task mudou de 3 para 4), não do agente — enquanto as duas existirem, um packet futuro pode ler a vazia | Fecha quando o João apagar ou mesclar uma das duas cópias no Notion; até lá, todo consumo de H.1.3.1 cita o ID `3a2bc9603dfa803b94bbf27c075b27d6` |
| P-25 | A D2 da spec `specs/archive/2026-08-02-abstracao-componentes-redator-design.md` pede `useFilePreview<T extends PreviewableFile>`; o implementado é `useFilePreview<T>` sem constraint | Decisão tomada na implementação e justificada no próprio arquivo (`shared/hooks/useFilePreview.ts`): pôr o constraint faria `shared/hooks` importar tipo de `shared/ui`, invertendo a direção de dependência da camada por uma garantia que já existe — quem restringe é o `AppFilePreviewDialog`, que só aceita `PreviewableFile \| null`. Sem risco de tipo. O review de 2026-08-02 classificou como divergência documental, não achado: o código está melhor que a spec, e a spec está arquivada. **Segunda ocorrência medida em 2026-08-04, na direção inversa:** a D3 de `hardening-tabela-e-testes-pre-sprint-4` fez a `SearchableTableFrame` declarar `SearchableTableState<T>` **estruturalmente compatível** com o `TableFilter<T>` em vez de importá-lo, para não criar a primeira aresta `shared/ui → shared/hooks` do projeto (medido: zero import nos dois sentidos hoje). Mesmo princípio — camada não importa tipo da outra — agora com dois casos no código e **nenhuma linha na rule**. O bloco tocou a `frontend-fsliced.md`, mas no parágrafo de query-em-componente, não no de fronteira de tipo | Fecha quando a direção "hook genérico não importa tipo de `shared/ui`" virar linha em `.claude/rules/frontend-fsliced.md` (o lugar natural é o parágrafo de wrappers/fronteira de tipo), ou quando um segundo consumidor de `useFilePreview` obrigar a revisitar o contrato. Revisar em **2026-09-30** se nada acontecer antes |
| P-26 | A spec e os commits de `hardening-guardrails-e-transportes` (2026-08-04) afirmam "nenhum comportamento observável muda"; a troca de `abort_unless` por `->scopeBindings()` mudou **403 → 404** para usuário autenticado **sem** a permissão da rota | Provado por sonda no review de 2026-08-04 (Q-3): `SubstituteBindings` roda antes do middleware `permission:`, então o binding escopado responde primeiro. Sem `commercial.budget.update`, arquivo do próprio pai dá **403** e arquivo de outro pai dá **404** — antes os dois davam 403, porque o `abort_unless` vivia depois do middleware. O par de status deixa um staff sem permissão sondar posse de arquivo. **Deferido pelo João** (ele aprovou Q-1 e Q-5): dano prático baixo — ~10 usuários internos, todos staff, e 404 vaza menos que 403. O que fica aberto é a afirmação, não o código: a spec §4 (invariante 1) e os commits negam o que o código faz, que é a lição 13 | Fecha quando a nota entrar na spec arquivada do bloco (ou no `progress.md` da entrega), ou quando o comportamento voltar a ser 403 uniforme por decisão de segurança. Revisar em **2026-09-30** se nada acontecer antes |
| P-21 | `backend/composer.json` declara `simplesoftwareio/simple-qrcode ^4.2` sem nenhum uso no código (`grep` por `SimpleSoftwareIO`/`QrCode::` em `backend/app` não retorna nada) e sem ADR | Achado na re-auditoria do doc-sync 2026-07-30 (Task 14, 3a rodada): dependência de peso legal (geração de QR do certificado — Domínio Certification, ainda placeholder) instalada antecipadamente, sem uso e sem decisão registrada — mesmo padrão de gap do P-20 **Gatilho venceu em 2026-08-07:** a lib passou a ser usada de verdade — `CertificatePdfService::html()` gera o QR (`QrCode::format('svg')->size(180)`) embutido em base64 no certificado, provado no PDF real do gate de fechamento. A dependência deixou de ser antecipada e virou decisão em produção **sem registro**, exatamente a forma do P-20 | Fecha quando o João decidir o formato do registro (ADR próprio ou nota em `docs/adrs.md`, mesma decisão pendente do P-20). O agente não escolhe numeração nem formato de ADR |
| P-27 | O plano `plans/2026-08-10-hardening-revisao-ui-assistida.md:364` transcreve o campo do template como `Chrome DevTools: used\|unavailable\|not-needed`; o contrato entregue usa `used\|complementary_unavailable\|not-needed` | Correção Q-3 do review de 2026-08-10: o enum do template divergia do vocabulário que a própria skill manda registrar (`SKILL.md:29-30`), e os cinco relatórios do bloco emitiram híbrido fora do enum — um deles gravou `not-needed` num run em que o DevTools estava indisponível. O template foi corrigido; **o plano aprovado não se reescreve retroativamente**, e os relatórios antigos em `.artifacts/` ficam como registro de auditoria | Fecha no `/fechar-sprint` deste bloco, com a nota do enum final no `progress.md` ou na spec arquivada |

## Encerradas (mantidas 1 sprint para rastro, depois saem)

| ID | Pendência | Como fechou |
|---|---|---|
| P-01 | `docs/der-fisico.md` em PT/ES vs. schema implementado em inglês | **2026-07-31** — o João aplicou no Drive o patch de `modelo-fisico-e-diagramas.md` (e o de `modelo-conceitual.md`, mesmo eixo E2-01) gerado pelo doc-sync 2026-07-30. Canônico do Drive agora descreve o schema em inglês; a pasta de patches (`audits/2026-07-30-drive-patches/`) foi removida por ter cumprido a função |
| P-14 | Drive (`tela-pessoas.md`, RF-ALU-01..07) escreve `GET /api/alunos`; implementado é `GET /api/students` | **2026-07-31** — patch `tela-pessoas.md` aplicado no Drive pelo João. Decisão registrada segue a mesma (spec D7 do bloco alunos: rota de API em inglês, rota de UI em espanhol) |
| P-17 | ADR-15 revisado (2026-07-17) e ADR-16/18/19 ausentes do Drive canônico (`decisao-stack.md`) | **2026-07-31** — patch único de `decisao-stack.md` aplicado no Drive pelo João; os 4 ADRs passam a existir no canônico. Nota de sync do ADR-16 em `docs/adrs.md` atualizada |
| P-19 | Drive (`tela-pessoas.md` §B, passo 4) descreve edição de aluno atualizando vínculo de cliente; implementado é `UpdateStudentAction` só alterar campos de `User` | **2026-07-31** — coberta pelo mesmo patch de `tela-pessoas.md` aplicado no Drive pelo João |
| P-06 | `der-fisico.md`: `turmas.redator_id` (FK 1:N) vs. pivot `turma_redator` (N:N) implementado | doc-sync 2026-07-30 (`hardening-doc-sync-sprint4`): `der-fisico.md` passou a descrever o schema real — `turmas` e `turma_redator` movidas para "IMPLEMENTADAS" com colunas reais (`active_quote_id` gerada STORED, enum `status` estreitado no MySQL, etc.), relação `redatores` 1:N trocada por N:N via `turma_redator`, conferido contra as duas migrations de turma (`create_turmas_table`, `alter_turmas_for_conclusao`) |
| P-24 | `UserPhotoService::store()` podia apagar o objeto NOVO mesmo depois do UPDATE já ter commitado | **2026-08-01** — bloco `hardening-debitos-integridade` (D5): `$user->update(['photo_path' => $new])` passou a rodar dentro de `DB::transaction`. Como a auditoria é síncrona, se o dispatch dela lançar, o rollback desfaz o UPDATE e a compensação (`deleteObject($new)`) volta a ser verdadeira — provado por `UserPhotoTest::test_falha_na_auditoria_desfaz_o_update_e_apaga_o_objeto_novo` |
