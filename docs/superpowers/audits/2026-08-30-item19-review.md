# Review do item 19 (`frontend-triagem-dos-audits-do-item-18`) — 2026-08-30

**Lane:** lane-c · **Branch:** `fix/frontend-triagem-audits-item-18` · **Intervalo:** `37e0e2d4...b8dd80f2`
(71 arquivos, +2734/-251, todos em `frontend/src/`) · **Revisor:** Claude (`/revisar-sprint`)

## Classificação de risco

**Baixo risco.** Bloco frontend puro — não toca `backend/`, `generated.ts`, migration/schema,
auth/Sanctum, auditoria ou RBAC (confirmado: `git diff 37e0e2d4...HEAD --stat -- backend/
frontend/src/shared/types/generated.ts` vazio). Executor: Claude, não Codex. Sem segunda lente
obrigatória — revisão só Claude, gabarito do projeto.

## Gabarito consultado

CLAUDE.md §5 · `docs/README.md` · `docs/adrs.md` (ADR-05, ADR-16) · `.claude/rules/frontend-fsliced.md` ·
`.claude/rules/frontend-estilizacao.md`.

## Método

1. Subagente (general-purpose) auditou o diff completo contra as quatro fontes do gabarito mais o
   catálogo universal: leis, `primereact` direto/feature cruzada, `key={item.id}`, `useEffect` de
   reset de form, kit `FormField` reintroduzido, `role="list"`, classificação dos 13→17 sítios de
   `BOTAO_SEM_PAPEL`, as duas catracas novas nos quatro arrays corretos, escopo visual vs. D1–D15,
   órfãos (`AppLineChart/legend.tsx`).
2. Verificação própria: `CrudDialog.tsx` e `fieldContext.ts` lidos por inteiro contra as duas
   rules; ledger (`2026-08-29-item19-triagem.md`) conferido — zero `pendente —` fora da linha do
   gabarito no cabeçalho; fichas D-63..D-68 conferidas em `backlog.md` (todas presentes, gatilho
   "decisão do João"); os dois achados abertos pela run 5 (foco no "Maximizar diálogo", par
   Rechazar/Aprobar em 40×44) conferidos corrigidos no código (`CrudDialog.tsx` — `focusOnShow=
   {false}` + `onShow={focarCorpo}`; `QuoteRow.tsx:64-83` — `compact` explícito nos dois).
3. Gate rodado de novo, do zero, nesta sessão (não só lido do `state.md`): `pnpm lint` → 0 erros;
   `pnpm build` → verde; `pnpm test` → **699/699 passed** (124 arquivos).

## Achados

Nenhum. O subagente reportou uma imprecisão textual no PLANO (a task 8/9 afirma que
`grep -rn "font-mono" src/app src/features` devolve zero; na prática devolve 4 linhas — um
docblock e três asserções de teste que citam a string `font-mono` para conferir o EFEITO da
constante, não um literal vivo). Não é achado de código: a invariante que a task protege — nenhum
`className` com `font-mono` cru fora de `shared/ui/typography.ts` — se sustenta; é o texto do
plano que descreveu mal o próprio grep. Não corrige nada em código; não abre ficha (não é
divergência de doc normativo, é nota de execução num plano já fechado).

## Verificação positiva

- As 4 raízes (R1 botão sem papel, R2 dado técnico, R3 invalidez/foco, R4 `dataKey`) corrigem em
  `shared/ui` com teste visto reprovar antes de passar (TDD real, não pós-hoc), confirmado nos
  arquivos `CrudDialog.test.tsx`, `fieldAssociation.test.tsx`, `AppSelectableCard.test.tsx`,
  `ArchiveSwitch.test.tsx`, `typography.test.ts`, `legend.test.tsx`.
- As duas catracas novas (`BOTAO_SEM_PAPEL`, `MONO_LITERAL`) nasceram medidas com o próprio
  seletor (não com o grep que originou o débito, conforme a lição de `frontend-fsliced.md`) e
  foram vistas reprovar por sonda negativa nas duas camadas (`features/` e `app/`) antes de
  entrarem — no plano e confirmado no commit `da2b52b8`/`780494c4`.
- O ledger fecha sem prova pendente; a run 5 mediu o runtime real com certificado emitido de
  verdade (`LOT-2026-1000`) e não achado sintético.
- O bloco autocorrigiu 3 divergências da própria rule durante a execução (Q-2/Q-3/Q-5 citadas em
  `.claude/rules/frontend-estilizacao.md`) — mecanismo citado errado, degrau de raio divergente do
  código, catraca só medindo `features/` — antes desta revisão chegar a elas.
- Os dois achados que a run 5 abriu contra o próprio trabalho do bloco (foco no maximizar, par
  40×44) foram corrigidos no mesmo bloco (commit `3278b8db`), não empurrados para ficha.
- Seis fichas de decisão do João (D-63..D-68) abertas em `backlog.md`, todas com raiz, recomendação
  e gatilho escritos — nenhuma correção visual não autorizada coube no código.

## Veredito

Revisão limpa. Nenhum achado aguardando decisão ou correção. Lane transiciona para
`ready_for_closure`.
