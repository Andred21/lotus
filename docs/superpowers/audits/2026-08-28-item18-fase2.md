BEGIN LOTUS UI REVIEW REPORT
## Run
Surface: Item 18 fase 2 — camada tipográfica e hierarquia de texto em Dashboard (`/`), Cursos (`/cursos`) e Perfil (`/perfil`)
Local URL: http://localhost:5175 (frontend Vite, offset +2) · backend http://localhost:8082
Branch/commit: refactor/frontend-estilizacao-componentes @ ddc37f36
Date/time: 2026-08-29
Agent: claude (lotus-ui-review, fase 2 de 4)
Playwright CLI: sessão `i18f2d`, headed (DISPLAY=:0), `--browser chromium` (o canal `chrome` não existe nesta máquina — `/opt/google/chrome` ausente)
Chrome DevTools: complementary_unavailable
Git working tree before/after: branch e commit inalterados (`refactor/frontend-estilizacao-componentes` @ `ddc37f36`).
  Antes: ` M .claude/skills/lotus-ui-review/SKILL.md` · `?? docs/superpowers/audits/2026-08-28-item18-fase1.md`.
  Depois: os dois acima, mais `?? docs/superpowers/audits/2026-08-28-item18-fase{2,3,4}.md`.
  Delta desta run = exclusivamente `?? docs/superpowers/audits/2026-08-28-item18-fase2.md` (a cópia exigida pela entrega). As fases 3 e 4 são das runs paralelas; o `SKILL.md` modificado e o relatório da fase 1 são WIP alheio, preservados intactos. Toda a evidência ficou em `.artifacts/ui-review/2026-08-28-item18-fase2/`, caminho ignorado por Git, e `.playwright-cli/` também é ignorado (`.gitignore:35`). Nenhum arquivo de código foi tocado.

## Coverage
| Journey step | Desktop | Tablet | Mobile | Evidence |
|---|---|---|---|---|
| Login com credencial de seed (`admin@lotus.cl`) e troca de idioma para es-CL pelo `LanguageMenu` | 1440x900 | — | — | console limpo após o 401 de bootstrap do `/api/me` |
| Dashboard `/` — h1, faixas de seção, títulos de card, KPI, listas, gráficos | 1440x900 | 1024x768 | 390x844 | `f2-dashboard-<vp>-{light,dark}.json`, `f2-es-dashboard-<vp>-light.png`, `f2-es-dashboard-390-agenda-clip.png` |
| Cursos `/cursos` — h1, subtítulo, tabela ativa | 1440x900 | 1024x768 | 390x844 | `f2-cursos-<vp>-{light,dark}.json`, `f2-es-cursos-<vp>-light.png` |
| Perfil `/perfil` — h1, três seções de formulário, rótulos e campos | 1440x900 | 1024x768 | 390x844 | `f2-perfil-<vp>-{light,dark}.json`, `f2-es-perfil-<vp>-light.png` |
| Alternância de tema pelo `iconToggle` (`lotus-ui.state.theme`), medida nos dois temas | 1440x900 | 1024x768 | 390x844 | 18 JSON `f2-*` (3 telas x 3 viewports x 2 temas) |

Método: toda afirmação numérica vem de `getComputedStyle` executado no navegador (script em
`run-code`, um JSON por tela/viewport/tema). Contraste calculado com conversão oklch→sRGB própria e
fundo efetivo resolvido subindo a árvore até o primeiro ancestral opaco. Screenshot só sustenta
layout; JSON medido sustenta grafia e contraste.

## Technical signals
Console: 1 erro em toda a jornada — `401 Unauthorized` em `GET http://localhost:8082/api/me`, emitido no bootstrap ANTES do login. Limpo após autenticar; nenhum erro ou warning novo nas três telas, três viewports e dois temas. Resposta de autorização esperada, não classificada como defeito.
Network: nenhuma requisição quebrada, nenhum 4xx/5xx após o login, nenhuma repetição inesperada observada na jornada read-only.
Performance: não medida — nenhuma alegação de performance neste relatório.
Untested states: loading (só alcançável interceptando rota — proibido); erro de rede/500 (idem); vazio de Cursos e de Dashboard (a base semeada tem dados; esvaziá-la seria mutação); RUT preenchido no Perfil (o usuário de seed `admin@lotus.cl` não tem RUT: o cartão "Identidad" mostra "Sin RUT registrado" e o `<span className="font-mono">` do valor nunca é renderizado — a grafia do RUT presente só pôde ser lida no código, nunca medida no navegador); disabled/read-only de campos do Perfil não exercitado por escrita.

## Findings
### UI-01 — o `h1` é a única peça tipográfica que já tem dono único e mede igual em toda a superfície
Classification: A
Surface/journey: Dashboard, Cursos e Perfil — título de página
Viewport: 1440x900, 1024x768, 390x844, temas claro e escuro
Reproduction: abrir cada tela em cada viewport e cada tema; ler `getComputedStyle` do `h1`.
Evidence: `f2-{dashboard,cursos,perfil}-{1440x900,1024x768,390x844}-{light,dark}.json`, campo `headings[0]`; `f2-es-dashboard-1440x900-light.png`.
Observed fact: em 18/18 medições o `h1` sai idêntico — `font-family: Archivo`, `font-size: 24px`, `font-weight: 600`, `letter-spacing: -0.6px`, `line-height: 32px`, `margin: 0 0 16px`, exatamente um `h1` por tela, `class` = `mb-4 font-display text-2xl font-semibold tracking-tight`. Contraste 9,45:1 no claro; `rgba(255,255,255,0.87)` no escuro. `grep -rn "font-display text-2xl font-semibold tracking-tight" frontend/src/` só acha `shared/ui/typography.ts:21` e o teste que o trava.
Inference: `pageTitleClass` + `PageHeader` cumpriram o que a rule promete: a grafia mora em `shared/ui` e não existe cópia literal em sítio nenhum.
Impact: o degrau de topo da hierarquia é confiável e a catraca (`typography.test.ts`) o mantém.
Recommendation: nenhuma. Registrado como linha de base do que o bloco 18 entregou de fato.
Rule/reference: `.claude/rules/frontend-estilizacao.md` §Tipografia; `frontend/src/shared/ui/typography.ts:21`

### UI-02 — a escala tipográfica anda ao contrário do nível semântico: o `h2` da página é menor e mais fraco que o `h3` do card que ele encabeça
Classification: B
Surface/journey: Dashboard — faixas "Requiere acción", "Contexto", "Análisis del período", "Cumplimiento y carga" e os cards sob elas
Viewport: 1440x900, 1024x768, 390x844 (idêntico nos dois temas)
Reproduction: abrir `/` em qualquer viewport; comparar a faixa "REQUIERE ACCIÓN" com o título "Pendientes" do card imediatamente abaixo dela.
Evidence: `f2-dashboard-1440x900-light.json` → `headings[1]` (h2) e `headings[2]` (h3); `f2-es-dashboard-1440x900-light.png`, faixa da esquerda logo abaixo da fileira de KPI.
Observed fact: o `h2` mede `font-size: 12px`, `font-weight: 600`, `letter-spacing: 0.6px`, `text-transform: uppercase`, `line-height: 16px`. O `h3` do card diretamente subordinado a ele mede `font-size: 16px`, `font-weight: 600`, `letter-spacing: normal`, `line-height: 24px`, caixa normal. O filho é 33% maior que o pai em corpo e ocupa 1,5x a altura de linha. As 4 faixas e os 10 cards repetem o par nas três viewports.
Inference: a faixa de seção foi desenhada como rótulo (peça `SectionLabel`, grafia `sectionLabelClass`) e o título de card foi escrito literal em `AppCard.tsx:147` como `text-base font-semibold`. Como as duas grafias nasceram em lugares diferentes, ninguém as comparou lado a lado — e o resultado é que peso e corpo não codificam profundidade.
Impact: na varredura da tela o leitor lê primeiro os títulos de card e só depois percebe que existem quatro grandes blocos; a divisão da página em "o que pede ação" e "o que dá contexto", que é a razão declarada da faixa existir, chega enfraquecida.
Recommendation: decidir um degrau explícito entre faixa de página e título de card — subir a faixa acima do título de card ou baixar o título de card — e travar os dois na mesma fonte (`typography.ts`), não um em `typography.ts` e o outro literal no componente.
Rule/reference: `.claude/rules/frontend-estilizacao.md` §Tipografia ("a grafia mora em `shared/ui`, nunca no sítio"); `frontend/src/shared/ui/typography.ts:27`; `frontend/src/shared/ui/AppCard/AppCard.tsx:147`

### UI-03 — o mesmo `h3` sai em duas grafias entre telas irmãs, e a do Perfil é indistinguível do `h2` do Dashboard
Classification: B
Surface/journey: Dashboard (títulos de card) x Perfil (seções "Identidad", "Datos personales", "Seguridad")
Viewport: 1440x900, 1024x768, 390x844, ambos os temas
Reproduction: abrir `/`, ler qualquer título de card; abrir `/perfil`, ler qualquer título de seção. Ambos são `h3`.
Evidence: `f2-perfil-1440x900-light.json` → `headings[1..3]`; `f2-dashboard-1440x900-light.json` → `headings[2]`; `f2-es-perfil-1440x900-light.png` e `f2-es-dashboard-1440x900-light.png`.
Observed fact: `h3` do Perfil mede 12px / 600 / `letter-spacing: 0.6px` / `text-transform: uppercase` / `line-height: 16px`. `h3` do Dashboard mede 16px / 600 / `letter-spacing: normal` / caixa normal / `line-height: 24px`. O `h3` do Perfil é byte a byte a mesma grafia do `h2` do Dashboard. Além disso o outline do Perfil é `h1,h3,h3,h3` — não existe `h2` na página, o nível é pulado nas três viewports.
Inference: `FormSection` compõe `SectionLabel as="h3"` (`FormSection.tsx:34`) enquanto `AppCard` escreve sua própria grafia; as duas peças cobrem o mesmo papel — "faixa que encabeça grupo" — e discordam. O pulo de nível vem de o Perfil não ter nenhuma faixa de página, só faixas de card.
Impact: pela aparência não se deduz o nível: a mesma grafia significa "seção de página" numa tela e "grupo dentro de card" na outra. Para leitor de tela, o Perfil anuncia h1 → h3 sem degrau intermediário.
Recommendation: fazer o título de card consumir a mesma peça das faixas (com o degrau que UI-02 pedir) e, no Perfil, ou introduzir a faixa `h2` que as três seções encabeçam, ou passar `as="h2"` nas seções que são divisões da página.
Rule/reference: `.claude/rules/frontend-estilizacao.md` §Tipografia; `frontend/src/shared/ui/SectionLabel/SectionLabel.tsx` (docblock: "`h2` divide uma PÁGINA; `h3` encabeça grupo dentro de card"); `frontend/src/shared/ui/FormSection/FormSection.tsx:34`

### UI-04 — o `AgendaPanel` escreve a grafia da faixa literal num `h4`, fora da peça e pulando nível
Classification: B
Surface/journey: Dashboard — card "Agenda", cabeçalho de janela ("Atrasadas", "Esta semana", …)
Viewport: 1440x900, 1024x768, 390x844, ambos os temas
Reproduction: abrir `/`, rolar até o card "Agenda"; o rótulo com bolinha colorida e contagem é um `h4`.
Evidence: `f2-dashboard-*-{light,dark}.json` → `outline` = `h1,h2,h3,h3,h2,h3,h4,h3,…` e `headings[6]`; `f2-es-dashboard-390-agenda-clip.png` (linha "ATRASADAS 3").
Observed fact: o `h4` mede 12px / 600 / `letter-spacing: 0.6px` / uppercase / `line-height: 16px` — a mesma assinatura do `h2` da página e do `h3` do Perfil. A `className` no sítio é `flex items-center gap-2 px-4 pt-3 pb-1 text-xs font-semibold tracking-wider uppercase`, que contém literalmente a string de `sectionLabelClass`. O outline salta de `h3` (título do card "Agenda") para `h4` e volta para `h3`. `grep` acha a string literal em dois sítios de página: `AgendaPanel.tsx:99` e `KpiRow.tsx:77`.
Inference: a peça `SectionLabel` só aceita `as: 'h2' | 'h3'`; para emitir `h4` o call site copiou a grafia em vez de estender a peça. É exatamente o defeito que a rule nomeia ("escrever a grafia literal no sítio é o defeito, não o atalho") e que gerou o achado A2 do audit de 2026-08-26.
Impact: a grafia deixa de ter dono único — uma mudança futura na faixa não alcança estes dois sítios; e três níveis semânticos distintos (h2 de página, h4 de janela, `p` de rótulo de KPI) vestem a mesma roupa, então a aparência não informa hierarquia nenhuma.
Recommendation: ou estender `SectionLabel` para admitir o nível que a Agenda precisa, ou reconhecer que a janela da Agenda é rótulo e não cabeçalho — e então não emitir heading. Em qualquer caso, consumir `sectionLabelClass` da fonte, não copiá-la.
Rule/reference: `.claude/rules/frontend-estilizacao.md` §Tipografia; `frontend/src/app/pages/Dashboard/AgendaPanel.tsx:99`; `frontend/src/app/pages/Dashboard/KpiRow.tsx:77`

### UI-05 — dado técnico não converge para mono: contagem sai ora em IBM Plex Mono, ora em Inter, na mesma tela
Classification: B
Surface/journey: Dashboard (pill de contagem do card, tabela de cumplimiento, painéis de barra) e Cursos (colunas "Carga horaria (h)" e "Redactores")
Viewport: 1440x900, 1024x768, 390x844, ambos os temas
Reproduction: abrir `/`, comparar a contagem ao lado de "Pendientes" (pill) com a contagem alinhada à direita no painel "Carga de relatores"; abrir `/cursos` e ler os números das duas últimas colunas.
Evidence: `f2-dashboard-1440x900-light.json` → `tech`; `f2-cursos-1440x900-light.json` → `tech`; `f2-es-dashboard-1440x900-light.png` (pill "8" ao lado de "Pendientes").
Observed fact: no mesmo Dashboard convivem `font-family: IBM Plex Mono` com `font-variant-numeric: tabular-nums` (`w-8 shrink-0 text-right font-mono text-sm tabular-nums`, `font-mono tabular-nums`) e `font-family: Inter` com `tabular-nums` (`rounded-full px-2 py-0.5 text-xs font-semibold tabular-nums`, `AppCard.tsx:154`; e `td` de tabela em `px-4 py-3`). Em Cursos, os seis valores numéricos das colunas "Carga horaria (h)" e "Redactores" (40, 3, 24, 5, 32, 2) medem `Inter`, 14px, weight 600, `tabular-nums` — nenhum é mono.
Inference: `tabular-nums` foi aplicado de forma ampla, mas o `font-mono` só chegou onde a peça foi trocada. As contagens que ainda vivem em markup de página ou no `AppCard` ficaram para trás.
Impact: a coluna de números de Cursos e o pill de contagem não pertencem visualmente à mesma família dos demais dados técnicos da aplicação; o sinal "isto é dado, não prosa" fica inconsistente numa aplicação em que folio, RUT e valor têm peso legal.
Recommendation: promover a contagem a peça de `shared/ui` (como `StatValue` fez com o número de estatística) e consumi-la no `AppCard`, na coluna de tabela e nos sítios de página, para que `font-mono` e `tabular-nums` saiam sempre juntos.
Rule/reference: `.claude/rules/frontend-estilizacao.md` §"Dado técnico é mono"; `frontend/src/shared/ui/AppCard/AppCard.tsx:154`

### UI-06 — `font-mono` sem `tabular-nums`: a regra pede o par e só a família chegou
Classification: B
Surface/journey: Perfil (campo "Teléfono" e, por código, o valor do RUT) e Dashboard (data de cada item de "Pendientes"/"Alertas" e da Agenda)
Viewport: 1440x900, 1024x768, 390x844, ambos os temas
Reproduction: abrir `/perfil` e ler `getComputedStyle` do input rotulado "Teléfono"; abrir `/` e ler qualquer data à direita de uma linha de pendência.
Evidence: sonda `run-code` sobre `/perfil` (input `_r_3_`, `label[for]` = "Teléfono": `IBM Plex Mono`, 16px, `font-variant-numeric: normal`, `class="w-full font-mono p-inputtext p-component"`); sonda sobre `/` (datas `06-07-2026`, `15-08-2026`, `31-07-2026`, `13-07-2026`, `20-07-2026`: `IBM Plex Mono`, `font-variant-numeric: normal`, `class="order-2 ml-auto shrink-0 font-mono text-xs …"`), contra `250 UF` no mesmo Dashboard, que mede `tabular-nums`; `f2-es-perfil-1440x900-dark.png`.
Observed fact: a família mono está correta nos dois casos medidos e `font-variant-numeric` sai `normal` em ambos, enquanto o valor em UF do mesmo Dashboard sai `tabular-nums`. O RUT deste usuário de seed não pôde ser medido: o cartão "Identidad" mostra o texto de ausência "Sin RUT registrado" (Inter, 14px), que a regra admite como prosa. No código, o RUT presente sai como `<span className="font-mono">` — `font-mono` sozinho, sem `tabular-nums` (`ProfileIdentityCard.tsx:75`), e o comentário ao lado registra que o mesmo token é usado para o RUT de terceiros em `StudentsTable`, `RedatoresTable` e `RedatorCard`.
Inference: a troca foi feita por `font-mono` isolado, sem o par que a rule exige. Em IBM Plex Mono o dígito já é de largura fixa por desenho, então a diferença não aparece enquanto a fonte carrega — a declaração é a garantia contra o fallback, que é exatamente a razão escrita na rule.
Impact: baixo hoje, latente amanhã, e sistemático: se o padrão do RUT é o mesmo token replicado em quatro sítios, o par ausente se propaga junto. A régua declarada do bloco 18 diz "com `tabular-nums`, sem exceção".
Recommendation: aplicar `tabular-nums` junto de `font-mono` nesses sítios, ou — melhor — fazê-los consumir a mesma peça de dado técnico proposta em UI-05, onde o par não pode ser separado. Verificar de uma vez os quatro sítios de RUT que o comentário do `ProfileIdentityCard` nomeia.
Rule/reference: `.claude/rules/frontend-estilizacao.md` §"Dado técnico é mono"; `frontend/src/features/identity/components/Profile/ProfileIdentityCard.tsx:75`; `frontend/src/app/pages/Dashboard/DashboardItemRow.tsx:68`

### UI-07 — a versão `v0.1.0` do rodapé da sidebar sai em Inter, e "versão" é dado técnico nomeado pela regra
Classification: B
Surface/journey: shell presente nas três telas — rodapé da sidebar
Viewport: 1440x900 e 1024x768 (a 390x844 o rodapé continua presente); ambos os temas
Reproduction: abrir qualquer uma das três telas em 1440x900 e ler o rodapé da sidebar.
Evidence: `es-measure-{cursos,perfil}-1440x900-light.json` → `tech[0]`; `f2-es-dashboard-1440x900-light.png`, canto inferior esquerdo.
Observed fact: `v0.1.0` mede `font-family: Inter`, 14px, weight 400, `font-variant-numeric: normal`, `class="px-4 py-3 text-sm text-(--shell-ink-muted) text-center"`, medido a partir de Cursos e de Perfil. A mesma string aparece no painel de marca da tela de login (snapshot pré-login), mas ali não foi medida — a jornada não voltou ao login.
Inference: a rule lista "folio, RUT, código, versão e contagem" como o conjunto que sai em mono; a versão ficou de fora da varredura do bloco 18, provavelmente por morar no shell e não numa das telas revisadas.
Impact: menor — é um rótulo de rodapé, sem leitura crítica. Registrado porque é o único item do conjunto nomeado pela rule que ainda não converge, e porque a mesma peça aparece em todas as telas.
Recommendation: aplicar `font-mono tabular-nums` ao rodapé de versão, ou remover "versão" do conjunto nomeado pela rule se a decisão for deliberada.
Rule/reference: `.claude/rules/frontend-estilizacao.md` §"Dado técnico é mono"

### UI-08 — no KPI de cotizaciones a contagem e o valor em UF se encostam e leem como um número só: "1 250 UF"
Classification: B
Surface/journey: Dashboard — cartão de KPI "Cotizaciones pendientes"
Viewport: 1440x900, 1024x768 e 390x844 (nas três)
Reproduction: abrir `/` em qualquer viewport e ler o quinto cartão da fileira de KPI.
Evidence: `f2-es-dashboard-1440x900-light.png` (cartão "COTIZACIONES PENDIENTES") e `f2-es-dashboard-390x844-light.png`; `f2-dashboard-1440x900-light.json` → o `p` que agrega os dois filhos tem `textContent` "1250 UF".
Observed fact: o cartão renderiza um `span` de contagem (`1`, Archivo 30px, weight 600, `tabular-nums`) seguido, com `gap-2`, de um `span` de valor (`250 UF`, IBM Plex Mono 12px, `tabular-nums`). O nó pai lê "1250 UF". Nenhum separador, rótulo ou pontuação distingue os dois valores.
Inference: são dois dados distintos — quantas cotizações e quanto elas somam — compostos como se fossem um par número+unidade. Em es-CL o espaço é separador de milhar válido, então a leitura "1250 UF" é gramaticalmente legítima.
Impact: um número financeiro pode ser lido com magnitude errada num painel cujo assunto é decisão comercial. É o único KPI da fileira com dois valores, então não há repetição que ensine a leitura correta.
Recommendation: separar os dois dados por rótulo, pontuação ou quebra de linha — por exemplo o valor em UF numa segunda linha sob a contagem, ou precedido de um rótulo curto — de modo que a fronteira entre eles não dependa só de tamanho de fonte.
Rule/reference: régua §Eixo 2 (hierarquia visual) e §Eixo 8 (formatação de dados visíveis); `frontend/src/app/pages/Dashboard/KpiRow.tsx`

### UI-09 — a legenda dos gráficos fica abaixo de AA no tema claro, a 12px
Classification: B
Surface/journey: Dashboard — seção "Análisis del período", legendas de "Evolución mensual" e "UF aprobada por mes"
Viewport: 1440x900, 1024x768, 390x844; tema CLARO (o escuro passa)
Reproduction: abrir `/` no tema claro, rolar até "Análisis del período" e ler as legendas sob os gráficos.
Evidence: `f2-dashboard-1440x900-light.json` → itens `.recharts-legend-item-text` em `secondary`; contraparte em `f2-dashboard-1440x900-dark.json`.
Observed fact: no claro, texto de 12px sobre fundo branco mede — "Clases iniciadas" `rgb(17,156,141)` 3,41:1; "Certificados emitidos" `rgb(236,72,153)` 3,53:1; "Clases concluidas" `rgb(212,98,19)` 3,78:1; "Matrículas" `rgb(168,85,247)` 3,96:1; "UF aprobada" `rgb(99,102,241)` 4,47:1. Os cinco ficam abaixo do mínimo de 4,5:1 de WCAG AA para texto normal. No escuro os mesmos cinco medem 4,52 a 6,89:1 e passam. É o menor contraste de texto medido nas três telas: o restante do produto fica entre 5,28 e 10,35:1.
Inference: a legenda herda a cor da série para amarrar texto e traço, e a rampa foi calibrada para o fundo escuro; no fundo claro as mesmas tintas perdem a régua.
Impact: a legenda é a única chave que liga cor de série a significado. Ilegível ou custosa, o gráfico deixa de ser lido e vira ornamento — e é justamente a seção "Análisis del período".
Recommendation: escrever o texto da legenda na tinta de corpo e deixar o marcador colorido carregar a identidade da série, ou escurecer a rampa de séries no tema claro até 4,5:1 com o fundo do card. A cor deve vir de variável do tema, não de utility de paleta.
Rule/reference: régua §Eixo 6 (contraste); `.claude/rules/frontend-estilizacao.md` §Cor

### UI-10 — a 390x844 o card "Agenda" corta 26px do próprio conteúdo, e a reticência do título fica fora do corte
Classification: C
Surface/journey: Dashboard — card "Agenda" (seção "Contexto")
Viewport: 390x844 (não reproduz em 1024x768 nem em 1440x900); ambos os temas
Reproduction: abrir `/` a 390x844, rolar até o card "Agenda" e ler o primeiro item, "Trabajos en líneas energizadas 220kV".
Evidence: `f2-es-dashboard-390-agenda-clip.png`; `f2-dashboard-390x844-{light,dark}.json` → `clipped[0]` = `div.rounded-lg.border.overflow-hidden`, `287>261`; sonda `run-code` sobre o card.
Observed fact: o card mede `clientWidth: 261px` e `scrollWidth: 287px` — 26px de conteúdo além da caixa, com `overflow-hidden` no card e nenhuma barra de rolagem. A `<section>` interna mede 287px e termina em x=384 contra x=359 da borda do card. O `<a>` do item mede 255px e termina em x=368, 9px além da borda. O `<span>` do título tem `truncate` (`text-overflow: ellipsis`, `white-space: nowrap`), mas a reticência cai nesses 9px cortados: na captura o texto termina em "220k" colado na borda, sem nenhum indicador de que continua. O segundo item ("Mantenimiento de subestaciones", mais curto) cabe e não mostra o efeito. Contexto medido da largura disponível: a 390px a sidebar continua ocupando 80px e o `main` fica com 310px; os próprios rótulos da sidebar truncam (`Dashboard` 52>45px, `Comercial` 48>45px, `Operación` 50>45px).
Inference: o item de grade não encolhe abaixo do próprio conteúdo (`min-width: auto` de item de grid/flex) e o `overflow-hidden` do card, que existe para o raio da superfície, passa a agir como tesoura. O truncamento previsto pelo componente ocorre — mas fora da área visível, então perde a função de avisar.
Impact: no telefone o nome do curso é cortado sem sinal de corte; o `title` que o código oferece como recuperação depende de hover, que não existe em toque — a mesma razão que o próprio comentário do `DashboardItemRow` usa para justificar duas linhas no mobile. A triagem que a Agenda promete passa a exigir abrir o item.
Recommendation: deixar o item de grade encolher (`min-w-0` na `<section>`/coluna da grade) para que o truncamento aconteça dentro do card e a reticência apareça. Vale rever, na mesma passada, se a sidebar deve manter 80px de 390 no viewport de telefone, já que é ela que produz a largura em que o corte aparece.
Rule/reference: régua §Eixo 3 (truncamento de conteúdo essencial) e §Eixo 4 (overflow indevido); `frontend/src/app/pages/Dashboard/AgendaPanel.tsx:96`

## Summary
A: 1
B: 8
C: 1
Total: 10 achados (teto do contrato).
Grupo A — além do UI-01, ficaram adequados e não viraram achado próprio: idioma efetivamente renderizado em `es-CL` com `<html lang="es-CL">` nas três telas; ausência de scroll horizontal do documento nas três telas x três viewports x dois temas; famílias reduzidas a Archivo (display), Inter (corpo) e IBM Plex Mono (dado técnico), sem fallback genérico vazando para texto visível — as ocorrências de Arial medidas estão todas dentro de `p-hidden-accessible` do PrimeReact, invisíveis; contraste do texto secundário do corpo (subtítulo de página 6,92:1 claro / 6,21:1 escuro; rótulo de campo 7,58:1) acima de AA nos dois temas; `StatValue` entregando `font-display` + `tabular-nums` nos 6 KPI.
Mutations performed: none
Code changes performed: none
END LOTUS UI REVIEW REPORT
