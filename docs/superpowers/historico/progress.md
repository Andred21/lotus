# Progresso — Lotus v2

> Histórico curto do que foi entregue e da feature em andamento. Este arquivo **não define a etapa
> operacional**. Para saber o que fazer agora, leia `docs/superpowers/state.md`.
> Fila futura: `docs/superpowers/backlog.md`. Histórico antigo: `progress-archive.md`. Narrativa
> integral de cada bloco fechado (o que era a parte de baixo do `state.md`): `state-archive.md`.
>
> **Cinco colunas, de propósito (P-23, decidido em 2026-08-22).** O formato antigo tinha sete, com
> `Contexto`/`Plano`/`Spec` separadas; aqui plano, spec, context packet e commits vivem juntos na
> coluna `Referências`. A coluna `Contexto` **não volta**: só uma parte das entregas nasce de packet,
> e a coluna separada era célula vazia na maioria das linhas. O `progress-archive.md` registra do
> lado dele a mesma convivência de duas arities — as linhas migram **verbatim**, porque o fechamento
> move histórico e não o reescreve.

| Data | Entrega | Status | Resultado | Referências |
|---|---|---|---|---|
| 2026-08-22 | Infra · runtime de produção: imagem, compose, origem única e os dois `memory_limit` (`infra-producao-runtime-e-aws`) | Entregue | **Item 10 da fila, promovido explicitamente pelo João** pela rota `context_required` — e o **brainstorming recortou o bloco ao meio, por decisão dele**: só o runtime versionado entra; o provisionamento em conta AWS vira bloco próprio quando as quatro decisões abertas do packet existirem (região, tamanho da EC2, DNS/SES, teto de custo). **Origem única contra os dois hosts do Drive**, e a base é medida: `VITE_API_URL` é lido por `import.meta.env` (`axios.ts:25`), então entra **dentro do bundle no build** — com dois hosts a imagem carregaria a URL do ambiente e o item 11 não poderia promover a mesma imagem por SHA. **Sete tasks, dez commits**, TDD por subagente: um `Dockerfile.prod` de quatro estágios e dois alvos (`app` 293MB, `web` 105MB), `docker-compose.prod.yml` sem MySQL/MinIO/Mailpit/worker — a ausência do worker **medida**, não suposta (`grep ShouldQueue` devolve uma linha, que é comentário) —, overlay de sonda separado para provar local e `.env.production.example` sem segredo. **A catraca do bloco não é teste de código, é teste de composição** (`frontend/tests/compose-prod.test.ts`): guarda as propriedades cuja violação é silenciosa, porque `docker compose up` fica verde com um serviço de dev de volta. **A P-50 fechou aqui**, e o impasse que ela nomeava — `conf.d` vale para os dois SAPIs — foi resolvido separando por SAPI: CLI 320M (dobro do pico medido de 129,00 MB) e FPM 256M com `pm.max_children = 5`. **Revisão de sprint em duas lentes** (Claude + Codex independente), risco ALTO, **nove achados aprovados e corrigidos, um commit cada** — os dois 🔴 eram brechas operacionais que a imagem carregava: o seeder que criaria `admin@lotus.cl`/`senha123` como superadmin num servidor novo, e o gate de env cego para a cadeia Sanctum que o próprio molde entrega vazia. Uma divergência entre revisores foi **medida e rejeitada** (o vazamento de `auth.json`/`storage` que o Codex reportou não existe na imagem). **Gate de fechamento:** login real ponta a ponta contra a stack de produção reconstruída — `/up` 200, `/sanctum/csrf-cookie` 204, `POST /api/login` 200 com `lotus-session` gravado, `GET /api/me` 200 com `roles: [superadmin]` —, e `db:seed --force` com `APP_ENV=production` instalando as roles e **recusando o admin de dev**. Suíte backend 867 passed / 5 skipped pelo comando do §6, sem contorno; frontend 88 arquivos / 500 testes, lint e build exit 0. **Nada de AWS foi provado** — limitação declarada da spec, não lacuna. | plano `plans/archive/2026-08-22-infra-producao-runtime-e-aws.md` · spec `specs/archive/2026-08-22-infra-producao-runtime-e-aws-design.md` · packet `context-packets/2026-08-22-infra-producao-runtime-e-aws.md` · `8b1fd6df..e0019bac`, correções `ed4cdc7b..bf896ba5` |
| 2026-08-23 | Identity/Operation · hardening de acesso: ownership de turma, revogação de sessão, nota do redator e o mutex do arquivamento (`hardening-acesso-ownership-e-integridade`) | Entregue | **Item 3 da fila consolidada, `lane-a`, main tree (gate P-03), executor `claude`**, a partir de `main@f6649297`; nasceu em `context_required` e o packet veio do Codex com `status: ready` e cinco fontes recuperadas. Quatro eixos, todos backend, com `generated.ts` regenerado como entregável. **Ownership (Notion 7.3.3):** o escopo mora em `TurmaQueryBuilder::visibleTo()` e entra por DOIS pontos — as listagens e `Turma::resolveRouteBinding()`, que alcança as **20** rotas com `{turma}` de uma vez e faz rota nova nascer coberta; turma alheia devolve **404** e não 403, porque 403 confirmaria que ela existe. **Revogação:** fecha nas duas pontas — purge de sessão na transição e `EnsureAccountIsActive` por request; e a **P-51** perdeu o default literal (`UserData::$is_active` virou `bool\|Optional`), com `d4a8553d` fechando o blast radius no `create`. **Capacidade:** `operation.enrollment.record_result` separa lançar nota/presença do Fluxo 3 (RN-02), por migration e não por seeder. **Integridade (P-49):** `Turma::lockForWrite()` e `Redator::lockForWrite()` nasceram no molde do `Client` e são tomados por **cinco** escritores de filho — o plano corrigiu três medições que a spec e as fichas traziam erradas: o lock é `lockForWrite()` e não `lockRow()` cru (a diferença é a recusa), `ImportStudentsAction` sai da lista porque não abre transação, e a **P-47** não se conserta no seeder (já certo desde `e3490d84`) mas por migration de backfill sobre o dado velho. A catraca é o `ParentLockOnChildWriteTest`, arch test de lista dupla onde **silêncio reprova**. **O `/revisar-sprint` (Claude + Codex) devolveu 2 achados, os dois aprovados e corrigidos:** **Q-1** — `EnsureAccountIsActive` nasceu em `api(append:)` e vazou para o grupo `api` inteiro, devolvendo 401 em `login`, `password/forgot`, `password/reset`, `invitation/accept` e `publico/certificados/{uuid}` para cookie de conta recém-desativada (a validação pública do QR, que tem peso legal, e a própria porta de saída do buraco); a checagem passou a andar casada com `auth:sanctum` no grupo `auth.active`, com `AuthenticatedRouteMiddlewareTest` exigindo que a superfície anônima seja **declarada**; **Q-2** — `.claude/rules/backend-ddd.md` ficou desatualizada sobre o data-scoping da Turma. **DoD provado contra a API real em `:8080` e o MySQL de dev, 12 provas:** listagem de Redator A traz só `[1,4,5,6]` e a de B só `[2]` (admin vê as 7); **19 dos 20** caminhos com `{turma}` alheia devolvem 404 e o vigésimo (`POST /turmas/{turma}/restore`) devolve **403** antes do binding, porque `permission:operation.turma.restore` não é do redator — ele não alcança a rota, que é o que a spec pede; `PUT .../resultado` dá 200 na turma própria e 404 na alheia; conta desativada com sessão viva cai em **401 `application/problem+json`** no request seguinte, enquanto as cinco rotas públicas seguem abrindo (422/200/404 dos próprios handlers, nunca 401); `PUT /api/users/87` omitindo `is_active` renomeia e **não** reativa; a corrida real de duas conexões bloqueou o `EnrollStudentAction` por **6,2 s** e ele **recusou** com `Esta clase fue archivada y ya no acepta cambios.`; as duas sondas do arch test foram vistas reprovar e revertidas; e `GET /api/permissions` traz a permissão nova. Gate: backend **906 passed / 5 skipped** (3227 asserções) **pelo comando canônico do `CLAUDE.md` §6**, que voltou a terminar — o bloco declarou `memory_limit=512M` em `phpunit.xml` para isso, e o **merge da `main` (2026-08-23) desfez o remendo**: a `lane-b` já tinha pago a P-50 por SAPI (`memory-cli.ini` a 320M, `www.conf` a 256M) e o número maior mascararia o teto da imagem; com 320M a suíte fecha igual, reprovado no merge; frontend **87 arquivos / 481 testes**, `pnpm lint` 0, `pnpm build` verde; Pint `passed` nos 42 arquivos da sprint; `typescript:transform` **sem diff novo**. Pendências: **P-47 encerrada**; **P-49** fecha os eixos redator e turma e segue aberta só na janela cotação × orçamento; **P-51** fecha o campo de acesso e segue aberta nos outros cinco; **P-50** foi encerrada pela `lane-b` no mesmo dia, por SAPI, e o remendo desta branch saiu no merge; **P-54** nasceu (o Q-4 herdado do bloco 1, que saiu do escopo por decisão do João e não em silêncio). **D-34 ficou fora por escrita explícita** (§2 da spec) e perdeu o bloco hospedeiro com o fechamento deste — precisa de novo dono. | `plans/archive/2026-08-22-hardening-acesso-ownership-e-integridade.md` · `specs/archive/2026-08-22-hardening-acesso-ownership-e-integridade-design.md` · `context-packets/2026-08-22-hardening-acesso-ownership-e-integridade.md` · 25 commits (`79c246c6`..`f815c507`) mais o de fechamento |
| 2026-08-24 | Frontend · revisão de UI por módulo, fatia 1 de 2: Dashboard `ready-redator` e Operação (`frontend-revisao-ui-por-modulo`) | Entregue (fatia) | **Item 16 da fila, `lane-c`, worktree `../fix-frontend`, executor `claude`**, sem Context Packet — o bloco nasceu de medição local (`audits/` + fichas `D-38`/`D-39`). Duas exceções declaradas na abertura pelo João: os docs de `docs/superpowers/**` foram escritos na worktree (o item 16 nasceu aqui e ainda não estava na `main`) e a branch seguiu sem merge até o fim. **Tasks 1 a 9 executadas e provadas; 10 a 13 cortadas do escopo pelo João em 2026-08-23** ("quero seguir logo para o review") — não é conclusão de plano, é corte declarado, e por isso a entrega é **fatia**, com o item 16 seguindo na fila. **Duas runs de `/lotus-ui-review`, 14 achados corrigidos** (5 da run 1, 9 da run 2), um commit por correção, cada uma medida na tela antes e depois: a pendência do redator virou linha-link para a turma, o código do enum virou rótulo traduzido nas 3 locales, a linha "Falta:" passou a quebrar em 390x844, os oito controles de escrita de turma concluída **somem** em vez de ficarem cinzas (dono único `registroAcademicoBloqueado()`), a `TurmasTable` ganhou política de largura e coluna de ações fixa e as réguas de abas passaram a dizer que continuam fora da tela. **A Task 1 pagou a `D-39`**: a fábrica `shared/testing/i18n.ts` deu forma real ao mock de `useTranslation` nos 17 testes que devolviam só `t`. **Review de sprint (BAIXO risco, uma lente): 4 achados, os quatro aprovados e corrigidos**, um commit cada — a aba da turma passou a ter nome e a viver na URL (`?tab=docs`, com catraca sobre a ordem dos cinco painéis), editar turma e designar redator passaram a consultar a permissão além da RN-15, o `scrollable` da régua de abas voltou a ser pedido por quem mediu a tela em vez de ligado por padrão, e o tipo de documento ganhou mapa exaustivo por compilador. Nasceu a **D-57** (o DTO tipa `missing_types` como `string[]`). **Depois do review, duas rodadas do João na mesma `TurmasTable`, e a lição é a segunda:** trocar o sorteio da sobra de largura por um destinatário fixo é o mesmo defeito com outro dono — a largura virou **porcentagem** (91% + a coluna de ações em `rem`), e o avatar do `IdentityCell` parou de virar elipse com `shrink-0` nos 14 sítios. O padrão da coluna de ações para as outras 11 tabelas virou o **item 17** do backlog, não poluiu esta execução. **A `main` entrou antes do fechamento** (merge `8a4df32a`, decisão do João): o fork estava 90 commits atrás e três blocos desatualizado, e fechar em cima dele perderia a verdade da `main` — o conflito de `state.md`/`backlog.md` foi resolvido pela `main`, que é quem manda no rótulo de lane (o item 16 é da `lane-c`). **Gate re-rodado sobre a árvore mesclada:** backend **906 passed / 5 skipped (3227 asserções)** pelo comando do `CLAUDE.md` §6 — a **P-50** foi paga na `main` e ele voltou a terminar; `pnpm lint` 0, `pnpm build` verde, `pnpm test` **100 arquivos / 555 testes**; fence de frontend puro **vazio**, então Pint e `typescript:transform` são N/A por escopo medido. **Critério de aceite provado no navegador contra a API real, não pela suíte:** a `TurmasTable` mede 1294px em 1600 e 1217px em 1280/1024, com as mesmas proporções e **zero elemento truncado** nos três viewports; os **13** avatares do corpo medem 48×48 (razão 1,000); `?tab=docs` abre `Documentation`, o quarto dos cinco painéis, e URL adulterada cai no primeiro. **A Task 13 Step 1 morreu de obsolescência:** mandava tirar a role `redator` do usuário 1 como resíduo, mas os 7 redatores do seed e os 2 usuários de gate carregam a role pela migration de backfill que chegou da `main` (`fa1abdf1`) — executá-la desfaria o backfill, e ela não foi executada. **A P-41 foi encerrada por decisão do João**: o `min-w-0` voltou em `1b9f82ad` pelo ramo do gatilho que pedia uma tabela real medida; o outro ramo (o teste medir `scrollWidth > clientWidth` em vez de contar classe) sai declarado para o `frontend-hardening-final`. **Fica aberto, como herança declarada:** run 3 (Comercial), fichas `D-*` da Task 12, Minors 2/3/5 da revisão da Task 9 e a senha definida para o redator na Task 3. | `plans/archive/2026-08-22-frontend-revisao-ui-por-modulo.md` · `specs/archive/2026-08-22-frontend-revisao-ui-por-modulo-design.md` · sem Context Packet (medição local) · relatórios `audits/2026-08-22-lotus-ui-review-dashboard-redator.md` e `audits/2026-08-23-lotus-ui-review-operacion.md` · commits `da34533d`…`806099d9`, merge `8a4df32a` e o commit de fechamento |
| 2026-08-24 | Infra · compose por worktree: portas de dev por variável, offset por árvore e duas stacks no ar (`compose-por-worktree`) | Entregue | **Não é item do `backlog.md`: nasce da ficha `P-03`**, cujo gatilho formal venceu em 2026-08-24 (o João decidiu paralelizar e restam quatro blocos de backend na fila, com o compose montando o main tree em portas fixas). O bloco transforma em mecanismo o override efêmero de 2026-08-19: as portas host do `docker-compose.yml` passam a vir de `LOTUS_DEV_*` com **default igual à porta histórica** (quem clona sem `.env` sobe idêntico a antes), um `.env` gitignored na raiz — molde em `.env.example` — escolhe o offset da árvore, o serviço `app` recebe injetadas as seis chaves que carregam porta (`APP_URL`, `FRONTEND_URL`, `SANCTUM_STATEFUL_DOMAINS`, `SESSION_COOKIE`, `AWS_ENDPOINT_PUBLIC`, `AWS_URL`) e o `vite.config.ts` deriva porta e `VITE_API_URL` do mesmo arquivo, com `strictPort` para falhar alto em vez de escorregar de porta em silêncio. Prefixo `LOTUS_DEV_`, não `LOTUS_`: o compose de **produção** lê o mesmo `.env` e reusar o nome vazaria o offset de dev para lá. **Nenhum arquivo de produção entrou no diff** e `backend/` só recebeu `.env.example` — `pint` e `typescript:transform` ficaram N/A por escopo medido. A prova é o DoD end-to-end com as duas árvores no ar ao mesmo tempo, que é exatamente o que a P-03 dizia não existir: `/up` 200 em `:8080` e `:8081`, volumes `lotus_lotus-db` e `lotus-infra_lotus-db` lado a lado, login Sanctum ponta a ponta em `:8081` (csrf 204, login 200, `/api/me` 200 com o admin do seed), objeto no MinIO existindo em `:9002` e ausente no do main tree, e Vite servindo `VITE_API_URL: http://localhost:8081`. **O review de duas lentes achou o que a medição original não via**: as duas árvores disputavam o mesmo cookie `laravel-session` em `domain=localhost`, então logar na segunda derrubava a sessão da primeira — corrigido com `SESSION_COOKIE` derivado do offset (`lotus_session_8081`), e as duas sessões passaram a coexistir no mesmo jar. Q-1 a Q-4 corrigidos (`??` → `||` porque o Compose cai no default também com variável **vazia**; receita de árvore nova no `.env.example`). Fecha a **P-03**; abre a **P-55** (a invariante do espelho proíbe o que toda lane precisa fazer) e a **P-56** (o `XSRF-TOKEN` não é isolado entre árvores — 419 medido). | `docs/superpowers/plans/archive/2026-08-24-compose-por-worktree.md` · `docs/superpowers/specs/archive/2026-08-24-compose-por-worktree-design.md` · `docs/superpowers/audits/2026-08-24-compose-por-worktree-dod.md` · sem context packet (fonte interna: a própria ficha) · `469dcf57`..`f514f596` na `infra/compose-por-worktree` |
| 2026-08-24 | Frontend · coluna de ações presa e largura de coluna por política nas 15 tabelas | Entregue | Item 17 da fila, promovido explicitamente pelo João em 2026-08-24 com a lane-c em `idle`; `Contexto: não`, sem packet. Uma peça em `shared/ui/AppDataTable/columnWidth.ts` publica o vocabulário de PESOS por classe de conteúdo (`COL`) e o normalizador `tableWidths(pesos, { actions, archived })` sobre três orçamentos — 90% (com ação), 66% (arquivada) e 100% (sem ação); cada tela ganhou um `<entidade>Columns.ts` que classifica as próprias colunas, e a coluna de ação segue em `rem` via `stickyActionsColumn`, porque é a única que não deve escalar. `table-layout: fixed` ligado no `AppDataTable` — sem ele a largura declarada era sugestão. 13 arquivos `*Columns.ts` novos, 10 tabelas passaram a prender a ação (só `TurmasTable` e `EnrollmentTable` já prendiam) e o par do rastreio de arquivados virou `ARCHIVED_COLUMN` fixo em 10%/14%. Duas catracas de `no-restricted-syntax` fecham o bloco: coluna sem largura declarada e coluna de ação sem âncora. **Prova na tela, não no diff:** 12 tabelas × 3 viewports (1440x900, 1024x768, 390x844) com a ação dentro da moldura, 7 visões arquivadas com o par igual, 3 tabelas sem ação sobre o orçamento cheio. Review classificou BAIXO RISCO (frontend puro) e devolveu 4 achados de esforço P, os quatro aprovados pelo João e aplicados: léxico único `<entidade>Widths` sempre função, `archivedEnrollmentWidths(!registroBloqueado)`, docblocks trocando LEI por RAZÃO e a catraca exigindo largura em vez da mera presença de `style`. Gate: lint 0, build verde, 101 arquivos / 561 testes. | Plano [`plans/archive/2026-08-24-tabelas-coluna-de-acoes-e-largura.md`](../plans/archive/2026-08-24-tabelas-coluna-de-acoes-e-largura.md) · spec [`specs/archive/2026-08-24-tabelas-coluna-de-acoes-e-largura-design.md`](../specs/archive/2026-08-24-tabelas-coluna-de-acoes-e-largura-design.md) · medição [`audits/2026-08-24-tabelas-coluna-de-acoes-e-largura-medicoes.md`](../audits/2026-08-24-tabelas-coluna-de-acoes-e-largura-medicoes.md) · packet: não · commits `ff5c29f6`..`e25db109` |
| 2026-08-24 | Certificação · histórico do aluno no detalhe (coluna de certificado na tabela de turmas) | Entregue | Item 2 da fila, promovido explicitamente pelo João, na `lane-a` e no main tree (gate P-03), com Context Packet `partial` (Figma indisponível, declarado — nenhuma afirmação de fidelidade ao protótipo). **A regra de "vigente" saiu do React e virou dono único no backend:** `CertificateDisplayStatus` (quatro valores, janela de 30 dias, fuso `America/Santiago` declarado porque `config/app.php` fixa `UTC` literal) viaja nos DTOs de certificado, e a derivação local do `/certificados` morreu — duas implementações de vigência num documento de peso legal eram respostas esperando para divergir. **Correção de rota contra a spec §4, medida e registrada no plano:** quem cruza a fronteira de domínio é um VO em `Certification\Services\StudentCertificateSummary`, não um DTO em `Data\` — a Regra A do `DomainDependencyTest` reprova camada interna antes de consultar a matriz (zero referências cross-domain a `…\Data\…` no repositório inteiro), e o precedente é o `AcademicResult`. `StudentCertificateHistory` resolve N matrículas em **uma** query; o `show` do aluno não ganha query por matrícula (medido na base real: 10 queries para 1 e para 2 matrículas). No frontend três peças subiram para `shared/` (mutation do PDF, `useBlobTabOpener`, mapa estado→severidade), e a tabela de turmas ganhou a quinta coluna com os quatro ramos, marca de reemissão (`+N ant.`) e PDF pela própria célula. **A Task 8 (DoD no navegador) pagou o próprio custo:** achou o que suíte, build e lint não viam — sob `React.StrictMode` o `useBlobTabOpener` deixava a trava de unmount armada após a remontagem e o PDF abria em `about:blank`, inclusive no `/certificados` que já estava pronto (`bec9c2e8`). Review de risco **ALTO** (toca `generated.ts`, DTO de documento legal, fronteira de domínio e RBAC) com o gabarito do projeto **e** revisão independente do Codex: três achados 🟢, todos aprovados e corrigidos — revalidação no foco da janela para o `display_status` congelado na virada da meia-noite (`5b91bc48`), fim do default silencioso em `StudentTurmaData::fromModel` (`80506ed9`) e catraca dos avisos do PDF (`7b64cfd8`). Divergência de revisores registrada e **não** alterada: a coluna expõe dados sob `identity.user.view` enquanto o PDF exige `certification.certificate.view` — decisão da spec (D11 e §7, "consequência aceita"). **P-15 encerrada** (a decisão que ela esperava saiu: certificados no **detalhe**; a coluna da listagem fica fora **por escrito**, spec §9), **P-59** e **P-60** abertas (nasceram `P-55`/`P-56` nesta branch e foram renumeradas no merge da `main`, que já usava os dois números). Gate de fechamento: backend **937 passed / 5 skipped**, frontend **105 arquivos / 577 testes**, lint limpo, build verde, Pint `passed` nos 18 arquivos do bloco, `generated.ts` em sincronia, e o DoD reprovado contra a API real de novo depois das correções — payload dos quatro ramos, PDF conferido com `pdfinfo`/`pdftoppm` (LOT-2026-1007, Catalina Bustamante), reemissão com `+1 ant.`, validação pública do QR devolvendo `revocado`. | Plano [`plans/archive/2026-08-24-certificacao-historico-do-aluno.md`](../plans/archive/2026-08-24-certificacao-historico-do-aluno.md) · spec [`specs/archive/2026-08-24-certificacao-historico-do-aluno-design.md`](../specs/archive/2026-08-24-certificacao-historico-do-aluno-design.md) · packet [`context-packets/2026-08-24-certificacao-historico-do-aluno.md`](../context-packets/2026-08-24-certificacao-historico-do-aluno.md) · commits `25b26c84`…`dcb7ed30` (`4761e3eb`, `f2cf1588`, `b168cf0a`, `3f403f4a`, `6c06109c`, `8f0e4d07`, `fff2412c`, `d62883a3`, `a867564f`, `e8bb46ef`, `fb3a073a`, `bec9c2e8`, `5b91bc48`, `80506ed9`, `7b64cfd8`) |
| 2026-08-25 | Frontend · revisão de UI por módulo, fatia 2 de 2: Comercial e Certificados, mais a `D-57` (`frontend-revisao-ui-por-modulo-f2`) | Entregue (fatia) | **Item 16 da fila, `lane-c`, worktree `../fix-frontend`, executor `claude`**, sem Context Packet — o bloco nasceu de medição local (`audits/` + fichas `D-*` da fatia 1). Árvore em **offset +2** (`:8082` API, `:5175` Vite), stack própria pelo `.env` da raiz — o gate P-03 não foi disparado. **Duas runs de `/lotus-ui-review` com relatório datado**: Comercial (3 achados, 1 `C` corrigido no wrapper `AppCardToolbar`, 1 `B` na tela, 1 ficha) e Certificados (4 achados, todos `B`, 2 corrigidos e 2 fichas) — **nenhum `C` aberto**. **A `D-57` foi paga inteira**: `TurmaDocumentType` atravessa a cadeia da RN-16 do `TurmaHabilitacaoService` aos DTOs e os **quatro** campos (`missing_types` ×2, `missing_document_types`, `present_types`) tipam o enum no `generated.ts`; o `turmaDocumentTypeLabel` perdeu o fallback de código cru, e agora é o `tsc` que barra string fora do enum. **Os Minors 2, 3 e 5 herdados do review da fatia 1** entraram junto (hover alcançando a coluna presa, sombra de rolagem, slot `actions` do `DetailHeader`). **Réguas de aba medidas e nenhuma tocada** — `[1134, 1134, false]` em 1440x900 e `[276, 276, false]` em 390x844 nas duas telas. **Seis fichas no `backlog.md`**: decisão da `D-38`, baixa da `D-57` e `D-58` a `D-62`. **Review (ALTO risco, duas lentes — Claude + Codex `read-only`): 3 achados, os três aprovados e pagos em `7e563910`**, cada um com guarda vista reprovar por mutação antes de entrar — `tags`/`actions` como itens flex irmãos sob `sm:contents` (Q-1), dois testes de borda HTTP no `DashboardEndpointTest` (Q-2) e a catraca do hover do tema em `tests/brand-theme.test.ts` (Q-3). **Fechamento provado contra a API real**: `present_types: ["MANUAL","PRUEBAS"]` e `missing_types: ["EVALUACION_REDATOR"]` em `GET /api/dashboard/metricas`, `missing_document_types: ["PRUEBAS","EVALUACION_REDATOR"]` em `GET /api/turmas/{id}`; e o Q-1 **medido na tela** em `/comercial/presupuestos/1` a 1440x900 (título `188/220`, tag `195/221` na linha de base, ações `196/244` centradas por conta própria), que era exatamente o que a execução tinha deixado sem medir. Gate: lint 0, build verde, **109 arquivos / 606 testes**, backend **940 passed / 5 skipped**, `pint --test` `passed` nos 11 PHP da branch, `typescript:transform` sem drift. **O item 16 continua na fila**: Cursos, Pessoas e Administração seguem sem run. | Plano `plans/archive/2026-08-25-frontend-revisao-ui-por-modulo-f2.md` · spec `specs/archive/2026-08-25-frontend-revisao-ui-por-modulo-f2-design.md` · packet `null` · `audits/2026-08-25-lotus-ui-review-comercial.md` e `audits/2026-08-25-lotus-ui-review-certificados.md` · commits `86d28227`, `7e563910`, `8d588511` · branch `refactor/frontend-revisao-ui-f2` (sem merge) |
| 2026-08-25 | Backend · hardening de API, arquivos e abuso: throttle nomeado, política única de arquivo e antivírus síncrono (`hardening-api-arquivos-e-abuso`) | Entregue | **Item 4 da fila, `lane-a`, main tree, executor `claude`**, a partir de `main@7fa1cb0a`, nascido em `context_required` com packet do Codex `status: ready` (quatro fontes, provenance conferida com `git hash-object`). Doze tasks. `Shared/RateLimiting/RateLimits` publica os limitadores nomeados e o grupo `api` ganha teto global, para rota nova nascer coberta; a chave do login é `email|ip`; o `ProblemDetails` passou a repassar `Retry-After`/`X-RateLimit-*` da própria exceção. `Shared/Files/ContentClass` virou a peça única de tipo/tamanho consumida pelos 13 sítios de upload, com o ClamAV embutido como regra de validação (`ScannedForMalware`) e scanner fora do ar **recusando** o upload (`503`), nunca deixando passar. O `X-Forwarded-*` de entrada é apagado no nginx — ligar `trustProxies` teria feito o IP do limitador ser escolhido pelo atacante, medição que **desmentiu a spec** e virou emenda datada (§9). `files.mime` corrigido por migration de backfill que **aborta** quando nenhum objeto pode ser lido. Tetos: `MAX_ITENS=200` no lote de certificados e `MAX_LINHAS=100` no import — o 500 do plano caiu para 100 depois que 500 linhas estouraram o `max_execution_time` (bcrypt ~185 ms/linha), exercendo a autoridade que o próprio plano dava. Review com segunda lente do Codex: sete achados (Q-1 a Q-7), os sete corrigidos com teste de regressão visto reprovar antes de valer — entre eles o teto do conjunto (`TETO_AGREGADO_KB`) no único sítio que recebe vários arquivos, o teto do import mordendo na linha **iterada** e o leitor de planilha despachando pelo MIME de **conteúdo**. Um achado virou ficha (**P-61**, `title` do `ProblemDetails` em português). Gate: **996 passed / 5 skipped**, lint 0, build verde, Pint nos 49 arquivos, `generated.ts` idêntico ao da `main` (`ContentClass` recebeu `#[Hidden]`). DoD reprovado contra a API real no fechamento, depois das correções: `429` com `Retry-After` e chave separada, QR público recuperando em 62 s, `503` com o ClamAV parado e `201` com ele de volta, ELF renomeado recusado, conjunto de 10,76 MB recusado, import de 101 linhas e de 300 linhas em branco recusados, CSV com nome `.xlsx` lido pelo `CsvReader` sem `500`. | Plano [`plans/archive/2026-08-25-hardening-api-arquivos-e-abuso.md`](../plans/archive/2026-08-25-hardening-api-arquivos-e-abuso.md) · spec [`specs/archive/2026-08-25-hardening-api-arquivos-e-abuso-design.md`](../specs/archive/2026-08-25-hardening-api-arquivos-e-abuso-design.md) · packet [`context-packets/2026-08-24-hardening-api-arquivos-e-abuso.md`](../context-packets/2026-08-24-hardening-api-arquivos-e-abuso.md) · commits `540cb522`…`4dd5075a` (26 commits) |
| 2026-08-26 | CI/CD · governança de `main` e artefato imutável por SHA (`cicd-ci-governanca-e-artefato`) | Entregue | **Item 11 da fila, `lane-b`, worktree `../lotus-infra`**, com Context Packet do Codex. Um `.github/workflows/ci.yml` dono-agnóstico: cinco gates de correção (`backend`, `frontend`, `types-drift`, `audit-prod`, `audit-dev` em `continue-on-error`), o job `procedencia` e o job `image`, este só em `push` na `main` e atrás de todos. **Fecha a D-08 e com ela o mecanismo da lei §5.3:** a CI regenera `typescript:transform` e reprova drift do `generated.ts` commitado, provado por sonda que editou o arquivo e viu o gate reprovar nomeando-o. Fonte única da versão do pnpm (`packageManager` no `frontend/package.json`, lido pelo Dockerfile e pela CI), `guzzle`/`commonmark`/`react-router` subidos para versões sem advisory. **O DoD 5 (branch protection) ficou COMPENSADO, não provado** — `Gatika-CL` é plano free e a API responde `403 Upgrade to GitHub Pro`; as duas saídas (pagar, ou abrir o repositório de um cliente regulado) foram recusadas pelo João, e a compensação é em três camadas: `.githooks/pre-push`, o job `procedencia` (commit que chegou em `main` sem PR mesclado não vira imagem) e `scripts/espelhar-corporativo.sh` + `.espelho-exclusoes` (árvore filtrada, um commit por release, trailer `Source-Commit`). Review de risco **ALTO** com gabarito do projeto **e** segunda lente do Codex: cinco achados aprovados e corrigidos — par `app`+`web` construído antes de qualquer push com verificação final dos dois manifestos e `cancel-in-progress` só fora de `push`; as quatro imagens base do `Dockerfile.prod` fixadas por digest mais guarda de idempotência (tag de SHA que existe não se reescreve); `procedencia` conferindo o trailer contra o histórico de `main` da origem (`ESPELHO_FONTE`); e o espelho lendo a lista de exclusões do blob do commit e recusando commit cujo CI não esteja verde. Gate de fechamento: backend **937 passed / 5 skipped**, frontend lint 0 e build verde, Pint `passed` nos 3 arquivos PHP do bloco, e o critério de aceite remedido no gatilho real sobre `5e5a661f` — run `32902834430` `success`, `image` com os 11 passos verdes e o par no GHCR (`lotus-app` `sha256:ea03ba10…`, `lotus-web` `sha256:75b5b455…`). Abre a **P-62** (a camada 0 que falta enquanto não houver GitHub Team; nasceu `P-61` e foi renumerada no merge da `main`). | Plano [`plans/archive/2026-08-24-cicd-ci-governanca-e-artefato.md`](../plans/archive/2026-08-24-cicd-ci-governanca-e-artefato.md) · spec [`specs/archive/2026-08-24-cicd-ci-governanca-e-artefato-design.md`](../specs/archive/2026-08-24-cicd-ci-governanca-e-artefato-design.md) · packet [`context-packets/2026-08-24-cicd-ci-governanca-e-artefato.md`](../context-packets/2026-08-24-cicd-ci-governanca-e-artefato.md) · evidências [`audits/2026-08-24-cicd-evidencias.md`](../audits/2026-08-24-cicd-evidencias.md) · PRs #71 (`7fa1cb0a`), #74 (`24c2105d`), #75 (`ac078a80`) e #77 (`5e5a661f`) |
| 2026-08-27 | Frontend · hardening final: rótulo do rail, foco do olho da senha, ban de PrimeReact em `src/app`, mini-reset escopado e o contrato do `IdentityCell` (`frontend-hardening-final`) | Entregue | **Item 8 da fila, `lane-c`, worktree `../fix-frontend`, executor `claude`**, sem Context Packet — as cinco fontes (`D-03`, `D-33`, `D-35`, `P-46` e a herança da `P-41`) são medição local e as fichas vivem no repositório. Árvore em **offset +2** (`:8082` API, `:5175` Vite); o bloco não toca `backend/`, então o gate P-03 não foi disparado e `pint`/`typescript:transform` são N/A por escopo (`git diff --stat main...HEAD -- backend/ generated.ts` **vazio**). **As cinco tasks entregues**: `D-35` (ban de `primereact` em `src/app/**` no `no-restricted-imports`, régua nascendo verde), `D-03` (o rótulo do módulo sempre no DOM, empilhado sob o ícone no rail de 80px), `D-33` (foco devolvido ao olho depois de alternar a máscara — e, no DoD, **também pelo teclado**: o Prime tem `onKeyDown` separado do `onClick`, que a primeira correção não alcançava), `P-46` (mini-reset escopado em `@layer base` no lugar das três grafias caso a caso, com catraca em `tests/preflight-escopado.test.ts`) e a herança da `P-41` (o teste do `IdentityCell` passa a guardar o par `truncate` + `min-w-0`). **DoD provado no navegador** (Chromium real, stack desta árvore, login `admin@lotus.cl`, `es-CL`), não em jsdom: sete rótulos legíveis a 390×844 com `<nav>` de 768px contra 844px de viewport, foco no `<svg>` do olho depois do Enter, `AppCardHeader` em 49px de faixa para 24px de texto (era 80px), as quatro famílias de lista sem marcador, e o `IdentityCell` cortando (118×102) com **sonda negativa** — sem `min-w-0` os dois empatam em 118. **Review: 7 achados, os 7 aprovados e pagos** — a catraca do mini-reset lendo o CSS inteiro e não só o primeiro `@layer base` (Q-1, com duas sondas vistas reprovar), `title` só colapsado para o leitor de tela não anunciar nome e descrição (Q-2), docblock do `KpiRow` corrigido (Q-3), a tabela `Ocupação corrente` do `state.md` realinhada ao frontmatter (Q-4), `mergePt` encadeando chave `on[A-Z]` em vez de substituir — terceira reincidência da família, virou regra na rule (Q-5), `role="list"` nas 16 listas com régua de lint, porque o WebKit tira a semântica junto do marcador (Q-6) e o marcador de volta nas duas listas de texto puro (Q-7). **Fechamento: DoD refeito contra o código final**, porque quatro dos sete achados mexem no que ele mede — os números acima são desse regate, mais a sonda de cascata que mostra o `list-disc` vencendo o mini-reset. Gate: lint 0, build verde, **111 arquivos / 622 testes**, backend **940 passed / 5 skipped**. **P-46 encerrada e remedida**; nasceram a **P-63** (o `role="list"` não alcança lista de biblioteca — as 2 legendas do Recharts) e o **item 18** da fila (padronização de estilização, que depende deste bloco). As fichas `D-03`, `D-33` e `D-35` saíram do registro canônico. | Plano `plans/archive/2026-08-26-frontend-hardening-final.md` · spec `specs/archive/2026-08-26-frontend-hardening-final-design.md` · packet `null` · `audits/2026-08-26-frontend-hardening-final-medicoes.md` e `audits/2026-08-26-estilizacao-componentes.md` · commits `ea16e768`, `4187d29b`, `a685fe64`, `4a27272c`, `e560df27`, `cde9ba4b`, `34719e33`, `922fe723` · branch `refactor/frontend-hardening-final` · **PR #80** |
