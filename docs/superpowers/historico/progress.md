# Progresso — Lotus v2

> Histórico curto do que foi entregue e da feature em andamento. Este arquivo **não define a etapa
> operacional**. Para saber o que fazer agora, leia `docs/superpowers/state.md`.
> Fila futura: `docs/superpowers/backlog.md`. Histórico antigo: `progress-archive.md`. Narrativa
> integral de cada bloco fechado (o que era a parte de baixo do `state.md`): `state-archive.md`.
>
> **Cinco colunas, de propósito (P-23, decidido em 2026-08-22).** O formato antigo tinha sete, com
> `Contexto`/`Plano`/`Spec` separadas; aqui plano, spec, context packet e commits vivem juntos na
> coluna `Referências`. A coluna `Contexto` **não volta**: só uma parte das entregas nasce de packet,
> e a coluna separada era célula vazia na maioria das linhas. O `progress-archive.md` registra do
> lado dele a mesma convivência de duas arities — as linhas migram **verbatim**, porque o fechamento
> move histórico e não o reescreve.

| Data | Entrega | Status | Resultado | Referências |
|---|---|---|---|---|
| 2026-09-01 | Frontend · as seis decisões de UI travadas saem com veredito escrito e código aplicado, e a `P-67` fecha atrás de catraca (`frontend-decisoes-de-ui-pendentes`) | Entregue | **Item 21 da fila, `lane-a`, main tree, executor `claude`**, branch aberta de `main@a73e83e6`; `Contexto: não` — as fontes são as próprias fichas e os audits locais. **Nenhuma ficha saiu "decidida e não aplicada":** `D-66` (raio), `D-68` (borda de controle), `D-63` (heading), `D-64` (separador do KPI), `D-67` (eco do código no `notFound`) e `D-32` (`order-*` do `/perfil`) têm veredito **e** commit. **O eixo é a `D-66`:** dois tokens de raio nascem no `@theme` de `index.css` (`--radius-surface`, e `--radius-control` lendo o `--border-radius` do tema do PrimeReact), `shared/ui` os consome, os **15** sítios de `features/`+`app/` migram atrás (eram 10 na ficha — o planejamento remediu o alcance, e os cinco a mais já escreviam `rounded-lg`/`rounded-md`) e só então a catraca `RAIO_LITERAL` liga, **verde e vista reprovar por sonda** com o arquivo restaurado do scratchpad, nunca por `git stash`. A `D-68` virou regra de **FORMA** no gerador de tema — não troca no mapa de hex —, e o brainstorming mediu que ela alcança 21 bordas de controle e nenhuma borda de card ou divisor: `#cbd5e1` (1,48:1) sai para `#64748b` (4,76:1) só no claro, com os 4 `background` e as 2 declarações de rampa intactos. **Três fichas foram remedidas contra o código antes de decidir, e uma saiu do bloco:** a `D-65` descrevia uma reserva de `8rem` fixa e são sete valores nas 12 tabelas — virou o **item 23** da fila, reescrita com os valores medidos. **Prova na tela, não no diff:** `h1` de `/validar` de 18px para 30px (mesmo degrau do folio) nas três viewports sem overflow; o `·` do KPI visível entre os algarismos nos seis cards, sem terceira linha; o `scrollTop` do `Tab` em `/perfil` **monotônico** abaixo de `xl` (antes: `0 → 1379 → 1558 → 0`); o eco do código quebrando dentro da caixa em vez de vazar 230px sob o `overflow-hidden` do `AppCard`. **Dois desvios medidos mudaram o remédio:** `whitespace-nowrap` anula `break-all`, então o eco trocou `identifierClass` por `technicalDataClass`, e o bloco inline estourou `max-lines`, extraindo `StatusHeading.tsx` e `NotFoundCard.tsx`. **Review de risco BAIXO, uma lente, 4 achados, os quatro aprovados pelo João e corrigidos:** o separador do KPI ia parar no meio do card de `sm` para cima (`sm:justify-between` sobre TRÊS filhos — o audit medira contagem de linha e altura, não a posição x do ponto); `--radius-control` sem fallback derrubaria o raio de todo controle para 0 em silêncio, porque `var()` órfã invalida a declaração e essa folha entra por `<link>` de runtime; a catraca não alcançava `rounded-t-lg`/`rounded-bl-md`/`rounded-[6px]`, que é a porta por onde a P-67 volta; e o eco cortava em 64 char sem reticência, e código truncado que parece completo é pior que eco nenhum numa página de peso legal. **Zero órfão:** os 12 `rounded` que sobram em `features/` são prova booleana do `AppButton`. **Ressalva registrada, sem devolver ficha:** em `xl` (1440x900) o `Tab` ainda retorna 513px uma vez — dentro do critério (a) do plano e 8% fora do (b) (1,63 dobras contra "uma e meia"). Gate de fechamento: backend **1149 passed / 5 skipped** (8930 asserções), frontend **125 arquivos / 712 testes**, `pnpm lint` **0**, `pnpm build` verde, `generated.ts` com diff **vazio** (`typescript:transform` re-rodado no fechamento), Pint **N/A por escopo medido** (zero `.php` no diff), a catraca vista reprovar as **três** formas por sonda e restaurada, e o bundle com `--radius-control:var(--border-radius,4px)`. **Fecha a `P-67`** (por mecanismo) e **abre a `D-69`** (4 sítios de utility de paleta, DoD mecanizado: `CATRACA_COR` chegar a `[]`), a **`D-70`** (`/validar` orienta a contatar a Lotus sem canal — decisão da Lotus) e o **item 23** da fila. | Plano [`plans/archive/2026-08-31-frontend-decisoes-de-ui-pendentes.md`](../plans/archive/2026-08-31-frontend-decisoes-de-ui-pendentes.md) · spec [`specs/archive/2026-08-31-frontend-decisoes-de-ui-pendentes-design.md`](../specs/archive/2026-08-31-frontend-decisoes-de-ui-pendentes-design.md) · medições [`audits/2026-08-31-decisoes-de-ui.md`](../audits/2026-08-31-decisoes-de-ui.md) · packet: não · commits `97661217`..`1ddcd557` mais o de fechamento |
| 2026-09-02 | Frontend · o campo de formulário liga no form: `useFormField` devolve um `Field` tipado que substitui quatro props por `name` (`frontend-campo-de-formulario-liga-no-form`) | Entregue | **Sem ficha na fila** (registrado como "item 24" no fechamento; o `24` do backlog é o `backend-projecao-de-arquivados` — rótulo corrigido em 2026-09-02 por decisão do João), **`lane-c`, worktree `../fix-frontend`, executor `claude`**, sem Context Packet — as fontes são o §3 do review de arquitetura de 2026-09-01 e o próprio código. Árvore em **offset +2** (`:8082` API, `:5175` Vite), a partir de `main@8efd85f2`. O `FieldContext` passou a carregar `bind` (`value` + `onChange`) além da acessibilidade que já publicava, os **5 wrappers** (`AppInputText`, `AppPassword`, `AppTextarea`, `AppDropdown`, `AppDatePicker`) pescam valor e setter dele, e o `useFormField(bundle)` devolve um `Field` de **identidade estável** (`useRef`, não `useMemo`) tipado em `keyof T` que desce como UMA prop no lugar de `label`/`error`/`readOnly`/`value`. **35 sítios migrados em 13 arquivos** de `commercial`, `identity`, `catalog` e `operation`; as **48** extrações `fieldErrors?.x?.[0]` caíram para **22**, todas nos arquivos que a spec §2 declarou fora de escopo (campo aninhado, diálogo sem bundle de form, login/senha). Nenhum teste existente foi editado — os 21 `<FormField` dos testes atuais passando sem diff é o que prova que o contrato do `FormField` não mudou. Catraca nova `ERRO_DE_CAMPO_A_MAO` (`no-restricted-syntax`), com `FORA_DO_CAMPO_LIGADO` particionando o mesmo glob, vista reprovar por sonda negativa (arquivo restaurado do scratchpad, nunca por `git stash`). **Review: 2 achados, os 2 aplicados** — a catraca media só uma grafia e passava limpo pela do próprio `TurmaConfigCard` (Q-1), e o hook passou a devolver `{ Field }` para que `<campo.Field>` dispensasse os 26 `eslint-disable` de `react-hooks/static-components` sem desligar a regra em arquivo nenhum (Q-2). **DoD provado no navegador contra a API real, e refeito no fechamento pós-Q-2**: 3 teclas no RUT sem trocar o nó focado (`data-sonda` + `id` `_r_1_`), `view` mostrando `Type` → "Client" traduzido, 422 real pintando "Este RUT já está cadastrado." sob a label com `aria-invalid="true"`, e **0 warnings** de controlled/uncontrolled no console; duas tentativas de POST, as duas recusadas com 422, `4` clientes antes e depois. Gate: backend **1149 passed / 5 skipped**, frontend **127 arquivos / 734 testes**, lint 0, build verde; `pint` e `typescript:transform` **N/A por escopo, provados** (`git diff --stat main...HEAD -- backend/ generated.ts` vazio). Nenhuma pendência nova; a **P-67** saiu de vez do rastro neste fechamento. | Plano [`plans/archive/2026-09-02-frontend-campo-de-formulario-liga-no-form.md`](../plans/archive/2026-09-02-frontend-campo-de-formulario-liga-no-form.md) · spec [`specs/archive/2026-09-02-frontend-campo-de-formulario-liga-no-form-design.md`](../specs/archive/2026-09-02-frontend-campo-de-formulario-liga-no-form-design.md) · packet `null` · prova [`audits/2026-09-02-item24-prova-de-navegador.md`](../audits/2026-09-02-item24-prova-de-navegador.md) · commits `75edf196`…`92774221` (19) · branch `refactor/frontend-campo-de-formulario-liga-no-form` · **PR #95** (rebasada sobre `origin/main@4bea7d27`) |
| 2026-09-02 | Backend · projeção de arquivados: a montagem da listagem existe uma vez e o carimbo do 200 existe num lugar só (`backend-projecao-de-arquivados`) | Entregue | **Item 24 da fila, `lane-a`, main tree, executor `claude`**, a partir de `main@14b25b6c`, `Contexto: não` — a fonte única é a revisão de arquitetura de 2026-09-02 (`audits/2026-09-02-arquitetura-deepening.html`, candidato 1), versionada em `main` junto com o próprio item. Duas peças novas em `app/Shared/`: `Audit/ArchivedListing` (`lista()` projeta a coleção que a query do agregado já materializou; `resolveArquivado()` absorve o `firstOrFail` e o comentário copiado 7×) e `Http/RespostaDeRecurso::ok()`, que desfaz num lugar só o 201 que o `ResponsableData` força em POST. Os oito `archived()` e os oito `restore()` passaram a consumir o module, mais os seis sítios de 200 que não são restore (`approve`, `reject`, `import`, `designateRedator`, `conclude`, `revoke`). **Nenhuma Action, DTO, rota ou query de agregado se moveu** — `generated.ts` com diff vazio e os 70 testes de endpoint de arquivamento passando sem edição, que é o que prova o contrato intacto. Duas catracas estáticas em `tests/Unit/Shared/ProjecaoDeArquivadosTest.php` fecham as portas, vistas reprovar por sonda no scratchpad (nunca `git stash`). **Review de alto risco com segunda lente do Codex: o código de produção passou sem achado**; os cinco achados foram de qualidade de teste/catraca e os cinco foram aprovados e pagos — cobertura fantasma provada por sonda (Q-1), catracas que escapavam por alias/FQN/`HTTP_OK`/argumento nomeado (Q-2), isenção por basename em vez de pasta (Q-3), reúso do `Tests\Support\ScansPhpSource` em vez de stripper por `preg_replace` (Q-4) e `lista()` estourando `InvalidArgumentException` nomeada em registro sem `deleted_at`, em vez de `toIso8601String() on null` (Q-5, com linha nova em `DEBITO_CONHECIDO`). **Gate:** backend **1162 passed / 5 skipped** (a `main` mede 1149), lint 0, build verde, Pint `passed` nos 15 arquivos, `frontend/` sem diff; DoD 8 zerado em código; critério de aceite provado contra a API real — `DELETE /api/clients/200` 204, `GET /api/clients/archived` com `archived_at` ISO e `archived_by` `"Admin Lotus"`, `POST /api/clients/200/restore` **200**, banco de dev devolvido. Nenhuma pendência nasceu ou fechou. | Plano [`plans/archive/2026-09-02-backend-projecao-de-arquivados.md`](../plans/archive/2026-09-02-backend-projecao-de-arquivados.md) · spec [`specs/archive/2026-09-02-backend-projecao-de-arquivados-design.md`](../specs/archive/2026-09-02-backend-projecao-de-arquivados-design.md) · packet `null` · fonte [`audits/2026-09-02-arquitetura-deepening.html`](../audits/2026-09-02-arquitetura-deepening.html) · commits `b4f9fdce`…`ba1a50fa` (7) mais o de fechamento · branch `refactor/backend-projecao-de-arquivados` |
| 2026-09-03 | Frontend · as seis dívidas que se provam por mecanismo: desmonte global de teste, catraca de cor a zero, `detail` do servidor na tela, warning na família amarela e duas decisões escritas (`frontend-dividas-de-mecanismo`) | Entregue | **Item 25 da fila, `lane-c`, worktree `../fix-frontend`, executor `claude`**, branch aberta de `3654b6dc` e rebasada sobre `origin/main@4a0080ce` no fechamento; `Contexto: não` — as seis fontes (`P-68`, `P-69`, `P-70`, `P-30`, `P-42`, `D-69`) são fichas do próprio repositório, e **três delas contradisseram o próprio texto quando remedidas contra `main@5f6daf8b`**: a variável de perigo da `D-69` já existia, a superfície da `P-30` é um botão (reprovando a 2,80:1) e o custo que travava a `P-69` mediu **zero**. Oito tasks, nenhuma abrindo navegador. **As seis fichas fecharam, cada uma por mecanismo verde ou decisão escrita:** `setupFiles` com `cleanup()` global mais as catracas `CLEANUP_A_MAO` (31 erros vistos reprovar) e a guarda estática do `desmonte-global.test.ts` (P-69); `CATRACA_COR` a `[]` com a partição removida do `eslint.config.js` (D-69); allowlist fechada `DETALHE_LOCALIZADO = {403, 404, 429}` no `screenDetail`, provada em unidade e com o texto na tela em jsdom (P-70); warning alinhado ao amarelo do `AppTag` por transformação de forma no gerador de tema, com régua de contraste medindo 4,55/5,12/5,82/5,70 onde antes reprovava (P-30); razão da assimetria de `max-lines` escrita, sem mudança de comportamento (P-68); emenda datada ao D1 da spec arquivada do `IdentityCell`, código intocado (P-42). Review de 2026-09-03: **7 achados, todos aprovados e corrigidos** (`2148b882`), entre eles a remoção do `const CATRACA_COR = []` que ninguém lia. Fechamento verde: `pnpm lint` 0, `pnpm build`, `pnpm test` **128 arquivos / 759 testes** terminando, suíte backend **1162 passed / 5 skipped**, e `git diff main...HEAD -- backend/ generated.ts` **vazio** — `pint` e `typescript:transform` N/A por escopo, provado. Abre a **P-74** (a família inteira de botão de severidade reprova AA no estado base do claro). | Plano [`plans/archive/2026-09-02-frontend-dividas-de-mecanismo.md`](../plans/archive/2026-09-02-frontend-dividas-de-mecanismo.md) · spec [`specs/archive/2026-09-02-frontend-dividas-de-mecanismo-design.md`](../specs/archive/2026-09-02-frontend-dividas-de-mecanismo-design.md) · packet `null` · commits `6a51391e`…`2148b882` (12) mais o de fechamento · branch `fix/frontend-dividas-de-mecanismo` |
| 2026-09-03 | Backend · envelope de erro e recusa de domínio: a exceção declara o TIPO da recusa e o `ProblemDetails` traduz tipo em status, título e frase localizada (`backend-envelope-de-erro-e-recusa-de-dominio`) | Entregue | **Item 26 da fila, `lane-a`, main tree, executor `claude`**, a partir de `main@4a0080ce`, `Contexto: não` — as fontes são as fichas de `pendencias/`, o review de arquitetura de 2026-09-02 (candidato 6) e o próprio código. Duas peças novas em `app/Shared/Exceptions/`: o enum `TipoDeRecusa` (`RegraDeNegocio` 422, `AcaoProibida` 403, cada caso dono do par status/título/URI) e a base abstrata `RecusaDeDominio`, que implementa `PublicDetail` e obriga `tipo()`. As quatro exceções de domínio **pararam de estender `HttpException`**, o `isForbidden()` que farejava o status de volta foi podado, e o `RegistraEventoDeErro` passou a consultar o mesmo mapa. **Nenhum DTO mudou** — `generated.ts` com diff vazio — e os testes de endpoint que afirmam 422/403 passaram intactos, que é o que prova o contrato HTTP preservado. As cinco recusas literais saíram para `lang/` nos três locales (a `P-71`), o 419 ganhou `problem.detail.csrf` (a `P-72`) e a `P-60` fechou a metade de comportamento **por decisão escrita** (D4: a rota pública do QR continua estourando 500; documento de peso legal não atesta o que não sabe). **Review de 2026-09-03: sete achados, os sete pagos.** Os dois graves eram invisíveis à suíte — o Q-1, em que sair de `HttpException` derrubou EM SILÊNCIO a carona no `$internalDontReport` do Laravel e toda recusa esperada passou a gravar `error` + stack trace (fechado com `dontReport(RecusaDeDominio::class)` e a catraca behavioral `RecusaNaoVaiAoLogTest`, que guarda a contra-prova do snapshot corrompido continuar indo ao log), e o Q-7, em que as seis recusas de `CertificateEligibility::refuse()` atravessaram literais em es-CL o bloco de i18n que traduziu as outras 41 — o `withMessages` recebia variáveis e o detector lia janela. O Q-2 trocou a régua de `extends HttpException` por `is_subclass_of(HttpExceptionInterface)`; o Q-3 destapou o `pint lang/` acidental (1.353 linhas de reformatação sem semântica, revertidas no fechamento) e nasceu a rule `.claude/rules/backend-lang.md`; o Q-6 ensinou o detector em vez de declarar dívida falsa. **Gate:** backend **1175 passed / 5 skipped** (a `main` mede 1162), lint 0, build verde, Pint `passed`, `generated.ts` sem diff, `lang/` com 58 linhas reais depois da reversão. Critério de aceite provado contra a API real: 419 com `detail` em três frases distintas nos três locales, 422 `operation.turma.quote_not_approved` e 403 `identity.errors.redator_only_action` em es-CL, e `storage/logs/laravel.log` **com zero linhas** depois das duas recusas. Fecham a `P-71`, a `P-72` e a metade de comportamento da `P-60`; nascem a `P-74` e a `P-75`. | Plano [`plans/archive/2026-09-02-backend-envelope-de-erro-e-recusa-de-dominio.md`](../plans/archive/2026-09-02-backend-envelope-de-erro-e-recusa-de-dominio.md) · spec [`specs/archive/2026-09-02-backend-envelope-de-erro-e-recusa-de-dominio-design.md`](../specs/archive/2026-09-02-backend-envelope-de-erro-e-recusa-de-dominio-design.md) · packet `null` · medições [`audits/2026-09-02-item26-medicoes.md`](../audits/2026-09-02-item26-medicoes.md) · commits `1836574c`…`8dc0fcaf` (11) mais o das correções de review e o de fechamento · branch `refactor/backend-envelope-de-erro-e-recusa-de-dominio` |
| 2026-09-04 | Frontend · a suíte arrumada como mecanismo: ambiente por projeto, home única de montagem de provedor e a catraca que fecha a porta (`frontend-arrumacao-de-testes`) | Entregue | **Item 27 da fila, `lane-c`, worktree `../fix-frontend`, executor `claude`**, a partir de `origin/main@182be2ab`, `Contexto: não` — as fontes são o levantamento de frontend medido contra `main@24bf770c`, a ficha `P-58` e o próprio código. **Nenhuma asserção de comportamento mudou.** O `test.projects` do `vite.config.ts` partiu a suíte em `unit` (jsdom, `src/**`, 118 arquivos) e `repo` (node, `tests/**`, 11 arquivos): os 11 pararam de montar um DOM que não usam e de herdar o `setupFiles` da app, com a guarda do par projeto/ambiente no mesmo commit e vista reprovar. `src/shared/testing/providers.tsx` virou a **única** construção de `QueryClient` em teste, com duas portas — `createWrapper` para os 24 arquivos de `renderHook` e `renderWithProviders` para os 9 de `render` —, client criado por chamada, override que **funde** sobre o default e router só nos 6 que têm rota como sujeito; as **sete grafias** de montagem à mão morreram nos 33 sítios. A catraca `QUERY_CLIENT_A_MAO` fecha a oitava nas **três** camadas (`features/`, `shared/`, `app/`), vista reprovar por sonda negativa em cada uma e restaurada por `cp` do scratchpad. A **`P-58`** fechou junto: o `compose-dev.test.ts` isola os `.env*` das duas raízes que o `vite.config.ts` lê, provado com `frontend/.env` legado posto e retirado (3 falhas antes, 12/12 depois). O achado 4 da ficha — juntar dois pares de teste — foi **recusado com veredito escrito**: a partição é a decisão de `max-lines` que a `P-68` ratificou, e juntar dá 224 linhas contra régua de 150. **Review: cinco achados, os cinco pagos** — o override que substituía em vez de fundir, os dois hooks com query real sem client próprio, a catraca cega para `.ts` de `shared/` e `app/`, a indireção vazia que a migração deixou e a medição do DoD 2, que entrou na spec §3.8 **desmentindo o motivo de tempo**: duas rodadas seguidas variam ~20%, o efeito procurado é menor que o ruído, e o que o bloco entrega é **isolação, não velocidade**. **Gate:** 129 arquivos / **771 testes** verdes (118+11, 613+158), backend **1175 passed / 5 skipped**, lint 0, build verde; `pint` e `typescript:transform` **N/A por escopo provado** — `git diff main...HEAD -- backend/ generated.ts` vazio. | plano `plans/archive/2026-09-03-frontend-arrumacao-de-testes.md` · spec `specs/archive/2026-09-03-frontend-arrumacao-de-testes-design.md` · sem context packet · `b0d8c4f3`, `3ab8c2f4`, `68d7e465`, `8354eb8f`, `0ad7846a`, `e02a8228`, `ab943106`, `32b05097`, `d3680500`, `7a4e2130`, `93b3045e`, `69fe121f`, `47ca848b`, `3e697f97` |
| 2026-09-04 | Backend/Frontend · as quatro decisões travadas de domínio, RBAC e semântica saem com veredito escrito e código aplicado (`dominio-decisoes-de-rbac-e-semantica`) | Entregue | **Item 22 da fila, `lane-a`, main tree (P-03), executor `claude`**, a partir de `main@182be2ab` — o tip da `origin/main` com o item 26 já mesclado (PR #99). `Contexto: não` na fila, então nasceu direto em `ready_for_planning`. As quatro fichas eram `D-09`, `D-10`, `D-11` e `D-16`; a `D-34` ficou **fora por escrito** e segue sem hospedeiro. **`D-10` — o índice de roles deixa de ser porta de enumeração:** `GET /api/roles` sobe para `identity.access.manage` e nasce `GET /api/roles/assignable` com `RoleOptionData` (`id` + `name`, sem `permissions` nem `is_system`), consumido pelo select do form de usuário; a aba de Roles só monta sob o gate (`RolesTab.tsx` nasce para isso). Medido na API real: admin comum **403** em `/api/roles` e **200** em `/assignable`, superadmin segue lendo permissão. **`D-11` — o lookup de empresa sai de baixo do gate comercial:** `GET /api/students/client-options` com `StudentClientOptionData` (`id` + `legal_name`) sob `identity.user.*`; role customizada sem `commercial.client.view` **lista o dropdown** e leva **403** em `/api/clients`, provado com fixture criada e removida. **`D-16` — o funil ganha o sétimo balde:** `PipelineStage::ConcludedWithoutIssuance` em posição terminal, partição exclusiva provada por mutação reversível (turma concluída sem aprovada migra de `concluded_pending_issuance` para o balde novo, soma dos sete **inalterada em 507**), rótulo nos três locales. **`D-09` — a camada que cede é o backend:** `PrimaryCollectionService::ensureSingle` vira `ensureExactlyOne`, a regra `UmContatoPrincipal` recusa coleção sem principal (ou com dois) com **422 localizado**, e zero principal **por efeito colateral** promove — `DeleteClientAddressAction` nasce para o lado do endereço. A assimetria da D20 (recusa de entrada só para contato; endereço re-promove em silêncio) está escrita no docblock, não deduzida. **Review de risco alto (RBAC + caminho de escrita auditado): 5 achados, todos aprovados pelo João e corrigidos** em `953ef360`. **Q-2 é regressão do próprio bloco:** a guarda de desmarque lia o contato vindo do **route binding**, isto é, o estado de *antes* do lock — um `DELETE` concorrente do principal fazia o 422 não disparar; passou a reler depois do `lockForWrite()`. **Q-1:** a Task 8 atravessou o STOP do plano — foram **7** edições comportamentais em **dois** arquivos (`PrimaryContactTest` e `PrimaryAddressTest`, este nem citado) onde o plano autorizava **quatro** testes nomeados, e `test_cliente_sem_principal_e_valido` foi apagado sem substituto, deixando o `POST /api/clients` sem prova direta do 422; fechado por dois testes novos em `ContatoPrincipalTest` e errata datada no audit. **Q-5:** nenhum balde do funil pode ser negativo — `fully_issued` nasce de subtração entre dois critérios escritos em lugares diferentes e a soma não pega isso. **Q-4:** a medição de N+1 virou helper único e o terceiro escape inline virou registro declarado (`CENARIOS_ATOR_PROPRIO`). **Gate de fechamento provado contra a API real em `HEAD`, não pela suíte:** `PUT /api/clients/1` sem principal → **422** `El cliente necesita exactamente un contacto principal.`; `PUT /api/contacts/9 {is_primary:false}` (o caminho que o Q-2 mudou **depois** do audit) → **422**, banco de dev intocado; `/api/roles/assignable` sem `permissions`; `/api/students/client-options` com 204 empresas; funil com os **sete** baldes. Placar: **1192 passed / 5 skipped (9195 assertions)** contra `1175/5` da `main` — +17 reconciliados task a task no audit; Pint `passed` nos 21 `.php` do bloco; `generated.ts` regenerado e sem diff residual; `pnpm lint` 0 e `pnpm build` verdes. **Nenhuma pendência nasceu ou fechou** — mas o bloco **disparou o gatilho de duas** sem pagá-las, por decisão do João no gate: a **P-51** (tocou `UpdateClientAction` e `ClientData`, casa do campo 2) e a **P-53** (tocou `.claude/rules/backend-ddd.md` por outro motivo, sem a metade *"e puder reconciliar"*). As duas fichas registram o disparo. As **oito** pendências em rastro desde 2026-09-03 saíram neste fechamento, cumprida a sprint de retenção. | Plano `plans/archive/2026-09-03-dominio-decisoes-de-rbac-e-semantica.md` · spec `specs/archive/2026-09-03-dominio-decisoes-de-rbac-e-semantica-design.md` · audit `audits/2026-09-03-item22-medicoes.md` · sem Context Packet (`Contexto: não` na fila) · commits `00d86e91`…`04973a63` (17, apos o rebase de 2026-09-09 sobre `origin/main@9bdaac90`; hashes originais `412117d3`…`953ef360`) · ledger `.superpowers/sdd/progress.md` |
| 2026-09-20 | Infra · provisionamento da produção na AWS: EC2 `t4g.small` em `sa-east-1` por cloud-init versionado, MySQL em container, S3 real por instance role, backup com restore provado e deploy por SHA (`infra-producao-provisionamento-aws`, v2) | Entregue | **Item 10 da fila, `lane-b`, worktree `../lotus-infra`, `executor: claude`**, sem Context Packet (decisão D9 da spec v2: os fatos externos já estavam medidos). Replanejado do zero em 2026-09-02 — a v1 e a R5 do formulário de contato foram descartadas para `archive/` com toda a medição. Plano de **20 tasks em duas fases**: a Fase A (repositório) atravessou as PRs **#101, #102 e #103**; a Fase B (console AWS, guiada, com o João executando) veio nesta branch, em 19 commits sobre `main@8efd85f2`. **Os sete DoD fecharam**: a EC2 nasce do `deploy/aws/user-data.sh` versionado com swap ativo; `deploy.sh <sha>` sobe o par corporativo multi-arch e `/up` responde **200 pelo EIP `18.230.53.197`**; upload pela UI chega ao bucket real por instance role (sem access key no `.env`) e o ClamAV discrimina de verdade; o certificado `LOT-2026-1000` foi emitido pela UI sem OOM; o **restore do dump num container MySQL limpo conferiu as contagens**; o teto de custo virou Budget de 30 USD (a métrica `AWS/Billing` não existe nesta conta — é conta membro de outra org, desvio previsto pela spec); e o TLS fechou pelo **ramo ficha** que a Task 19 prevê, porque o registro A de `app.lotusotec.cl` segue na hospedagem antiga. O review devolveu **nove achados**, todos aprovados pelo João e corrigidos em seis commits — o tema dos três 🔴 era o mesmo: o caminho do TLS detonava **no dia em que o registro A chegasse** (`/up` virando 301 no gate pós-deploy, o `SESSION_SECURE_COOKIE` fora do molde, a renovação do certificado documentada de um jeito que não podia funcionar). Nasceram `deploy/bin/verificar-backup.sh` (o ADR-09 passou a ter quem dispare o gatilho de reversão), `frontend/tests/nginx-conf.test.ts` e `frontend/tests/verificar-backup.test.ts`; o Q-7 foi a **terceira** ocorrência da lição 19, que por isso foi emendada em `docs/README.md` para alcançar as confs e scripts que produção monta. Abriu a **P-77**, a **P-78** e a **P-79**; o gate de fechamento abriu a **P-80** (previsão de 35,74 USD contra teto de 30, na mesma conversa do resize) e a **P-81** (a access key `AKIA3B7BDINPPYESZA6T` segue ativa), e **disparou a `P-05` sem pagar** — a produção subiu com as 30 migrations não consolidadas, por decisão do João no gate. Fechou a **P-58**, que cumpria rastro desde 2026-09-04. Gate: backend **1192 passed / 5 skipped**, frontend **132 arquivos / 807 testes**, lint 0, build verde; pint e `typescript:transform` N/A por escopo, provados por `git diff main...HEAD -- backend/ generated.ts` vazio. | Plano [`plans/archive/2026-09-02-infra-producao-provisionamento-aws.md`](../plans/archive/2026-09-02-infra-producao-provisionamento-aws.md) · spec [`specs/archive/2026-09-02-infra-producao-provisionamento-aws-design.md`](../specs/archive/2026-09-02-infra-producao-provisionamento-aws-design.md) · evidências [`audits/2026-09-17-item10-v2-provisionamento.md`](../audits/2026-09-17-item10-v2-provisionamento.md) e review [`audits/2026-09-20-item10-v2-review.md`](../audits/2026-09-20-item10-v2-review.md) · sem packet (D9) · PRs #101/#102/#103 (Fase A) · `248bf6a9..25086f5e` (Fase B e correções) |
| 2026-09-20 | Harness · cinco hooks de guarda em bash, com suíte própria: escrita na `main` fechada por `Edit/Write` **e** por Bash, `.env` e marca de credencial negados, estado das lanes injetado na abertura da sessão e aviso de conclusão cobrando verificação (`harness-hooks-de-guarda`) | Entregue | **Item 28 da fila, `lane-a`, worktree `../lotus-harness`, executor `claude`**, a partir de `main@4dba322b`. 10 tasks, 7 arquivos de teste, suíte verde. O review de branch achou **2 Críticos + 1 Importante** (barra invertida em flag anulava toda regra de flag longa; a família "leitura pura" tinha quatro primitivas de escrita; campo vazio deslocava a linha inteira do `session-start` e pulava a comparação de estado da lane) e o `/revisar-sprint` achou mais **cinco** (`\|&` não separava comando; `git -C`/`--git-dir` para fora da raiz virava escrita cruzada; o sentinela `main-tree` não resolvia; lane reclamada por branch fazia 3 de 4 árvores parecerem órfãs). Os oito corrigidos e provados nos dois sentidos. **DoD item 4 provado por esta sessão**, que é a primeira aberta depois do `settings.json`: o `SessionStart` imprimiu o inventário das lanes e acusou duas divergências reais de estado. Fecha a **P-82**; abre a **P-83** (sete decisões de política que ficaram só no ledger gitignorado) e a **P-84** (o harness não existe em doc versionado) | [plano](../plans/archive/2026-09-20-harness-hooks-de-guarda.md) · [spec](../specs/archive/2026-09-20-harness-hooks-de-guarda-design.md) · sem context packet · `1557811d`..`623c1c5b` mais o de fechamento e o merge de `origin/main@cff022d4` · **PR #106** |
| 2026-09-25 | Backend · a data de calendário que o servidor deriva passa a ser a de Santiago sem reinterpretar instante gravado, e o QR do certificado ganha chave própria, obrigatória e https em produção (`backend-config-e-conteudo-de-documento`) | Entregue | **Item 29 da fila, `lane-a`, main tree (gate P-03), executor `claude`**, a partir de `main@5be61b63`. 11 tasks; suíte **1221 passed / 5 skipped** (+29 sobre os 1192 da base). Nasce `FusoDoNegocio`, dono único da data de calendário, com a catraca `DataDeCalendarioTest`; `config/app.php` mantém `UTC` **literal** por decisão escrita (spec D2), na direção contrária à que a `P-59` propunha; `CertificateValidationUrl` e `ValidacaoDeCertificadoNaoConfigurada` fazem a produção recusar PDF e emissão sem a chave em https (500 nomeado e logado, antes da transação). O `/revisar-sprint` achou **dois 🟡**, aprovados e corrigidos: Q-1 (o período do dashboard, em dias de Santiago, era comparado cru com instante UTC e cortava as últimas horas do dia — limites e balde mensal migraram juntos) e Q-2 (`agora()` não grava instante, agora com catraca). QR decodificado do PDF nos dois sentidos na execução. **E2E no fechamento às 21:57 de Santiago (00:57 UTC de 26/09):** o `LOT-2026-1001` saiu com `Emisión: 25-09-2026` e `valido_ate 2027-09-25`, o QR decodificado com o fallback de `local`, e o dashboard contou o certificado no dia e no mês de Santiago; revogado em seguida. Fecha a **P-59** e a **P-79** por mecanismo e a **P-75** por veredito escrito; abre a **P-85**; dispara pela segunda vez, sem pagar, a **P-53** | [plano](../plans/archive/2026-09-24-backend-config-e-conteudo-de-documento.md) · [spec](../specs/archive/2026-09-24-backend-config-e-conteudo-de-documento-design.md) · [medições](../audits/2026-09-24-item29-medicoes.md) · sem context packet · `dbac2c37`..`6fe8a80d` mais o de fechamento |
