# Progresso — Lotus v2

> Histórico curto do que foi entregue e da feature em andamento. Este arquivo **não define a etapa
> operacional**. Para saber o que fazer agora, leia `docs/superpowers/state.md`.
> Fila futura: `docs/superpowers/backlog.md`. Histórico antigo: `progress-archive.md`. Narrativa
> integral de cada bloco fechado (o que era a parte de baixo do `state.md`): `state-archive.md`.
>
> **Cinco colunas, de propósito (P-23, decidido em 2026-08-22).** O formato antigo tinha sete, com
> `Contexto`/`Plano`/`Spec` separadas; aqui plano, spec, context packet e commits vivem juntos na
> coluna `Referências`. A coluna `Contexto` **não volta**: só uma parte das entregas nasce de packet,
> e a coluna separada era célula vazia na maioria das linhas. O `progress-archive.md` registra do
> lado dele a mesma convivência de duas arities — as linhas migram **verbatim**, porque o fechamento
> move histórico e não o reescreve.

| Data | Entrega | Status | Resultado | Referências |
|---|---|---|---|---|
| 2026-08-19 | Identity · ativação de acesso do redator: convite, primeiro acesso, recuperação e revogação | Entregue | **Item 4 de "Próximos blocos" do `backlog.md`, promovido explicitamente pelo João em 2026-08-18** com o estado em `idle`; rota `context_required`, porque "como o redator recebe a credencial" é decisão de produto e a fonte é externa ao repositório. Worktree `fix-frontend`, branch `feat/identity-ativacao-acesso-redator`, a partir de `main@2c7b249` — **bloco de backend em worktree linkada por decisão explícita do João**, contra a regra do `/planejar-bloco`, com a exceção declarada na abertura e não descoberta na execução (ver P-03). **O bloco fecha a RN-01 pela metade que faltava:** o redator passa a existir com credencial. Dois brokers, um por janela — `invites` (`config/auth.php`, tabela própria `invitation_tokens`, 7 dias) para o primeiro acesso e `users` (60 min) para a recuperação, porque `expire` é aplicado por quem valida e uma tabela só daria ao token de recuperação a janela do convite. Três rotas públicas sob `throttle:6,1` (`/password/forgot`, `/password/reset`, `/invitation/accept`), duas Notifications (`RedatorAccessInvitation`, `PasswordResetLink`) e transporte de e-mail novo no compose (Mailpit). `is_active` vira **controle explícito do admin** — conceder acesso não é efeito colateral de reenviar convite —, e revogar purga as sessões vivas (`PurgeOtherSessionsAction`), porque `auth:sanctum` não reconsulta senha nem `is_active` a cada request. A role `redator` passa a ser atribuída no cadastro. Frontend: tela pública `/definir-clave/:token`, com `?flow=` escolhendo o endpoint. **Emenda de 2026-08-19, pedida pelo João com o bloco em `ready_for_review`:** `/login` e `/recuperar-clave` viram rotas irmãs do mesmo layout (`AuthPanel` + `useAuthPanel`), `ForgotPasswordPage` morre e o e-mail digitado sobrevive à troca — premissa medida no `react-router@7.18.0` (`_renderMatches` monta `RenderedRoute` **sem `key`**, então irmãs com o mesmo `element` reconciliam em vez de remontar) e confirmada na tela. O `/revisar-sprint` devolveu **6 achados, os seis aprovados pelo João e corrigidos**: **Q-1** — reenvio de convite não atribuía a role `redator`; **Q-2** — as rotas públicas de senha enumeravam usuário (mensagens distintas para `INVALID_USER` e `INVALID_TOKEN`, e 500 vs 200 com SMTP fora do ar) e a recuperação não derrubava sessão; **Q-3** — o convite de senha alcançava cliente e aluno, que pela RN-01 não autenticam (filtro `is_active` no `sendResetLink`); **Q-4** — falha que não nomeia campo (429/419/500) ficava muda no `SetPasswordPage`; **Q-5** — o reset agora purga em transação junto com a troca de senha; **Q-6** — o TTL dos dois brokers provado por **comportamento**, não por leitura de config. **DoD provado no fechamento contra a API real** (nginx 8080, Mailpit, banco de dev): reenvio de convite atribuiu a role a `juan.morales@lotus.cl` (`roles=[]` → `[redator]`) e entregou o e-mail com `?flow=invite` e "vence en 7 días"; `/invitation/accept` definiu a senha e o login devolveu as 4 permissões da role; `forgot` respondeu **byte a byte igual** para e-mail que existe, que não existe e de **cliente**, entregando só no primeiro; `reset` com token falso devolveu resposta idêntica para e-mail existente e inexistente; o reset derrubou a sessão viva (`sessions` 1 → 0, cookie antigo em 401 RFC 7807); a revogação por `PUT /api/redatores/1` derrubou a sessão e recusou o login novo com "Esta cuenta no está activa."; no navegador, o e-mail digitado atravessou a troca `/login` → `/recuperar-clave` com **uma** entrada de navegação e foco no `<h1>`, e o 429 real apareceu como alerta na tela do `/definir-clave` (a prova do Q-4). RN-01 medida no fim: cliente e aluno ativos = **0**. Catracas: backend **710 passed / 5 skipped**, `pnpm lint` 0, `pnpm build` verde, frontend **69 arquivos / 409 testes**, Pint verde nos 29 arquivos do bloco e `typescript:transform` sem diff em `generated.ts`. **Nasce a P-47** (os 7 redatores do seed não têm a role — dado de seed, não código do bloco); **P-03 e P-45 remedidas sem fechar**; a sonda `gate.task14@lotus.cl` criada pelo gate da Task 14 foi removida no fechamento. | `plans/archive/2026-08-18-identity-ativacao-acesso-redator.md` + `plans/archive/2026-08-19-login-recuperacao-inline.md` · `specs/archive/2026-08-18-identity-ativacao-acesso-redator-design.md` + `specs/archive/2026-08-19-login-recuperacao-inline-design.md` · `context-packets/2026-08-18-identity-ativacao-acesso-redator.md` · 34 commits (`03a0b72`..`929b1e6`) mais o de fechamento |
| 2026-08-19 | Cadastros · arquivados e restauração nos seis roots restantes (`Budget`, `Quote`, `User`, `Redator`, `Turma`, `Enrollment`) | Entregue | Item 1 de "Próximos blocos", promovido explicitamente pelo João com o estado em `idle`; branch `feat/arquivados-roots-restantes` nasceu de `feat/arquivados-e-restauracao@6fd0ad8` — não da `main` — porque `ArchivesChildren`, `useArchivedPage` e o `archived` do `createCrudResource` só existem lá. **O bloco não era a replicação que o backlog previa:** a medição separou os seis roots em três classes e achou um **500 alcançável** — `turmas.active_quote_id` é coluna gerada STORED com `UNIQUE`, e `CreateTurmaAction` deixava criar turma nova sobre a cotação de uma arquivada, então restaurar a primeira estourava `SQLSTATE[23000]`. Sete decisões do João (D1–D7) e 15 tasks em três fases; o `RestoreTurmaAction` converte o conflito em **422**, `Turma` ganha a cascata que nunca teve (matrículas + documentos), `Redator` ganha gate de turma em andamento e `Turma::redatores()` passa a `withTrashed` — sem isso o redator arquivado sumia da emissão e **recusava certificado de turma concluída que ele ministrou**. Três colunas novas (`quotes`, `files`, `enrollments`), cinco permissões `*.restore` (staff **não** ganha uma: seu `destroy` é `identity.access.manage`), dois QueryBuilders novos e a dívida de copy do molde paga (o "no se puede deshacer" de orçamento e cotação). Review de risco **ALTO** com duas lentes: seis achados, nenhum 🔴, todos aprovados e corrigidos — o restore de cotação passou a exigir orçamento ativo (422), o aviso de nome perdido passou a valer nos dois modos do `QuotesList`, o `useRestoreQuote` passou a invalidar o detalhe do pai, e o **kit de arquivados virou um só** (`useArchiveAction`, `ArchiveRowActions`, `ArchiveConfirmDialog`: −603/+403 linhas, regra escrita em `.claude/rules/frontend-fsliced.md`). DoD no navegador nas três fases mais os dois 422 novos provados no fechamento (2026-08-19), com o banco de dev devolvido ao estado anterior. Gate: backend **797 passed / 5 skipped**, frontend **67 arquivos / 394 testes**, lint 0, build verde, Pint `passed` nos 54 PHP do bloco, `typescript:transform` sem drift. Encerra o rastro da **P-45**, atualiza a **P-47** (o eixo da cotação ganhou tomador dos dois lados), a **P-35** e a **P-44**, e registra a **P-48** (o `title` do RFC 7807 em português com `detail` em es-CL). | `plans/archive/2026-08-18-arquivados-roots-restantes.md` · `specs/archive/2026-08-18-arquivados-roots-restantes-design.md` · `context-packets/2026-08-18-arquivados-e-restauracao.md` (herdado) · 36 commits (`29acb5db`..`6a1c0bac`) mais o de fechamento |
| 2026-08-20 | Frontend · a superfície de arquivados: data, coluna e escolha de fonte (BD-17) | Entregue | Bloco de dívida promovido pelo João depois do `/revisar-frontend` de 2026-08-19, executor `claude`, frontend puro — zero `backend/`, zero `generated.ts`, fence provado por `git diff main...HEAD` vazio nos dois paths. **Entrega:** as duas colunas de rastreio viram `archivedColumns(t)` em `shared/ui` (**função**, nunca componente — o `DataTable` lê o filho DIRETO, então um Fragment achataria as duas numa coluna lixo sem estourar build, lint ou teste; a catraca prova as duas formas), o `formatDate` do D-51 passa de 8 pousos para 1 (o nono, `ArchivedQuotesList`, é layout flex e foi corrigido direto), `ArchivableRow<T>`/`ListSource<T>`/`archivableSource()` em `shared/lib` matam as 8 declarações de `XRow` à mão e o quarteto de ternários das 6 páginas, e a `OperationPage` — o pior sítio do D-52, que derivava `loadError` dentro da prop — passa a consumir `useTurmasPage()`/`usePendingQuotesPage()`. **DoD provado na tela** (navegador `en-US`, interface `es-CL`), não no diff, no molde do precedente `AppFileRow.test.tsx`. **Dois débitos nasceram da medição, nenhum é regressão do bloco:** **D-54** (o `refetch` do `useLoadState` engole a promise do Q-14 — por isso os dois aliases novos nasceram sem ele) e **D-55** (o `DataTable` não repinta células `body` na troca de idioma ao vivo; isolado como limitação de plataforma pelo `ÚLTIMO ACCESO` e pelo `AppTag`, que congelam igual, e pelo `ArchivedQuotesList` fora de tabela, que troca ao vivo). **Review:** zero violação de lei, zero 🔴/🟡, dois 🟢 — Q-1 corrigido no branch (o alias de pendentes foi para arquivo próprio) e Q-2 registrado como **D-56**, com o texto da linha de rule guardado na ficha para quando o débito for pago. **Gate:** `pnpm build` verde, `pnpm lint` 0, `pnpm test` 81 arquivos / 453 testes (baseline 77 / 435), backend 828 passed / 5 skipped intocado. | `plans/archive/2026-08-20-bd17-superficie-de-arquivados.md` · `specs/archive/2026-08-20-bd17-superficie-de-arquivados-design.md` · sem packet · 18 commits (`82c1d0c4`..`1a92a82f`) mais o de fechamento |
| 2026-08-20 | Backend · contrato de entrada: o que o corpo da requisição pode escrever (`bd14-contrato-de-entrada`) | Entregue | Quatro itens num bloco só, backend puro, no main tree. **D-13:** `WritableAttributes::from()` vira a tradução única de `Optional` para atributo gravável — omissão preserva o valor guardado, `null` explícito segue sendo o único jeito de apagar; 10 campos em 5 `Update*Action`, e as três grafias divergentes (`UpdateStudentAction` por ternário, `UpdateProfileAction` por `if`, o spread de `is_active` do redator) convergiram para a fonte única. **D-12:** chave `#[Computed]` no corpo de escrita vira **422** por `missing` (e não `prohibited`, que é `! required` no vendor e deixaria passar presente-mas-vazio), a partir da lista única de `ComputedFields`; 6 DTOs de entrada mais arch test dos 11 campos de foto. **P-29 encerrada:** colisão de índice único de `users` traduzida em 422 do campo na porta única (`UserProvisioner::writing()`), pelas duas grafias de driver. **P-35 encerrada:** `seq_in_budget` fora do `$fillable`, escrito por atribuição explícita sob o lock que já existia. Review de risco ALTO com duas lentes (Codex independente sem achado) devolveu dois achados de **alcance**, os dois corrigidos: a lei da D-12 passou a valer para as chaves `#[Computed]` não-foto (`last_login`, `current_client_id`, `current_client_name`, `enrollments_count`) e o arch test passou a varrer também `app/Shared/*/Data`. **Nasceu a P-51:** o `DefaultValuesDataPipe` do Spatie entrega o default literal antes do ramo que preencheria `Optional`, então `WritableAttributes` não alcança propriedade com default — e `UserData::$is_active = true` significa que um `PUT` que omite a chave **reativa staff desligado**. Fora do escopo medido do bloco, com os dois remédios custeados; espera decisão do João. | `plans/archive/2026-08-20-bd14-contrato-de-entrada.md` · `specs/archive/2026-08-20-bd14-contrato-de-entrada-design.md` · sem context packet (escopo interno) · 22 commits (`9b194538`..`02e93684`) mais os do review e o de fechamento |
| 2026-08-20 | Frontend · `useLoadState`: a promise do `refetch`, a forma da lista e os dois ramos crus (BD-18) | Entregue | Bloco de dívida promovido pelo João com a árvore em `idle`, pela rota direta a `ready_for_planning` — os três débitos nasceram de medição local (D-54 e D-56 no review e no fechamento do BD-17, D-14 no review do BD-6), então não havia fonte externa a recuperar e `context_packet` ficou `null` do começo ao fim. **O escopo era maior do que as fichas registravam, e isso foi medido antes de desenhar:** a varredura por forma (`void <query>.refetch()`) contra `93acf6a7` achou **14 produtores em 12 arquivos** — não os 7 da ficha —, dos quais **três travavam a promise por TIPO** (`() => void` na assinatura), e a política `loadFailure` estava escrita à mão em **12** sítios, não em 6. **Duas peças novas em `shared/hooks/listSource.ts`, não uma:** `listSource` para a forma de página e `loadFailure` para os dois hooks de carga, que falam outra grafia e não caberiam na primeira. O `refetch` é **anotado** `(): Promise<unknown>` e não deixado inferir — o inferido vaza `QueryObserverResult` para cima por `ReturnType<>`. **`useRetryPending` saiu junto** e é consumido pelos dois componentes de falha: sem ele o `InlineLoadState` seguiria descartando a promise recém-corrigida em 12 usos. **Duas exceções declaradas, não esquecidas:** `useHistorial` e `useEmissionPanelState` devolvem `null` onde a política devolve `{}` — outra política, e trocá-la mudaria tela sem DoD que a cobrisse. **DoD provado no navegador** contra a API real, com falha isolada por rota: o "Reintentar" de tela cheia fica `disabled` com o GET segurado em voo, o do `InlineLoadState` ganha o spinner que não tinha, e a seção de redatores mantém os três em cache quando o GET falha. **Review:** risco BAIXO, uma lente, zero violação de lei, nenhum 🔴 — quatro achados aprovados e corrigidos (`loadFailure` no último ramo à mão do dashboard; catraca do `AppErrorState`, que não tinha arquivo de teste; `loadMessage` em `shared/lib`, fechando a mensagem repetida em 13 sítios de 8 componentes; e o terceiro sítio do D-14, no detalhe do aluno). **Fechamento:** o ramo do Q-1 provado na tela — com o `GET /api/students/35` em 500 e cache em mão, "Company links" e "Turma history" permanecem e o aviso vai ao lado; sem cache o `AppErrorState` substitui as duas, e o Retry recupera. **Gate:** `pnpm lint` 0, `pnpm build` verde, `pnpm test` 86 arquivos / 479 testes (baseline 81 / 453), backend 872 passed / 5 skipped intocado (binário direto, P-50). | `plans/archive/2026-08-20-bd18-useloadstate-promise-e-forma.md` · `specs/archive/2026-08-20-bd18-useloadstate-promise-e-forma-design.md` · sem packet · 20 commits (`93acf6a7`..`f3f971e8`) mais o de fechamento |
| 2026-08-22 | Frontend · a célula do DataTable na troca de idioma, e o catálogo vazio remedido (BD-12) | Entregue | **BD-12 dos blocos de dívida, frontend puro, um arquivo de código.** D-55: o comparador do `BodyCell` do PrimeReact compara dado e não função (`defaultKeysToCompare` lista `rowData` e `field`, não lista `body`), então a closure nova que a troca de idioma produz nunca chegava à célula — cabeçalho repintava, VALOR congelava até o F5. `cellMemo={false}` no `AppDataTable` desliga a memoização de célula em todas as tabelas; o rekey em `i18n.language` foi recusado com custo medido (remontar zera ordenação, página e filtro client-side). Catraca de render vista reprovar antes de passar. Provado no navegador nos três idiomas sem recarga, com dois controles positivos (`Último acceso` e o `AppTag` de estado destravando junto) e um negativo (`ArchivedQuotesList`, fora de DataTable, sem regressão). P-40 encerrada: com `GET /api/courses` devolvendo 200 e `[]`, o passo 1 do wizard mostrou `No hay cursos.` e **não** a mensagem de falha; catálogo devolvido com ids `[1,2,3]` idênticos. Fence de `backend/` e `generated.ts` vazio — Pint e `typescript:transform` N/A por escopo medido. | plano `plans/archive/2026-08-20-bd12-load-state-e-listas.md` · spec `specs/archive/2026-08-20-bd12-load-state-e-listas-design.md` · `0dc894bd`, `f80d0425`, merge da `main` em `aab9dc54` |
| 2026-08-22 | Feedback · escopo resolvido sem domínio: as permissões órfãs saem de ponta a ponta (`feedbacks-resolver-escopo`) | Entregue | **Bloco 1 da fila consolidada, lane-a, main tree (gate P-03), executor `claude`.** A medição contra o Drive e o código mostrou que a lacuna registrada no backlog — "requisito canônico de feedbacks × ausência de `Domains/Feedback`" — **não era de comportamento**: RF-FBK-01/02/04 já rodam como documentação de turma (`files` polimórfica + `TurmaDocumentType` `PRUEBAS`/`EVALUACION_REDATOR` + gate da RN-16 em `ConcludeTurmaAction`), e o que divergia era nomenclatura e registro. Decisão do João (D1–D5): **feedback é absorvido pelo documento de turma; não nasce domínio nem tabela**, nenhum `TurmaDocumentType` novo, e RF-FBK-03 (avaliação do cliente) fica **diferido nomeado**, no encerramento da OS junto de RF-TUR-07 — não vira pendência silenciosa. **A catraca entrou antes da limpeza e foi vista morder:** `PermissionI18nParityTest` passou a cobrir `permGroup.*` além de `perm.*` (sonda em `en.json` reprovou com `Faltando: feedback`), e a remoção do catálogo fez as **duas** metades falharem juntas antes de as locales serem limpas. **A migration é o mecanismo, não o seeder:** `2026_08_22_000001` apaga por nome literal com guard `web`, o FK cascade de `role_has_permissions` leva o vínculo junto, e `down()` recria as duas linhas **sem devolver capacidade a role nenhuma** — 4 testes, incluindo idempotência e não-alcance de outras permissões. **Divergência do plano corrigida por medição:** o plano dizia 43→41 permissões; o base `666d9d2a` tinha **42**, então o real é **42→40** — o delta `-2` sempre esteve certo. **DoD provado fora da suíte:** banco de dev 42→40 com órfãs em 0 e role `redator` de 4 para 2; `migrate:rollback` devolveu as 2 e `migrate` removeu de novo; `GET /api/permissions` com sessão de admin real devolveu **40 itens, zero `feedback.*`**, em 5 grupos; **sessão de redator ativo (`Juan Morales`) trouxe exatamente 2 permissões e o `POST /api/turmas/6/documents` respondeu 201** — a prova de que a remoção não tirou capacidade em uso; picker percorrido nos 3 idiomas sem F5, sem grupo Feedback e sem chave crua. **Duas observações medidas, nenhuma regressão deste bloco:** `php artisan test` estoura o `memory_limit` de 128M no `ManualTurmaTest` — **reproduzido no `main` limpo**, contornado com `php -d memory_limit=1G vendor/bin/phpunit` (877 passed / 5 skipped); e o `DeleteTurmaDocumentAction` é **soft delete**, então a sonda de upload deixou `files#43` arquivada e inerte (index devolve `[]`), sem hard-delete por ser escrita destrutiva fora dos paths do plano. **Gate:** backend 877 passed / 5 skipped, `pnpm lint` 0, `pnpm build` verde, `pnpm test` 87 arquivos / 481 testes, Pint PASS nos 5 PHP tocados, `typescript:transform` sem drift em `generated.ts`. **Registro externo (Task 7) escrito com OK do João, documento a documento** — e o MCP do Drive **não edita conteúdo no lugar** (`update_file` só troca título e pasta), então os dois documentos foram **recriados** com o texto aprovado e os originais foram para a lixeira, pela rota que o próprio João escolheu. A fidelidade byte a byte foi provada antes de sobrescrever: o `requisitos-negocio.md` original foi reconstruído por 7 blocos base64 até bater **exatos 14240 bytes** com o `fileSize` do Drive, e o arquivo novo subiu com **14897 bytes**, idênticos ao patch local — zero drift de transcrição. **IDs novos:** `requisitos-negocio.md` = `1Nt8XARvd_EIRWEJ9YXa3DKV45xPMQkk-`, `entidade-feedback.md` = `16YxxQ52VnEeoah_SCja6TubnvtOtMDql`; Notion 7.4.1 (`39dbc960-3dfa-81ef-ad6f-d908331d5059`) em **`Concluída`** — o status não tem opção `Done` — com o resumo, a evidência 42→40 e o RF-FBK-03 nomeado como diferido. **Review de alto risco (RBAC + migration) com segunda lente do Codex:** órfãos limpo, leis §5 intactas, 4 achados de registro; Q-1..Q-3 aplicados em `dfb18d8d` (a rule `backend-ddd.md` e o `estrutura-monolito.md` paravam de prometer domínio `Feedback`; `README.md` alinhado a 25/18; o plano corrigido de 43→41 para os 42→40 medidos) e **Q-4 deferido** para o bloco 3 — os testes da migration não cobrem o filtro `guard_name` nem o `forgetCachedPermissions()`. **Fechamento (2026-08-22):** DoD reprovado contra o banco de dev e a API real — ciclo `rollback`/`migrate` devolvendo 42 e voltando a 40 com órfãs em 0, `GET /api/permissions` em **200 com 40 itens e 5 grupos**, sessão de redator com exatamente 2 permissões e `GET /api/turmas/6{,/documents}` em 200, provando capacidade intacta. Suíte **877 testes / 3131 asserções / 0 falha** pelo binário direto (o comando documentado morre na **P-50**, já registrada), `pnpm lint` 0, `pnpm build` verde, `pnpm test` 87/481, Pint passed, `generated.ts` sem diff. **A P-43 fechou no gate:** o bloco tocou `der-fisico.md`, o gatilho venceu, e a correção — decidida pelo João porque mexia no contador que o Q-2 acabara de escrever — foi maior que a ficha previa: as duas tabelas de Certification saíram de `PLANEJADAS` para `IMPLEMENTADAS`, documentadas pela migration e não pelo rascunho PT/ES, que prometia um `qr_code_hash` inexistente. **A P-40 saiu** do rastro e a **P-50** ganhou a terceira medição. | `plans/archive/2026-08-22-feedbacks-resolver-escopo.md` · `specs/archive/2026-08-22-feedbacks-resolver-escopo-design.md` · `context-packets/2026-08-22-feedbacks-resolver-escopo.md` · commits `f6b04b45`..`8c6dea02` mais o de fechamento |
| 2026-08-22 | Docs · guardrails e sincronização: ADRs, DER, pendências, catraca de arestas e o Notion (BD-15) | Entregue | **Item 14 da fila, promovido explicitamente pelo João** pela rota `context_required` e nesta worktree, com as duas exceções declaradas **na abertura** e não descobertas no gate: escrita externa no Notion (não-destrutiva) e um arquivo de `backend/` fora do main tree — o gatilho literal da P-03 (dois blocos de backend em paralelo) medido como não vencido. 13 tasks, executor `claude` do início ao fim. **A ferramenta separou Drive de Notion e reabriu meio bloco:** o `update_file` do Drive aceita só `title`/`parentId`, então a **P-31 segue não-fechável** e a ficha estava certa; o `notion-update-page` escreve propriedade, o que destravou a P-18 e o sync obrigatório, congelados desde que foram escritos como "fecha quando o João corrigir manualmente". **18 páginas escritas por ID, zero busca por título**, relidas por ID no gate: `8.4.0`–`8.4.7`, `8.5.1`–`8.5.9` e `9.1.4`, todas `Concluída`. **Três divergências foram medidas e deliberadamente NÃO escritas** (a autorização era só o não-destrutivo): a duplicata da P-22, a troca de corpo entre `8.4.0` e `8.4.7` — confirmada nos dois sentidos, não repetida do packet — e as duplicações genéricas do workflow, em `audits/2026-08-22-bd15-notion-sync.md`. **Duas premissas escritas do plano caíram na medição e foram escaladas em vez de seguidas:** `der-fisico.md:74` não estava correta (omitia `redator_id`, `snapshot`, `revoked_at`, `revocation_reason`, `timestamps` e a coluna gerada `active_enrollment_id`, e inventava um `qr_code_hash` inexistente) e as duas tabelas viviam sob `## Tabelas PLANEJADAS` — o João autorizou ampliar a Task 4, `certificates` e `certificate_sequences` foram para `## Tabelas IMPLEMENTADAS` reescritas a partir da migration, e o total virou **28 tabelas — 21 de domínio (20 implementadas, `feedbacks` no papel) + 7 RBAC/transversal**; e as coordenadas que a lição 18 ia ensinar estavam velhas (`CourseController.php:24`, `Catalog/routes.php:18` contra HEAD). **A D-17 nasceu verde e por isso a sonda é obrigatória:** 47 arestas, 0 órfãs, e a Regra C **vista reprovar** com aresta falsa em `ALLOWED` na Task 1, no gate de review e de novo no fechamento. **A P-32 não ganhou guarda por medição, não por preguiça:** 167 identificadores PascalCase entre crases, 28 sem declaração, **0 achado real**, divididos exatamente nas três famílias que a ficha previa. **O review devolveu 4 achados, todos de mecanismo do próprio workflow, e o João aprovou os quatro:** o `state.md` encolheu de **1499 para 305 linhas** com a narrativa descendo verbatim para `state-archive.md` (diff de 10 linhas, todas de cabeçalho); a invariante de `docs/superpowers/**` passou a se dividir por **dono**, não por árvore, porque a redação anterior foi quebrada por 21 commits da própria lane no dia em que foi escrita; os gates de review e fechamento passaram a resolver a lane pela **árvore** antes de ler estado, com troca de `focused_lane` proibida por escrito; e as Regras B e C passaram a ler a **mesma** varredura, com três sondas — uma por caminho reescrito — vistas reprovando e revertidas. **Pendências: 30 abertas viram 25.** Encerram com remédio e path: P-18, P-20, P-21, P-23, P-39, P-43. Seguem abertas com a medição que as mantém: P-22, P-31, P-32. Nasceram a **P-52** (`invitation_tokens` sem ficha de colunas) e, no gate de fechamento, a **P-53** — a `auditar-docs` mediu 13 divergências, 1 desta sprint (âncora de `P-43` quebrada, corrigida) e 12 fora do escopo de qualquer bloco, com `Certification` descrito como scaffold vazio e o domínio `Dashboard` ausente dos dois docs de estrutura à frente. **Gate medido, nada herdado:** suíte **873 / 3096 / 5 skipped** pelo binário direto do `phpunit` com `-d memory_limit=512M` (o `artisan test` documentado morre no meio por **P-50**, pendência aberta e não falha deste bloco), sonda da Regra C reprovando e revertida com árvore limpa, Pint `passed`, `repo-docs-refs` 14 verde. Fence de escopo: **um** arquivo em `backend/` (`tests/Feature/Shared/DomainDependencyTest.php`) e **zero** em `frontend/` — `pnpm test`/`build`/`lint` e `typescript:transform` **N/A por escopo medido**, não por suposição. | plano `plans/archive/2026-08-22-bd15-docs-guardrails-e-sincronizacao.md` · spec `specs/archive/2026-08-22-bd15-docs-guardrails-e-sincronizacao-design.md` · packet `context-packets/2026-08-22-bd15-docs-guardrails-e-sincronizacao.md` · auditoria do Notion `audits/2026-08-22-bd15-notion-sync.md` · commits `108d248b`…`d4907d01` |
| 2026-08-22 | Infra · runtime de produção: imagem, compose, origem única e os dois `memory_limit` (`infra-producao-runtime-e-aws`) | Entregue | **Item 10 da fila, promovido explicitamente pelo João** pela rota `context_required` — e o **brainstorming recortou o bloco ao meio, por decisão dele**: só o runtime versionado entra; o provisionamento em conta AWS vira bloco próprio quando as quatro decisões abertas do packet existirem (região, tamanho da EC2, DNS/SES, teto de custo). **Origem única contra os dois hosts do Drive**, e a base é medida: `VITE_API_URL` é lido por `import.meta.env` (`axios.ts:25`), então entra **dentro do bundle no build** — com dois hosts a imagem carregaria a URL do ambiente e o item 11 não poderia promover a mesma imagem por SHA. **Sete tasks, dez commits**, TDD por subagente: um `Dockerfile.prod` de quatro estágios e dois alvos (`app` 293MB, `web` 105MB), `docker-compose.prod.yml` sem MySQL/MinIO/Mailpit/worker — a ausência do worker **medida**, não suposta (`grep ShouldQueue` devolve uma linha, que é comentário) —, overlay de sonda separado para provar local e `.env.production.example` sem segredo. **A catraca do bloco não é teste de código, é teste de composição** (`frontend/tests/compose-prod.test.ts`): guarda as propriedades cuja violação é silenciosa, porque `docker compose up` fica verde com um serviço de dev de volta. **A P-50 fechou aqui**, e o impasse que ela nomeava — `conf.d` vale para os dois SAPIs — foi resolvido separando por SAPI: CLI 320M (dobro do pico medido de 129,00 MB) e FPM 256M com `pm.max_children = 5`. **Revisão de sprint em duas lentes** (Claude + Codex independente), risco ALTO, **nove achados aprovados e corrigidos, um commit cada** — os dois 🔴 eram brechas operacionais que a imagem carregava: o seeder que criaria `admin@lotus.cl`/`senha123` como superadmin num servidor novo, e o gate de env cego para a cadeia Sanctum que o próprio molde entrega vazia. Uma divergência entre revisores foi **medida e rejeitada** (o vazamento de `auth.json`/`storage` que o Codex reportou não existe na imagem). **Gate de fechamento:** login real ponta a ponta contra a stack de produção reconstruída — `/up` 200, `/sanctum/csrf-cookie` 204, `POST /api/login` 200 com `lotus-session` gravado, `GET /api/me` 200 com `roles: [superadmin]` —, e `db:seed --force` com `APP_ENV=production` instalando as roles e **recusando o admin de dev**. Suíte backend 867 passed / 5 skipped pelo comando do §6, sem contorno; frontend 88 arquivos / 500 testes, lint e build exit 0. **Nada de AWS foi provado** — limitação declarada da spec, não lacuna. | plano `plans/archive/2026-08-22-infra-producao-runtime-e-aws.md` · spec `specs/archive/2026-08-22-infra-producao-runtime-e-aws-design.md` · packet `context-packets/2026-08-22-infra-producao-runtime-e-aws.md` · `8b1fd6df..e0019bac`, correções `ed4cdc7b..bf896ba5` |
| 2026-08-23 | Identity/Operation · hardening de acesso: ownership de turma, revogação de sessão, nota do redator e o mutex do arquivamento (`hardening-acesso-ownership-e-integridade`) | Entregue | **Item 3 da fila consolidada, `lane-a`, main tree (gate P-03), executor `claude`**, a partir de `main@f6649297`; nasceu em `context_required` e o packet veio do Codex com `status: ready` e cinco fontes recuperadas. Quatro eixos, todos backend, com `generated.ts` regenerado como entregável. **Ownership (Notion 7.3.3):** o escopo mora em `TurmaQueryBuilder::visibleTo()` e entra por DOIS pontos — as listagens e `Turma::resolveRouteBinding()`, que alcança as **20** rotas com `{turma}` de uma vez e faz rota nova nascer coberta; turma alheia devolve **404** e não 403, porque 403 confirmaria que ela existe. **Revogação:** fecha nas duas pontas — purge de sessão na transição e `EnsureAccountIsActive` por request; e a **P-51** perdeu o default literal (`UserData::$is_active` virou `bool\|Optional`), com `d4a8553d` fechando o blast radius no `create`. **Capacidade:** `operation.enrollment.record_result` separa lançar nota/presença do Fluxo 3 (RN-02), por migration e não por seeder. **Integridade (P-49):** `Turma::lockForWrite()` e `Redator::lockForWrite()` nasceram no molde do `Client` e são tomados por **cinco** escritores de filho — o plano corrigiu três medições que a spec e as fichas traziam erradas: o lock é `lockForWrite()` e não `lockRow()` cru (a diferença é a recusa), `ImportStudentsAction` sai da lista porque não abre transação, e a **P-47** não se conserta no seeder (já certo desde `e3490d84`) mas por migration de backfill sobre o dado velho. A catraca é o `ParentLockOnChildWriteTest`, arch test de lista dupla onde **silêncio reprova**. **O `/revisar-sprint` (Claude + Codex) devolveu 2 achados, os dois aprovados e corrigidos:** **Q-1** — `EnsureAccountIsActive` nasceu em `api(append:)` e vazou para o grupo `api` inteiro, devolvendo 401 em `login`, `password/forgot`, `password/reset`, `invitation/accept` e `publico/certificados/{uuid}` para cookie de conta recém-desativada (a validação pública do QR, que tem peso legal, e a própria porta de saída do buraco); a checagem passou a andar casada com `auth:sanctum` no grupo `auth.active`, com `AuthenticatedRouteMiddlewareTest` exigindo que a superfície anônima seja **declarada**; **Q-2** — `.claude/rules/backend-ddd.md` ficou desatualizada sobre o data-scoping da Turma. **DoD provado contra a API real em `:8080` e o MySQL de dev, 12 provas:** listagem de Redator A traz só `[1,4,5,6]` e a de B só `[2]` (admin vê as 7); **19 dos 20** caminhos com `{turma}` alheia devolvem 404 e o vigésimo (`POST /turmas/{turma}/restore`) devolve **403** antes do binding, porque `permission:operation.turma.restore` não é do redator — ele não alcança a rota, que é o que a spec pede; `PUT .../resultado` dá 200 na turma própria e 404 na alheia; conta desativada com sessão viva cai em **401 `application/problem+json`** no request seguinte, enquanto as cinco rotas públicas seguem abrindo (422/200/404 dos próprios handlers, nunca 401); `PUT /api/users/87` omitindo `is_active` renomeia e **não** reativa; a corrida real de duas conexões bloqueou o `EnrollStudentAction` por **6,2 s** e ele **recusou** com `Esta clase fue archivada y ya no acepta cambios.`; as duas sondas do arch test foram vistas reprovar e revertidas; e `GET /api/permissions` traz a permissão nova. Gate: backend **906 passed / 5 skipped** (3227 asserções) **pelo comando canônico do `CLAUDE.md` §6**, que voltou a terminar — o bloco declarou `memory_limit=512M` em `phpunit.xml` para isso, e o **merge da `main` (2026-08-23) desfez o remendo**: a `lane-b` já tinha pago a P-50 por SAPI (`memory-cli.ini` a 320M, `www.conf` a 256M) e o número maior mascararia o teto da imagem; com 320M a suíte fecha igual, reprovado no merge; frontend **87 arquivos / 481 testes**, `pnpm lint` 0, `pnpm build` verde; Pint `passed` nos 42 arquivos da sprint; `typescript:transform` **sem diff novo**. Pendências: **P-47 encerrada**; **P-49** fecha os eixos redator e turma e segue aberta só na janela cotação × orçamento; **P-51** fecha o campo de acesso e segue aberta nos outros cinco; **P-50** foi encerrada pela `lane-b` no mesmo dia, por SAPI, e o remendo desta branch saiu no merge; **P-54** nasceu (o Q-4 herdado do bloco 1, que saiu do escopo por decisão do João e não em silêncio). **D-34 ficou fora por escrita explícita** (§2 da spec) e perdeu o bloco hospedeiro com o fechamento deste — precisa de novo dono. | `plans/archive/2026-08-22-hardening-acesso-ownership-e-integridade.md` · `specs/archive/2026-08-22-hardening-acesso-ownership-e-integridade-design.md` · `context-packets/2026-08-22-hardening-acesso-ownership-e-integridade.md` · 25 commits (`79c246c6`..`f815c507`) mais o de fechamento |
