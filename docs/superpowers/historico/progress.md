# Progresso — Lotus v2

> Histórico curto do que foi entregue e da feature em andamento. Este arquivo **não define a etapa
> operacional**. Para saber o que fazer agora, leia `docs/superpowers/state.md`.
> Fila futura: `docs/superpowers/backlog.md`. Histórico antigo: `progress-archive.md`.

| Data | Entrega | Status | Resultado | Referências |
|---|---|---|---|---|
| 2026-08-17 | Dashboard · revisão de UI e passe de correção (fora de bloco) | Entregue | **Não é bloco promovido** — o João invocou `/lotus-ui-review dashboard` (passo dele, `disable-model-invocation: true`) e, lido o relatório, mandou cruzar os achados com a skill `frontend-design` e corrigir. Estado ficou em `idle` do começo ao fim, sem spec e sem plano, por decisão dele. **A revisão não era redundante, e o fechamento do B1 já dizia por quê:** `f38585e` tocou `DashboardItemRow`, `KpiRow` e `PipelineFunnel` **depois** do commit da revisão visual `3273cbf` sem re-rodar os passos visuais do DoD. Medido agora, as cinco correções de 2026-08-16 se sustentam. **6 achados B, 0 C, todos corrigidos, com número antes e depois:** agenda com uma janela deixava 351,5px vazios enquanto o nome do curso truncava em 142px para 255px de texto (passa a uma coluna de 703px, caixa de 493px, sem truncar); card de KPI media 212px com 75–95px de área morta e empurrava as duas listas para y=622 de 688px úteis em 1024 e para 1274px de um scroll de 3210 em 390 (passa a 92px, morto uniforme de 15px, listas em 406px e 698px de 2500) — o que contradizia a tese escrita no docblock da própria página; os seis números ficavam em duas linhas de base, 81px e 101px, conforme a locale quebrasse o rótulo (passam a 47px nos seis); o número em tom de aviso media **2,86:1** no tema claro, abaixo até do 3,0:1 de texto grande, e os rótulos secundários sobre card tingido mediam 4,28–4,52:1 (passam a 10,35 e 4,76:1); a árvore de cabeçalhos saltava `h1`→`h3` (ganha duas faixas `h2`); e o primeiro focável dentro de `main` era o de índice 11 (passa a 1, com Enter pondo o foco em `main#contenido`). **A tese de desenho que amarra o passe é uma só: cor de sinal vive em traço e marca, texto fica em contraste cheio** — o tom sai do número, onde precisava de 4,5:1 e entregava 2,86, e vira trilho de 3px, onde 3:1 basta e entrega 5,2–5,8. **A causa da UI-04 estava no token, não no Dashboard:** `color-mix(hue 70%, --text-color)` só fechava contraste no escuro, e elevar a participação do corpo fecharia às custas do matiz (40% de amarelo sobre slate resolve para oliva). Entram cinco tokens `--tone-*-ink` que trocam o **degrau da rampa** por tema, que é a mecânica que o `--brand-ink` já usava — e o que iguala os quatro no claro é a razão medida, não o índice do degrau (info-700, success-800, warning-800, danger-700). Guarda: `tests/tone-ink.test.ts`, 18 asserções, com o controle que reprova a fórmula antiga. **Três achados apareceram ao corrigir e não estavam no relatório:** a faixa do `AppCardHeader` media 80px para 24px de texto em **todo card da aplicação** (o `h3` sem `m-0`); a barra do funil reprovava o 3:1 de elemento gráfico contra o próprio trilho nos dois temas (2,33 e 2,77:1 — precisou trocar barra E trilho, porque o 3:1 se cobra contra a cor adjacente); e `QuoteRow`/`DocumentTypeCard` pintavam texto com `var(--red-500)` cru a 3,52:1, que é o caso da **P-36**. **Alcance fora da superfície revisada, declarado:** as quatro tintas mudam de valor em ~20 sítios, o `AppCardHeader` encurta nos 8 consumidores, `variant="stat"` também é do `BudgetStatCard` (tela de orçamento conferida no navegador) e o link de salto é do shell. **Nasce a P-46** — a causa raiz da UI-02 que o passe NÃO fechou: sem Preflight, toda tag de bloco herda margem do agente do usuário, e o reset escopado que resolveria a classe inteira mexe no espaçamento de todas as telas de uma vez. **O que continua não provado, sem maquiagem:** os estados `loading`, `error`/`staleError`, `empty` das três seções, funil todo-zero, `unauthorized`, `unsupported` e os alertas `certificate_*`/`redator_document_*` — alcançá-los exige mutação de dado ou interceptação de rota, que a skill proíbe. Gate: `pnpm lint` exit 0, `pnpm build` verde, **39 arquivos / 223 testes**; zero mutação; `backend/config/cors.php` (WIP do João) fora de todo `git add`. | `audits/2026-08-17-lotus-ui-review-dashboard.md` · branch `fix/dashboard-revisao-visual-2026-08-17` de `main@18bf487` · commits `abff4be`…`2fc0bd8` · sem spec, plano ou packet (não é bloco) |
| 2026-08-17 | Dashboard · seções analíticas, filtro de período e view do Redator (Sprint 5, bloco B2) | Entregue | **Último bloco da Sprint 5 (`backlog.md:45`), promovido explicitamente pelo João** com o estado em `idle`, pela rota `context_required` que o backlog exige — e o gate do `/planejar-bloco` reprovou pela **décima primeira** vez o mesmo erro: o argumento era linha do backlog, com numeração e markdown, não slug. **Quatro decisões dele fecharam o gate:** o slug, a rota `context_required`, **main tree** (frontend puro, gatilho da P-03 não vencia) e **a view do Redator entra inteira**, com o custo declarado na abertura e não descoberto no gate — `CreateRedatorAction.php:20` cria com `is_active=false` e `AuthController.php:52` recusa inativo, então a prova seria por payload e render. **A metade externa do Context Packet não foi aceita de chegada:** o Drive foi remedido por ferramenta própria (`modifiedTime` idêntico ao declarado) e a EAP 8.4.0 buscada direto pelo ID — **a troca 8.4.0 × 8.4.7 persiste, agora medida em terceira mão independente**, e a medição direta trouxe o fato que muda o DoD deste bloco: o corpo de UI review da 8.4.0 exige validar "separadamente perfil administrativo e Redator", **coisa que o B1 não podia satisfazer**. **A D1 se decidiu por medição de mecanismo, não por preferência de biblioteca:** o tema troca em runtime trocando o `href` de um `<link>` (`primeTheme.ts:15`), sem re-render React; SVG aceita `stroke="var(--chart-1)"` e acompanha a troca **sem JS**, canvas exigiria `getComputedStyle` mais redraw forçado. **Recharts 3.10.1**, peer medido (`^16 \|\| ^17 \|\| ^18 \|\| ^19`) e não suposto. **As 5 cores da D2 saíram com zero hex novo**, apontando para degraus que a rampa já tinha: no claro 3,41 / 3,78 / 3,96 / 3,53 / 4,47:1 sobre o card, no escuro 6,89 / 6,28 / 4,86 / 5,16 / 4,52:1, com `tests/chart-tokens.test.ts` cobrando 3:1 e 30° de matiz e reprovando a alternativa rejeitada. **Onze tasks, uma por commit**, com uma inversão deliberada de ordem: a régua `max-lines` da D8 entra na Task 5, **antes** das cinco tasks que escrevem seções novas — catraca ligada depois viraria refatoração retroativa. **Três emendas à spec, todas medidas durante o trabalho e registradas na §11:** a **D6 trocou de mecanismo, não de objetivo** — medido no observador da versão instalada (`@tanstack/query-core@5.101.1`, `src/queryObserver.ts:486-491`), `placeholderData: keepPreviousData` só entra com `status === 'pending'`, então cobre a troca normal e **não cobre a troca falhada**, que era o objetivo declarado; substituído por um piso único no hook; a **§4 e a D8 não se satisfaziam juntas** (`DashboardPage.tsx` tinha 159 linhas ANTES das 4 seções novas), resolvido pelo critério da própria D4 com `admin/AdminView.tsx`, `SectionLabel.tsx` e `DashboardSkeleton.tsx`; e a **chave i18n do KPI virou completa**, mesma correção que o Q-1 do review de 2026-08-16 já fizera naquele arquivo. **Quatro coisas que o plano mediu em vez de supor mudaram código escrito:** `common.yes`/`common.no` não existiam em nenhuma locale; a contagem de rodapé do repositório não usa plural do i18next; não existe rota de detalhe de relator; e o Meu Perfil é `/perfil`. **DoD provado item a item, e não por ferramenta verde:** janela alcança só séries e rankings (7 seções byte-idênticas entre janelas); janela invertida devolve 422 com `La fecha de término no puede ser anterior a la de inicio.` e a tela **mantém** o dado; **gate `null` provado com papel-sonda criado e removido por API** — `redatores: null` some a tabela inteira, `series.uf_aprovada: null` some do gráfico E da legenda, `rankings.*.uf_aprovada: null` devolve zero barras; view do Redator provada por payload real capturado com `acting-as` e alimentado a sonda de render (zero `client`/`Cliente`, zero `UF`, `href=/perfil` presente); 3 locales sem chave crua; régua da D8 verde no HEAD e reprovando com sonda de 200 linhas (`File has too many lines (230). Maximum allowed is 150`); **zero mutação** por `COUNT(*)` nas 35 tabelas em 6 chamadas (`SEM MUTACAO`); e o escopo **medido** — `git diff main...HEAD -- backend/` e `-- generated.ts` vazios, Pint e `typescript:transform` N/A por medição e não por suposição. **O `/lotus-ui-review` do Step 9 é passo do João e foi o mais caro do bloco: 10 achados, 2 C e 8 B, todos corrigidos e cada um provado no navegador contra a API real.** Os dois C eram leitura errada de número, não incômodo: o `AppLineChart` fixava `dot={false}` e **série de um mês dentro de um gráfico de vários não desenhava nada** — três das quatro curvas eram `M526,207.405Z`, e o eixo ia a 60 por causa das 55 matrículas que ninguém via; e em 390px o CTA "Ir a Mi Perfil" saía do card **e da viewport**, sem rolagem que o alcançasse. **Três mecanismos tiveram de ser trocados no meio, os três por medição:** `mt-auto` no `KpiRow` é letra morta porque o `[&_p]:m-0` do `AppCard` casa `.classe p` (0,1,1) e vence utility (0,1,0), então a base do número passou a ser ancorada por `justify-between` e `pt`; a sombra de rolagem da tabela nasceu invisível porque o Lara pinta a linha do corpo em branco **opaco** por cima do invólucro, o que obrigou a contraparte na `brand-theme.css`; e a barra de rolagem sempre visível foi tentada duas vezes e descartada — com o CSS comprovadamente carregado, `offsetHeight - clientHeight` ficou **0**, porque este Chromium força barra em overlay. **Uma escolha de API foi além do relatório de propósito:** o `emptyHint` do `AlertList` entrou **obrigatório**, sem default, porque com default um call-site esquecido volta a herdar a frase do admin em silêncio — que é exatamente o defeito UI-07. **Gate final medido:** `pnpm lint` exit 0, `pnpm build` verde, **44 arquivos / 262 testes** (baseline 39/223), bundle `index.js` de **1.264,48 kB para 1.672,59 kB** (gzip 350,52 → 464,97) — o custo do Recharts, que só entrou no bundle quando a Task 7 lhe deu consumidor. **As quatro limitações da spec §9 seguem válidas e declaradas:** a view do Redator não roda com sessão criada pelo fluxo normal (o João **ativou** um redator para a revisão de UI, e essa ativação é dele, não do bloco); a carga de relatores mostra os dois nomes de sonda da **P-44**, "E2E Gate Redator 1" e "E2E Gate Redator 2", residência de gates anteriores; permissão parcial não se anuncia (D7); e o detalhe de item do servidor segue em espanhol nas outras duas locales (D-18). **O que NÃO ficou provado, sem maquiagem:** os seis estados que o próprio relatório lista como inalcançáveis pelo navegador sem mutação ou interceptação de rota — skeleton, `unauthorized`, admin parcialmente gateado, `error` de tela cheia, aviso `InlineLoadState` do Redator e empty state dos rankings com o gate comercial fechado; o gate `null` do admin fica provado fora do navegador, pelo papel-sonda. `backend/config/cors.php` (WIP do João, o outro lado da P-45) ficou fora de todo `git add`. **O review de fechamento devolveu 4 achados, nenhum de lei, e o João aprovou os quatro.** O aviso de falha do admin morava DENTRO do seletor de período e sumia junto com ele quando `series` e `rankings` eram nulas — e o caso foi **provado alcançável contra a API real**, não argumentado: papel só com `identity.user.view` devolve as duas seções nulas e ainda assim `ready-admin`, sustentado por um único alerta de documento de relator. A métrica de UF era oferecida com o gate comercial fechado e o vazio mentia: `uf_aprovada` nula em TODA linha **é** o gate, porque `AnalyticsQuery.php:319` preenche `'0.0000'` sempre que `includeUf` — medido no payload real, 4 cursos e 4 clientes, todas nulas. A âncora de meio-dia estava copiada em cinco componentes e virou `formatIsoDate` em `shared/lib/datetime.ts`, com dois testes que não dependem do fuso da máquina. E `retry` era campo sem consumidor nas duas variantes `ready`, morto pelo mesmo critério que a D3 usou contra o `unsupported` — o teste que o usava passou a forçar o refetch por `queryClient.refetchQueries()`, que é o que o TanStack Query faz sozinho no produto. **Gate de fechamento medido:** `pnpm lint` exit 0, `pnpm build` verde, **44 arquivos / 264 testes**; `php artisan test` **684 passed / 5 skipped / zero falha** com `FRONTEND_URL` de origem única, e **12 failed / 672 passed** com o `.env` como está — a diferença é a variável, não o código, e é a **P-45** medida idêntica pela segunda vez, fora do escopo deste bloco. Pint e `typescript:transform` seguem N/A por escopo medido: `git diff main...HEAD -- backend/` e `-- generated.ts` continuam vazios. | Plano `plans/archive/2026-08-17-dashboard-frontend-analitico-e-redator.md` · spec `specs/archive/2026-08-17-dashboard-frontend-analitico-e-redator-design.md` · packet `context-packets/2026-08-17-dashboard-frontend-analitico-e-redator.md` · revisão de UI `audits/2026-08-17-lotus-ui-review-dashboard-analitico-redator.md` · commits `e48b4ae`…`a71e11d` |
| 2026-08-18 | UI · `/perfil` + kit compartilhado: contraste, contenção, semântica e densidade (BD-16) | Entregue | **BD-16 do `backlog.md`, promovido explicitamente pelo João em 2026-08-17** a partir da auditoria `audits/2026-08-17-perfil-ui-review-e-design.md` (1 achado **C**, 8 **B** do `/lotus-ui-review`, mais 7 estéticos do `frontend-design`). Worktree `fix-frontend`, branch `feat/bd16-perfil-e-kit-compartilhado` a partir de `254d691` — frontend puro, a P-03 não dispara. **16 tasks, 15 commits de task na ordem do plano** (`8ffdefa`…`b77ce75`): as 8 primeiras entregam o kit em `shared/ui`, as 7 seguintes aplicam em `/perfil`, a 16ª é o gate. **A Task 8 virou registro medido, não correção** — o `onToggleMaskKeyDown` do Prime já trata Espaço, e um handler próprio alternaria duas vezes: o ramo que o planejamento previu. **O `aria-pressed` da D-24 foi recusado com motivo** (o nome do olho alterna a cada clique, então o estado já é anunciado uma vez). **O gate rendeu 10 correções que build, lint e suíte não veem** (`6a5df00`…`a38aec5`), uma por commit, e a D-25 sobreviveu à primeira delas: o `focusOnShow` do Prime foca o primeiro FOCÁVEL, que num PDF é o próprio `<iframe>`, e o visor nativo ainda toma o foco ~200ms depois de abrir — a devolução é única por abertura e o limite está declarado no docblock, não maquiado. **A revisão de UI (Step 6, do João) achou 0 defeitos, 7 melhorias e nenhum item A pendente**, e as 7 viraram commit próprio (`4006ead`…`058b80f`), cada uma medida no navegador antes e depois: tinta `danger` de texto 3,44:1 → **5,83:1** no claro; nome acessível do upload passa a conter o rótulo visível; alvo do olho da senha 16×16 → **28×28**; baixar arquivo deixa de ser `<a>` sobre `<button>` e vai de 6 paradas de Tab para 3, todas nomeadas; ação do slot vazio volta ao eixo em 390px (297 → **348**, como os outros três); vão rótulo/valor do resumo em 1024px 548px → **214px**; e a ordem de foco (UI-01) foi corrigida e **revertida no mesmo dia, por decisão do João**: virar as colunas em `xl` alinhava DOM e pintura nas três larguras, mas tirava a identidade da esquerda no desktop, e o layout venceu — o achado segue válido, o preço dele foi aceito com o número na mão e virou o débito **D-32**. **P-36 e P-37 encerradas pelo gatilho literal.** Gate do bloco: `pnpm build` verde, `pnpm lint` 0, **54 arquivos / 321 testes** contra a baseline de 45/250. **A revisão de sprint (`/revisar-sprint`, risco BAIXO, uma lente) achou 3 e nenhuma violação de lei**, os três aprovados pelo João e aplicados um por commit (`e9f53f3`, `a4eac5c`, `fb2d38b`): o `role="button"` do upload ganhou a metade que faltava do contrato — Espaço ativa e `aria-disabled` anuncia o estado —, o `pt` morto do `QuoteRow` saiu e o `mergePt` passou a compor função nos **dois** sentidos. **Fechado com o critério de aceite provado em Chromium real contra a API em `:8080`:** Espaço e Enter ativam o disparador uma vez cada, `aria-disabled` vai de ausente a `true` com o POST `/api/profile/documents` segurado em voo (e o alvo segue focável, de propósito), os nomes por documento seguem de pé em EN e es-CL, e o `maximizableButton` do `AppDialog` — a função que o `mergePt` novo compõe — ainda alterna `Maximizar diálogo`/`Restaurar diálogo`. Zero resíduo no banco de dev. Suíte de backend em **684 passed / 5 skipped** com `FRONTEND_URL` de uma URL só; as 12 falhas do primeiro run são a **P-45**, do `.env` do main tree, e não deste bloco (zero arquivo em `backend/`). Gate final: build verde, lint 0, **54 arquivos / 327 testes**. Fica registrado o **D-33** — o foco cai no `<body>` quando o olho da senha alterna —, medido no fechamento e **pré-existente**: o main tree devolve `BODY` igual. | `plans/archive/2026-08-17-bd16-perfil-e-kit-compartilhado.md` · `specs/archive/2026-08-17-bd16-perfil-e-kit-compartilhado-design.md` · `audits/2026-08-17-perfil-ui-review-e-design.md` · 40 commits (`86ec2dd`..`0a1918b`) mais o de fechamento · sem context packet (o bloco nasceu de medição local) |
| 2026-08-18 | Frontend · listagens e abas: plural, ausência e custo de montagem (BD-13) | Entregue | **BD-13 do `backlog.md`, promovido explicitamente pelo João em 2026-08-18** com o estado em `idle`; worktree `fix-frontend`, branch `feat/bd13-listagens-e-abas`, a partir de `main@b758068`. **Frontend puro** — `git diff main...HEAD -- backend/ frontend/src/shared/types/generated.ts` devolve zero arquivo, o que mantém suíte de backend, Pint e `typescript:transform` N/A por escopo medido, e a P-03 sem disparar. Cinco débitos fechados em 9 tasks: **D-02** (plural i18next em 17 chaves das 3 locales, com `plural.test.ts` novo provando a paridade), **D-31** (chaves órfãs de `/perfil`), **D-06** (a linha do certificado corrompido diz qual campo falta, em vez da célula vazia), **D-05** (`screenDetail` — só chega à tela o `detail` que o próprio front escreveu, marcado com `localDetail: true`; a única exceção é o `CertificateViewDialog`, pela D8) e **D-04** (a `PeoplePage` virou casca de abas e o dado desceu para `RedatoresTab`/`StudentsTab`, onde o `renderActiveOnly` do TabView alcança, com `staleTime: 30_000` repassado pelo `useCrudPage`). O `/revisar-sprint` classificou **risco BAIXO** (uma lente, sem Codex) e devolveu **3 achados**, os três aprovados e corrigidos: **Q-1** — a dica da falha de carga passa a vir do **status** (`loadErrorHint` devolve chave i18n: 403, 404 e 422 deixam de virar "revise sua conexão"), em 21 sítios e nos 3 produtores de estado de carga; **Q-2** — `certificate.certCount` era órfã e a D-02 a tinha pluralizado, então foi apagada das 3 locales; **Q-3** — o import duplicado de `@shared/lib` em `StudentDetailSections.tsx`, **sem** a regra `no-duplicate-imports` sugerida, porque a medição achou 55 pares legítimos `import`/`import type` contra 1 duplicata real. **DoD provado no navegador contra a API real**, que era o passo que faltava: em `/personas`, 1 GET `/api/redatores` e **zero** `/api/students` na montagem, 1 GET `/api/students` ao abrir a segunda aba e **nenhuma** request ao voltar dentro dos 30s; e um 404 real de `/api/turmas/999999` chega à tela como "No encontramos este registro.", sem o `detail` cru (`No query results for model …`). Gate: frontend **65 arquivos / 398 testes** verdes, `pnpm lint` 0, `pnpm build` verde; backend intocado (a suíte de 713 que rodou é a do main tree, em outra branch). Débito que sai do bloco: **D-36** (o envelope RFC 7807 não é localizado). | `plans/archive/2026-08-18-bd13-listagens-e-abas.md` · `specs/archive/2026-08-18-bd13-listagens-e-abas-design.md` · 18 commits (`319994a`..`ddff65b`) mais o de fechamento · sem context packet (o bloco nasceu de débito medido) |
| 2026-08-18 | Cadastros · arquivados e restauração de soft-delete (`Client` + `Course`, fatia vertical) | Entregue | Item 1 de "Próximos blocos", promovido explicitamente pelo João. **O lado do arquivar já existia; o que faltava era o restore** — nenhum hook `restoring`, nenhuma Action, nenhum endpoint, nenhuma tela, e nenhuma forma de saber qual filho a cascata arquivou. A **D2** resolve com uma coluna marcadora, `archived_with_parent`, em `client_addresses`, `client_contacts`, `users`, `course_modules` e `course_certificate_templates`: sem índice e fora do `$fillable`, porque quem escreve é hook. O par de hooks é `deleting` (antes) / `restored` (depois), nunca `restoring` — com `restoring` os filhos voltariam a ativos enquanto o pai ainda está arquivado. **O `/revisar-sprint` classificou risco ALTO e rodou duas lentes** (Claude + Codex read-only), com 7 achados aprovados pelo João e mais o Q-8 herdado da nota do DoD. O 🔴 do bloco: `markAndDelete` não tinha guarda de filho já arquivado, e como `Client::user()` é `withTrashed()`, o User arquivado de propósito ANTES do pai voltava na cascata **e** tinha o `deleted_at` reescrito. A correção mora em `App\Shared\Concerns\ArchivesChildren` — home única, porque os outros seis roots replicam o padrão e a guarda copiada teria de ser consertada oito vezes. O Q-8 ganhou o gêmeo de leitura, `LoadsCascadedChildren`: a lista de Arquivados mostrava comuna `—` e `0` contatos, negando o registro justamente onde o operador precisa reconhecê-lo. Duas divergências do Codex foram **verificadas e recusadas**, mostradas ao João em vez de resolvidas em silêncio. Fechado com o critério de aceite provado **contra a API real em `:8080`** (comuna `Providencia`, 1 contato, o contato arquivado antes do pai fora da lista e ainda arquivado depois do restore; `POST /api/clients/abc/restore` = 404) **e em Chromium real**: `Santiago`/`1` na linha de Arquivados, toast `Record archived.` e `Record restored.`, e os botões de Restaurar `disabled` com a mutação em voo (Q-2). Zero resíduo no banco de dev. Gate: backend **717 passed / 5 skipped** (2636 asserções), `pnpm test` **62 arquivos / 378 testes**, lint 0, build verde, `typescript:transform` sem drift, Pint `passed`. Encerra a **P-45** (o `TestCase` lia `FRONTEND_URL` cru e derrubava 12 testes de sessão) e registra a **D-37** (nascida como `D-34`), dívida de backfill da migration da coluna. | `plans/archive/2026-08-18-arquivados-e-restauracao.md` · `specs/archive/2026-08-18-arquivados-e-restauracao-design.md` · `context-packets/2026-08-18-arquivados-e-restauracao.md` · 17 commits (`34aa0be`..`3d02a46`) mais o de fechamento |
| 2026-08-19 | Identity · ativação de acesso do redator: convite, primeiro acesso, recuperação e revogação | Entregue | **Item 4 de "Próximos blocos" do `backlog.md`, promovido explicitamente pelo João em 2026-08-18** com o estado em `idle`; rota `context_required`, porque "como o redator recebe a credencial" é decisão de produto e a fonte é externa ao repositório. Worktree `fix-frontend`, branch `feat/identity-ativacao-acesso-redator`, a partir de `main@2c7b249` — **bloco de backend em worktree linkada por decisão explícita do João**, contra a regra do `/planejar-bloco`, com a exceção declarada na abertura e não descoberta na execução (ver P-03). **O bloco fecha a RN-01 pela metade que faltava:** o redator passa a existir com credencial. Dois brokers, um por janela — `invites` (`config/auth.php`, tabela própria `invitation_tokens`, 7 dias) para o primeiro acesso e `users` (60 min) para a recuperação, porque `expire` é aplicado por quem valida e uma tabela só daria ao token de recuperação a janela do convite. Três rotas públicas sob `throttle:6,1` (`/password/forgot`, `/password/reset`, `/invitation/accept`), duas Notifications (`RedatorAccessInvitation`, `PasswordResetLink`) e transporte de e-mail novo no compose (Mailpit). `is_active` vira **controle explícito do admin** — conceder acesso não é efeito colateral de reenviar convite —, e revogar purga as sessões vivas (`PurgeOtherSessionsAction`), porque `auth:sanctum` não reconsulta senha nem `is_active` a cada request. A role `redator` passa a ser atribuída no cadastro. Frontend: tela pública `/definir-clave/:token`, com `?flow=` escolhendo o endpoint. **Emenda de 2026-08-19, pedida pelo João com o bloco em `ready_for_review`:** `/login` e `/recuperar-clave` viram rotas irmãs do mesmo layout (`AuthPanel` + `useAuthPanel`), `ForgotPasswordPage` morre e o e-mail digitado sobrevive à troca — premissa medida no `react-router@7.18.0` (`_renderMatches` monta `RenderedRoute` **sem `key`**, então irmãs com o mesmo `element` reconciliam em vez de remontar) e confirmada na tela. O `/revisar-sprint` devolveu **6 achados, os seis aprovados pelo João e corrigidos**: **Q-1** — reenvio de convite não atribuía a role `redator`; **Q-2** — as rotas públicas de senha enumeravam usuário (mensagens distintas para `INVALID_USER` e `INVALID_TOKEN`, e 500 vs 200 com SMTP fora do ar) e a recuperação não derrubava sessão; **Q-3** — o convite de senha alcançava cliente e aluno, que pela RN-01 não autenticam (filtro `is_active` no `sendResetLink`); **Q-4** — falha que não nomeia campo (429/419/500) ficava muda no `SetPasswordPage`; **Q-5** — o reset agora purga em transação junto com a troca de senha; **Q-6** — o TTL dos dois brokers provado por **comportamento**, não por leitura de config. **DoD provado no fechamento contra a API real** (nginx 8080, Mailpit, banco de dev): reenvio de convite atribuiu a role a `juan.morales@lotus.cl` (`roles=[]` → `[redator]`) e entregou o e-mail com `?flow=invite` e "vence en 7 días"; `/invitation/accept` definiu a senha e o login devolveu as 4 permissões da role; `forgot` respondeu **byte a byte igual** para e-mail que existe, que não existe e de **cliente**, entregando só no primeiro; `reset` com token falso devolveu resposta idêntica para e-mail existente e inexistente; o reset derrubou a sessão viva (`sessions` 1 → 0, cookie antigo em 401 RFC 7807); a revogação por `PUT /api/redatores/1` derrubou a sessão e recusou o login novo com "Esta cuenta no está activa."; no navegador, o e-mail digitado atravessou a troca `/login` → `/recuperar-clave` com **uma** entrada de navegação e foco no `<h1>`, e o 429 real apareceu como alerta na tela do `/definir-clave` (a prova do Q-4). RN-01 medida no fim: cliente e aluno ativos = **0**. Catracas: backend **710 passed / 5 skipped**, `pnpm lint` 0, `pnpm build` verde, frontend **69 arquivos / 409 testes**, Pint verde nos 29 arquivos do bloco e `typescript:transform` sem diff em `generated.ts`. **Nasce a P-47** (os 7 redatores do seed não têm a role — dado de seed, não código do bloco); **P-03 e P-45 remedidas sem fechar**; a sonda `gate.task14@lotus.cl` criada pelo gate da Task 14 foi removida no fechamento. | `plans/archive/2026-08-18-identity-ativacao-acesso-redator.md` + `plans/archive/2026-08-19-login-recuperacao-inline.md` · `specs/archive/2026-08-18-identity-ativacao-acesso-redator-design.md` + `specs/archive/2026-08-19-login-recuperacao-inline-design.md` · `context-packets/2026-08-18-identity-ativacao-acesso-redator.md` · 34 commits (`03a0b72`..`929b1e6`) mais o de fechamento |
| 2026-08-19 | Cadastros · arquivados e restauração nos seis roots restantes (`Budget`, `Quote`, `User`, `Redator`, `Turma`, `Enrollment`) | Entregue | Item 1 de "Próximos blocos", promovido explicitamente pelo João com o estado em `idle`; branch `feat/arquivados-roots-restantes` nasceu de `feat/arquivados-e-restauracao@6fd0ad8` — não da `main` — porque `ArchivesChildren`, `useArchivedPage` e o `archived` do `createCrudResource` só existem lá. **O bloco não era a replicação que o backlog previa:** a medição separou os seis roots em três classes e achou um **500 alcançável** — `turmas.active_quote_id` é coluna gerada STORED com `UNIQUE`, e `CreateTurmaAction` deixava criar turma nova sobre a cotação de uma arquivada, então restaurar a primeira estourava `SQLSTATE[23000]`. Sete decisões do João (D1–D7) e 15 tasks em três fases; o `RestoreTurmaAction` converte o conflito em **422**, `Turma` ganha a cascata que nunca teve (matrículas + documentos), `Redator` ganha gate de turma em andamento e `Turma::redatores()` passa a `withTrashed` — sem isso o redator arquivado sumia da emissão e **recusava certificado de turma concluída que ele ministrou**. Três colunas novas (`quotes`, `files`, `enrollments`), cinco permissões `*.restore` (staff **não** ganha uma: seu `destroy` é `identity.access.manage`), dois QueryBuilders novos e a dívida de copy do molde paga (o "no se puede deshacer" de orçamento e cotação). Review de risco **ALTO** com duas lentes: seis achados, nenhum 🔴, todos aprovados e corrigidos — o restore de cotação passou a exigir orçamento ativo (422), o aviso de nome perdido passou a valer nos dois modos do `QuotesList`, o `useRestoreQuote` passou a invalidar o detalhe do pai, e o **kit de arquivados virou um só** (`useArchiveAction`, `ArchiveRowActions`, `ArchiveConfirmDialog`: −603/+403 linhas, regra escrita em `.claude/rules/frontend-fsliced.md`). DoD no navegador nas três fases mais os dois 422 novos provados no fechamento (2026-08-19), com o banco de dev devolvido ao estado anterior. Gate: backend **797 passed / 5 skipped**, frontend **67 arquivos / 394 testes**, lint 0, build verde, Pint `passed` nos 54 PHP do bloco, `typescript:transform` sem drift. Encerra o rastro da **P-45**, atualiza a **P-47** (o eixo da cotação ganhou tomador dos dois lados), a **P-35** e a **P-44**, e registra a **P-48** (o `title` do RFC 7807 em português com `detail` em es-CL). | `plans/archive/2026-08-18-arquivados-roots-restantes.md` · `specs/archive/2026-08-18-arquivados-roots-restantes-design.md` · `context-packets/2026-08-18-arquivados-e-restauracao.md` (herdado) · 36 commits (`29acb5db`..`6a1c0bac`) mais o de fechamento |
| 2026-08-20 | Frontend · a superfície de arquivados: data, coluna e escolha de fonte (BD-17) | Entregue | Bloco de dívida promovido pelo João depois do `/revisar-frontend` de 2026-08-19, executor `claude`, frontend puro — zero `backend/`, zero `generated.ts`, fence provado por `git diff main...HEAD` vazio nos dois paths. **Entrega:** as duas colunas de rastreio viram `archivedColumns(t)` em `shared/ui` (**função**, nunca componente — o `DataTable` lê o filho DIRETO, então um Fragment achataria as duas numa coluna lixo sem estourar build, lint ou teste; a catraca prova as duas formas), o `formatDate` do D-51 passa de 8 pousos para 1 (o nono, `ArchivedQuotesList`, é layout flex e foi corrigido direto), `ArchivableRow<T>`/`ListSource<T>`/`archivableSource()` em `shared/lib` matam as 8 declarações de `XRow` à mão e o quarteto de ternários das 6 páginas, e a `OperationPage` — o pior sítio do D-52, que derivava `loadError` dentro da prop — passa a consumir `useTurmasPage()`/`usePendingQuotesPage()`. **DoD provado na tela** (navegador `en-US`, interface `es-CL`), não no diff, no molde do precedente `AppFileRow.test.tsx`. **Dois débitos nasceram da medição, nenhum é regressão do bloco:** **D-54** (o `refetch` do `useLoadState` engole a promise do Q-14 — por isso os dois aliases novos nasceram sem ele) e **D-55** (o `DataTable` não repinta células `body` na troca de idioma ao vivo; isolado como limitação de plataforma pelo `ÚLTIMO ACCESO` e pelo `AppTag`, que congelam igual, e pelo `ArchivedQuotesList` fora de tabela, que troca ao vivo). **Review:** zero violação de lei, zero 🔴/🟡, dois 🟢 — Q-1 corrigido no branch (o alias de pendentes foi para arquivo próprio) e Q-2 registrado como **D-56**, com o texto da linha de rule guardado na ficha para quando o débito for pago. **Gate:** `pnpm build` verde, `pnpm lint` 0, `pnpm test` 81 arquivos / 453 testes (baseline 77 / 435), backend 828 passed / 5 skipped intocado. | `plans/archive/2026-08-20-bd17-superficie-de-arquivados.md` · `specs/archive/2026-08-20-bd17-superficie-de-arquivados-design.md` · sem packet · 18 commits (`82c1d0c4`..`1a92a82f`) mais o de fechamento |
| 2026-08-20 | Backend · contrato de entrada: o que o corpo da requisição pode escrever (`bd14-contrato-de-entrada`) | Entregue | Quatro itens num bloco só, backend puro, no main tree. **D-13:** `WritableAttributes::from()` vira a tradução única de `Optional` para atributo gravável — omissão preserva o valor guardado, `null` explícito segue sendo o único jeito de apagar; 10 campos em 5 `Update*Action`, e as três grafias divergentes (`UpdateStudentAction` por ternário, `UpdateProfileAction` por `if`, o spread de `is_active` do redator) convergiram para a fonte única. **D-12:** chave `#[Computed]` no corpo de escrita vira **422** por `missing` (e não `prohibited`, que é `! required` no vendor e deixaria passar presente-mas-vazio), a partir da lista única de `ComputedFields`; 6 DTOs de entrada mais arch test dos 11 campos de foto. **P-29 encerrada:** colisão de índice único de `users` traduzida em 422 do campo na porta única (`UserProvisioner::writing()`), pelas duas grafias de driver. **P-35 encerrada:** `seq_in_budget` fora do `$fillable`, escrito por atribuição explícita sob o lock que já existia. Review de risco ALTO com duas lentes (Codex independente sem achado) devolveu dois achados de **alcance**, os dois corrigidos: a lei da D-12 passou a valer para as chaves `#[Computed]` não-foto (`last_login`, `current_client_id`, `current_client_name`, `enrollments_count`) e o arch test passou a varrer também `app/Shared/*/Data`. **Nasceu a P-51:** o `DefaultValuesDataPipe` do Spatie entrega o default literal antes do ramo que preencheria `Optional`, então `WritableAttributes` não alcança propriedade com default — e `UserData::$is_active = true` significa que um `PUT` que omite a chave **reativa staff desligado**. Fora do escopo medido do bloco, com os dois remédios custeados; espera decisão do João. | `plans/archive/2026-08-20-bd14-contrato-de-entrada.md` · `specs/archive/2026-08-20-bd14-contrato-de-entrada-design.md` · sem context packet (escopo interno) · 22 commits (`9b194538`..`02e93684`) mais os do review e o de fechamento |
| 2026-08-20 | Frontend · `useLoadState`: a promise do `refetch`, a forma da lista e os dois ramos crus (BD-18) | Entregue | Bloco de dívida promovido pelo João com a árvore em `idle`, pela rota direta a `ready_for_planning` — os três débitos nasceram de medição local (D-54 e D-56 no review e no fechamento do BD-17, D-14 no review do BD-6), então não havia fonte externa a recuperar e `context_packet` ficou `null` do começo ao fim. **O escopo era maior do que as fichas registravam, e isso foi medido antes de desenhar:** a varredura por forma (`void <query>.refetch()`) contra `93acf6a7` achou **14 produtores em 12 arquivos** — não os 7 da ficha —, dos quais **três travavam a promise por TIPO** (`() => void` na assinatura), e a política `loadFailure` estava escrita à mão em **12** sítios, não em 6. **Duas peças novas em `shared/hooks/listSource.ts`, não uma:** `listSource` para a forma de página e `loadFailure` para os dois hooks de carga, que falam outra grafia e não caberiam na primeira. O `refetch` é **anotado** `(): Promise<unknown>` e não deixado inferir — o inferido vaza `QueryObserverResult` para cima por `ReturnType<>`. **`useRetryPending` saiu junto** e é consumido pelos dois componentes de falha: sem ele o `InlineLoadState` seguiria descartando a promise recém-corrigida em 12 usos. **Duas exceções declaradas, não esquecidas:** `useHistorial` e `useEmissionPanelState` devolvem `null` onde a política devolve `{}` — outra política, e trocá-la mudaria tela sem DoD que a cobrisse. **DoD provado no navegador** contra a API real, com falha isolada por rota: o "Reintentar" de tela cheia fica `disabled` com o GET segurado em voo, o do `InlineLoadState` ganha o spinner que não tinha, e a seção de redatores mantém os três em cache quando o GET falha. **Review:** risco BAIXO, uma lente, zero violação de lei, nenhum 🔴 — quatro achados aprovados e corrigidos (`loadFailure` no último ramo à mão do dashboard; catraca do `AppErrorState`, que não tinha arquivo de teste; `loadMessage` em `shared/lib`, fechando a mensagem repetida em 13 sítios de 8 componentes; e o terceiro sítio do D-14, no detalhe do aluno). **Fechamento:** o ramo do Q-1 provado na tela — com o `GET /api/students/35` em 500 e cache em mão, "Company links" e "Turma history" permanecem e o aviso vai ao lado; sem cache o `AppErrorState` substitui as duas, e o Retry recupera. **Gate:** `pnpm lint` 0, `pnpm build` verde, `pnpm test` 86 arquivos / 479 testes (baseline 81 / 453), backend 872 passed / 5 skipped intocado (binário direto, P-50). | `plans/archive/2026-08-20-bd18-useloadstate-promise-e-forma.md` · `specs/archive/2026-08-20-bd18-useloadstate-promise-e-forma-design.md` · sem packet · 20 commits (`93acf6a7`..`f3f971e8`) mais o de fechamento |
