# Progresso arquivado — Lotus v2

> Histórico anterior às dez entregas mais recentes. Consultar somente sob demanda.
> O estado atual está em `state.md`; o histórico curto está em `progress.md`.
>
> **Duas arities convivem nesta tabela, de propósito.** As linhas antigas têm sete colunas
> (`Contexto`/`Plano`/`Spec` separadas); as movidas de `progress.md` a partir de 2026-08-13 têm
> **cinco**, com plano, spec, packet e commits na coluna `Referências` — elas entram **verbatim**,
> porque o fechamento move histórico e não o reescreve. É a mesma divergência de formato que a
> **P-23** registra desde 2026-07-30, e ela fecha quando o João decidir o formato do `progress.md`.

| Data | Feature | Status | Resultado | Contexto | Plano | Spec |
|---|---|---|---|---|---|---|
| 2026-08-13 | Auditoria · rastro, unicidade e gate no eixo de peso legal (BD-8) | Entregue | **BD-8** do backlog, promovido explicitamente pelo João em 2026-08-12 com **paralelismo autorizado** (a outra frente, `faixa-visivel-e-acessibilidade-dos-dialogos`, é frontend, então a P-03 não dispara) e **rota direta a `ready_for_planning` sem Context Packet**, por ausência medida de fonte externa — o bloco nasceu de revisão do próprio repositório. Backend e schema → main tree, sem worktree. **O terreno foi medido antes de desenhar e mudou o desenho:** os call-sites crus de pivot eram exatamente **cinco** (a guarda nasce verde, allowlist de um arquivo); o rastro de pivot **não era fraco — não existia** (as 14 asserções sobre `audits` cobriam dois eventos, nenhum de `sync`/`attach`/`detach`); o `version` tinha **três** caminhos de escrita, não um; o replace nested do `UpdateCourseAction` obriga `withTrashed()` na derivação, senão a segunda salvada bate no `unique`; e um **quarto** caminho sem gate que o relatório não listou apareceu no próprio código (`DeleteTurmaAction`, "hoje sem gate" escrito no docblock), o que levou o gate de dez para **onze** caminhos por decisão do João. **As peças:** `App\Shared\Audit\PivotAudit` como fonte única da escrita de pivot auditada nas duas portas de emissão (`turma_redator`, `course_redator`); guarda estática em `PersistenceLawsTest` que reprova pivot cru; `UNIQUE(course_id, version)` em `course_certificate_templates`; `CreateCertificateTemplateAction` derivando `MAX(version)+1` sob `lockForUpdate` **com `withTrashed()`**, com `version` fora do `$fillable` (a Action é a escritora exclusiva — payload com `version: 99` é ignorado); e `Turma::assertAcademicallyWritable()` nos onze caminhos, com nome e mensagem **verbatim** para não churnar os testes que afirmam o texto. Execução em **SDD**, seis tasks com commit, todas Approved. **Dois vermelhos de audit não discriminavam e um teria passado falso** — as contagens literais do brief incluíam a linha `created` que a própria fixture grava, e um `assertSame(1, …)` casava com ela contra o código velho; corrigido nos **testes**, filtrando por evento, sem tocar o helper para caber em asserção. **Review de sprint ALTO RISCO em duas lentes** (Claude com o gabarito do projeto + `mcp__codex__codex` read-only): **6 achados, todos aprovados pelo João e corrigidos** em `bd769f8`, órfãos zero, **dois achados do Codex recusados com evidência** (o gap lock do InnoDB torna a corrida do `lockForUpdate` bloqueio e não corrida, e a D16 já declarava a degradação; o TOCTOU do gate é forma anterior ao bloco). **Q-2 é o achado que o bloco existia para consertar e tinha consertado pela metade:** o `auditSync` do pacote grava `old->diff(new)` (conferido em `vendor/owen-it/laravel-auditing/src/Auditable.php:828-829`), então habilitar um curso a mais gravava `old_values {"courses":[]}` — numa porta que decide **quem assina certificado**, o estado anterior não era reconstruível, e com a D2 (sem backfill) nem pela soma das linhas. O helper deixou de delegar e passou a montar o `AuditCustom` à mão, com o **conjunto** dos dois lados lido do banco antes e depois da escrita. **Q-6 é o segundo mais grave e revelou um buraco anterior a ele:** o gate perguntava `=== Concluida` (fail-**open** — um terceiro status futuro abriria os onze caminhos sem nenhum teste virar vermelho) e voltou a `!== EmAndamento`; com o fail-closed, **sete casos da suíte reprovaram sem falar de conclusão**, porque `Turma::create([...])` sem `status` devolve a instância com `status` **NULO** — o default é do INSERT, não do objeto. Corrigido no model (`$attributes`), com guarda que varre `TurmaStatus::cases()`. Os outros quatro: Q-1 (a guarda de pivot era cega para `->Sync(` — o dispatch em PHP é case-insensitive — e varria só `app/`), Q-3 (pivot e audit não eram atômicos nos três call-sites sem transação externa; a `DB::transaction` entrou **dentro** do helper e vira savepoint em quem já a abria), Q-4 (o caso do no-op **não discriminava**: com `sync()` cru de volta na Action ele passava) e Q-5 (`makeTemplate()` engolia chave desconhecida em silêncio). `.claude/rules/migrations.md` dizia "Pivot não audita sozinho: use `auditSync`" — linha que a Q-2 tornou **falsa** e que é carregada por quem toca em schema; reescrita apontando para o `PivotAudit`, com a razão (delta vs. conjunto) junto. **Gate de fechamento refeito contra a API real, não herdado** (as correções entraram depois do e2e de execução e mexeram no helper, no gate e no model): sessão Sanctum por cookie + CSRF; **o conjunto provado nas três portas**, com o `auditable_type` do model que o usuário tocou (D13) — designar o redator 3 na turma 4 gravou `old {"redatores":[1]}` → `new [1,3]` (com o `auditSync` viria `[]`), o `detach` gravou `[1,3]` → `[1]`, a habilitação pelo lado do curso gravou em `course` e o `PUT /api/redatores/2` gravou em `redator` **de dentro da transação externa** (savepoint, sem 500); **as três repetições idênticas gravaram zero linha** (D12); derivação com `version: 99` no payload → **92**, e o `withTrashed()` **discriminado de verdade**, não só exercitado (arquivada a v92, a criação seguinte deu **93** — sem `withTrashed()` daria 92 e o `unique` estouraria); `INSERT` direto do par repetido recusado pelo banco (`Duplicate entry '8-92'`); **seis caminhos da RN-15** em turma concluída devolvendo **422 `application/problem+json`** com a mensagem exata sob a chave `turma` (designar, `DELETE` da turma, `PUT` da turma, matricular, remover matrícula e resultado acadêmico), enquanto a turma nova aceitou designação, matrícula e remoção — o fail-closed não fechou o caminho normal. Placar: backend **573 passed, 5 skipped (2104 assertions)** contra o baseline **548/2025** medido na abertura; frontend **27 arquivos / 131 testes**, `pnpm lint` limpo e `pnpm build` verde; Pint `passed` nos 33 `.php` do bloco; `typescript:transform` **sem diff** em `generated.ts`; sem sonda no diff. **Mutação declarada no banco de dev**, append-only: turma 6 criada da cotação 1, templates 9/11 (v92 arquivado, v93 vivo), audits 482-497 e um aluno de gate; os pivots tocados foram **restaurados ao estado original** (curso 1 = [1,3,4], redator 2 = [2,3,8], `turma_redator` intocado) e `LOT-2026-1001` segue corrompido de propósito, intocado. **O que o gate NÃO provou, sem maquiagem:** a derivação continua **sem prova de concorrência MySQL** (D16, escolha declarada — `lockForUpdate` é no-op em SQLite e o `unique` é a defesa de integridade); 5 dos 11 caminhos da RN-15 só foram exercitados em SQLite (o de importação para em validação antes do gate); a cadeia template → certificado não foi percorrida ponta a ponta, então "o resolver escolhe o template certo" segue não provado; nenhuma tela vista renderizada (bloco de backend, e o diff de `frontend/` são só os dois arquivos gerados); e **sem backfill (D2)** — o rastro dos dois pivots começa aqui e o passado não é recuperável. A retenção de `audits` segue aberta (P-02/P-30) e este bloco **aumenta** o volume. Nasceu a **P-35** (o `seq_in_budget` do `CreateQuoteAction` segue por mass assignment enquanto o `version` saiu do `$fillable` — dois consumidores do ADR-17 com defesas diferentes; entrou como P-34 no fechamento e foi renumerada ao mesclar a `main`, que já tinha dado esse número à lacuna da catraca de cor). **A renumeração do `P-30` duplicado que este fechamento fez não sobreviveu ao merge:** o fechamento do BD-3 já tinha resolvido o mesmo choque pelo critério oposto — renumera quem chegou à `main` por último, então a retenção de `login_logs` virou **P-33** e o `ámbar-aviso` ficou com o **P-30** —, e a decisão publicada prevalece sobre a que ainda não tinha saído da branch. | `plans/archive/2026-08-12-rastro-unicidade-e-gates.md` · `specs/archive/2026-08-12-rastro-unicidade-e-gates-design.md` · sem Context Packet (ausência medida de fonte externa) · commits `e15aee0`…`bd769f8` · ledger `.superpowers/sdd/progress.md` |
| 2026-08-13 | Identity/Commercial · contrato de entrada: identidade e coleção nested | Entregue | **BD-9** do backlog, promovido explicitamente pelo João em 2026-08-13 depois de o gate do `/planejar-bloco` reprovar pelo motivo de sempre (argumento era título de seção, estado `idle`). Três decisões dele fecharam o gate: o slug, **rota direta a `ready_for_planning` sem Context Packet** por ausência **medida** de fonte externa (o HTML da revisão de arquitetura que abriu o bloco não cita Drive, Notion nem Figma — grep zero — e depois nem existia mais: vivia no `/tmp` de outra sessão; não bloqueou, porque os achados 4 e 5 estavam transcritos integralmente no `backlog.md`), e **main tree sem worktree** (P-03). **Os dois defeitos eram de forma, não de linha:** `provision()` fechava só o RUT e quatro dos nove caminhos de escrita de identidade não chamavam a outra metade — colisão de e-mail virava 500 genérico —, e `ClientData::$addresses`/`$contacts` nasciam `array = []`, então PUT que omitisse a chave **apagava a coleção em silêncio**. **O que entrega:** `UserProvisioner::ensureIdentityAvailable` como **porta única dos nove caminhos** (`?string $rut` para caber no staff, cujo RUT é nullable), com os dois erros **agregados num 422 só** (D7) e `deleted_at` lido na mesma query, de modo que o operador ouve *restaure* em vez de *já existe* (D8, quatro mensagens); `ensureRutAvailable`/`ensureEmailAvailable` **deletados**, não privados — grep zero em `app/`, `tests/` e `database/`; as duas coleções do cliente viram `Optional` com `generated.ts` regenerado e os **5 consumidores TS** corrigidos no mesmo commit; e a lei da `der-fisico.md:103-106` ganha **catraca de reflexão** em `PersistenceLawsTest`, com `#[ReadOnlyCollection]` declarada no sítio para projeção de saída. **Cinco caminhos sem defeito (staff e aluno) mudaram de forma — consequência declarada na spec, não descoberta depois:** é o preço da porta única, e a prova de que o comportamento deles não mudou entrou no DoD. **Quatro coisas foram medidas antes de virar desenho, e três corrigiram o que o backlog escrevia:** a assinatura `ensureIdentityAvailable(string $rut, …)` do item **não cobria** o staff; o `Optional` no DTO **não** era inerte no frontend — sonda com `| undefined` no `generated.ts` devolveu **17 erros de `tsc -b` em 4 arquivos**, com a árvore restaurada em seguida; o universo da lei era **cinco** propriedades em três DTOs, não duas (minha primeira contagem errada, corrigida antes de virar decisão), e `BudgetData::$quotes`/`$files` **nunca são lidos da entrada** — são projeção de saída, e uma guarda que só olhasse o atributo nasceria vermelha nelas; e quem produz o `| undefined` é o **docblock**, não o tipo PHP. **Execução em 6 tasks via SDD**, uma por commit, cada uma revisada antes de avançar; a Task 6 foi gate sem commit. **A revisão final de branch achou dois Important, ambos reais:** a guarda nova checava só o TIPO (`array|Optional` admite `Optional`) e nunca o DEFAULT, então `array|Optional $x = []` passaria e ainda apagaria em silêncio — o mesmo defeito que o bloco existe para fechar, sob outra grafia, confirmado por reflexão (`BudgetData::from(['client_id'=>1])->files` devolve `array(0){}`); e a spec afirmava que o frontend ficava sem teste novo porque "o runner só cobre hooks de `shared/`" — **falso**, e a mesma lição 13 que o `frontend-fsliced.md` já registrava duas vezes: a lacuna real era a normalização `?? []` nunca exercitada, coberta em `useClientForm.test.tsx` (138→139). **Review de sprint ALTO RISCO** pelo gabarito (`generated.ts` regenerado, forma do 422 em quatro rotas, eixo de identidade) — **a segunda lente foi recusada pelo João**, então saiu com lente única, declarado e não resolvido em silêncio. **4 achados, todos aprovados e corrigidos**, cada um com o vermelho visto antes do verde. **O Q-1 é o mais grave e é do próprio mecanismo do bloco:** `#[ReadOnlyCollection]` era **isenção auto-declarada** — a guarda dava `continue` na marca antes de qualquer checagem, e uma sonda com `#[DataCollectionOf] public array $itens = []` passava só por ganhar a marca; a read-only-ness, premissa inteira da exceção, era a única parte não mecanizada. Agora a marca é verificada nas duas pontas: exige `#[DataCollectionOf]` e reprova nomeando o arquivo de `app/` que ler `$data-><campo>` da entrada (provado com `SondaQ1Action`, sonda apagada e árvore conferida). Q-2: a checagem de contato rodava **depois** de escrever, então POST sem contatos com e-mail ocupado devolvia só `email` — subiu para o topo do `execute()`, antes de `provision()`, com a transação contendo só escrita; **o que isso NÃO faz, dito sem maquiagem:** o caso combinado segue devolvendo um campo por vez (agregar exigiria plumbar regra de Commercial dentro de Identity, e a D7 agrega o que mora na MESMA chamada). Q-3: uma regra, **três redações e duas línguas** ("precisa de", "precisa ter" e `El campo contacts debe tener al menos 1 elementos.` pelo locale `es` do validador) — vira `ClientData::CONTATO_OBRIGATORIO`, com as três asserções vagas (`fn ($m) => is_string($m)`, vagas **porque** o texto variava) apertadas para a frase literal, 3 reprovando primeiro. Q-4: marca inerte em `BudgetData::$files` (sem `#[DataCollectionOf]`, a guarda não olhava a propriedade); `QuoteData::$files`, mesmo defeito na classe irmã e invisível por não ter nem a marca, recebeu os dois atributos no mesmo commit — escopo declarado além do achado. **Gate de fechamento refeito contra a API real, não herdado** (as correções entraram depois do e2e de execução e mexeram em ordem, mensagens e guarda), com sessão Sanctum por cookie + CSRF: **10 cenários, todos por corpo de resposta**. RUT e e-mail ocupados **agregam num 422 só** (`errors.rut` **e** `errors.email`, `application/problem+json`); POST sem contatos com e-mail ocupado devolve **`contacts`, sem `email`** (Q-2 na API); as **três portas** da regra de contato falam a mesma frase (POST, `contacts: []` no PUT pelo `min:1`, e DELETE do último contato pela rota nested); **PUT sem as chaves `addresses`/`contacts` devolve 200 e preserva 1 endereço + 2 contatos** — o coração do bloco —, enquanto o replace explícito continua trocando o endereço; a porta única recusa e-mail ocupado **no redator** (caminho que antes só checava RUT) e RUT de outro usuário **no update de cliente**; e o cliente soft-deletado devolve as **duas** mensagens de arquivado. Ferramentas: backend **591 passed, 5 skipped (2149 assertions)** contra o baseline **573**; frontend **28 arquivos / 139 testes**, `pnpm lint` limpo e `pnpm build` verde; Pint `passed` nos **21** `.php` do bloco; `typescript:transform` **sem diff** e `git status frontend/` vazio depois de rodar; zero órfão. **Dois docs vivos nomeavam método morto e foram corrigidos no fechamento** (lição 13): a lição 8 do `docs/README.md` e a **P-29** citavam `ensureRutAvailable`/`ensureEmailAvailable`. A P-29 **não fecha** — o BD-9 unificou e agregou o 422, não fechou a corrida entre transações distintas. A `.claude/rules/backend-ddd.md` ganhou a catraca da lei, a exceção `#[ReadOnlyCollection]` e a regra "obrigatoriedade que depende do verbo mora na Action": sem isso, um DTO novo com coleção de saída reprovaria num teste cujo remédio a rule não documentava. **O que o gate NÃO provou, sem maquiagem:** corrida de unicidade concorrente (a suíte roda sqlite `:memory:` e a defesa real é o `unique` do MySQL — é a P-29, que segue aberta); **nenhuma tela vista renderizada** (o frontend só mudou de tipo mais a normalização `?? []`); e a listagem `GET /api/clients` não recebeu asserção formal. **Mutação declarada no banco de dev**, append-only: cliente 14 criado pela API e soft-deletado no próprio gate (com o user de RUT `21.111.111-9`), endereços 22-23 e contatos 35-36; **nenhuma linha pré-existente alterada**, nenhum `migrate:fresh`/`refresh`/seeder rodado. Nove achados Minor da execução seguem triados como backlog, e o deferimento do `rut` do `UpdateStaffUserAction` (mesmo defeito de família num campo escalar) está em `## Débitos técnicos`. | `plans/archive/2026-08-13-contrato-de-entrada-identidade-e-nested.md` · `specs/archive/2026-08-13-contrato-de-entrada-identidade-e-nested-design.md` · sem Context Packet (ausência medida de fonte externa) · commits `0bd994e`…`59a39e3` · ledger `.superpowers/sdd/progress.md` |
| 2026-08-12 | Estilização · tema custom (ADR-16), shell e tipografia | Entregue | Item 4 de "Próximos blocos", promovido explicitamente pelo João em 2026-08-11; frontend-only, worktree `fix-frontend`. **O ADR-16 fecha com identidade própria:** os dois temas Lara viram cópias **geradas** por script versionado (`frontend/scripts/generate-brand-theme.mjs`) com a escala celeste no lugar do azul, neutros gray→slate, radius 4px e Inter self-hosted — a **D5'** nasceu na escrita do plano porque o Lara compila as cores inline (97 ocorrências de `#3b82f6`) e o override de token da spec aprovada não alcançaria botão, foco nem highlight. Uma camada fina (`brand-theme.css`) cobre o que é regra nova. Shell: sidebar navy fixa nos dois temas, header vira barra utilitária de 80px (valor cravado pelo João no working tree), dono único de título e aria-labels no i18n. Tipografia em 3 papéis (Archivo/Inter/IBM Plex Mono) com `tabular-nums` em célula. **Dezesseis emendas declaradas (D-P1..D-P16), nenhuma silenciada** — a lição 13 aplicada dezesseis vezes: seis achadas revisando o plano contra o Lara **instalado** (duas delas mudavam o construído e foram decididas pelo João antes de qualquer código: a D6 vira propriedade do tema gerado, alcançando as 9 superfícies onde branco sobre celeste media 2,77:1; e o anel de foco volta à spec porque o entregue media ~1,4:1 e o DoD passaria verde com o foco invisível, que era o próprio UI-03), duas durante a execução, e o resto no checkpoint, no re-run e no review. **Duas vezes a inspeção pegou o que 96 testes verdes não pegaram** (dois degraus da escala com o mesmo hex; a utility morta que o scanner do Tailwind emitia por ler um comentário — armadilha reincidente três vezes no mesmo bloco). **O checkpoint visual do João era bloqueante e produziu três decisões dele:** o logo fica como está, a caixa branca do toggle sobre a navy passa a ser estética assumida (ele mandou reverter as variantes `onNavy` que eu havia entregue) e o achado 3 vira a D-P14 — celeste deixa de ser cor de primeiro plano no claro, com traço de foco em azul-poste (13,37:1) e texto no degrau 700 (5,88:1), **desvio declarado** da proposta original porque azul-poste no texto apagaria o celeste da aplicação inteira. **Review em duas frentes por decisão da spec (D8), não pela régua de risco:** 8 achados (Q-1..Q-8) aprovados e corrigidos em subagentes paralelos com commits unitários por conjunto disjunto de arquivos, mais um re-review das correções com 4 achados (S-1..S-4), todos fechados. Um achado do Codex foi **recusado com evidência** (o papel mono está implementado em 5 sítios). Quatro desvios do alvo escrito, todos com prova: o `text-md` inexistente virou remoção e não `text-base` (a utility do Tailwind v4 carrega `line-height` e cresceria cada nó ~5px); o Q-5 fechou o **contrato** em vez de repetir o conserto (`title` do `DetailHeader` passou a obrigatório — "detalhe sem h1" virou erro de tipo); e a guarda de drift passou a classificar a família azul por **geometria** (croma/saturação/matiz) em vez de lista manual, provada com o `#4f8ff7` injetado reprovando e os três limiares load-bearing. **Gate de fechamento medido no navegador com sessão real, não herdado:** UI-01 `rightEdge` 378 e `scrollWidth` 390 a 390px; UI-02 zero `aside button` em compact com a pref persistida intacta (`true`); UI-03 Tab real com `outline: solid 2px` celeste sobre a navy e azul-poste (`rgb(15,43,61)`) fora do shell; UI-05 um `h1` por rota (dashboard e `/personas`); rótulo de marca em `rgb(24,107,148)`; corpo Inter, título Archivo, `--surface-ground` humo, sidebar `rgb(15,43,61)` nos dois temas, radius 4px; wordmark legível na navy nos dois temas (screenshots 1440). Suíte **26 arquivos / 127 testes**, build, lint e paridade das 3 locales verdes; backend intocado (**547 passed / 5 skipped**, nenhum arquivo `backend/` no diff — Pint não se aplica); `generated.ts` sem diff. **Fecha dois débitos** ("Shell fora de conformidade com o ADR-16 §4" e "Toggle da sidebar sem efeito abaixo de 1024px"), removidos de `## Débitos técnicos` e de "Fora dos BDs". **Nasceram P-30** (o `ámbar-aviso` da spec §4 nunca foi construído — cinco donos de cor, não seis; decisão do João de não trocar paleta de severidade aqui) **e P-31** (o ponto 5 do ADR-16 está em `docs/adrs.md` e não no espelho do Drive; o agente só tem leitura no Drive, e o João decidiu fechar registrando em vez de segurar o bloco). O `P-28` duplicado foi renumerado para **P-32** e o ponto final de `operation.detail.notFound` saiu do `h1`, os dois por decisão dele neste gate. | `plans/archive/2026-08-11-estilizacao-adr16-shell-tipografia.md` · `specs/archive/2026-08-11-estilizacao-adr16-shell-tipografia-design.md` · sem Context Packet (ausência de fonte externa medida: repositório, report de UI review e ADR-16) |
| 2026-08-11 | Hardening · integridade e concorrência no backend (BD-2) | Entregue | **BD-2** do backlog, promovido explicitamente pelo João em 2026-08-11 — os quatro itens mantidos na íntegra contra o recorte alternativo que teria devolvido dois deles à fila. Backend puro, main tree sem worktree (P-03), **zero schema**: `git diff main...HEAD` de `backend/database/` e `frontend/` vazio, `generated.ts` sem diff. **A medição que mudou o desenho, feita antes de escrever a spec:** sonda contra MySQL real mostrou que, depois de acordar do `lockForUpdate()` no `Client`, o `SELECT` comum de `ensureSingle()` **continua lendo o snapshot** de REPEATABLE READ e não enxerga o principal que a transação concorrente já commitou — o Q-16 ao pé da letra entregaria mecanismo que promete e não fecha. O lock virou **duplo**: mutex no `Client` mais leitura travada da coleção. **E a escrita do plano mudou o desenho de novo (D-P1):** com o mutex dentro do serviço ele é tomado **depois** de a Action já ter escrito, o que inverte a ordem dos locks e produz `SQLSTATE[40001] ... 1213 Deadlock found` matando um dos processos (500 para o perdedor) — medido, não deduzido. O mutex subiu para as Actions; `CreateClientAction` fica de fora com a razão escrita no código (o cliente nasce ali, não há concorrente disputando id que ainda não existe). Os outros três itens: unicidade de RUT/e-mail para **dentro** da `DB::transaction` nos **três** sítios medidos (o débito nomeava um), `UserData::fromModel` chamando `getRoleNames()` uma vez, e os quatro testes que faltavam — cada um **visto reprovar** contra o código antigo antes de valer como prova. O harness de concorrência do `CertificateNumberTest` saiu para `tests/Support/ProbesMysqlConcurrency` e passou a servir os dois arquivos, conferido linha a linha contra `09a11d9` para não afrouxar o caso do certificado. **Review ALTO RISCO em duas lentes** (RBAC, auth/identidade e caminho de escrita auditado — a §8 da spec dizia MÉDIO na escala dela e a divergência ficou registrada): Claude mais Codex read-only, **6 achados, todos aprovados pelo João e corrigidos** em `e2a251c`, órfãos zero. **Q-2 🔴 é regressão do próprio bloco:** o mutex tinha dois escritores que o ignoravam — `DeleteClientContactAction` travava a coleção sem tomá-lo antes (ordem invertida, ciclo real por aquisição incremental de lock) e `ClientController::destroy` chamava `$client->delete()` cru, com o hook `Client::booted deleting` enumerando os filhos sem transação; um contato criado depois da enumeração ficava **ativo sob cliente arquivado**. Nasce `DeleteClientAction` (transação + mutex). **Q-1:** o guard de lock-out de superadmin continuava check-then-act **fora** da transação, no mesmo commit que moveu a unicidade para dentro pela razão oposta — o `SuperadminGuard` passou a travar o **conjunto inteiro** com `pluck`, porque excluir o alvo do `FOR UPDATE` quebrava o mutex (T1 trava {B}, T2 trava {A}, sem conflito). **Q-4:** os dois serviços de principal eram idênticos byte a byte e o bloco acrescentara as mesmas dez linhas aos dois — viram `PrimaryCollectionService` com três linhas cada. **Q-5:** `lockForWrite()` devolvia `void` e descartava o `->first()` (no-op indetectável); passou a devolver o cliente travado com `firstOrFail()` e a **recusar cliente arquivado**, que é a outra ponta do Q-2 fechada num ponto só em vez de quatro linhas repetidas em seis Actions. **Q-6:** a sonda contava qualquer transação em espera no schema; passou a correlacionar por `PROCESSLIST_ID` lido do `CONNECTION_ID()` que o filho imprime — medido com uma sessão `mysql` CLI alheia bloqueada, a consulta antiga devolvia `1` e a correlacionada devolve `0`. **Gate de fechamento provado contra a API real, não pela suíte:** cliente criado com dois contatos `is_primary=true` sai com **um**; a rota nested promove e rebaixa; **20 `PUT` concorrentes em 10 rodadas** (dois contatos do mesmo cliente disputando) devolveram **200 nos 20**, com a invariante em **1 principal** em todas as rodadas e **`Innodb_row_lock_waits` subindo 11** — houve espera real de lock no caminho HTTP, e `SHOW ENGINE INNODB STATUS` sem uma seção de deadlock; a coleção de endereços provada no mesmo caminho (a subclasse nova). **O Q-2 provado pela API, nos dois efeitos:** `DELETE /api/clients/{id}` concorrente com `POST .../contacts` devolveu **204** e **422 RFC 7807** (`Este cliente foi arquivado e não aceita mais alterações.`), com **0 contatos vivos** sob o cliente arquivado conferido em SQL cru. Unicidade: `PUT` com RUT de outro usuário → **422** com `errors.rut`, **sem escrita parcial** (o `name` novo não entrou), e auto-colisão com o próprio RUT → **200**. Guard: `DELETE` do único superadmin → **422**, e rebaixá-lo com outro superadmin **inativo** no banco → **422** também (o caso que o item 4 existia para cobrir), com a role intacta em SQL. Placar: backend **538 passed, 5 skipped (1999 assertions)** em sqlite; contra MySQL real, **9 sondas de concorrência verdes (48 assertions)**, das quais 4 disputam de verdade; Pint `passed` nos 29 `.php` do bloco; `pnpm lint` e `pnpm build` verdes (frontend intocado); `typescript:transform` sem diff. **O que o gate NÃO provou, sem maquiagem:** a corrida de unicidade **entre transações distintas** segue aberta — dois cadastros do mesmo RUT no mesmo instante ainda colidem no índice único e sobem **500**, recusa registrada na D3 da spec e agora rastreada como **P-29**; e a suíte em sqlite não enxerga lock nenhum (`SQLiteGrammar::compileLock()` é no-op), então tudo que prova o item 1 é MySQL-only — daí os 5 skipped, que aqui são sinal honesto e não cobertura ausente. **Duas mutações declaradas no banco de dev:** os registros criados pelo e2e (2 clientes, 3 usuários staff) foram removidos com `forceDelete` ao fim, restando 15 linhas de `audits` apontando para ids que não existem mais; e o `LOT-2026-1001` corrompido de propósito **segue lá**, conferido antes e depois — nenhum passe rodou `migrate:fresh`. | Plano `plans/archive/2026-08-11-integridade-e-concorrencia-backend.md` · spec `specs/archive/2026-08-11-integridade-e-concorrencia-backend-design.md` · sem Context Packet (ausência medida de fonte externa) · commits `542e3cc`…`e2a251c` · ledger `.superpowers/sdd/progress.md` |
| 2026-08-11 | Hardening · guardas que faltam (BD-1, mecanismo) | Entregue | **BD-1** do backlog, promovido explicitamente pelo João em 2026-08-10 depois de o gate do `/planejar-bloco` reprovar por estado `idle`. Branch `hardening/guardas-que-faltam` no main tree (P-03), `executor: claude`, 8 commits de conteúdo + 2 de correção de review. **Nenhuma das oito guardas corrige defeito visível** — a superfície das oito foi medida limpa em 2026-08-10, não herdada de relatório, então o DoD **não podia ser suíte verde**: é guarda vista reprovando com sonda deliberada (lição 10). **Backend:** `PersistenceLawsTest` dá mecanismo às leis §5.1 (nenhuma classe `Repository` em `app/`, com `QueryBuilders/` fora por ser o padrão do ADR-02) e §5.2 (nenhum `CREATE TRIGGER`/`unprepared()`), sobre o trait `ScansPhpSource` extraído do `DomainDependencyTest`; `NestedRouteOwnershipTest` deixa de filtrar por `signatureParameters()` e passa a contar os `{}` da URI — quem esquecesse de **tipar** o binding saía do universo em silêncio, que era o escape conhecido. **Frontend:** `axios.test.ts` nasce sem mock e assere a lição 6 na instância real; `useCrudForm` reprova chave declarada em `mapped` **e** em `summaryOnly` (declaração contraditória que passava despercebida), dentro do `import.meta.env.DEV` que já existia; o barrel de `shared/hooks` para de exportar mecanismo interno; `useEntityPhoto` ganha os seis casos que faltavam para 161 linhas de hook sem um teste. **Doc:** a lição 13 vira mecanismo em `frontend/tests/repo-docs-refs.test.ts` — todo path citado em doc normativo tem de existir —, e mora no vitest porque o container `app` monta só `./backend` e `./frontend`, então PHPUnit não lê `CLAUDE.md`, `.claude/rules/` nem `docs/` (conferido de dentro do container); a `frontend-fsliced.md` recebe a linha da P-25 e a correção da frase vencida sobre o corte do runner. **Review de BAIXO RISCO** (nenhum gatilho de ALTO: zero schema, zero `generated.ts`, zero auth/auditoria/RBAC/dinheiro/documento legal, `executor: claude`) — **a classificação divergiu do MÉDIO da própria spec e a divergência foi declarada**, porque o gate da skill é binário. Uma lente, sem segunda do Codex. **4 achados, todos aprovados pelo João e corrigidos**, cada um provado por sonda antes de virar achado: **Q-1** — a guarda do axios só olhava `defaults.headers`, e o arquivo tem um interceptor seis linhas abaixo do `axios.create`; quem escrevesse `headers.set('Content-Type', …)` ali reproduzia o bug da lição 6 inteiro **com os 16 arquivos / 79 testes verdes**. **Q-2** — a D4 declara o escopo da guarda de doc com **glob** (`.claude/rules/*.md`) e a lista era literal: rule nova entrava sem ser conferida, em silêncio, na pasta onde a lição 13 já reincidiu duas vezes. **Q-3** — a guarda da §5.2 varria só `database/`, mas a lei não tem escopo e um trigger criado de `app/` viola igual. **Q-4** — o `include` do vitest era `tests/**/*.test.ts`, então um teste `.tsx` em `tests/` seria ignorado **em silêncio**. **O desenho do Q-1 foi recusado por medição, não por gosto:** a primeira forma mandava requisição real pelo `adapter` e lia o header final; medido, o axios do jsdom escreve `application/x-www-form-urlencoded` para FormData — artefato do ambiente, não configuração nossa —, e assertar ali **reprovaria o estado correto**, que é o mesmo erro da D-E2 deste bloco. O universo virou o que a app declara. **Gate de fechamento com as oito sondas REFEITAS, não herdadas do review:** `FooRepository.php` e `TriggerAction.php` plantados em `app/` e `SondaTriggerSeeder.php` em `database/` produzem as quatro ocorrências nomeadas; `api/sonda/{parent}/{child}` reprova nomeando a rota; `Content-Type` no `axios.create` **e** no interceptor reprovam em casos distintos (`expected [ 'application/json' ] to deeply equal []`); `docs/README.md:145` com um path inventado e a rule `zz-sonda.md` fora da lista reprovam nos dois testes que as prometem; `phone` em `mapped` e `summaryOnly` faz o `useStudentForm` lançar; e a mutação do gate de `sizeError` no `useEntityPhoto` é pega pelo caso que promete exatamente isso. Árvore restaurada limpa depois de cada uma. Placar: backend **524 passed, 1 skipped (1963 assertions)**, frontend **16 arquivos / 82 testes** (baseline 13/47), `pnpm lint`/`pnpm build` verdes, Pint `passed` nos 4 `.php`, `typescript:transform` sem diff em `generated.ts`, e `backend/app/`, `backend/database/` e `frontend/src/features/` **sem uma linha**. **P-04 e P-25 fechadas aqui** — não pelo bloco existir, mas pelo gate que provou o DoD, como as duas linhas diziam. **P-28 aberta com o escape medido no próprio gate:** a guarda da lição 13 confere path, e `LibreOfficeConverter` — a terceira reincidência, que a motivou — passa **verde** por ser classe sem `/`. Outros dois escapes ficam declarados e não fechados: §5.1 casa por nome de arquivo com exclusão de `QueryBuilders/` por path, e a guarda de rota exige **declaração**, não correção. | Plano `plans/archive/2026-08-10-guardas-que-faltam.md` · spec `specs/archive/2026-08-10-guardas-que-faltam-design.md` · sem Context Packet (bloco sem fonte externa) · commits `ec3ad2a`…`eec8075` |
| 2026-08-10 | Documentos oficiais · fidelidade ao template e saída DOCX do manual | Entregue | Item 1 do backlog, escrito e selecionado explicitamente pelo João na mesma instrução de 2026-08-10 (o backlog foi redigido a partir da descrição dele; não promoveu nada sozinho). Sem Context Packet por **ausência medida de fonte externa** — o item não cita Drive, Notion nem Figma, e as fontes são o repositório mais os três templates já versionados (`docs/templates/certificado.pdf`, `manual.pdf`, `fundo-certificado.png`). Toca `backend/` → main tree, sem worktree (P-03); **zero schema** (`git diff main...HEAD -- backend/database/` vazio), `generated.ts` sem diff. **Refatoração, não construção:** os dois documentos já eram Blade via Gotenberg; o que não existia era DOCX. **Quatro decisões de abertura do João (D1–D4):** manual com fonte de verdade única em Blade WordprocessingML, com o PDF saindo do mesmo `.docx` pela rota LibreOffice; ofício paisagem igual ao template; fundo JPEG só no certificado; tipografia do certificado por fonte versionada com `@font-face`. **A D2 reabriu explicitamente a D4/D6 do bloco 6d** — o A4 retrato do manual, justificado na própria Blade com "o cliente arquiva em A4" —; as três saídas foram apresentadas e ele escolheu fidelidade literal ao arquivo aprovado pela Lotus, o que faz do manual o único documento oficial fora do A4. **O desenho repousa sobre medição, não sobre prosa:** o fundo entregue (`fundo-certificado.png`, 1414×2000 RGBA, **1.245.172 B**) é o mesmo que dentro do `certificado.pdf` aprovado é um JPEG de **98.258 B** — 12,7× mais leve, e o teto do bloco deixou de ser palpite; as fontes do template foram identificadas **apesar da ofuscação do Word** (`pdffonts` só devolve `___WRD_EMBED_SUB_1235`; descomprimindo os oito programas e lendo o `name` table saem Lexend, Montserrat ExtraBold, Comfortaa, Roboto e ArialMT, todas OFL/Apache); e a rota LibreOffice foi provada **antes** de virar decisão — pacote OOXML montado à mão (1.207 B) → `/forms/libreoffice/convert` → `http=200`, PDF de 18.671 B, `Page size: 1008 × 612 pts`. **A escrita do plano produziu nove desvios declarados**, cinco deles mudando a spec: `docs/templates/manual.docx` **existe no repo** e dele saíram o papel exato (`w:pgSz w:w="20183" w:h="12246"`), as larguras de coluna, a cor institucional **`25A5E4`** (não `29A3E0`) e a descoberta de que o template declara **Arial** — Liberation Sans é só a substituição métrica do LibreOffice (D-P3/D-P4); a conversão PNG→JPEG saiu **sem mudança de infra**, pela rota `/forms/chromium/screenshot/html` do próprio Gotenberg (**74.604 B** com `quality=92`), em vez de pôr `libjpeg-turbo-dev` na imagem de produção para converter um asset uma vez (D-P1); são **duas** faces WOFF2 e não quatro, porque Lexend e Montserrat são variáveis (39.680 + 19.012 B cobrem os quatro pesos, D-P2); `short_open_tag` está `On` no container, então Blade que abra com `<?xml …?>` literal morre em `Parse error: syntax error, unexpected identifier "version"` — as quatro Blades do pacote abrem por uma diretiva `@xmlDecl`, e `{!! … !!}` foi recusado por reintroduzir a interpolação crua que a guarda de escape proíbe (D-P9); e `printBackground` **não** era necessário (PDFs com e sem o campo saem byte a byte do mesmo tamanho), então `PageOptions` e `GotenbergHtmlToPdf`, compartilhados com o certificado, não mudaram (D-P7). As grades do manual são formulário impresso com linha fixa (22/20/20) e o plano fixou `max(N, fixas)` — truncar esconderia aluno (D-P5). **Execução em 11 tasks no thread principal** (`executor: claude`, porque metade fecha por comparação visual página a página), com **doze desvios declarados**; três mudam o que o plano dizia: `Xml::lines()` nasceu emitindo `<w:br/>` **dentro** do `<w:t>`, que é bem-formado e **inválido** contra o schema (`CT_Text` é tipo simples) e passou a fechar e reabrir o `<w:t>` (D-E6); as fontes, bordas e margens saíram **medidas do `manual.docx`** tabela a tabela, em vez do `sz 13/17` uniforme que o plano supunha (D-E7); e `@else@xml(...)` **não compila** — o regex de diretiva do Blade exige `\B@` e o `e` de `@else` colado no `@` é fronteira de palavra, então a diretiva saía literal dentro do documento (D-E11). **Três decisões do João no checkpoint visual:** o QR do certificado **mudou de lado** — a §3.4 da spec pedia topo direito e o `certificado.pdf` aprovado abre a folha com o retângulo do QR à **esquerda**; ele decidiu pelo template, e o lado ficou **assertado no teste**, porque a divergência veio de uma releitura da spec e sem guarda a próxima releitura reverteria em silêncio uma decisão tomada contra ela (`ee285df`); o fundo foi **aceito agora para tratar depois** → nasce a **P-28** (faltam as cunhas diagonais das quinas, que são **vetor** no PDF aprovado e não estão no raster, e a faixa azul/preta repete na página 2, onde o aprovado é cinza limpo — nenhuma das duas estava nas exclusões da §7); e o registro de arquitetura virou **nota no ADR-12**, não ADR novo, porque `App\Shared\Office\` e a rota LibreOffice são a segunda porta do **mesmo** Gotenberg — isso **venceu o gatilho da P-20** e resolveu metade da **P-21**, as duas atualizadas e não fechadas. **Review ALTO RISCO em duas frentes** (lente Claude com o gabarito do projeto + `mcp__codex__codex` read-only sobre `8ee1d9e..HEAD`, 11 commits / 38 arquivos): **7 achados, todos aprovados pelo João na íntegra (`Todos de Q-1 a Q-7`) e corrigidos em `96d7256`**; órfãos zero e leis §5 limpas. **Uma divergência entre as lentes foi mostrada em vez de resolvida em silêncio:** o achado nº 1 do Codex — `w:tcPr` emitindo `vAlign`/`tcMar`/`shd`/`tcBorders` fora da sequência ECMA-376 — foi **rejeitado por medição**, porque descompactado o `manual.docx` escrito pelo Word usa exatamente a mesma ordem (`tcW, vAlign, tcMar, …, shd, tcBorders` ×706; `trHeight, cantSplit` ×81); também não foi reportada a ausência de validação de corpo no `GotenbergDocxToPdf`, que é o comportamento idêntico do `GotenbergHtmlToPdf` já em produção pelo ADR-12 — apertar só um lado criaria assimetria entre as duas portas do mesmo serviço. **Q-1 e Q-2 têm o template como árbitro:** o cabeçalho é um partial incluído cinco vezes e escrevia `wp:docPr id="1"` nas cinco — o Word identifica figura pelo id e **pede reparo do arquivo** quando ele repete, e o gate havia declarado que o manual nunca fora aberto no Word do cliente; o template aprovado numera os seus dez desenhos **1..10** e escreve `pic:cNvPr id="0"` nos dez, e as duas convenções foram medidas e seguidas. A Q-2 trocou o `load('enrollments…')` por `orderByStudentName()`: as três grades numeram linha a linha e são **assinadas** por linha, e PDF e DOCX são dois requests — sem ORDER BY a mesma turma podia sair com duas numerações. **A Q-3 corrigiu o próprio achado:** ele dizia "HTTP 200 com zero byte" e isso **não reproduz** — sob o handler do Laravel o `file_get_contents` de arquivo ausente vira `ErrorException`; o que o vermelho do teste mediu é pior de outro jeito, um 500 sem tipo de domínio com o caminho `/tmp/ooxmlXXXXXX` dentro do `detail` e o `unlink` pulado, e a porta que devolveria bytes vazios em silêncio é o `open()` falhando, que é a que a checagem nova fecha. Q-4: o teste `500_rfc7807` afirmava só o status (um 500 em HTML puro passaria) e passou a afirmar `content-type`, `type`, `title`, `status` e o `detail` que nomeia o conversor. Q-5: a nota do ADR-12 citava `LibreOfficeConverter`, classe que **nunca existiu** (lição 13), e passou a nomear `DocxToPdf`/`GotenbergDocxToPdf`. Q-6/Q-7 (frontend): o download do DOCX revogava o objectURL no mesmo stack do `click()` de uma âncora **nunca anexada ao DOM** — duas causas conhecidas de download que não começa, e assimetria com o caminho do PDF ao lado, que fazia certo —, e o estado estava fundido: `useMutationErrors` devolve o **primeiro** erro truthy, então o erro do PDF sobrevivia a um DOCX baixado com sucesso, e o `pending` compartilhado girava os dois botões juntos, anunciando o Word quando o pedido tinha sido o PDF. **Gate de fechamento refeito contra a API real, não herdado.** Ferramentas: backend **522 passed, 1 skipped (1961 assertions)**; Pint `--test` **`passed`** nos **22** `.php` vivos do bloco; `typescript:transform` **sem diff** em `generated.ts` e árvore limpa depois de rodar; `backend/database/` sem uma linha; frontend `pnpm lint` limpo, `pnpm build` verde, **13 arquivos / 47 testes**; as três locales com **538 chaves e zero diff** entre si. **E2E com sessão Sanctum por cookie + CSRF, só GETs — zero mutação no banco:** `GET /api/turmas/1/manual` → **200** `application/pdf` + `inline; filename="manual-turma-1.pdf"`, **`Pages: 5`**, **`Page size: 1008 x 612 pts`**; `GET /api/turmas/1/manual/docx` → **200** `…wordprocessingml.document` + `attachment; filename="manual-turma-1.docx"`. **O pacote real foi aberto, não suposto:** as cinco parts (`[Content_Types].xml`, `_rels/.rels`, `word/document.xml`, `word/_rels/document.xml.rels`, `word/media/lotus-logo.png`), `w:pgSz w:w="20183" w:h="12246" w:orient="landscape"` **idêntico ao template**, `wp:docPr` numerado **1,2,3,4,5** e `pic:cNvPr id="0"` cinco vezes — as duas convenções da Q-1 vivas no documento entregue. **Preenchimento provado por contagem (DoD 4):** turma de **12 matrículas**, cada nome aparecendo **3 vezes** (uma por grade), grades em 23/21/21 linhas (cabeçalho + as 22/20/20 fixas) — com N=12 o `max(N, fixas)` mantém as linhas em branco, como o formulário impresso —, e o rodapé de horas fechando com a soma dos módulos: 8+6+4 = **18 T**, 4+10+8 = **22 P**. **Pesos medidos (DoD 1):** manual `.docx` **19.269 B**, manual `.pdf` **52.954 B** (template de referência 444.830 B), certificado **199.830 B** contra a linha de base de **40.119 B** e o teto de **251.450 B** do documento aprovado. **Fonte de verdade única confirmada no código (DoD 9):** `ManualDocumentService::pdf()` é `converter->render($this->docx($turma))` — não há segundo caminho de montagem. **Contrato do certificado intacto (DoD 7):** `GET /api/certificates` **200** com os treze campos de sempre e `snapshot_ok` **false só** no `LOT-2026-1001`; `show` do são **200**; rota pública **sem cookie** **200**; o corrompido devolve **500 `application/problem+json`** no `show` e no `pdf`, nomeando `LOT-2026-1001` **e** o campo `aluno.name`. **Visto renderizado (DoD 8):** 14 PNGs comparados página a página — as **cinco** páginas do manual batem com o template em grade, colunas, cores e contagem de linhas, e no certificado as únicas divergências são as já declaradas (assinatura da gerente e carimbos SENCE/NCH pela §7; cunhas das quinas e faixa na página 2 pela P-28), confirmadas na imagem em vez de assumidas. **Órfãos e leis:** 54 consumidores das classes de `App\Shared\Office\`; zero sobra de `ManualPdfService`, `manual-turma.blade`, `class="accent"` ou `class="meta"` (os únicos hits são um comentário que explica a renomeação e a asserção de **ausência** de `class="qr"`); zero `abort(` em `Domains/Operation/`; zero Repository. **O que o fechamento NÃO provou, sem maquiagem:** o manual **aberto no Word do cliente** — a Q-1 remove a causa conhecida de "pedir reparo", medida contra o template, mas o segundo leitor independente continua sendo o do João; fidelidade **pixel a pixel** (a comparação foi de grade, cor e posição); e a Q-6 segue **sem teste**, porque o runner do frontend cobre os hooks de `shared/` e hook de feature com DOM está fora dele — dizer o contrário seria cobertura fantasma (lição 10). **Uma imprecisão do plano, registrada em vez de corrigida retroativamente:** o Step 2 da Task 10 escreve `"password":"password"` como credencial do seed, e a senha real é `senha123` — todos os outros planos do repositório escrevem certo; o plano aprovado não foi reescrito (precedente da P-27), e o e2e deste gate rodou com a credencial correta. `migrate:fresh --seed` **não** foi rodado: o banco de dev segue carregando o `LOT-2026-1001` corrompido de propósito para o checkpoint visual do João. | `plans/archive/2026-08-10-documentos-oficiais-template-e-docx.md` · `specs/archive/2026-08-10-documentos-oficiais-template-e-docx-design.md` · sem Context Packet (ausência medida de fonte externa) · commits `8ee1d9e`…`96d7256` · ledger `.superpowers/sdd/progress.md` |
| 2026-08-10 | Hardening · revisão UI/UX assistida por navegador | Entregue | Item 1 do backlog, selecionado explicitamente pelo João no Gate 4 em 2026-08-09, com a **ordem da fila reconciliada antes da seleção**: o plano-mestre de 2026-08-08 punha o hardening antes de “Certificação · frontend”, mas esse bloco já estava entregue e fechado quando a implementação começou — a spec descreve a ordem real em vez de repetir a premissa obsoleta. `executor: codex`, worktree `fix-frontend`, branch `chore/hardening-ui-review` a partir de `032332b`, sem Context Packet (nenhuma regra de negócio externa a recuperar). **Bloco de infraestrutura de agente: zero código de aplicação** — `git diff origin/main..HEAD -- backend/ frontend/ .mcp.json .codex/` **vazio**, `frontend/package.json` sem dependência nova, `generated.ts` intocado. **O que entrega:** fonte canônica agent-neutral em `.agents/skills/lotus-ui-review/` (protocolo de 17 passos, `scripts/preflight.sh`, `references/review-rubric.md` com a régua A/B/C, `references/report-template.md` como contrato auditável), adaptador de 15 linhas em `.claude/skills/` que só roteia, comando legado `/revisar-ui` preservado, e `.playwright-cli/` + `.artifacts/ui-review/` fora do Git. **Piloto read-only na aba Clientes de `/comercial`, executado nos dois agentes** em runs separadas, nas viewports `1440x900`, `1024x768` e `390x844`, com snapshots, screenshots, console, rede e `git status --short` antes/depois: os agentes concordaram nos cinco fatos reproduzíveis centrais, e as divergências de severidade ficaram registradas em vez de conciliadas. **Três grupos B/C reproduzidos pelos dois foram para os débitos técnicos do backlog, sem uma linha de UI corrigida** (o bloco entrega o protocolo, não a correção): faixa visível perdida em tablet/mobile, foco não retornando ao disparador ao fechar o diálogo com controle de maximizar sem nome acessível, e campos desabilitados truncando valor no modo visualização. **Review ALTO RISCO por `executor: codex`** — o gatilho é a execução delegada, não o conteúdo (o bloco não toca schema, auth, auditoria, RBAC, dinheiro nem documento legal) → duas lentes, Claude com o gabarito do projeto + `mcp__codex__codex` read-only. **10 achados, todos aprovados pelo João e corrigidos.** **O 🔴 (Q-1) é de lei, não de estilo:** a matriz de descoberta declarava PASS em quatro linhas cuja “evidência” era citar o texto que o próprio bloco acabara de escrever — §5.8 e lição 10 na mesma frase. Refeito como **sonda comportamental em contexto novo, com frontend e backend em 200** para que nenhuma recusa pudesse ser creditada ao ambiente: o agente respondeu `BLOCKED` ao pedido de revisão do backend de certificação declarando não ter inspecionado os arquivos, e recusou explicitamente comparar com “o Figma do projeto” sem `fileKey`/`node ID`, sem listar divergência inventada. **Mecanismo (Q-4, Q-6):** o `preflight.sh` ganhou validação de loopback **antes** de qualquer `curl` — host fora de `localhost`/`127.0.0.0/8`/`::1`/`0.0.0.0` retorna `BLOCKED: non-local <label> url` inclusive quando `curl` não existe — e passou a reprovar `5xx` como `BLOCKED: unhealthy`; `4xx` continua passando de propósito, porque a raiz do backend pode responder `404` num serviço saudável e bloquear aí criaria falso positivo. **Contrato (Q-3, Q-7, Q-8):** enum único **`used\|complementary_unavailable\|not-needed`** (fecha a P-27), separadora GFM na tabela `## Coverage`, e o relatório passa a ser gravado em `<run-dir>/report.txt` a partir de uma cópia, nunca editando o template versionado. Rejeitados com evidência: a exigência de mecanismo que force read-only após o login (é a decisão §11.4 da spec — execução manual na v0.1) e o `none` fixo no rodapé do template (contrariado pelo próprio `SKILL.md`, que prevê o run não conforme). **Gate de fechamento: o item 0 foi refeito, não herdado** — as correções mexeram no `preflight.sh` depois do gate técnico, então os **nove modos** foram reprovados/aprovados de novo com o Lotus local de pé: `bash -n` limpo; `BLOCKED: non-local` para URL de produção; `BLOCKED: unreachable` para porta morta; `BLOCKED: non-local` **mesmo sem `curl` no PATH**; `BLOCKED: unhealthy ... status=503` contra servidor sintético; `BLOCKED: missing command: playwright-cli`; e `PREFLIGHT_OK` com `frontend=200 backend=200`. **O gate achou um ❌ real e ele não foi maquiado:** o teste literal do plano (Task 4, Step 4) reprovou — o adaptador tinha **18 linhas** contra o teto de 15 do DoD item 3, estouro vindo das próprias Q-2 e Q-10 aprovadas. Por decisão do João foi **resolvido, não registrado como desvio**: comprimido para 15 linhas sem perder nenhuma das duas cláusulas. **Uma divergência de estado parou o fechamento antes de qualquer arquivamento:** dois `active_work_item` em `ready_for_closure` no mesmo repositório — este bloco e o `turma-habilitacao-listagem`, com sessão paralela fechando o segundo em tempo real. Não foi resolvida por heurística; por decisão do João o outro fechou primeiro (PR #34) e esta branch absorveu a main por merge, preservando o arquivo da main e reinserindo só o que é deste bloco. **Placar:** backend **503 passed, 1 skipped (1868 assertions)** — código byte a byte idêntico ao da main —, frontend **13 arquivos / 47 testes**, `pnpm lint` e `pnpm build` verdes, Pint e `typescript:transform` N/A (zero `.php`, zero DTO). **O que o bloco NÃO provou, sem maquiagem:** o Chrome DevTools MCP **nunca esteve disponível** em nenhum run — todos registraram `complementary_unavailable`, então a degradação foi exercitada de verdade mas o caminho `used` segue sem prova; e nenhum achado do piloto foi corrigido, por escopo declarado. | `plans/archive/2026-08-10-hardening-revisao-ui-assistida.md` · `specs/archive/2026-08-10-hardening-revisao-ui-assistida-design.md` · sem Context Packet (ausência medida de fonte externa) · commits `a81c41c`…`6e2ccc7` · evidências em `.artifacts/ui-review/` (ignoradas pelo Git) |
| 2026-08-10 | Operation · habilitação da turma em VO único e seam de listagem | Entregue | Item 4 do backlog, selecionado explicitamente pelo João em 2026-08-10; nasceu da revisão de arquitetura de 2026-08-09 com as 5 decisões já tomadas por ele. Backend puro — `git diff main...HEAD -- frontend/` e `-- backend/database/` **vazios**, `generated.ts` sem diff —, main tree sem worktree (P-03), ADR/DER não abriram. **O 2N+1 foi medido na API antes de desenhar, não suposto:** `GET /api/turmas` custava **15 queries para 4 turmas** no banco de dev — 8 de carga e **7 em `files`** —, e o 7 não é `2N` porque `isHabilitada()` curto-circuitava em `status !== EmAndamento`: turma em andamento pagava 2 (a mesma pergunta feita por `habilitada` e por `missing_document_types`), concluída pagava 1. **Uma divergência do item foi fechada antes do desenho (D-B1):** ele escrevia que `isHabilitada()` é "literalmente `missingTypes() === []`", e ao pé da letra **toda turma concluída passaria a responder `habilitada: true`** (concluir exige documentação completa) contra um teste vivo — decisão do João: o gate de status entra **no VO**, e "uma pergunta, uma resposta" passa a significar que a resposta é o `HabilitacaoStatus`, não que a regra some. Zero mudança de contrato HTTP, em forma **e em valor**. **As peças:** relação nomeada `Turma::documentacaoObrigatoria()` com o `whereIn` embutido (que também mata a 2ª cópia da pergunta, no `TurmaDocumentController::index`); VO `HabilitacaoStatus` com `TurmaHabilitacaoService::for()` como **única** API pública (a antiga morreu — `grep` por `isHabilitada($`/`missingTypes($` em `app/` vazio); `TurmaQueryBuilder::LISTING` consumido por `withListingData()` **e** `Turma::loadListingData()`, com o `present()` perdendo o `findOrFail` e virando dono único da carga. **D-B3:** o `?? $turma->enrollments()->count()` do `enrolled_count` **morreu** — mesma classe de defeito (query por linha escondida atrás de fallback) no mesmo `fromModel`. **Dois desvios declarados em vez de silenciados (lição 13):** o mutante "chamar `for()` duas vezes" **não reproduz** com a relação eager-loaded (a segunda leitura é de memória — a classe de defeito deixou de existir em vez de passar a ser vigiada), então a guarda protege o **eager-load**, que é o que pode regredir (D-P1); e a linha `'documentacaoObrigatoria'` foi **antecipada** da Task 3 para a Task 2 porque o service passou a ler a relação como propriedade e sem a carga a listagem viola `Model::preventLazyLoading()` — RED visto, causa confirmada por `git stash` (D-E2). Consequência que **corrige a medição de abertura**: `preventLazyLoading` não enxergava a forma antiga (`$turma->files()->…`, query **na** relação) e enxerga a nova — o bloco ganhou uma segunda guarda de graça. **Guarda de contagem** (D-B2, forma escolhida pelo João): `DB::listen` sobre `from "files"` num `GET /api/turmas` com 2 turmas, asserindo **1** — contagem, e não `preventLazyLoading`, porque pega **duas** classes de regressão. Os dois mutantes foram vistos vermelhos com a mensagem literal, pelo implementador e de novo pelo revisor: sem a relação no `LISTING`, `Failed asserting that 2 is identical to 1.`; sem o `loadCount`, `TurmaData::__construct(): Argument #15 ($enrolled_count) must be of type Spatie\LaravelData\Optional|int, null given` — a diferença que a D-B3 compra. **Review BAIXO RISCO** conforme o §8 da spec (zero `generated.ts`, locales, auth/RBAC, schema, dinheiro e rota pública; `executor: claude`) → **uma frente, lente Claude**, sem Codex. Órfãos zero, leis §5 limpas, **4 achados, todos aprovados pelo João e corrigidos** em `a8ddb80`. **Q-1 🟡 é o achado que o gate de execução não podia ter pego, e é do próprio seam do bloco:** o gate da RN-16 no `ConcludeTurmaAction` passou a decidir sobre a relação **em cache**. `for()` lê `documentacaoObrigatoria` como **propriedade**, e com o model já carregado — que é exatamente o que `loadListingData()` faz — um documento arquivado depois daquela carga deixava o cache dizer "completa" e **concluía a turma**, escrita **TERMINAL** (D5) que habilita emissão de certificado. **Provado nos dois sentidos com sonda (lição 10):** com o código do bloco a conclusão passa; com os dois arquivos vindos de `main`, recusa com `Documentación obligatoria incompleta (RN-16). Falta: MANUAL.` **Não era bug vivo** — os 2 chamadores entregam turma sem a relação —, mas é regressão latente a uma linha de distância, e o §5 da spec declarava o cache só como consequência de **leitura**, não de escrita. Corrigido com `load('documentacaoObrigatoria')` **dentro da transação**, declarado no ponto que exige a leitura fresca, mais guarda nova **vista vermelha primeiro**. Os outros três 🟢: `readonly` no VO (imutável pela engine, não por convenção), `TurmaDocumentType::values()` dando um dono só à lista canônica que ainda tinha duas cópias, e o docblock do `fromModel` escrevendo a exigência de `enrollments_count` que a D-B3 criou sem registrar. **Gate de fechamento refeito contra a API real, não herdado** (as correções entraram depois do e2e de execução e mexeram no `conclude`): `GET /api/turmas` **200** com as 4 turmas coerentes com a tabela `files` conferida antes — turma 1 (só `MANUAL`) `false`/`['PRUEBAS','EVALUACION_REDATOR']`, turma 2 `true`/`[]`, turma 4 `false`/`['EVALUACION_REDATOR']` —, e a **D-B1 medida onde o usuário vive**: a turma 3, concluída com os três tipos presentes, responde **`habilitada: false`** com `missing: []`. **A contagem medida na API, não só na suíte:** `general_log` do MySQL em `TABLE`, truncada, um `GET /api/turmas` → **1 query em `files`** para as 4 turmas (`select * from files where type in ('MANUAL','PRUEBAS','EVALUACION_REDATOR') and fileable_id in (1,2,3,4)`), com a listagem inteira em **9** (turmas, redatores, users, courses, quotes, budgets, clients, users, files) contra os **15** medidos na abertura **no mesmo banco** — a decomposição bate com a da spec linha a linha. **O caminho que a Q-1 tocou, nos dois sentidos pela API:** turma 1 → **422** RFC 7807 nomeando `PRUEBAS, EVALUACION_REDATOR`; turma 2 (completa) → **200** `concluida`, `habilitada: false`, `concluded_at` preenchido, `enrolled_count: 8` inteiro (o caminho sem o `??`). `GET /api/turmas/{id}/documents` devolve a lista pela relação sem mudar uma linha da resposta, com o doc soft-deletado fora e o `PRUEBAS` duplicado do seed dentro. Placar: backend **503 passed, 1 skipped (1868 assertions)** contra o baseline **500/1858**; Pint `--test` **`passed`** nos **17** `.php` do bloco; `pnpm lint` e `pnpm build` verdes. **O que o gate NÃO provou, sem maquiagem:** nada foi visto renderizado — o bloco é backend puro e não tocou uma linha de `frontend/`, e o contrato saiu idêntico em forma e valor, então não há tela nova a conferir. **Duas mutações declaradas no banco de dev:** o `migrate:fresh --seed` **não** foi rodado, porque o banco carrega o `LOT-2026-1001` corrompido de propósito para o checkpoint visual do João, que segue pendente — e é este mesmo banco onde os 7 da abertura foram medidos, o que torna a comparação mais forte, não mais fraca; e a turma 2, concluída pela API no gate, foi devolvida a `em_andamento` por `UPDATE` direto, porque a conclusão é terminal e não tem rota de reversão (a auditoria guarda o rastro da conclusão, não o da reversão). **P-09 ficou mais barata de fechar e o texto dela foi corrigido:** mudar de 3 para 4 tipos de documento não exige mais tocar o `TurmaHabilitacaoService` — a lista canônica tem um dono só. | `plans/archive/2026-08-10-turma-habilitacao-listagem.md` · `specs/archive/2026-08-10-turma-habilitacao-listagem-design.md` · sem Context Packet (ausência medida de fonte externa) · commits `31576c7`…`a8ddb80` · ledger `.superpowers/sdd/progress.md` |
| 2026-08-10 | Certificação · lote em Action e gate único de snapshot | Entregue | Item 4 do backlog, selecionado explicitamente pelo João em 2026-08-10; nasceu da revisão de arquitetura de 2026-08-09 com as decisões já tomadas por ele na entrevista. **Uma divergência foi fechada antes do desenho:** o item dizia "13 decisões", o backlog escrevia 6 aqui e 5 no item 5 — decisão dele, **as 6 escritas são tudo** (o "13" contava a entrevista inteira, incluindo o que virou fora-de-escopo). Backend mais um arquivo de frontend → main tree, sem worktree (P-03); **zero schema**, ADR/DER não abriram. **D1/D2:** o laço do lote sai do controller e vira `BatchIssueCertificatesAction`, **sem `DB::transaction` de propósito** — exceção declarada à regra de Action da `backend-ddd.md`, e é o ponto do bloco: falha de um item não pode desfazer certificado que já saiu. **D3:** `App\Shared\Validation\ValidationMessages::squash()` nasce com dois adapters no mesmo commit (o Action do lote e o `ImportStudentsAction`), trocando `->first()` por `implode(' ')`. **D4/D7:** a política de "este snapshot é apresentável?" tinha 2 cópias e vira gate único — `missingRequiredFields()` privado, com `isPresentable(): bool` e `assertPresentable(string $codigo)` adjacentes no molde `assert*`/`constrain*` do `CertificateEligibility`. **D5/D8/D9:** `CertificateData` ganha `snapshot_ok`; `index` **degrada marcando a linha**, `show`/`pdf`/rota pública **falham alto** (500 RFC 7807); o `HistorialTable` troca a tag de estado por `AppTag severity="danger"` e a linha corrompida **mantém o botão Ver**, que cai no estado de erro do `CertificateViewDialog` — é onde o suporte lê quais campos faltam. **Consequência declarada na spec, não descoberta depois:** corrompido **não** virou um quinto `CertDerivedStatus` (promovê-lo contaminaria o dropdown de filtro e os quatro contadores), então filtrar por "Vigente" continua trazendo a linha corrompida cujas datas dizem vigente — corrupção é defeito do documento, não estado dele. **Um desvio forçado pelo schema (D-E2):** o cenário do `index` não pode ter dois `Emitido` na mesma matrícula (`certificates_active_enrollment_unique` sobre a coluna gerada), então o certificado são virou `Revocado` — é o `NULL` da coluna gerada que permite duas linhas revogadas. **Três mecanismos foram vistos reprovando em primeira mão, não aceitos por relatório (lição 10):** envolver o laço do Action num `DB::transaction` reprova `BatchIssueTest.php:299` com `Entries found: 0` (o guard mudou de casa com **zero linha de diff** no arquivo de teste, que era o critério do refactor); `snapshot_ok` derivado de `status` — proxy **perfeito** no cenário original — deixava a suíte **inteira** verde, fechado com uma terceira linha revogada **e** corrompida; e remover `curso.name` da política obrigatória também não matava nenhum teste (Q-3). **Review ALTO RISCO em duas frentes** (lente Claude com o gabarito do projeto + `mcp__codex__codex` read-only sobre `7227d04..HEAD`): **6 achados, todos aprovados e corrigidos** em `d01c279`, órfãos zero, **sem divergência de fato entre os revisores** (o Codex viu 5 dos 6; cada um conferido no código antes de aceito, com o escopo do Q-3 corrigido). **Q-1 é o achado que o gate de execução não podia ter pego:** `ProblemDetails::detailFor()` troca o `detail` de **todo** 500 por `'Ocorreu um erro inesperado. Tente novamente.'` quando `app.debug` é falso, e a D8 promete o contrário — em produção o suporte leria "erro inesperado", sem código e sem campo. Nem a suíte nem o e2e viam, e o motivo foi medido: `backend/.env` tem `APP_DEBUG=true` e **não existe `.env.testing`**, então os dois provaram a D8 num caminho que a produção não percorre. Nasce `App\Shared\Exceptions\PublicDetail`, interface marcadora para a exceção cuja mensagem foi escrita para quem lê a resposta; o default segue mascarando e só quem declara passa. A mensagem também saiu de **PT-BR para es-CL** — ela agora chega à tela de um usuário chileno. Os outros cinco: Q-2 (o docblock afirmava ser a listagem a **única** projeção sem gate; `store()`/`revoke()` também são, e o texto vinha verbatim do plano — lição 13), Q-4 (dois decodes de JSON por linha numa listagem que não pagina), Q-5 (o teste do `show` afirmava só status e content-type, passa a afirmar o `detail` de que a D8 depende) e Q-6 (a **fiação** do `squash()` no Action não tinha guarda — voltar para `->first()` ficava verde). **Gate de fechamento refeito contra a API real, não herdado** (as correções entraram depois do e2e de execução e mexeram em `show`, listagem e `ProblemDetails`): `migrate:fresh --seed` no MySQL, sessão Sanctum por cookie + CSRF; porta destravada pela própria API (`sin_plantilla` → `null` com o template v1 e `layout_config.city = 'Santiago'`, `modules` **omitidos** do payload e devolvidos intactos); emissão individual **201 `LOT-2026-1000`** com `snapshot_ok: true`; lote `[22,21,23,24]` com a falha provocada → **200** com relatório por item, `LOT-2026-1001`/`1002`/`1003` **contíguos** (a recusa **não consumiu número**) e a falha nomeada (`Ya existe un certificado vigente para esta matrícula.`); `[26,26]` → **422** problem+json, o `distinct` vivo. `aluno.name` corrompido direto na coluna do certificado 2, com **o resto do JSON conferido por MD5 como byte-idêntico**: `index` **200** com `snapshot_ok: false` **só** naquela linha; `show`, `pdf` e rota pública **sem cookie** → **500 `application/problem+json`** nomeando `LOT-2026-1001` **e** o campo faltante; certificado são → **200**, PDF são → **200 `application/pdf`**, rota pública do são sem cookie → **200**. **A prova nova, que nenhum gate anterior tinha feito, nos dois sentidos: com `APP_DEBUG=false`** (o `.env` em modo produção, `config:clear`), o `detail` da recusa **sobrevive** no `show` e na rota pública — a promessa da D8, agora medida onde a produção vive —, e um 500 **comum** (Gotenberg parado de propósito) **continua mascarado** com `Ocorreu um erro inesperado. Tente novamente.`; `.env` restaurado e PDF são de volta em 200 no mesmo passe. Placar: backend **500 passed, 1 skipped (1858 assertions)**; frontend **13 arquivos / 47 testes**, `pnpm lint` e `pnpm build` verdes; `typescript:transform` **sem diff** em `generated.ts`; `backend/database/` sem uma linha. **Uma exceção honesta no Pint:** `passed` em 13 dos 14 `.php` do bloco, e `ProblemDetails.php` reprova — mas **já reprovava na versão base**, conferido rodando `pint --test` sobre o arquivo de `7227d04` com a mesma lista de 6 fixers; é dívida de estilo pré-existente, e reformatá-lo inteiro seria ruído de diff (lição 9). **Limitação declarada, não item silenciosamente cumprido:** a tag da linha corrompida **não foi vista renderizada** — o WSL segue sem browser utilizável (Playwright sem as libs de sistema, herdado de 2026-08-08); a prova aqui é o `snapshot_ok` na API real, o build/lint e a paridade das 3 locales, e o **checkpoint visual fica com o João** — o banco de dev fica com `LOT-2026-1001` corrompido de propósito para ele encontrar a linha marcada. Fica registrado como decisão consciente, não como pendência: o `IssuedDialog` lê o certificado por caminho **não gateado** (o `useIssueCertificate` semeia o cache com a resposta do POST), porque fechar isso seria uma **quinta** mudança de comportamento e o §5 da spec é lista fechada de quatro. | `plans/archive/2026-08-10-certificacao-lote-e-snapshot.md` · `specs/archive/2026-08-10-certificacao-lote-e-snapshot-design.md` · sem Context Packet (ausência medida de fonte externa) · commits `66e0911`…`d01c279` |
| 2026-08-08 | Bloco 7 · Sprint 4 · Certificação — frontend (módulo próprio) | Entregue | Item 1 do backlog, selecionado pelo João em 2026-08-08 — o que sobrou do Bloco 7 depois do D-P8, com as Tasks 9–13 **replanejadas**, não copiadas. **As 4 frentes num bloco (decisão dele):** módulo `/certificados` (Emisión + Historial), validação pública `/validar/:uuid` + D19 (descida do `SessionBootstrap` — rota pública não dispara `GET /api/me`), resultado acadêmico na tela da turma e os dois Blades herdados (Manual em A4, certificado sem transbordo). Backend: `emission-panel` **substitui** `issuable` — turma não emissível aparece **bloqueada** com motivo calculado no servidor (`emission_blocked`, D-P1/D-P3; as portas são 6, não 4 — D-P2), DTOs `Issuable*` mortos no mesmo commit; `POST /api/certificates/batch` com relatório por item, cada matrícula na própria transação — falha de um item não impede os demais **nem consome número** da sequência (guarda `be58466`: item emitido sobrevive a falha inesperada no seguinte). Execução SDD em 12 tasks; a Task 10 **adotou o WIP do João** em vez de reescrever (`1023c5b`); dois defeitos de peso legal achados em review de task e vistos falhando antes do fix: o relatório do lote perdia o nome de quem recebeu certificado porque a invalidação repintava o diálogo por baixo (`6c57888`), e `grades` vazio gravava `[]` com auditoria de mudança que não houve (`b85b736`). **Review de sprint ALTO RISCO em duas frentes** (lente Claude + Codex read-only sobre `3d7ee5c..bbe1f39`): 9 achados aprovados (3 🟡, 6 🟢), todos corrigidos (`c02f29e` backend, `3884101` frontend) — destaque Q-1, 4ª ocorrência da classe vivo×congelado (`IssuedDialog` lia nome vivo com snapshot carregado; o canal de dado vivo morreu inteiro) e Q-3 (face de lista morta do `CertificateEligibility` removida, testes de invariante migrados para o alvo real `EmissionPanelQuery`); **3 achados do Codex rejeitados com evidência**. Decisões do João fecham o review: a regra vivo×congelado proposta **não entra** na rule (decisão consciente registrada), e as 4 decisões de negócio ficaram "ok como está" (elisão da descrição, penhasco dos 68 chars, `revoke` superadmin-only, ramo `expired` só com cabeçalho). **Gate de fechamento re-provado contra a API real em 2026-08-08 (item 0 refeito, não herdado — as correções mexeram nos caminhos de listagem):** `migrate:fresh --seed`; painel destravado **porta a porta** (`sin_plantilla` → `plantilla_sin_ciudad` → `null`) criando o template pela API; resultado `"6,9"` → **200** com a vírgula chilena preservada na turma viva, `[]` → **422** RFC 7807 es-CL, e a turma concluída recusando com **RN-15**; emissão individual **201 `LOT-2026-1000`** com snapshot congelado (razão social); certificado com `description` de **3.689 chars** → **200, 2 páginas A4**, descrição clampada com reticências e QR/assinatura/disclaimer ancorados na página 1 (`pdftoppm` inspecionado); Historial → revogação **200** com motivo → pública dizendo `revocado` com `revoked_at` presente e `revocation_reason` **ausente** → reemissão **201 `LOT-2026-1002`** só por ser revocado, uuid novo; batch com falha provocada → relatório por item com a falha **nomeada** e números contíguos `1003`/`1004` (falha não consumiu número), id duplicado → **422** (Q-5); Manual **200, 1 página A4**; pública **sem cookie, sem CSRF, sem `Origin`**: 200 emitido/revocado e 404 RFC 7807 para uuid inexistente. Placar final: backend **493 passed, 1 skipped (1833 assertions)**; frontend **13 arquivos / 47 testes**, `pnpm lint`/`pnpm build` verdes; Pint `passed` nos 26 `.php` vivos do bloco; `typescript:transform` sem diff; greps de lei todos limpos (zero import cross-feature — o hit único é docblock). **Limitação declarada, por decisão explícita do João:** os Steps 1 (tela real) e 5 (checkpoint visual) do gate **não foram executados** — nenhuma tela do módulo foi vista renderizada por ninguém (browser indisponível no WSL; Playwright sem libs de sistema); a prova é de API real, PDF página a página, suíte e lint. O conteúdo do QR (`frontend_url + /validar/{uuid}`) ficou provado no gate de execução, que nenhum Q tocou. P-15 reescrita no fechamento: o bloco entregou módulo próprio e **não tocou o módulo de alunos** — a decisão de expor coluna/card lá segue com o João. | `context-packets/certificacao-sprint-4.md` (reutilizado por ponteiro) · commits `a87ad8f`…`3884101` · ledger `.superpowers/sdd/progress.md` | `plans/archive/2026-08-08-certificacao-frontend.md` | `specs/archive/2026-08-08-certificacao-frontend-design.md` · spec base `specs/archive/2026-08-05-certificacao-sprint-4-design.md` (arquivada em 2026-08-08, seu último consumidor) |
| 2026-08-08 | Profundidade de module · backend B4–B7 | Entregue | Item 2 do backlog, selecionado pelo João em 2026-08-07. Backend-only, main tree (P-03), **zero mudança de schema** — ADR/DER não abriram. **B4:** a cadeia `turma->quote->budget->client`, soletrada em 10 sítios de 3 domínios (2 a mais que a spec media — D-P3), vira `ContratanteData` com seams em `Turma::contratanteClient()`/`contratante()` e `Quote::contratante()`; o VO nasce em `App\Shared\Data`, não em `Commercial/Data` como a spec pedia, porque a Regra A do `DomainDependencyTest` só expõe `Models/Enums/Services` (D-P1) — a aresta Certification→Commercial some por mediação em vez de virar aresta declarada, e `generated.ts` **não muda**. **Catraca:** `ContratanteSeamTest` varre `->budget->client` em `app/Domains/**` (comentários strippados por `token_get_all`) e em `resources/views/**` (RAW — Blade não passa pelo tokenizer), com allowlist dos dois donos do seam. **B5:** builders para os 4 models medidos (Enrollment, Quote, Client, Course); o lazy-load silencioso do `EnrollmentController::result` morre. O RED do bug **não** saiu como o brief mandava: `Builder::hydrate()` só liga `preventsLazyLoading` por instância quando `count($items) > 1`, então busca singular nunca reproduz — provado no fonte do vendor e conferido duas vezes antes de o desvio ser aceito. **B6:** `AcademicResult` em `Operation\Services` (D-P2), `grades.final` não-imprimível passa a ser **422 na escrita** e o snapshot lê do VO. **B7:** `IssuableEnrollmentBuilder` absorve os 8 setUps de Certification com contagem antes/depois idêntica em cada arquivo; `->jaEmitido()` ficou de fora (D-P4 — emissão é ato do teste, não setup). **Review de ALTO RISCO em duas frentes** (Claude + `mcp__codex__codex` read-only): 7 achados, todos aprovados e corrigidos. **Os dois que só o Codex viu foram provados por MUTAÇÃO nos dois sentidos, não aceitos por relatório.** Q-1 🔴 — o seam do B4 **introduziu N+1 em 4 listagens** (`TurmaQueryBuilder`, `TurmaController::pending`, `CertificateEligibility::issuableTurmas`, `ManualPdfService`): `Client::contratante()` lê `user->rut` e nenhum sítio carregava `client.user` — 4 turmas custavam 11 queries contra 7 com o eager-load. Ironia registrada: o bloco cujo B5 existe para matar lazy-load **adicionou** quatro, e o gate da Task 9 provou `/alunos` sem nunca medir `/turmas`. Guarda nova: `ContratanteEagerLoadTest`, companheiro de runtime da catraca estática. Q-2 🔴 — a migração do B7 **apagou o poder discriminante de dois testes de peso legal**: `CertificateEligibilityTest` não exigia qual porta recusou (redator próprio por cenário fazia a porta 6 recusar todas) e `PublicCertificateTest` ficou com a cadeia viva byte-idêntica ao snapshot congelado — mutantes davam verde no migrado e vermelho no `main`. Q-3/Q-4/Q-5/Q-6/Q-7: `LISTING` vira fonte única nos 6 sítios, `ContratanteData::$rut` vira `?string` (`users.rut` é nullable e cinco projeções estouravam `TypeError`), chave `'matrícula não aprovada'`, método órfão `enrollment()` morto e `assertSemColisaoDePorta()` nova. **Padrão reincidente virou regra** (3ª ocorrência): três parágrafos em `.claude/rules/backend-ddd.md` §Testes. Gate de fechamento provado contra a API real (lição 12), não pela suíte: `PUT .../resultado` com `"6,9"` → **200** com a vírgula preservada e com `[]` → **422** RFC 7807 es-CL (`La nota final debe ser un número o un texto no vacío.`); os **4 sítios do Q-1 exercitados na API** (`/turmas`, `/turmas/pendientes-configuracion`, `/certificates/issuable`, `/turmas/3/manual` → `application/pdf`); emissão `LOT-2026-1000` **201** com o seam conferido em **SQL cru** (`snapshot.cliente.name` = `clients.legal_name` = `Enel Distribución`, enquanto `users.name` = `USUARIO-EMPRESA Enel` — os dois textos precisaram ser diferenciados à mão, porque o `OperationDemoSeeder` grava `name == legal_name`); PDF do certificado **200 `application/pdf`, 2 páginas, A4 (594.96 × 841.92 pts)** imprimindo a razão social; `/turmas/3/alunos` **200** com 15 matrículas, todas com o aluno aninhado; e o Q-4 provado com `users.rut` **NULL** num cliente das duas listagens, sem 500. Suíte **477 passed, 1 skipped (1698 assertions)**; Pint `passed` nos 16 `.php`; `pnpm lint`/`pnpm build` verdes; `typescript:transform` sem diff em `generated.ts`; `frontend/` e `backend/database/` sem uma linha. | sem Context Packet (ausência de fonte externa medida) · commits `d926faf`…`8a62536` | `plans/archive/2026-08-07-profundidade-backend-b4-b7.md` | `specs/archive/2026-08-07-profundidade-backend-b4-b7-design.md` |
| 2026-08-07 | Bloco 7 · Sprint 4 · Certificação (backend + documentos) | Entregue | Item 1 do backlog, selecionado pelo João em 2026-08-05, **corte alterado por ele em 2026-08-05 (D-P8): o frontend virou bloco próprio** e as Tasks 9–13 migraram inteiras — o que fecha aqui é a vertical até a API pública, mais os dois Blades. Backend: `certificates`/`certificate_sequences`, numeração anual atômica `LOT-ANO-SEQ` (duas emissões concorrentes contra o MySQL produzem 1000 e 1001), snapshot congelado no ato da emissão, emissão com as seis portas, `issuable`, revogação terminal, PDF sob demanda pelo Gotenberg sem materialização (§4.7) e rota pública de validação por UUID sem cookie. Task 15 trouxe o Blade para o documento oficial da Lotus (D-P9) e a Task 16 provou o papel: `pdfinfo` do PDF real diz A4 (594.96 × 841.92 pts) depois do `preferCssPageSize`, contra o Letter do default do Gotenberg. **Quatro rodadas de revisão, todas com segunda lente independente do Codex por ser documento de peso legal:** backend do bloco (limpo), Task 15 (6 achados), correções (5 achados, R-1…R-5) e as três refatorações de arquitetura B1–B3 (7 achados, Q-1…Q-7). As três refatorações vieram do review de arquitetura de 2026-08-07: transporte do Gotenberg virou `App\Shared\Pdf` (B3, ADR-12), o snapshot virou contrato tipado com `schema_version` (B2) e as seis portas da emissão passaram a ter fonte única, com `assert*`/`constrain*` adjacentes (B1) — a classe de bug que já mordeu duas vezes era a lista prometer certificado que o POST recusa com 422. O 🔴 da última rodada foi provado por sonda, não deduzido: o cast de objeto entrava no cache de casts do Eloquent e qualquer `save()` posterior a uma leitura reescrevia o snapshot congelado — `withoutObjectCaching` fecha, e a mesma sonda agora diz `REESCREVEU? NAO`. Q-3 e Q-4 mudam comportamento de propósito: nota não-numérica (`"6,4"`, como se escreve no Chile) volta a ser impressa, e snapshot sem `aluno.name`/`curso.name`/`emissor.name` vira 500 em vez de 200 dizendo `emitido` com nome vazio. Gate de fechamento provado contra a API real (lição 12), não pela suíte: login Sanctum → `issuable` → emissão `LOT-2026-1004` → PDF 200 `application/pdf` A4 com `nota 6,4` no papel → revogação com o MD5 do snapshot **idêntico** antes e depois → rota pública sem cookie 200 → snapshot corrompido 500 RFC 7807. Suíte 457 passed, 1 skipped (1655 assertions); Pint verde; `pnpm lint`/`pnpm build` verdes; `generated.ts` regenerado sem diff. **Fica aberto e registrado:** o frontend da certificação (item 1 do backlog, agora só ele), o Manual de Classe em Letter por não declarar `@page`, e o transbordo de página quando `courses.description` é longa — reproduzido no gate com uma descrição de 3.689 caracteres do seed. | `context-packets/certificacao-sprint-4.md` | `plans/archive/2026-08-05-certificacao-sprint-4.md` | `specs/archive/2026-08-05-certificacao-sprint-4-design.md` (spec arquivada em 2026-08-08 no fechamento do bloco de frontend, seu último consumidor) |
| 2026-08-05 | Profundidade de module · formulário CRUD e hidratação de DTO | Entregue | Item 1 do backlog, os dois candidatos do review de arquitetura de 2026-08-04 num bloco só. **A (backend):** os 8 `fromModel` param de resolver serviço pelo container. Sete sítios de URL assinada viram `#[WithTransformer(SignedUrlTransformer::class, N)]` — a propriedade carrega o path e a assinatura acontece na serialização; `UploadFileAction::temporaryUrl()` e `UserPhotoService::urlFor()` **morreram** (D5). O oitavo (`TurmaData`) recebe `TurmaHabilitacaoService` por parâmetro (D7). **O brainstorming corrigiu o próprio backlog antes de desenhar** (6ª ocorrência da lição 13): o backlog escrevia "cast", e `Cast::cast()` roda na **criação** — quem transforma na saída é `Transformer::transform()`; como os 8 DTOs constroem por `fromModel` explícito, um cast jamais dispararia. **B (frontend):** nasce `useCrudForm` sobre o `useEntityForm`, com `toPayload(form, mode)` e classificação obrigatória de toda chave de payload; 5 hooks migraram (role, usuário staff, aluno, orçamento, cliente). **A escrita do plano achou dois defeitos na spec aprovada:** a guarda do `mapped` estava com a direção invertida e reprovaria o primeiro hook do piloto, induzindo justamente a supressão silenciosa que a D11 existe para impedir — nasceu `summaryOnly`, a terceira caixa; e a lista de migração caiu de 4 para 3 (`useRedatorForm` monta `FormData`, que `toPayload` não modela). `executor: misto` — Tasks 2, 3 e 5 no Codex, sem commit próprio. **Review de alto risco** (domínio §5.3, documento de peso legal, Codex no misto): lente Claude + `mcp__codex__codex` read-only, **4 achados, nenhum 🔴, Codex com zero achado e sem divergência entre revisores**. Só o Q-1 foi aprovado: `useCrudForm` devolvia `setForm` e os 5 hooks devolvem o módulo inteiro, então um setter que **nenhum diálogo tinha antes** passaria a chegar aos 5 por spread — a rule nomeia o `patchContact(setForm, ...)` como contra-exemplo. Corrigido em `577751b` com `{ crud, setForm }`, provado nos dois sentidos (diálogo consumindo `setForm` reprova no `tsc` com `TS2339`; devolver a chave para dentro de `crud` reprova a asserção nova). **Fecha a D10** do bloco `hardening-guardrails-e-transportes`: a família que assinava URL pelo container deixou de existir. **DoD provado e2e contra a API real com sessão Sanctum**, não por suíte verde: `photo_url` baixou **200 `image/png`** e `download_url` **200 `application/pdf`**, com `X-Amz-Expires` de **3600** e **600** — os dois TTLs preservados —, assinados contra `localhost:9000` e não `minio:9000`; `photo_url` segue `null` sem foto; `habilitada`/`missing_document_types` presentes nas 8 turmas; e o nível 3 do aninhamento `Budget → Quote → File` saiu assinado, que é exatamente o que "serviço por parâmetro" não alcançava e justificou o transformer. Backend **378 passed (1368 assertions)** — o placar **cai** na Task 3 e o caminho é declarado (`+2/+3`, `−2/−3` com os 2 testes do `urlFor` morto, `+1/+1`); frontend **35 passed**; build, lint e Pint (17 `.php`) verdes; `generated.ts`, `locales/` e `backend/database/` sem diff. | sem Context Packet (ausência de fonte externa medida, não pressa) | `plans/archive/2026-08-05-profundidade-form-crud-e-hidratacao-dto.md` | `specs/archive/2026-08-05-profundidade-form-crud-e-hidratacao-dto-design.md` |
| 2026-08-04 | Hardening · tabela e testes pré-Sprint 4 (H.4.4, H.4.5, H.4.9) | Entregue | Terceiro e último recorte do item 1 do backlog, que **fecha por inteiro** — as 10 tasks do conjunto Notion saíram em 3 blocos de 2026-08-04. `executor: misto` (Task 4 no Codex, resto Claude), branch `hardening/tabela-e-testes` no main tree (P-03), 5 commits de conteúdo + 1 de correção de review. **H.4.4** — `SearchableTableFrame` nasce em `shared/ui` e absorve toolbar, empty de busca e a chamada ao `AppDataTable` que 5 tabelas busca-só repetiam literalmente (cursos, clientes, usuários, redatores, alunos); as 2 com dropdown e as 2 fora do padrão ficam de fora por medição, não por esquecimento. **A decisão de desenho foi não criar a primeira aresta `shared/ui → shared/hooks` do projeto** (D3): a moldura declara `SearchableTableState<T>` **estruturalmente compatível** com o `TableFilter<T>` em vez de importá-lo, seguindo o precedente do próprio `AppDataTable`. **H.4.5** fechou pelo argumento e não pelo nome — `CallExpression > Identifier.arguments[name=/Api$/]` reprova `useCrudPage(budgetsApi)` num componente, e os 7 aliases `useXPage` ficam **justificados**, não eliminados: matá-los moveria a query para as 4 páginas e **passaria no lint antigo**. **H.4.9** concentrou em `tests/Support/CreatesDomainRecords` os 2 setups que 48 arquivos repetiam, matando 20 helpers privados locais com 4 nomes divergentes. Checkpoint visual das 5 telas aprovado pelo João. **O review (ALTO RISCO — Task 4 no Codex; Claude + Codex read-only) devolveu 4 achados e o João aprovou os 4:** **Q-1** — a moldura recalculava "estou filtrando?" com `term === ''` em vez de consumir o `filtering` do hook, que é o **mesmo defeito** que `BudgetsTable` e `TurmasTable` cometeram juntas em 2026-08-03; nasceria errada por construção para quem usa `where`. **Q-2** — o seletor novo casava só `arguments.0`, então `useEntityForm(mode, clientsApi)` escapava: o guardrail **reproduzia o próprio buraco** que existe para fechar. **Q-3** (achado do Codex, verificado no código antes de aceito) — a migração tratou os 6 `actingAdmin` como repasse puro, e um não era: `PendingQuotesTest` tinha um user com **exatamente** a permissão que gateia a rota, trocado por `actingAsAdmin()`, que tem todas — **e o placar não viu**, porque escopo de permissão é setup, não asserção. **Q-4** — 11 sítios remendavam o `rut` com `$client->user()->update()` depois da criação, mass update no query builder que **não dispara evento** em model `Auditable` (ADR-08, lição 5); o trait ganhou `$userOverrides` e a criação voltou a ser atômica. **Divergência entre revisores, mostrada e não resolvida em silêncio:** o Codex declarou H.4.4 e H.4.5 limpos onde a lente Claude achou Q-1 e Q-2 (o Q-2 provado com sonda executada). **O placar mudou de propósito, por decisão explícita do João:** 376/1366 → **377 passed (1367 assertions)**, a asserção a mais sendo a guarda do Q-3 — `test_permissao_vizinha_de_turma_nao_abre_a_rota`, **vista reprovando** com `operation.turma.create` antes de virar verde (lição 10), sem tocar `backend/app/` (D14). Gate: `pnpm test` **21 passed**, build/lint verdes, Pint `passed` nos 51 arquivos `.php` com a guarda de lista vazia (lição 9); `backend/app/`, `backend/database/`, `generated.ts` e `locales/*.json` **sem diff**. **Prova e2e contra a API real com sessão Sanctum:** os 5 endpoints de lista que as tabelas migradas consomem responderam **200** com dados reais e **todos os campos `searchable` presentes no payload** (`name`/`technical_name`, `legal_name`/`rut`, `name`/`email`, `name`/`rut`); a contagem de `<AppColumn` de cada uma das 5 bate com `main` (coluna que some ou nasce é mudança de comportamento, não extração); e sonda fresca em `catalog` reprovou o seletor do H.4.5 nas **duas** posições de argumento, removida com árvore limpa. | `context-packets/hardening-tabela-e-testes-pre-sprint-4.md` | `plans/archive/2026-08-04-hardening-tabela-e-testes-pre-sprint-4.md` | `specs/archive/2026-08-04-hardening-tabela-e-testes-pre-sprint-4-design.md` |
| 2026-08-04 | Hardening · guardrails e transportes pré-Sprint 4 (H.3.1, H.4.6, H.4.7, H.4.8) | Entregue | Bloco `executor: misto` (Tasks 1/4 no Codex, resto Claude), branch `hardening/guardrails-e-transportes` no main tree (P-03), 4 commits de conteúdo + 2 de correção de review. **Nenhuma das 4 tasks corrigia defeito visível** — nos dois lugares que ganham mecanismo o estado já estava correto, então o DoD foi guardrail visto reprovando com sonda fresca, não suíte verde. **A medição do Notion errou 3 vezes e o repositório decidiu:** H.3.1 não tinha o furo que a task supõe (6 URI patterns / 7 rotas com ≥2 bindings, 5 já guardadas, e as 3 rotas que a task nomeia são shallow — sem pai na URL, sem posse a cruzar), e H.4.8 já estava em paridade perfeita (443 chaves × 3 locales, zero diff). H.3.1 virou declaração obrigatória: `NestedRouteOwnershipTest` lê a rota por `signatureParameters()`, não por regex de URI, e **silêncio reprova** — os 3 `abort_unless` viraram `->scopeBindings()` e saíram dos controllers (a relação `MorphMany` preserva o 404 pelo TIPO), e as 2 rotas N:N de redator declaram `->withoutScopedBindings()` com o motivo ao lado, no lugar de uma allowlist que envelheceria longe da rota. H.4.6 pilotou `BudgetData` sem `app()` (**D9 fechou como sinal 1**: produção limpa em 1 nível, `TurmaData` e a família de assinatura de URL ficam fora por D8/D10). H.4.7 concentrou 6 dos 7 `new FormData()` em `postMultipart`, com o comentário da lição 6 existindo uma vez só. **O review (ALTO RISCO, Claude + Codex read-only) achou 5, o João aprovou 2:** Q-1 🔴 — o mecanismo entregue nascia invisível (a rule ainda ensinava o padrão antigo e nada impedia `new FormData()` numa feature), virou `no-restricted-syntax` com catraca de um, montado para **não** repetir a colisão de merge raso do flat config que foi o Q-2 do review anterior; Q-5 🟢 — o `flatten` do teste de paridade media `typeof === 'string'` e sumia com chave de valor não-string. Deferidos: Q-2 e Q-4 (§Débitos técnicos do backlog), Q-3 → **P-26** (403 virou 404 sem permissão, porque `SubstituteBindings` roda antes do `permission:` — a spec afirma o contrário). Gate: backend **376 passed (1366 assertions)**, `pnpm test` **21 passed**, build/lint/Pint verdes, `generated.ts` e locales sem diff. **Prova e2e contra a API real:** foto 204 com a URL pré-assinada devolvendo os 70 bytes como `image/png`, documento de turma 201 com `size: 69` (a lição 6 quebraria isso em silêncio), e o 404 cross-pai exercitado na própria API, não só em teste. | `context-packets/hardening-guardrails-e-transportes-pre-sprint-4.md` | `plans/archive/2026-08-04-hardening-guardrails-e-transportes-pre-sprint-4.md` | `specs/archive/2026-08-04-hardening-guardrails-e-transportes-pre-sprint-4-design.md` |
| 2026-08-04 | Hardening estrutural pré-Sprint 4 (H.4.1–H.4.3) | Entregue | Bloco `executor: misto` (Tasks 1/2/5 no Codex, resto Claude), branch `hardening/estrutural-pre-sprint-4` a partir do `main`, sem worktree (toca `backend/`, `eslint.config.js`, `vite.config.ts`). Três guardrails novos: H.4.1 — `DomainDependencyTest` (PHPUnit puro, Pest não instalado) fixa a matriz de dependência entre os 5 domínios como mecanismo, não doc; os 42 imports cross-domain medidos não continham acoplamento indevido, então `git diff -- backend/app/` fica vazio. H.4.2 — as 3 fronteiras da lei §5.6 (PrimeReact fora de `shared/ui`, feature→feature, `shared`→feature) viram `no-restricted-imports` no `eslint.config.js`, achando e corrigindo um bug real de flat config (merge raso de `rules` apagava a fronteira 1 em silêncio). H.4.3 — vitest instalado com regressão de `useTableFilter`/`useCrudPage` (12 casos). H.3.1, H.4.4–H.4.9 ficaram de fora por decisão do João no brainstorming — voltam para `backlog.md`. **Review com segunda lente do Codex (`/revisar-sprint`, alto risco) achou 4 buracos + 1 falso positivo nos 3 guardrails, todos por sonda:** import de NAMESPACE (`use App\Domains\X\Enums;`) escapava das Regras A/B em silêncio; comentário do `eslint.config.js` afirmava cobrir subida relativa cross-feature e não cobria (`no-restricted-imports` casa a string escrita); regex de group use cruzava comentário/docblock e reprovava por menção, não import; `arquivosDeDominio()` lia as chaves de `ALLOWED`, então domínio novo nasceria sem guardrail. Mais 2 achados de doc: os docs ainda negavam o runner vitest que o próprio bloco instalou (`CLAUDE.md`/`frontend-fsliced.md`); e um bug real achado pelo João no checkpoint visual — `filtering` media a presença do `where`, não o efeito dele, fazendo tabela com escopo permanente mostrar o empty state de filtro sobre lista legitimamente vazia (`TurmasTable`/`BudgetsTable`). Todas as correções reprovadas de novo com sonda fresca antes de aceitas (lição 10), inclusive no próprio fechamento desta sprint (namespace-import e cross-feature relativo reprovados de novo, sondas removidas, árvore limpa). Um sétimo achado fora dos guardrails: `startEdit` guardava por `id`, não por entidade — deep link com GET pendente entrava em modo `edit` com `entity` nula. Gate final: suíte backend **375 passed (1365 assertions)**, `pnpm test` **14 passed**, `pnpm build`+`pnpm lint` verdes, Pint sem alteração pendente, `generated.ts`/`backend/app/` sem diff. Checkpoint visual do João aprovado (Operação e Presupuestos, 5 pontos: vazio sem filtro, cheio sem filtro, filtro sem resultado, busca sem resultado, alternância Todos↔estado real). P-04 passa a **parcialmente resolvida** (§5.6 e a matriz de domínios ganharam mecanismo; §5.1/§5.2 seguem só instrução, reavaliar 2026-08-15). | ``context-packets/hardening-estrutural-pre-sprint-4.md`` | ``plans/archive/2026-08-03-hardening-estrutural-pre-sprint-4.md`` | ``specs/archive/2026-08-03-hardening-estrutural-pre-sprint-4-design.md`` |
| 2026-08-03 | Frontend · Abstração de componentes de `catalog` | Entregue | Bloco 100% frontend (D1, `git diff main...HEAD -- backend/` vazio), branch no main tree sem worktree — o DoD se prova na tela contra o compose do main tree. Fecha os 7 achados do `/revisar-frontend` de `catalog` (4 C + 3 B), lei §6 já limpa na entrada. `CourseDialog.tsx` cai de **251 para 96 linhas**: o quadro de módulos (76 linhas, 5 campos irmãos por item) vira `ModuleFields` + `ModuleCard` no molde `ContactFields`/`ContactCard` do `ClientDialog` (C-1), com `Fragment` no lugar de `<div>` porque os filhos são irmãos diretos do `section` com `space-y-4` — um nó novo mudaria o espaçamento; a seção de redatores (ternário de 4 ramos) vira `CourseRedatoresSection` **sem** achatar em guarda sequencial (C-2) — diferente do `PickerBody`/`SlotBody` dos blocos anteriores, aqui o 3º ramo é modo de diálogo, não estado de carga, e transformar em guarda mudaria a condição de forma. `CoursesTable.tsx:87` perde o template literal quebrado (`` `pi pi-book }` ``, mandando classe lixo `}` para o DOM) e o hex `'#25A5E4'` hardcoded, que passa a ler `BRAND_COLOR` de `shared/config/brand` (C-3/C-4, no-op visual por construção). `modulesTotal`/`hoursMismatch` sobem para o `useCourseForm`, dono de `form.modules`/`form.workload_hours` (B-1); `useNavigate`/`usePermissions`/`openRedator` sobem para o `useCourseRedatores(enabledIds, onClose)`, que passa a expor `canOpenRedator`/`openRedator` com `onClose` antes do `navigate` (B-2); o B-3 (alias `enabledIds`) desaparece como efeito colateral da extração da seção. **As extrações foram provadas literais, não assumidas:** `ModuleCard` é idêntico byte a byte às linhas 97-170 do original, com exatamente 4 divergências previstas (`key={i}` migrou para o `.map`, `i === 0`/`i === length-1` viraram `isFirst`/`isLast`, os 3 handlers viraram props). **O achado do review não era defeito do diff — era doc que descrevia intenção não-construída (lição 13):** spec e `state.md` citavam "a régua de ~150 linhas **da rule**", e a régua não existia em lugar nenhum — `grep` na `frontend-fsliced.md` voltava vazio. Pior, o padrão que ela deveria conter (bloco coeso preso dentro de componente grande) custou **três blocos consecutivos** de refactor. Pela cláusula de reincidência + lição 14, virou mecanismo: `max-lines` (150) em `eslint.config.js` sobre `src/features/*/components/**`, limite tirado da distribuição real (53 dos 57 componentes de feature já ficavam abaixo), em **bloco de config separado** do `no-restricted-syntax` de propósito — `ignores` compartilhados reabririam em silêncio a catraca de query-em-componente zerada no bloco anterior. Entrou com catraca de 4 legados (`StudentDialog` 189, `RedatorDialog` 189, `RedatorDocumentSlot` 175, `BudgetDetailPage` 171), lista que só encolhe, registrada em §Débitos técnicos do `backlog.md`. **Provado nos dois sentidos (lição 10)**, na correção e de novo no fechamento: sonda de 160 linhas em `catalog/components/Course/` reprova **com a catraca ativa** (ela não acoberta arquivo novo) e a **mesma** sonda em `catalog/hooks/` fica em silêncio (hook longo é legítimo, componente inchado não); catraca esvaziada reprova exatamente os 4, com as contagens batendo o `wc -l`. Divergência de DoD resolvida pelo próprio achado: `CoursesTable` ficou em 125 linhas, acima do "~110" que a spec §5 pedia — número avulso de um bloco, substituído pela régua de projeto de 150, na qual passa com folga; não é dívida aberta. Review de baixo risco (zero `backend/`/`generated.ts`/locales/auth/RBAC/schema/dinheiro no diff, `executor: claude`; só lente Claude, sem Codex): órfãos zero, leis §5 limpas. Gate: suíte backend **372 passed (1360 assertions)** como regressão, `pnpm build`+`pnpm lint` verdes, Pint n/a (zero arquivo de backend), `generated.ts`, locales e `shared/` sem diff, greps de query-em-componente, `primereact` direto, cross-feature e `#25A5E4` em `features/` sem saída. Prova visual do João em 1 checkpoint (D10), sem baseline capturada (mesma limitação de browser dos blocos anteriores): Cursos, diálogo nos 3 modos e erro de redatores com Reintentar — aprovada em 2026-08-03; a correção pós-review tocou só `eslint.config.js` e `.claude/rules/`, nenhum componente, então a aprovação continua válida. | — | ``plans/archive/2026-08-03-abstracao-componentes-catalog.md`` | ``specs/archive/2026-08-03-abstracao-componentes-catalog-design.md`` |
| 2026-08-03 | Frontend · Zerar a catraca de query-em-componente + abstração de `commercial` | Entregue | Bloco 100% frontend (D1, `git diff main...HEAD -- backend/` vazio), branch no main tree sem worktree — toca `eslint.config.js`, que vale para o repositório inteiro. Duas dívidas que se provam na mesma tela, por isso um bloco só. **Metade 1 — a catraca zerada.** A regra `no-restricted-syntax` de 2026-08-02 tinha nascido com 7 componentes legados em `ignores`; enquanto a lista existisse, o lint verde afirmava menos do que parecia. Cada arquivo saiu da lista **no mesmo commit** que perdeu a query para um hook novo: `useCommercialClients` (`BudgetsTable`+`BudgetDialog`, único com 2 consumidores — mesma lista, mesmo lookup, chave do TanStack já compartilhada), `useQuoteCourseSearch` (`QuoteWizard`), `useQuoteFiles` + `useQuotesListCourses` (`QuotesList`, fatiado em 2 por razão de mudar — só o primeiro sabe de mutation, D4), `useCourseRedatores` (`catalog/CourseDialog`, preserva os 3 estados loading/erro/lista da D11 do bloco de cards), `useStaffRoleOptions` (`identity/StaffUserDialog`, o filtro `!== 'redator'` da RN-01 viaja com a query), `useStudentClients` (`identity/StudentDialog`, preserva o `enabled: mode === 'create'` e a distinção erro-de-GET vs. lista-vazia). **A decisão mais importante foi o que os hooks NÃO passaram a fazer** (D3): `useQuoteCourseSearch` e `useQuotesListCourses` não expõem `isError`, porque distinguir GET falho de catálogo vazio muda o que a tela afirma — é o B-7, débito registrado, fora do corte; API morta em hook novo é órfão parcial e mente sobre o que a tela trata. **Metade 2 — estrutura de `commercial` (B-1 a B-6).** `EMPTY_ADDRESS` deixou de existir em dois lugares (`useClientForm` devolve `addr` resolvido); `ClientGeneralFields` + `ContactCard` tiraram o `ClientDialog` de 199 para 132 linhas; `QuoteRow`, `CourseStep`/`DataStep` e `BudgetDocumentsCard` tiraram bloco coeso de dentro de `.map`/ternário — todos componentes **locais** da feature (vocabulário de domínio), nada subiu para `shared/` (D5, diff vazio). **Mecanismo (o ponto do bloco):** o array `ignores` saiu inteiro do `eslint.config.js` — a regra vale sem exceção. Provado nos dois sentidos (lição 10) durante a execução, no review e de novo no fechamento: sonda com `clientsApi.useList()` + `useQuery` reprova as duas linhas, e `useMutationErrors` no mesmo arquivo **não** dispara, o falso positivo que o `$` da regex protege. `.claude/rules/frontend-fsliced.md` registra a catraca como zerada, não como lista a repovoar. **Review de baixo risco** (zero `backend/`/`generated.ts`/locales/auth/RBAC/schema/dinheiro no diff, `executor: claude`; só lente Claude, sem Codex): órfãos zero (7 hooks + 6 componentes, todos com consumidor, nenhum campo de retorno sem leitor), leis §5 limpas, as 6 extrações conferidas linha a linha como movimento literal e as 9 invariantes de comportamento da spec verificadas uma a uma. **2 achados 🟢, aprovados e corrigidos** (`5e74a28`): Q-1 — `catalog/api/useCourseRedatores.ts` exportava `useSyncCourseRedatores` e passou a colidir de basename com o hook novo em `catalog/hooks/`, renomeado para `api/useSyncCourseRedatores.ts` (a colocação já estava certa pela rule; o nome é que não dizia o conteúdo); Q-2 — `enabledRedatores` era re-alias puro no `CourseDialog`, resíduo da extração. Gate: suíte backend **372 passed (1360 assertions)** como regressão, `pnpm build`+`pnpm lint` verdes, Pint n/a (zero arquivo de backend), `generated.ts` e locales sem diff, greps de query-em-componente, `ignores`, `primereact` direto e cross-feature todos sem saída. Prova visual do João em 2 checkpoints por feature, não por metade (D7 — provar `commercial` antes de a metade 2 reescrever o mesmo markup invalidaria a aprovação, que foi o que aconteceu no bloco do redator), sem baseline capturada (D8, mesma limitação de browser dos blocos anteriores); a correção pós-review é substituição de identificador pelo próprio valor, JSX idêntico por construção, então o CP-2 não precisou ser refeito. | — | ``plans/archive/2026-08-03-zerar-catraca-e-componentes-commercial.md`` | ``specs/archive/2026-08-03-zerar-catraca-e-componentes-commercial-design.md`` |
| 2026-08-02 | Frontend · Abstração de componentes de `operation` | Entregue | Bloco 100% frontend (D1, `git diff main...HEAD -- backend/` vazio), branch no main tree sem worktree (D2 — toca `shared/hooks/useTableFilter.ts` e o DoD se prova na tela contra o compose do main tree). Fecha os 6 achados do `/revisar-frontend` de `features/operation` (3 C + 3 B), zero chave i18n nova (D10). `useTableFilter` ganha `searchable` **opcional** em vez do `() => []` que o backlog propunha (D4): os 7 consumidores antigos não mudam uma linha — provado por diff vazio nas 3 features consumidoras — e o `EnrollmentTable` vira o 8º, apagando estado de `first`, clamp e `onPage` que estavam copiados literalmente (C-2). Query do curso desce de `TurmaConfigCard` para `useTurmaConfigForm`, que passa a expor `workloadHours` (C-1); `useImportStudentsFlow` novo absorve mutation/`result`/`sizeError`/`close` do `ImportDialog` no molde do `useEnrollStudentFlow` (C-3); o ternário de 4 níveis do picker de redator vira `PickerBody` com guardas sequenciais erro > loading > vazio > lista (B-1); `useTurmaManualOpener` absorve mutation do blob, refs de objectURL/aba e cleanup de unmount do `ManualButton` (B-2); o handler de upload de 13 linhas do `DocumentTypeCard` sobe para `handleUpload` acima do `return`, **sem** hook — estado local que não cruza componente (B-3, D8). **O achado do review não era defeito deste diff, era reincidência medida:** "query dentro de componente de feature" custou um bloco de refactor em duas sprints seguidas (Q-4 do `abstracao-componentes-redator`/`RedatorCourseSelector`, C-1 deste/`TurmaConfigCard`) e sobrevivia em 7 pontos de `catalog`/`commercial`/`identity` — a regra existia, mas era parágrafo, e o grep do DoD era por-pasta, então só provava a feature recém-limpa (lição 14). Virou mecanismo: `no-restricted-syntax` em `eslint.config.js` sobre `src/features/*/components/**`, provado nos dois sentidos (lição 10) — dispara nos violadores reais (`QuoteWizard.tsx:20`, `QuotesList.tsx:23`, des-ignorados temporariamente) e **não** dispara no `useMutationErrors` do mesmo arquivo, o falso positivo que o `\b` do grep protegia e aqui o `$` da regex protege. Entrou com **catraca de débito**: os 7 legados em `ignores`, lista que só encolhe e não recebe arquivo novo — esvaziá-la é bloco próprio no `backlog.md`. Review de baixo risco (sem schema/auth/RBAC/`generated.ts`/dinheiro), só lente Claude; a única condicional que mudou de forma (`course ? …` → `f.workloadHours != null ? …`) foi conferida nos 4 casos, com `workload_hours: 0` seguindo "0 horas". Gate: suíte backend **372 passed (1360 assertions)** como regressão, `pnpm build`+`pnpm lint` verdes, `generated.ts` sem diff, greps da lei §5.6 e de query-em-componente sem saída, Pint n/a (zero arquivo de backend). Prova visual do João em 2 checkpoints (D11), sem baseline capturada — a sessão não tinha ferramenta de browser, mesma limitação do bloco anterior; a correção do Q-1 só tocou `eslint.config.js`/rules/docs, então não invalidou o CP-2. | `plans/archive/2026-08-02-abstracao-componentes-operation.md` · `specs/archive/2026-08-02-abstracao-componentes-operation-design.md` |
| 2026-08-02 | Frontend · Abstração de componentes do Redator | Entregue | Bloco 100% frontend, zero arquivo de `backend/` tocado (D1), branch no main tree sem worktree (D12 — o DoD se prova na tela e o compose aponta para o main tree). `RedatorDialog.tsx` cai de 448 para 189 linhas: `RedatorIdentityFields`, `RedatorCourseSelector` e `RedatorDocumentSlot` são subcomponentes **locais** de `identity`, não `shared/ui` (têm vocabulário de domínio). Para `shared/` sobem só `AppFileActions` (olho → baixar → `children` → lixeira) e `useFilePreview`, adotados também por `operation/DocumentTypeCard` e `commercial/FileList`; `IDONEIDADE_SEVERITY`/`DOC_STATUS_SEVERITY` e `DOC_TYPES`/`DocType` vão para `shared/lib/redatorStatus.ts`, matando as 3 cópias divergentes do mapa de cor que o comentário do `RedatorCard` já pedia para não multiplicar. **A decisão que moldou o bloco foi não fazer o que o review pedia:** o `/revisar-frontend` propôs promover `AppFileList`/`AppDocumentSlot` a `shared/ui` sem ter lido o `D8` da spec de upload de 2026-07-31, que já tinha avaliado e rejeitado exatamente isso — o contrato confirmou a previsão dele (~14 props, ~6 só para um consumidor diferir do outro), então o `D8` foi honrado e só a linha de ações virou compartilhada. **D11 (baseline de screenshots antes do corte) NÃO foi executada** — a sessão não tinha ferramenta de browser; risco aceito explicitamente pelo João, não escolhido por heurística, o que deixou a prova visual sem referência anterior. **Review (baixo risco — sem schema/auth/RBAC/`generated.ts`, só Claude): 4 achados, todos aprovados e corrigidos.** Q-1 e Q-2 são a mesma lição: refatorar é fácil de fazer pela metade. Q-1 — `canRemove: boolean` + `onRemoveDoc` eram par redundante e a asserção `redator!.id!` tinha migrado para fora da guarda que a protegia; se `canRemove` virasse permissão, o DELETE de documento sairia com `undefined` na URL, falha silenciosa em caminho de peso legal. `canRemove` morreu e a ausência do callback passa a desligar a lixeira — o mesmo contrato que o próprio bloco tinha acabado de estabelecer no `AppFileActions.onRemove` e contrariado no slot. Q-2 — o `RedatorDocumentSlot` tinha recortado o emaranhado para outro arquivo sem cumprir a D5: três `mode === 'x' && (doc ? A : B)` irmãos dentro de um `return`, com o bloco "não carregado + upload" duplicado entre `create` e `edit`; virou `SlotBody` com guardas sequenciais + `EmptySlot`. Q-3 — o `AppFileActions` fixava `aria-label={t('common.delete')}` e apagava o rótulo próprio de `operation`, mudança de comportamento fora do §7 da spec e contra a direção da D10; prop `removeLabel?` com default. Q-4 — `RedatorCourseSelector` carregava query + derivação, extraídas para `useRedatorCourses`. Gate: suíte backend 372 passed (1360 assertions) como regressão, `pnpm build`+`pnpm lint` verdes, `git diff main...HEAD -- backend/` vazio, `generated.ts` sem diff, greps da lei §5.6 sem saída, i18n sem chave nova. Prova visual do João aceita — **duas vezes**, porque as correções de Q-1/Q-2 reescreveram o markup do slot depois da primeira aprovação, e fechar sobre a lembrança da tela anterior seria assinar o item 0 do gate em falso. P-25 aberta (a D2 pedia `useFilePreview<T extends PreviewableFile>`; o implementado soltou o constraint para `shared/hooks` não importar tipo de `shared/ui` — o código está melhor que a spec). | `plans/archive/2026-08-02-abstracao-componentes-redator.md` · `specs/archive/2026-08-02-abstracao-componentes-redator-design.md` |
| 2026-08-01 | Hardening · Débitos de integridade | Entregue | Backend-only, main tree (P-03). Fecha 6 débitos onde o estado gravado podia divergir do que o sistema afirmava ter gravado: `UploadFileAction` quebrado em `put`/`register`/`discard`, upload sai da transação nos 4 chamadores de documento de redator (create/update, D3/D3b) com `discard` no rollback; `UserPhotoService::store()` entra em transação (P-24 fecha); `DeleteClientContactAction` ganha `lockForUpdate` na contagem (Q-5 fecha, serialização provada à mão em MySQL com 2 sessões); `is_primary` vira `Optional` em `ClientContactData`/`ClientAddressData` (PUT parcial para de rebaixar principal em silêncio); `PrimaryAddressService` novo espelha o de contatos; `PermissionI18nParityTest` guarda a paridade permissão↔i18n nas 3 locales, visto reprovando antes de valer como prova. **Review de alto risco** (`generated.ts` mudou) em 2 rodadas: a 1ª (Claude + segunda lente Codex) achou Q-1 — a spec assumia "endereço não tem rota nested", premissa falsa: `ClientAddressController` (`5bc1d87`, anterior ao bloco) já escrevia direto no Eloquent ignorando `PrimaryAddressService`, mesma classe de bug que o bloco existe para fechar — e Q-2, `UploadFileAction::discard()` engolindo `false` de `delete()` sem logar. Corrigidos em `ca02c9b` (`CreateClientAddressAction`/`UpdateClientAddressAction` novos espelhando os de contato, `winner` no `PrimaryAddressService::ensureSingle()`), com os 6 testes novos vistos reprovando contra o código antigo antes do fix (stash dos 3 arquivos de produção, lição 10). A 2ª rodada (Codex sobre `ca02c9b`) achou só Q-16 — `ensureSingle()` sem lock no `Client` permite dois principais sob escrita concorrente, mesmo padrão pré-existente em `PrimaryContactService`, não regressão deste commit — registrado no backlog, não bloqueou. DoD provado contra API real com sessão Sanctum: dois endereços `is_primary=true` no create deixam só um; POST na rota nested com principal já existente idem (prova ao vivo do fix de Q-1); PUT de contato sem `is_primary` mantém o principal; DELETE do contato único → `422`. Suíte 372 passed (1360 assertions), Pint limpo, `generated.ts` sem diff, `pnpm build`+`pnpm lint` verdes. P-24 sai de `pendencias.md`. | `plans/archive/2026-08-01-hardening-debitos-integridade.md` · `specs/archive/2026-08-01-hardening-debitos-integridade-design.md` |
| 2026-08-01 | Catálogo e Personas · Cards da relação Curso ↔ Redator | Entregue | Bloco 100% frontend, zero arquivo de `backend/` tocado (D1). Substitui as duas representações textuais mais pobres das telas — checkbox+nome em `RedatorDialog`, `<div>{r.name}</div>` em `CourseDialog` — por `AppSelectableCard` (novo primitivo em `shared/ui`) com conteúdo de feature: `RedatorCard` (avatar, RUT, tag de idoneidade) em `catalog`, `CourseCard` (carga horária contratada, contagem de módulos) em `identity`. `redatorStatus.ts` sobe de `features/identity/lib` para `shared/lib` (D2) — só assim `catalog` consegue pintar a idoneidade sem importar `identity` (lei §5.6). Botão-olho do `CourseDialog` não abre `RedatorDialog` direto (importaria outra feature); navega para `/personas?redator=<id>` e `PeoplePage` resolve o deep link com `openViewById` novo em `useCrudPage`, lendo o parâmetro pelo padrão "adjust state during render" da casa (nunca `useEffect` com `setState`) — primeiro caso concreto do FUT-2 registrado no backlog. Ordem dos cursos no `edit` do redator congela na abertura (`useEnabledFirstCourses`, D9): reordenar a cada toggle faria o card clicado saltar sob o ponteiro. Falha de GET nas duas seções deixa de se disfarçar de lista vazia (D11) — antes, `?? []` fazia um 403 por falta de `identity.user.view` virar "sem redatores habilitados" num curso que tem três; agora distingue loading/erro-com-Reintentar/vazio de verdade. Baixo risco (sem migration/RBAC novo/`generated.ts`) — revisão só Claude, sem segunda lente do Codex; zero achado. Suíte backend 347 passed (1083 assertions) como regressão, `pnpm build`+`pnpm lint` verdes, i18n paritário nas 3 locales, greps da lei §5.6 sem saída. Prova visual do João aceita nos 6 critérios comportamentais do DoD, dois temas, 1400px e 768px. | `plans/archive/2026-08-01-cards-relacao-curso-redator.md` · `specs/archive/2026-08-01-cards-relacao-curso-redator-design.md` |
| 2026-08-01 | Identidade visual · Foto/avatar das entidades + contatos do cliente | Entregue | Bloco único juntando os itens 1 e 2 do backlog por decisão do João. `photo_url` (`#[Computed]`) nos 4 contratos (User/Client/Redator/Student), `UserPhotoService` em `users.photo_path` FORA de `files` (foto não é documento — D3), 8 rotas nested, uma por módulo dono, para nunca acoplar RBAC cross-módulo (D1). `AppAvatar` com fallback duplo (D7 — URL pré-assinada expira, círculo vazio parece defeito), `AppPhotoField` + `useEntityPhoto` (buffer no create D10, `flush` nunca lança D11), avatar nas 4 tabelas e contatos do cliente em cards com exclusão (D12–D14). **Dois achados críticos apareceram só na prova visual do João, depois do DoD:** (1) `UploadedFile::store()` devolve `false` sem lançar — `photo_path` virava `'0'` no banco e o objeto ANTERIOR, que ainda funcionava, era apagado; 2 clientes reais de dev ficaram assim (`9197d08`); (2) não existe valor único de `AWS_ENDPOINT` que sirva para os dois lados — `minio:9000` escreve mas o navegador não resolve, `localhost:9000` lê mas a escrita falha em silêncio; resolvido assinando leitura contra um disco `{disco}_public` separado (`b6dc068`), no mesmo choke point que os documentos de redator/turma/orçamento já usavam. **Dois reviews de duas lentes cada** (Claude + `mcp__codex__codex` read-only): o primeiro sobre `4dfe3a9..b6dc068` (7 achados — Q1 **falso positivo**, medido: o resolver do spatie desvia do `CannotSetComputedValue` quando a propriedade é promovida no construtor; Q2 mínimo de 1 contato escapava pela rota nested `DELETE /api/contacts/{id}`; Q4 a rota de foto do staff aceitava User de qualquer tipo, driblando a permissão do módulo dono; Q7 aceito como débito → P-24), o segundo sobre a própria rodada de correção `34ab3c2` (4 achados aprovados: o gate de fechamento não cobria o botão **Salvar**, quarta saída do diálogo; o retry ressurgia no erro de TAMANHO reenviando o arquivo errado; o teste do Q5 provava menos do que o Q5 pedia; e o `closeBlocked` sem `timeout` no axios virava trava dura). **Lição 10 reapareceu dentro do fix da própria lição 10:** o teste novo de auditoria passou VAZIO na primeira prova — sem `photo_path` no diff, todos os valores viram `null` e `null === null` aprova tudo; só com `assertNotNull` nos caminhos ele foi visto reprovando. DoD e fixes provados contra API real com sessão Sanctum (contato único → `422` sem apagar; `/api/users/{userDeCliente}/photo` → `404`; foto → `204` e a URL pré-assinada devolvendo `200 image/png`); suíte 347 passed, `pnpm build`+`pnpm lint` verdes, Pint limpo, `generated.ts` regenerado sem diff. Prova visual do João aceita. | `plans/archive/2026-07-31-foto-avatar-e-contatos-cliente.md` · `specs/archive/2026-07-31-foto-avatar-e-contatos-cliente-design.md` · `context-packets/foto-avatar-e-contatos-cliente.md` |
| 2026-07-31 | Hardening · Upload e visualização de arquivos | Entregue | Erro reportado como CORS era `client_max_body_size` de 1 MB (default nginx) cortando antes do Laravel — nenhuma das 4 camadas concordava em teto. Nginx e PHP sobem para 12 MB de transporte (folga de multipart, D2); o `max:10240` (10 MB) dos 5 controllers, já existente, passa a ser sempre quem rejeita, com envelope RFC 7807 e header CORS. `TurmaDocumentData` e `RedatorDocumentData` sobem ao núcleo comum de `FileData` (`mime`, `size`/`created_at`, `download_url`) — a turma ganhou download, que não tinha. `shared/ui` ganha `AppFileRow` + `AppFilePreviewDialog` (imagem/PDF inline, fallback explícito nos demais tipos — D9), adotados pelos 4 consumidores (orçamento, cotação, turma, redator); `AppFileUpload` barra arquivo acima do teto antes da requisição (D4, o modo básico do Prime não mostra a validação dele). Execução em 2 partes: Tasks 1–4 (infra+backend) pelo Codex, Tasks 5–10 (frontend) por Claude. Um achado de review virou decisão do João: a Task 9 só colara um botão de preview em vez de adotar `AppFileRow`, divergindo de D8 — corrigido por completo. **Review de alto risco** (Codex tocou infra+backend): segunda lente via `mcp__codex__codex` (read-only) sobre o intervalo `dfadb0c..f271c12` encontrou 1 achado real — `UploadSizeLimitTest` só cobria cotação e orçamento, sem regressão do teto nos outros 3 endpoints (redator, turma, import de matrícula/D11); corrigido com 3 casos novos (`faf7c78`). DoD provado contra API real com sessão Sanctum: 5 MB → `201`, 11 MB → `422 application/problem+json` com `detail` de tamanho (nunca `413` opaco), `GET /api/turmas/1/documents` e `GET /api/redatores/1` expondo os campos novos; suíte 321 passed, `pnpm build`+`pnpm lint` verdes. Prova visual do João (preview de imagem/PDF, fallback `.docx`, upload de 3 MB) aprovada nos 4 consumidores. | `plans/archive/2026-07-31-hardening-upload-visualizacao-arquivos.md` · `specs/archive/2026-07-31-hardening-upload-visualizacao-arquivos-design.md` · `context-packets/hardening-upload-visualizacao-arquivos.md` |
| 2026-07-30 | Hardening · Sincronização de documentação (H.1.3.1) | Entregue | Auditoria código↔`/docs`↔Drive↔Notion nos 4 eixos (E1–E4): 16 achados consolidados, 7 correções de texto diretas + 2 writes reais no Notion (releitura confirmada) + 4 patches de Drive entregues (aplicação manual pendente — P-01/P-14/P-17) + 5 decisões ratificadas pelo João (D7→P-14, D3→P-19, D10→P-15, ADR-16 e revisão do ADR-15→`docs/adrs.md`). P-06 fechada com schema real (`turma_redator` N:N). **Achado crítico do próprio João**: a varredura E3 (Notion) inicial usou uma base "Tasks · Lotus Fase 2" obsoleta/duplicada — corrigida em `ab2b17f`, caiu de 15 divergências falsas para 1 real (EAP 7.4.2). Re-auditoria (Task 14) rodou 4 vezes até zero achado fora de `pendencias.md`: 5+2+1 divergências pré-existentes corrigidas direto (seeders, `Console/`, `Support/`, front `Admin`/`Student`, `tsconfig.app.json`, `TypeScriptTransformerServiceProvider.php`, `budgets.code`), 2 novas pendências abertas (P-20 `openspout`, P-21 `simple-qrcode`, ambas libs sem uso/ADR). Todos os 7 critérios do DoD da spec verificados. Bloco só tocou `/docs`, `.claude/rules/`, `AGENTS.md`, `.agents/skills/` — sem código, sem suíte/Pint a rodar. **A revisão e o gate acharam o que o bloco não achou:** o `/revisar-sprint` devolveu 6 achados (Q-1 a Q-6, todos aprovados e fechados) — o maior foi **H.1.3.1 duplicada dentro da base Notion canônica**, com o write caindo numa cópia e a outra seguindo vazia (P-22); Q-2 virou regra ("fonte externa se referencia por ID, nunca por nome de exibição") em `AGENTS.md` §3, na SKILL do packet e em `auditar-docs`, porque a lição dos 12 falsos positivos tinha ficado só no relatório. Depois, a re-auditoria do `/fechar-sprint` devolveu **14 divergências** contra as 0 da 4ª rodada do próprio bloco — 12 fatos corrigidos no fechamento (contagem de tabelas 25→26, "um redator por turma" nos ADRs, `RouteServiceProvider` na regra acionável, `.gitkeep` inexistente, `5 de 25`→`4 de 34` wrappers, ponteiros mortos para "backlog do progress.md", path do Pint, `createbuckets`, 4 cabeçalhos com data defasada, `app/Http/Controllers` fora da árvore) e P-23 aberta. **Lição do bloco:** "re-auditoria devolveu zero" é evidência sobre uma execução do subagente, não propriedade dos docs — o DoD que se satisfaz com uma rodada limpa mede o auditor, não a documentação. | `plans/archive/2026-07-30-hardening-doc-sync-sprint4.md` · `specs/archive/2026-07-30-hardening-doc-sync-sprint4-design.md` · `context-packets/hardening-doc-sync-sprint4.md` · `audits/2026-07-30-doc-sync-sprint4.md` |
| 2026-07-27 | Bloco Pessoas · Alunos (módulo novo) | Entregue | 10 tasks — backend pelo Codex (Tasks 1–5), frontend por Claude (Tasks 6–10). Nasce o CRUD de aluno que faltava: `index`/`show`/`store`/`update` sob `identity.user.*` (D8), `StudentData` + `StudentDetailData` com vínculos e histórico de turmas, e a aba `Alumnos` de `PeoplePage` deixa de ser um `<p>` inline. Cliente obrigatório na criação, edição não toca vínculo (D3); sem `destroy` (D1). Backend bloqueou uma vez por `docker.sock` no sandbox do Codex (`0cfd369`), resolvido com sessão nova em `danger-full-access`. Seis fixes pós stop-gate review, todos sobre o mesmo ponto: o dropdown de empresa do create depende de `commercial.client.view`, permissão de outro módulo — duas tentativas de contornar na UI foram revertidas por serem piores que o problema, e o estado final torna a falha **visível** (dropdown desabilitado + motivo + "Reintentar") em vez de escondida; o alinhamento de RBAC de verdade foi para o backlog. Um achado transbordou o bloco: `useLogout()` nunca limpava o `QueryClient`, então cache de qualquer recurso atravessava a fronteira de autorização entre dois usuários na mesma aba. **Q-1 do review virou regra**: `belongsTo` para model soft-deletable atravessado por `fromModel` estourava `Attempt to read property on null` — era padrão em 7 DTOs de 4 domínios, corrigido com `->withTrashed()` em 12 relações, institucionalizado em `.claude/rules/backend-ddd.md` + lições 16/17 e guardado por `SoftDeletedRelationProjectionTest` (9 casos). 313 testes verdes; DoD provado contra a API real com Sanctum. | `context-packets/bloco-alunos-modulo.md` | `plans/archive/2026-07-27-bloco-alunos-modulo.md` | `specs/archive/2026-07-27-bloco-alunos-modulo-design.md` |
| 2026-07-26 | Bloco 6-frontend · Seed operacional | Entregue | `OperationDemoSeeder` (gate local/demo + aborta se já há clientes) monta o cenário pelas Actions reais: 4 clientes, 7 redatores com REUF válido, 3 cursos, 6 orçamentos / 8 cotações nos 3 estados, 4 turmas (3 em andamento, 1 concluída, 1 habilitada derivada, 1 sem redator), 45 matrículas e 17 arquivos no MinIO. 17 checks de orfandade em zero; suíte 281 passed. Os 3 RUTs do protótipo Figma tinham DV inválido — corrigidos via `Rut` do projeto. Achados abertos: P-13 (turma não tem código próprio; protótipo mostra `TR-45`) e débito de arquivo órfão no MinIO em rollback (pré-existente, revisão do Codex). | — | — (sem plano separado — gate inline) | `specs/archive/2026-07-21-bloco6-frontend-operacao-design.md` §7 |
| 2026-07-23 | Integração Codex · Fases 3–8 | Entregue | Roteamento por estado, handoff de execução, revisão por risco e piloto de packet (`partial`). | — | `plans/archive/2026-07-23-integracao-codex-fases-3-8.md` | — |
| 2026-07-27 | Bloco visual · Refinamento de UI por módulo | Entregue | 39 tasks em 4 partes, cada uma com review próprio e prova visual do João. Camada `shared/ui` nova (`AppCard`+`tone`, `AppDataTable` com densidade/hover/paginador unificado, `AppEmptyState`, `AppErrorState`, `AppSkeleton`, `DetailHeader`, `FormSection`, `AppTag` semântico) e as 7 telas de módulo + 2 de detalhe migradas. A ação primária saiu do `PageHeader` para o cabeçalho do container mais próximo (D1). Achado que dominou a Parte 4: erro de listagem sumia em silêncio — `useCrudPage` só expunha `loading`, então GET falho rendia empty state convidando a cadastrar sobre falha de rede; D16 fechou isso nas 10+ telas. Responsividade a 768px por scroll horizontal, não coluna colapsável (D20 — esconder coluna em tela de auditoria é perda silenciosa). **P-11 encerrada** (zero `window.confirm`); P-13 mantida aberta com decisão D8. Q-6 virou regra em `.claude/rules/frontend-fsliced.md` (tabela em card = `useTableFilter` + `AppCard{Toolbar,Footer}`), não só refactor. DoD comportamental provado na tela pelo João nos dois temas, 1400px e 768px, com API derrubada nas 7 telas. | `plans/archive/2026-07-26-bloco-visual-refino-ui.md` · `specs/archive/2026-07-26-bloco-visual-refino-ui-design.md` · `context-packets/bloco-visual-refino-ui.md` |
| 2026-07-23 | Sprint 3 · Operação frontend | Entregue | Execuções 1–3: Turmas, Alumnos, Documentación e Conclusión na UI; manual PDF abre no gesto do clique; P-07 e P-12 encerradas. Gate e2e contra API real: `GET /api/turmas/1/documents` 200, `GET /api/turmas/1/manual` 200 `application/pdf`, `POST /api/turmas/1/conclude` 422 terminal. | — | `plans/archive/2026-07-21-bloco6-frontend-exec1-turmas.md` · `plans/archive/2026-07-22-bloco6-frontend-exec2-alumnos.md` · `plans/archive/2026-07-23-bloco6-frontend-exec3.md` | `specs/archive/2026-07-21-bloco6-frontend-operacao-design.md` |
| 2026-07-21 | Bloco 6b · Turma + redator | Entregue | Turma por cotação, designação N:N e gate RN-09 validados. | — | `plans/archive/2026-07-21-bloco6b-turma-designacao.md` | `specs/archive/2026-07-21-bloco6b-turma-designacao-design.md` |
| 2026-07-21 | Bloco 6d · Conclusão + manual | Entregue | Habilitação derivada, conclusão terminal e manual via Gotenberg. | — | `plans/archive/2026-07-21-bloco6d-conclusao-manual.md` | `specs/archive/2026-07-21-bloco6d-conclusao-manual-design.md` |
| 2026-07-21 | Bloco 6c · Matrícula + importação | Entregue | Matrícula idempotente e import tolerante a erros por linha. | — | `plans/archive/2026-07-21-bloco6c-matricula-import.md` | `specs/archive/2026-07-21-bloco6c-matricula-import-design.md` |
| 2026-07-04 | Login + store de sessão | Entregue | Auth SPA Sanctum, guard de rota, sessão em Zustand hidratada no boot | — | `plans/archive/2026-07-04-login-flow-session.md` | `specs/archive/2026-07-04-login-flow-session-design.md` |
| 2026-07-06 | App Shell + Sidebar RBAC | Entregue | Shell (sidebar+header), nav filtrada por RBAC, dark mode layout-only em uiStore | — | `plans/archive/2026-07-06-app-shell-sidebar.md` | — |
| 2026-07-07 | Sprint 1 · Cadastros backend (Cliente + Redator) | Entregue | Schema, models, DTOs, validação RUT, actions, controllers; provado em sqlite `:memory:` | — | `plans/archive/2026-07-07-sprint1-cadastros-backend.md` | `specs/archive/2026-07-07-sprint1-cadastros-backend-design.md` |
| 2026-07-08 | Sprint 1 · Cursos backend | Entregue | `courses` + `course_certificate_templates` + `course_redator` (N:N habilitação) | — | `plans/archive/2026-07-08-sprint1-cadastros-cursos-backend.md` | — |
| 2026-07-09 | Frontend · Fundação da UI | Entregue | Molde reutilizável: ModulePage, CrudDialog, useCrudPage, useEntityForm, i18n, tema dark | — | `plans/archive/2026-07-09-frontend-fundacao-ui.md` | — |
| 2026-07-09 | Sprint 1 · Cadastros frontend + docs tipados | Entregue | Telas Cliente/Redator sobre hooks CRUD; documentos tipados do redator no backend | — | `plans/archive/2026-07-09-sprint1-cadastros-frontend.md` | `specs/archive/2026-07-09-sprint1-cadastros-frontend-design.md` |
| 2026-07-09 | Sprint 1 · Correções code-review | Entregue | 7 defeitos do review corrigidos; build verde, comportamento provado por teste | — | `plans/archive/2026-07-09-sprint1-correcoes-code-review.md` | — |
| 2026-07-10 | Sprint 1 · Catálogo frontend | Entregue | `/cursos`: listagem + form com habilitação de redatores (fecha Sprint 1) | — | `plans/archive/2026-07-10-sprint1-catalog-frontend.md` | `specs/archive/2026-07-10-sprint1-catalog-frontend-design.md` |
| 2026-07-10 | Sprint 2 · Comercial backend | Entregue | Budget agrupa Quotes independentes; status/totais derivados (bcmath); aprovação superadmin; anexos polimórficos. ADR-17 (seq atômica) nasceu aqui | — | `plans/archive/2026-07-10-sprint2-commercial-backend.md` | `specs/archive/2026-07-10-sprint2-commercial-backend-design.md` |
| 2026-07-13 | Sprint 2 · Comercial frontend | Entregue | Lista de orçamentos, detalhe com cotações, wizard de cotação (passo em useState), aprovação/recusa, anexos | — | `plans/archive/2026-07-13-sprint2-commercial-frontend.md` | `specs/archive/2026-07-13-sprint2-commercial-frontend-design.md` |
| 2026-07-14 | Bloco 0 · Sync de docs pós-Sprint 2 | Entregue | Docs voltam a bater com o código: `budgets` sem status/total (derivados no `BudgetSummaryService`), `quotes` sem `client_id`; ADR-17 fechado | — | — (docs-only) | — |
| 2026-07-16 | Bloco 1 · Refino de código Sprint 2 | Entregue | Kit de form em `shared/ui/FormField/` mata a duplicação dos 6 diálogos; `Delete{Budget,Quote}Action` tiram a guarda de peso legal do controller e ganham teste | — | `plans/archive/2026-07-16-bloco1-refino-sprint2.md` | `specs/archive/2026-07-16-bloco1-refino-sprint2-design.md` |
| 2026-07-16 | Bloco 2 · CR Cliente (cargo, principal único, complemento) | Entregue | `client_contacts.job_title`; `PrimaryContactService::ensureSingle()` fecha "no máximo 1 principal" nos 2 caminhos de escrita; `AppRadioButton` em `shared/ui` | — | `plans/archive/2026-07-16-bloco2-cr-cliente.md` | `specs/archive/2026-07-16-bloco2-cr-cliente-design.md` |
| 2026-07-17 | Bloco 3 · CR Curso: `course_modules` 1:N (backend) | Entregue | Quadro de módulos da proposta vira entidade 1:N de `courses`; `sort_order` derivado do índice do array e totais derivados em runtime — nada persistido | — | `plans/archive/2026-07-16-bloco3-course-modules-backend.md` | `specs/archive/2026-07-16-bloco3-course-modules-backend-design.md` |
| 2026-07-17 | Bloco 4 · CR Curso: AppTextarea + módulos reordenáveis (frontend) | Entregue | Tela de módulos com reordenação, totais derivados no render e aviso âmbar de divergência de carga (nunca bloqueia); `AppTextarea` em `shared/ui` | — | `plans/archive/2026-07-17-bloco4-course-modules-frontend.md` (+ roteiro) | `specs/archive/2026-07-17-bloco4-course-modules-frontend-design.md` |
| 2026-07-17 | Bloco 5.0 · Nested `Optional` | Entregue | Campo ausente preserva coleção; `[]` apaga explicitamente. | — | — | — |
| 2026-07-17 | Bloco 5.1 · ADR-19 + docs | Entregue | Dinheiro, i18n e paths de DTO sincronizados com a implementação. | — | — | — |
| 2026-07-17 | Bloco 5.2a · Usuarios | Entregue | CRUD de staff, roles e proteção do último superadmin. | — | `plans/archive/2026-07-17-bloco5.2a-usuarios.md` | `specs/archive/2026-07-17-bloco5.2a-usuarios-design.md` |
| 2026-07-18 | Bloco 5.2b · Roles y Permisos | Entregue | Roles customizadas e segregação de permissões implementadas. | — | `plans/archive/2026-07-18-bloco5.2b-roles-permisos.md` | `specs/archive/2026-07-18-bloco5.2b-roles-permisos-design.md` |
| 2026-07-19 | Bloco 5.3 · Refino Comercial | Entregue | Forms compartilhados, detalhe declarativo e date picker ISO. | — | `plans/archive/2026-07-19-refino-comercial-frontend.md` | `specs/archive/2026-07-19-refino-comercial-frontend-design.md` |
| 2026-07-20 | Bloco 5.4 · Refino Administração | Entregue | Erros 422, toolbar e descrições i18n de permissões corrigidos. | `plans/archive/2026-07-20-refino-administracao-frontend.md` · `specs/archive/2026-07-20-refino-administracao-frontend-design.md` |
| 2026-07-20 | Bloco 6a · Aluno + vínculo | Entregue | Resolução por RUT e vínculo histórico único validados em MySQL. | — | `plans/archive/2026-07-20-bloco6a-aluno-vinculo.md` | `specs/archive/2026-07-20-bloco6a-aluno-vinculo-design.md` |
| 2026-08-12 | Identity · histórico de logins e coluna de último acesso (BD-7) | Entregue | **BD-7** do backlog, promovido explicitamente pelo João em 2026-08-12 **fora da ordem escrita dos BDs** — o item dizia "coluna `last_login`" e o bloco **não** entregou coluna nenhuma. Backend mais frontend no main tree (P-03: `estilizacao-adr16-shell-tipografia` corria em paralelo na worktree, mas é frontend, então o gatilho de dois blocos de **backend** não venceu), branch `feat/last-login`. **O desenho mudou no meio, por duas perguntas do João lendo a spec:** (1) *"não existe nada nativo do Laravel, tipo a tabela `sessions`?"* — medido: Laravel não tem `last_login` nativo, e a `sessions` deste repo não fecha o requisito (`last_activity` é reescrito a cada request, `session()->invalidate()` no logout **apaga a própria evidência**, o dado expira em `SESSION_LIFETIME` e morre inteiro se o driver virar `redis`/`file` — feature de negócio pendurada em config de infra); (2) ele quer **histórico**, em tabela própria. Daí `login_logs` append-only (só logins **bem-sucedidos**; tentativa falha e logout recusados com razão escrita), com o "último acesso" **derivado** por `User::latestLogin()` — `users.last_login` não existe. **A revisão simplificou metade do bloco e complicou a outra:** como nunca mais se escreve em `users` no login, morreram juntos a armadilha do `$auditInclude` (atributo fora da lista gera audit com `old_values`/`new_values` vazios — uma linha inútil **por login, para sempre**, numa tabela de peso legal), o `saveQuietly` e o `timestamps = false`; em troca a leitura virou travessia de relação, que é onde este repo tem cicatriz. **A escolha do mecanismo foi por MODO DE FALHA, não por custo:** `withMax()` é mais barato (subselect, zero query extra) mas **falha em silêncio** — controller que esqueça a carga projeta `null` e a tela diz "nunca acessou" para todos; `hasOne(...)->latestOfMany(['created_at','id'])` custa uma query e **falha alto**, estourando no `preventLazyLoading()`. Ficou `latestOfMany`, com guarda de runtime própria (Task 4) — o N+1 do seam do B4 (2026-08-08, quatro listagens) é o precedente. **A escrita do plano achou dois defeitos no próprio rascunho, corrigidos antes de gravar:** `latestOfMany()` ordena por `id` e não por `created_at` (conferido no vendor), e `MAX` numa coluna só devolve **duas** linhas quando dois logins caem no mesmo segundo — daí a forma com array; e o teste da projeção seria **falso-positivo por mass assignment**, porque `created_at` não está no `$fillable` e o backdate seria descartado em silêncio, fazendo as duas linhas nascerem com a mesma data (virou `forceFill(...)->save()`; o `$fillable` **segue** sem `created_at` de propósito — a data do acesso não se forja). Execução em **SDD** por instrução do João a meio da Task 2: seis tasks, cada uma com implementador isolado e revisor dedicado, todas Approved; a captura vive numa `RecordLoginAction` chamada **depois** do gate de `is_active` (a ordem é o ponto — `attempt()` bem-sucedido de usuário inativo é login que a API recusa com 422), com mutation-proof registrado (`Failed asserting that 1 is identical to 0.`). **Review ALTO RISCO em duas lentes** (Claude + Codex read-only), com os três gatilhos aplicáveis — auth, schema e `generated.ts`: **3 achados, todos aprovados pelo João e corrigidos** em `ff1f304`, órfãos zero, **três sub-afirmações do Codex recusadas com razão escrita**. **Q-1 🟡 é o que nenhum gate anterior podia pegar:** a D3 recusou o listener do evento `Login` afirmando que "o caminho de cookie remember me está morto", mas a medição que sustentava isso varreu o **frontend**, não a superfície da API — `AuthController` seguia passando `$request->boolean('remember')` ao `attempt()`, e com o recaller no request o `SessionGuard` **reconstrói a sessão sem passar pelo controller**, então aquele acesso não geraria linha e a coluna envelheceria numa conta em uso diário. Fechado apagando o argumento (uma linha), o que torna a justificativa da D3 verdadeira em vez de aproximada; **RN-01 foi verificada e não é ferida** (o `logout()` do gate faz `unqueue` do recaller incondicionalmente). Q-2 🟡: o caso de usuários do teste de N+1 afirmava só `assertOk()` e pararia de discriminar se a listagem caísse para ≤1 linha (`Builder::hydrate()` só marca a instância com `count($items) > 1`) — virou `assertJsonCount(3)`. Q-3 🟢: `user_id` sai do `$fillable` do `LoginLog`, pelo mesmo argumento que já mantinha `created_at` fora. **Um erro de método dentro da própria correção, registrado sem maquiagem:** a primeira versão do teste do Q-1 afirmava `assertNull($user->fresh()->remember_token)` e foi vista reprovar **pelo motivo errado** — a `UserFactory` já semeia o token, então a coluna fica idêntica nos dois estados do código; a asserção passou para o **cookie recaller**, que é o que `queueRecallerCookie()` de fato produz. **Gate de fechamento refeito contra a API real, não herdado** (as correções entraram depois do e2e de execução e mexeram no `attempt()`), com o banco de dev **intocado** (`LOT-2026-1001` segue corrompido para o checkpoint visual do João): login real → **200** e `login_logs` de 5 para 6 linhas com IP e user-agent reais, `users.updated_at` **inalterado** e `audits` **inalterado em 435** — a escrita silenciosa medida onde precisa valer; `GET /api/users` projetando `2026-08-12T17:06:12.000000Z`, byte a byte a `created_at` da linha nova; **a lacuna que o gate de execução declarou aberta foi fechada aqui** — o redator foi exercitado ponta-a-ponta (o seed o cria com `is_active=false` "até o fluxo de ativação", que não existe, então a senha e o flag foram mutados **temporária e reversivelmente** por `saveQuietly`, com restauração conferida hash a hash), e `GET /api/redatores` passou a mostrar `last_login` **só naquela linha**; segundo login levando `login_logs` a 8 e a projeção de `17:06:12` para `17:08:13`; e o **Q-1 provado na superfície onde ele vivia** — `"remember": true` **no corpo** devolve 200 com `XSRF-TOKEN` e `laravel-session` e **zero** cookie `remember_web_*`. Placar: backend **548 passed, 5 skipped (2025 assertions)**; frontend **17 arquivos / 86 testes**, `pnpm lint` e `pnpm build` verdes; Pint `passed` nos 12 `.php`; `typescript:transform` **sem diff**. **O que o gate NÃO provou, sem maquiagem:** nenhuma tela foi vista renderizada (WSL sem browser) — o **checkpoint visual das duas colunas fica com o João**; login falho e logout seguem fora de escopo (D2); `SessionUserData` não ganhou o campo de propósito (a captura precede a montagem do payload, então `/me` diria "último acesso = agora" — campo que mente por construção); sem backfill; alunos e clientes não entram porque não autenticam; e o gate de `is_active` em requisição **já autenticada** continua não existindo, anterior a este bloco. Nasce a **P-30**: `ip_address` e `user_agent` são dado pessoal e a tabela não tem política de retenção — atada à P-02, que já está aberta pela mesma razão para `audits`. | `plans/archive/2026-08-12-last-login.md` · `specs/archive/2026-08-12-last-login-design.md` · sem Context Packet (decisão do João de responder o formato ele mesmo no brainstorming, não ausência de fonte) · commits `b86b387`…`ff1f304` · ledger `.superpowers/sdd/progress.md` |
| 2026-08-12 | UI · faixa visível e acessibilidade dos diálogos (BD-3) | Entregue | **BD-3** do `backlog.md`, promovido explicitamente pelo João com o estado em `idle`. Frontend puro — `git diff main...HEAD -- backend/ frontend/src/shared/types/generated.ts` **vazio** —, em worktree por instrução dele (P-03 não dispara em bloco de frontend), o que devolveu a `main` à árvore principal. **A spec já existia antes da promoção** (`397548c`, literalmente `NAO promovido`) e foi **remedida contra a base nova** antes de executar: das 41+35+9 medições, só duas mudaram (o `last-login` inseriu coluna no `UsersTable`; a estilização baixou o `LoginPage` de 3 para 2 ocorrências) — a colisão temida com o PR #41 **não existiu**, porque os 47 arquivos daquele diff não incluem um único alvo deste bloco. **7 tasks de código, cada uma em commit próprio.** Task 4 fechou a faixa **desligando o `thead` sem linhas**: zerar a largura mínima não bastava, porque os seis `<th>` com `px-4 py-2.5` sustentavam a tabela sozinhos. Task 5 precisou de emenda porque o critério cru fazia o CTA sumir **durante a busca** — virou `!table.filtering && rows.length === 0`, já que busca sem resultado não é lista vazia. Task 7 concretizou o risco que o plano previa: as regras de lint novas nasceram em blocos `files` próprios casando o mesmo glob e **apagavam em silêncio** o `no-restricted-syntax` existente (Q-2 de 2026-08-04) — corrigido fundindo-as nos arrays já existentes. **Gate da Task 8 no navegador** (`/lotus-ui-review`, 4 páginas × 3 viewports, zero mutação): os seis itens passaram com sonda — foco de volta ao gatilho exato em **12/12**, `aria-label` do maximizar alternando, **zero** input de texto nos diálogos, wrapper com `scrollWidth == clientWidth` a 390, exatamente **1** CTA em 12/12, Q-14 com 1 clique = 1 GET e Q-15 lendo `Loading...` em vez de `0`. **O estado de erro veio de `Network.setBlockedURLs`, não de mock** — offline emulado não serve, porque o TanStack Query trata rede offline como `fetchStatus: "paused"` e nunca produz erro. **Review BAIXO RISCO, uma lente, 5 achados, os cinco corrigidos por decisão do João** — todos do foco declarado no plano (o que `shared/ui` faz na tela que o bloco não abriu). **Q-1 é o achado de método:** o débito foi medido por **string** e não por **forma** — o grep `disabled={readOnly}` achou 41 sítios, o seletor pela forma do defeito achou **17 a mais** (`disabled={f.readOnly}`, `disabled={readOnly || !isCreate}` e o par estático `<AppInputText disabled readOnly />`, este com **dado de peso legal** truncado em input cinza, 11 deles na certificação); 2ª vez que uma catraca deste `eslint.config.js` nasce enumerando em vez de medindo, e virou **parágrafo na rule** no fechamento. **Q-2:** o `onRetry` foi alargado nas 3 camadas de `shared/ui` e não nas **10** de feature — a promise passava por baixo do tipo `() => void` e `void x.refetch()` a apagava; o alargamento quebrou o build em 7 lugares e expôs que `ListableResource.refetch` mentia na raiz. **Q-3:** `loading` não é lista vazia — o `thead` sumia e voltava nas 14 tabelas e o CTA não existia em lugar nenhum até o GET responder. **Q-4/Q-5:** `COR_HARDCODED` passa a rodar em `src/shared/**` (nasceu verde) e as 19 cópias da fórmula do vermelho ganham dono em `shared/styles/tokens.ts`. **Gate de fechamento com `/lotus-ui-review` refeito, não herdado** — as correções mexeram no carregamento das 14 tabelas e em 5 diálogos que o gate da Task 8 nunca abriu: `/operacion` e `/certificados` nas três viewports, **zero mutação** (26 requisições, todas GET), árvore limpa antes e depois. `TurmaConfigCard` e `CertificateViewDialog` com `input,textarea` = **0** e o valor inteiro em texto; `thead` montado durante o GET com rodapé `Loading...` nas duas listas; **Q-14 provado num 500 REAL do servidor, sem mock** (o `LOT-2026-1001` corrompido de propósito por bloco anterior serviu de fixture): Reintentar `p-disabled p-button-loading` em **91/91** amostras a 4 ms, 1 clique = 1 GET, duplo clique = 1 GET; e o token `dangerText` medido invertendo com a folha (`srgb 0.76 0.244 0.237` no claro, `srgb 1 0.446 0.415` no escuro). Placar: frontend **28 arquivos / 138 testes**, `pnpm lint` limpo, `pnpm build` verde; backend **569 passed, 5 skipped (2092 assertions)** sem uma linha de diff; Pint N/A (zero `.php`); `generated.ts` intocado. **O que NÃO ficou provado, sem maquiagem:** lista genuinamente vazia (esvaziá-la exige mutação), `ConfirmIssueDialog` e `IssuedDialog` (o primeiro fica a um clique da emissão de documento de peso legal, o segundo só existe depois dela — ambos usam o mesmo `<FormField readOnly value={…}>` provado em três outros sítios), `BudgetDialog`, travessia por Tab e escuro fora de 1440x900. Foco, `aria-label`, largura, CTA e feedback de retry **seguem sem teste automatizado** — componente com PrimeReact no jsdom está fora do corte do runner, e o único mecanismo que sobrevive ao bloco é o lint, que só vê cor e `disabled`. **Seis achados B de UI pré-existentes** foram para `## Débitos técnicos` e a lacuna do shell na catraca de cor virou **P-34**. | Plano `plans/archive/2026-08-12-faixa-visivel-e-acessibilidade-dos-dialogos.md` · spec `specs/archive/2026-08-12-faixa-visivel-e-acessibilidade-dos-dialogos-design.md` · sem Context Packet (dispensado pelo João, ausência medida de fonte externa) · commits `0533303`…`dfc3f4b` |
| 2026-08-13 | UI · catraca do `max-lines` e adoção da moldura (BD-4) | Entregue | **BD-4** do `backlog.md`, promovido explicitamente pelo João em 2026-08-13 com o estado em `idle`; rota direta a `ready_for_planning` **sem Context Packet** por ausência medida de fonte externa, e worktree `fix-frontend` na branch `feat/catraca-max-lines-e-moldura` (bloco **frontend puro**, a P-03 não dispara). **O terreno foi medido antes de desenhar e derrubou cinco premissas do próprio item:** três dos quatro números da catraca estavam vencidos (`StudentDialog` **281**, não 283; `RedatorDialog` **206**, não 199; `BudgetDetailPage` **187**, não 171), o débito não nasceu do modo leitura do BD-3 (que fechou o `StudentDialog` com saldo **−2 linhas**; os dois blocos grandes vêm de `501b731`, 2026-08-05), a adoção da moldura **não** tira linha de diálogo nenhum, não existe regra de validação de `phone` em DTO algum de `Identity` (zero `Max(`) — o 422 é de **tipo**, não de formato —, e os dois diálogos do item (c) não são o mesmo caso (`useStudentForm` roda sobre `useCrudForm` e já entrega `errorSummary`; `useRedatorForm` não). O ponteiro `FormErrorSummary.tsx:62-67`, citado 4× em doc normativo, **apontava para arquivo que não existe** — o componente é export nomeado em `FormField.tsx:79-107` — e foi corrigido na Task 9. **Uma conta do brainstorming estava errada e foi refeita antes da spec:** colapsar o campo de cliente corta **~9** linhas, não ~46, porque `FormField` em modo leitura troca os **filhos inteiros** — com a conta certa o desenho passou a extrair **dois** blocos do `StudentDialog` e **duas** seções do `RedatorDialog`. Nove decisões (D1–D9) e **dez tasks, dez commits**, via SDD com revisor independente por task, na ordem testes do resumo → `StudentDialog` → slot → `RedatorDialog` → `BudgetDetailPage` (que **zera o `ignores`** e reescreve a rule) → UI-01 → `BudgetsTable` → `TurmasTable` → docs → gate. Dois loops de fix na execução: o `StudentClientField` devolvia `Fragment` onde o original tinha `<div>` (o `<p>` do aviso `clientLocked` não era irmão direto da section — escalado, o João escolheu `<div>`) e um número esquecido em `backlog.md:143`. **Review final de branch inteira** achou 3 Important, todos verificados antes de agir e corrigidos (`eb9bc47`, `d50d7f8`): `SearchableTableFrame` sem `flex-wrap` (regressão real de layout em telas estreitas nas duas tabelas migradas) e **duas entradas do próprio `state.md` corrompidas por um find-replace cego da Task 9** — o achado histórico do ponteiro fantasma virou afirmação invertida e a descrição de uma spec ARQUIVADA passou a mentir sobre o que ela cita. **`/revisar-sprint` de BAIXO risco** pelo gate binário da skill (zero schema, `generated.ts`, Sanctum, auditoria, RBAC, dinheiro ou documento legal; `executor: claude`) — divergência declarada contra o MÉDIO da spec §9, que escala por alcance e margem —, **uma lente, sem Codex**, com **4 achados, todos aprovados pelo João e corrigidos**. **Q-2 é o que virou mecanismo:** o contrato "quem passa `filterSlot` passa um `clear` COMPOSTO" era **prosa**, três telas remontavam o mesmo `clearAll` à mão e este bloco trouxe o terceiro consumidor — pela lição 14, `SearchableTableFrameProps` deixou de ser interface e virou união discriminada (`{ filterSlot?: undefined; onClearFilter?: undefined } | { filterSlot: ReactNode; onClearFilter: () => void }`), com a composição migrando dos chamadores para a moldura; **provado nas duas direções**, removendo o `onClearFilter` da `TurmasTable` e vendo `TS2322` do `tsc -b`. Os outros três: Q-1 (`removeDoc.error` **nunca lido** — exclusão reprovada de documento de redator ficava silenciosa, D16, sobre dado que alimenta idoneidade; adotou o molde do `useBudgetDetail` com `useMutationErrors` num banner só), Q-3 (o cast `options as { label: string; value: number }[]` morreu no **dono do dado**: `useStudentClients` descarta o `id` nulo com `flatMap`) e Q-4 (docblock do `RedatorDocumentSlot` apontando para onde `preview` passou a morar). **Gate de fechamento: `pnpm lint` exit 0, `pnpm build` verde, `pnpm test` 29 arquivos / 142 testes** — exatamente a projeção do plano —, e **a catraca provada nos dois sentidos, não por lint verde**: 30 linhas em branco apensadas ao `StudentDialog` fazem o lint reprovar com `File has too many lines (154). Maximum allowed is 150`, e a árvore volta limpa. Os seis alvos pousaram em **124 / 125 / 34 / 133 / 105 / 116** linhas, o bloco `ignores` do `max-lines` não existe mais e a régua vale sem exceção. Suíte backend, Pint e `typescript:transform` **N/A por escopo, medido**: `git diff main...HEAD -- backend/` e `-- generated.ts` **vazios**. **A dívida de e2e da execução foi PAGA aqui, não herdada:** o ambiente que a bloqueou (500 em `/api/students`) voltou, e com sessão Sanctum viva (cookie + CSRF, `Origin` e `Accept`) o payload forjado `"phone": []` devolveu **422 `application/problem+json` com `errors.phone`** em `PUT /api/students/37` **e** `PUT /api/redatores/1` — o insumo que o resumo do item (c) mostra, já que `phone` não está em `mapped` em nenhum dos dois. **Um achado do próprio fechamento, corrigido no commit:** o comentário de `useStudentForm.ts:36-38` ainda dizia que o `StudentDialog` **não tem** `FormErrorSummary` — o bloco tornou a frase falsa e o arquivo ficou fora do diff (lição 13, mesma classe da Q-4). **O que o fechamento NÃO provou, sem maquiagem:** **nada foi visto renderizado** — o Step 6 do plano (`/lotus-ui-review` nos seis roteiros, incluindo o wrap da toolbar a 390x844 e os quatro casos de CTA da D8) segue não executado, e a equivalência das extrações continua sendo visual sem virar mecanismo, porque nenhum diálogo tem teste de componente; o e2e rodou contra a stack do main tree, que serve o código da branch alheia `feat/contrato-de-entrada-identidade-e-nested` — `git diff main...HEAD -- backend/app/Domains/Identity/Data/` **vazio** ali, então o caminho medido é o mesmo, mas a medição não é de uma stack limpa; e seguem sem guarda automatizada a D8 (CTA em lista-vazia-com-termo) e a D2 (`mapped` literal do redator). | `plans/archive/2026-08-13-catraca-max-lines-e-moldura.md` · `specs/archive/2026-08-13-catraca-max-lines-e-moldura-design.md` · sem Context Packet (ausência medida de fonte externa) · commits `7c8d6c5`…`7c28699` + o commit de fechamento · ledger `.superpowers/sdd/progress.md` |
| 2026-08-13 | Forms · `useCrudForm` mais fundo e absorção do trio da foto (BD-5) | Entregue | **BD-5** do backlog, promovido explicitamente pelo João em 2026-08-13 com o estado em `idle`. Frontend-only, worktree `fix-frontend`, branch `feat/usecrudform-mais-fundo` a partir de `d0cc270`, **sem Context Packet por ausência medida de fonte externa** — grep por `drive.google`, `notion.so`, `figma.com`, `docs.google` e `http` nas 22 linhas do BD-5 devolve **zero ocorrência**. **Zero backend e zero `generated.ts`:** `git diff main...HEAD -- backend/ frontend/src/shared/types/generated.ts` vazio, então Pint e `typescript:transform` são N/A **por escopo medido**, não por suposição. **A invariante "existe no máximo um `active_work_item`" quebrou e ficou com exceção declarada, não resolvida (D6):** duas sessões promoveram itens distintos a partir do mesmo `d0cc270` (`5bf54f3`, este bloco; `0e3ce3b`, `login-fora-do-adr16`), e o João decidiu que as duas execuções correm em paralelo — o BD-5 na worktree, o login no main tree. O custo caiu onde o precedente BD-4×BD-9 dizia: o bloco perde o main tree como área de trabalho e passa a usá-lo **só como servidor** do `:8080` para o e2e do S3, com banco de dev compartilhado. **O terreno foi medido antes de desenhar, e quatro afirmações do backlog não sobreviveram:** `useQuoteForm` **não** é candidato legítimo (não satisfaz `MutableResource` — `useCreateQuote` recebe `{ budgetId, payload }` —, **não** manipula coleção nested e **não** usa `setForm`, ao contrário do que o `backlog.md:304-306` afirmava); a absorção do trio **não cabe inteira no hook** (metade é JSX e o quarto diálogo não roda sobre `useCrudForm`), então mora em dois sítios; `useCourseForm` só cabe com o hook mais fundo de verdade; e o texto do Q-4 estava impreciso — o `SignedUrlTransformer` roda na serialização, então o front recebe URL pré-assinada, não path interno de storage. **Seis decisões do João (D1–D6)**, e o plano acrescentou um desvio que mudou o construído (**D-P1**): a D2 dizia "`useCrudForm` ganha `photo`" e isso é **impossível na forma literal** — `useEntityPhoto` chama `useQueryClient`, e montá-lo sempre faria os oito testes de `useCrudForm.test.ts` (que rodam **sem** `QueryClientProvider` de propósito) lançarem `No QueryClient set`; a capacidade nasceu como hook **irmão**, `useCrudFormWithPhoto`. **Onze tasks, onze commits, cada um com revisão individual antes de avançar**, mais um commit fora de task (`ae86d0a`) corrigindo type errors que T2 e T3 deixaram passar porque `vitest run` não faz type-check completo — lição registrada no ledger. `StaffUserDialog` saiu de exatamente **150** linhas (margem zero na régua) para **125**. **Review de BAIXO risco pelo gate binário, uma lente, 1 achado:** **Q-1** — o `catch` de `runAfterCreate` engolia **qualquer** erro sem log, num hook de `shared/hooks` com **5 consumidores** que mexe em registros de peso legal; a premissa do docblock ("o erro já está no `fieldErrors`") é contrato do chamador, não garantia do tipo. Aprovado e corrigido em `f766860` (`console.error` no branch do catch). **Gate de fechamento refeito contra a API real, não herdado do review:** com a stack do main tree conferida **no momento da prova** (`git diff main...HEAD -- backend/` **vazio** naquele tree, `/api/students` sem cookie devolvendo **401**), sessão Sanctum viva (cookie + CSRF, `Origin` e `Accept`), os **dois caminhos** provaram foto real no S3 — `create` (aluno novo id 58) e `edit` (aluno pré-existente id 37), ambos com `POST .../photo` **204**, `photo_url` não nulo na leitura seguinte e **`http=200 bytes=70 type=image/png` na signed URL**, que é a falha de zero byte da lição 6 medida em vez de assumida; objetos conferidos em `/data/lotus/user-photos/{49,91}` no MinIO. Limpeza: `DELETE .../photo` nos dois (o 37 volta a `photo_url: null`, como estava) e `forceDelete` do aluno 58 e do user 91 via tinker; **7 linhas de `audits` remanescentes ficam declaradas, não limpas** (molde do BD-2). **A régua foi provada nos dois sentidos (lição 10), não por lint verde:** 30 linhas em branco no `StaffUserDialog` fazem o lint reprovar com `File has too many lines (155). Maximum allowed is 150`, e a árvore volta limpa. Ferramentas: `pnpm lint` exit 0, `pnpm build` verde, `pnpm test` **31 arquivos / 156 testes** (baseline 29/143, projeção do plano batida exata); suíte backend **591 passed, 5 skipped (2149 assertions)** — rodada, mas **medindo o código da `main`**, porque o container monta o main tree e o diff de backend dele está vazio. Alvos em **125 / 97 / 97 / 127 / 129** linhas, todos sob 150; órfãos **zero** (`FormPhotoRow` em 7 arquivos, `useCrudFormWithPhoto` em 6). O trio morreu nos três que migraram e sobrevive no quarto **por critério**: `closeBlocked={pending \| photo.pending}` tem uma ocorrência, em `RedatorDialog.tsx:70`, cujo hook não migra por ser create multipart. **Saem do backlog o BD-5 e os dois débitos que ele cobriu por inteiro** (a absorção do trio nos 4 diálogos e os 4 hooks fora do `useCrudForm`, cada um com o critério agora decidido). **Do Q-4 só o lado do frontend foi pago:** `FORBIDDEN_PAYLOAD_KEYS` faz a chave lançar em DEV, mas o backend segue aceitando `photo_url` no corpo — **remedido neste fechamento**, `PUT /api/students/37` com `"photo_url":"http://evil/x.png"` devolve **200** e o campo volta `null`, porque a promoção no construtor do DTO desvia do `CannotSetComputedValue`; o resíduo ficou como débito próprio em `## Débitos técnicos`, com saída no próximo bloco de backend que tocar DTO com campo `#[Computed]`. **Pendências: nenhum gatilho venceu e nenhuma nasceu.** A **P-03** ganhou uma **contraprova**: mesmo arranjo de duas execuções em paralelo do BD-4, e desta vez o e2e rodou inteiro — o custo da falta de compose por worktree não é constante, é contingente ao que a branch alheia toca, e a prova só vale com a conferência feita na hora. **O que o bloco NÃO provou, sem maquiagem:** nenhum diálogo tem teste de componente, então a composição `FormPhotoRow` + diálogo (Tasks 6-9) não é exercitada por teste automatizado — o e2e do S3 bateu na API direto, não através do `AppPhotoField`/`FormPhotoRow` renderizado; e **`/lotus-ui-review` não rodou**, então os quatro diálogos migrados nunca foram vistos no navegador nesta execução. **Uma divergência de projeção fica declarada em vez de corrigida retroativamente** (precedente da P-27): a spec projetou `useCourseForm` em ~110 linhas e o plano em ~115; o entregue tem **129**. | `plans/archive/2026-08-13-usecrudform-mais-fundo.md` · `specs/archive/2026-08-13-usecrudform-mais-fundo-design.md` · sem Context Packet (ausência medida de fonte externa) · commits `6ff9565`…`f766860` · ledger `.superpowers/sdd/progress.md` |
| 2026-08-13 | Login · a tela que ficou fora do ADR-16 | Entregue | Item 4 de "Próximos blocos", promovido explicitamente pelo João em 2026-08-13; frontend puro — `git diff main...HEAD -- backend/ frontend/src/shared/types/generated.ts` devolve **zero arquivo**, o que mantém suíte de backend, Pint e `typescript:transform` N/A por escopo medido. **O login entra no tema:** `--brand-gradient` novo em `brand-theme.css` (composto de `--primary-900` e `--brand-navy`, sem hex novo), superfícies e tipografia pelas variáveis do tema, copy da RN-01 na tela ("Acceso para administradores y redactores") e ajuda de senha como `<p>`, não `<a>` sem `href`. **A catraca `COR_HARDCODED` encolheu 7 → 5**, provada nos dois sentidos: com `LoginPage`/`LoginForm` fora do `CATRACA_COR` o lint sai 0 e, reintroduzindo um `text-slate-800`, reprova nomeando o arquivo. O `/lotus-ui-review` do João achou **4 defeitos de UI** (largura do campo de senha, nome acessível somando o olho, `role="switch"` com `aria-checked` invertido, painel de marca cortando o logo no telefone) e os quatro foram corrigidos na raiz. O `/revisar-sprint` devolveu **3 achados**, os três aprovados e corrigidos: a causa raiz do painel mobile (margem de 1em do user-agent com Preflight desligado) paga com `my-0` + `min-h`, o wordmark de volta a 150px e o `overflow-hidden` removido; `aria-describedby`/`aria-invalid` no erro de campo, **medidos contra um 422 real** da API; e a fusão de `pt` por chave em `shared/ui/mergePt.ts`, com 7 casos de teste e 4 deles falhando quando o spread raso volta. Gate de fechamento: backend **591 passed / 5 skipped**, `pnpm lint` 0, `pnpm build` verde, `pnpm test` **30 arquivos / 150 testes**, e a aparência medida no navegador em **3 viewports × 2 temas** (contraste, divisa, `scrollWidth`, wordmark, `aria-label` em es-CL). Fecha também a não-regressão do `AppPassword` no `StaffUserDialog`, que o bloco havia declarado como inferência: 292px, igual aos irmãos do grid. Nasceram **P-36** (catraca de cor não vê `style={{…}}`) e **P-37** (`FormField` soma o nome acessível) | `plans/archive/2026-08-13-login-fora-do-adr16.md` · `specs/archive/2026-08-13-login-fora-do-adr16-design.md` · sem Context Packet (ausência medida de fonte externa) · commits `0e3ce3b`…`024673a` + o commit de fechamento |
| 2026-08-14 | UX de carga · falha vs. lista vazia (BD-6, débito B-7) | Entregue | **BD-6 do `backlog.md`, promovido explicitamente pelo João** com o estado em `idle`; o gate do `/planejar-bloco` reprovou pelo motivo de sempre (argumento era título de seção) e três decisões dele o fecharam: o slug, **rota direta a `ready_for_planning` sem Context Packet** por ausência **medida** de fonte externa (grep por `drive.google`, `notion.so`, `figma.com`, `docs.google` e `http` nas 15 linhas do BD-6 = zero), e **main tree** na branch `feat/falha-vs-lista-vazia` a partir de `0a1439f` — o main tree venceu por ausência de disputa, não por costume, então a invariante de um `active_work_item` volta a valer sem a exceção de 2026-08-13. Frontend puro: `git diff main...HEAD -- backend/ frontend/src/shared/types/generated.ts` **vazio**, o que mantém suíte de backend, Pint e `typescript:transform` N/A **por escopo medido**. **O bloco paga o B-7 — GET falho virando "não há cursos" — separando carregando / falhou / veio vazia nos sítios onde a tela mentia.** A medição derrubou um dos três sítios escritos no item (`clientName` do `useCommercialClients` tem um consumidor só, a `BudgetsTable`, que já faz erro-vence-vazio, então o `'—'` nem chega a renderizar sob falha) e o trocou pelo disfarce vivo do mesmo hook: `clientOptions` no `BudgetDialog`, onde GET falho rendia dropdown vazio, sem motivo e sem Reintentar. Achou também que **o molde já existia um módulo ao lado** (`useRedatorCourses` + `RedatorCourseSelector`, `useStudentClients` + `StudentClientField`), então nada aqui é padrão novo: o par erro/dica sob campo virou `InlineLoadState` em `shared/ui` e o `StudentClientField` perdeu a cópia local (precedente exato do `mergePt`). **Duas decisões mudam comportamento e foram declaradas:** a falha de cursos na `QuotesList` é local no card e **não** sobe ao `loadError` da página (D2, divergindo da D16 por motivo medido — lá o nome era campo de busca, aqui não, e esconder valor UF, status e arquivos por falha de nome seria o erro inverso); e `canAdvance` fica `course_id > 0` (D3), para não repetir o `03280c6`, revertido por travar com lista utilizável em cache. **Seis tasks, seis commits, via SDD** (implementador + revisor por task, todas Approved), com o mesmo impasse do BD-4/BD-8/login resolvido do mesmo jeito: Agent tool autorizado pelo João por pergunta direta. **Review BAIXO risco pelo gate binário, mas em DUAS lentes por pedido do João** (Claude + Codex read-only sobre `0a1439f..HEAD`): **5 achados (3 + 1 + 1), todos aprovados e corrigidos**, um commit cada. **Três coincidiram nas duas lentes sem que o prompt do Codex citasse achado algum do Claude.** **Q-1 🟡 é o próprio erro do bloco, sob outra grafia:** `isError` cru ignora o cache nos dois sítios que compartilham a query `['courses','list']` — com `staleTime` 0, abrir o wizard sobre a `QuotesList` refaz o GET, e um refetch falho apagava lista utilizável **e** o termo digitado (o inverso da D3 do próprio bloco); premissa medida por sonda `renderHook`, não assumida — refetch falho deixa `isError` verdadeiro **com `data` ainda definido**. **Q-2 🟡:** a derivação foi duplicada, não extraída — `isError`/`errorDetail`/`refetch` em 6 hooks e o predicado de vazio **verbatim** em 3, sob dois nomes (`isEmpty` × `showEmptyHint`); o bloco extraiu a **view** e replicou a **fonte**. Nasce `shared/hooks/useLoadState.ts`, com `failedWithoutData` — falhou **e** não há cache — como a única chave que autoriza substituir a tela. **Q-3 🟢:** dropdown desabilitado por `unusable`, que também é verdadeiro carregando, e sem sinal nenhum: controle morto e mudo. **Q-4 é só do Codex e foi verificado antes de aceito** (regra da skill): o `BudgetDialog` desabilitava o dropdown e **não** passava `disabled` ao `CrudDialog`, então Criar seguia vivo com `client_id: 0` — o gêmeo `StudentDialog.tsx:57` já fazia certo e o docblock do `CrudDialog` nomeia o caso. **Uma divergência entre as lentes foi mostrada ao João em vez de resolvida em silêncio** (o aviso da `QuotesList` quando o cache resolve todos os nomes: Codex leu como bom, Claude como falha anunciada e invisível) — **ele aprovou os dois lados**, e o aviso passou a depender do nome perdido. **Gate de fechamento REFEITO contra a API real, não herdado:** as cinco correções entraram depois do e2e de execução e mexeram exatamente no ramo de falha, então as provas foram remedidas ao vivo (locale es-CL, sessão Sanctum, falha injetada por patch de XHR) — `QuotesList` sob falha de cursos mantém as **3 cotações visíveis** (120/80/250 UF e status) com alerta, Reintentar e nomes `—`, e o unpatch + Reintentar resolve os nomes e limpa o alerta; **com cache quente, zero `[role=alert]`** e nomes renderizados (`baf08e9`); o wizard com cache quente mantém os 4 cursos selecionáveis com a falha inline (`08cb01a`) e, com cache frio, troca o passo pelo erro com `Siguiente [disabled]`, recuperando no Reintentar; o `BudgetDialog` sob falha de clientes mostra motivo + Reintentar, dropdown `p-disabled`/`data-p-disabled=true` e **Crear presupuesto `[disabled]`** (Q-4), enquanto a `BudgetsTable` — o consumidor antigo — segue com o erro próprio dela, que é a **aditividade** das quatro chaves novas provada ao vivo; e o retrofit de `identity` (Personas → Alumnos) segue com motivo + Reintentar no dropdown de empresa, liberando o formulário depois do unpatch. Placar: `pnpm lint` exit 0, `pnpm build` verde, `pnpm test` **35 arquivos / 176 testes** (baseline 32/163, projeção do plano 35/174 mais 2 casos de regressão escritos para falhar no código antigo); backend **591 passed, 5 skipped (2149 assertions)** sem uma linha no diff. Zero import de `primereact` em `features/`, zero import cruzado `@features/*`, zero órfão. **O que o fechamento NÃO provou, sem maquiagem:** a **Prova B** (catálogo vazio de verdade) rodou na execução, em `d20bebc`, com soft-delete e restauração conferida (`vivos=4 trashed=0`), e **não foi remedida** contra HEAD — o `artisan tinker` foi recusado pelo classificador de auto mode e não há substituto pela API (o índice não aceita filtro e o wizard filtra client-side); o que sustenta o ramo é leitura de código (predicado idêntico, só mudou de casa; o gate que mudou é o anterior e não dispara sem erro) mais dois testes, e isso virou a **P-40** por decisão do João. Nasceram também a **P-39** (o plano afirma que `GET /api/courses` só tem `auth:sanctum`, e o `CourseController` declara `permission:catalog.course.view` — nenhuma prova cai, mas a premissa escrita fica errada; plano e spec não foram retro-editados, precedente da P-27) — **corrigida aqui em 2026-08-22 pelo BD-15, e encerrada:** `catalog.course.view` segue declarado em `CourseController.php:19` via `HasMiddleware`, então a rota **é** gateada por RBAC e o `routes.php:11` sozinho não conta a história; quem reusar a receita de injeção de falha do BD-6 deve ler o controller, não só o arquivo de rotas. A lição transversal virou a **18** de `docs/README.md`; no review já haviam nascido a **P-38** (a rule diz que teste de componente PrimeReact em jsdom fica fora do corte, e o bloco montou três) e o segundo sítio da **P-37**. O padrão reincidente do Q-1 virou **regra** em `.claude/rules/frontend-fsliced.md` ("o que ramifica a tela é o dado que falta, não o `status` da query"), e os dois sítios fora de escopo (`RedatorCourseSelector`, `CourseRedatoresSection`) foram para o `backlog.md` com o fix de uma linha já descrito. | `plans/archive/2026-08-14-falha-vs-lista-vazia.md` · `specs/archive/2026-08-14-falha-vs-lista-vazia-design.md` · sem Context Packet (ausência medida de fonte externa) · commits `f64ba33`…`d20bebc` (execução) + `501d98c`…`2511501` (correções e doc) · ledger `.superpowers/sdd/progress.md` |
| 2026-08-14 | UI · célula de identidade (avatar + título + descrição) e a foto que nunca aparecia | Entregue | **Item 4 de "Próximos blocos", promovido explicitamente pelo João em 2026-08-14**, com quatro decisões fechando o gate do `/planejar-bloco`: o slug, a **rota `context_required`** (contra a medição — grep por fonte externa nas 105 linhas do item devolveu zero —, e ela **se pagou**: o packet consultou três fontes do Drive por file ID e a base do Notion por collection ID e devolveu **prova de ausência**, nenhuma delas prescreve célula de identidade, uso de foto ou fallback sem imagem; `status: partial`, as duas capturas de tela viraram lacuna declarada, não fonte `unavailable`, e o bloco fica **sem aceite por equivalência visual**), o **paralelismo** com o BD-6 e a worktree `fix-frontend`. **Terceira exceção declarada à invariante de um `active_work_item`**, com a sobreposição medida em três pontos ANTES da decisão, não depois. **A spec (D1–D11) desmentiu o próprio rótulo do bloco: ele deixou de ser frontend puro.** A D2/D3 resolve o Grupo C alargando DTOs no backend, o que regenera `generated.ts` (lei §5.3 / ADR-04) e sobe o risco de review a **ALTO** pelo gatilho binário do projeto. O custo foi **medido e desmentiu minha própria precificação**: `TurmaQueryBuilder::LISTING` já trazia `redatores.user` e `quote.budget.client.user` — zero query nova, zero eager load novo, zero migration (spec §3). A única incerteza real declarada — se um `WithTransformer` dispara dentro de `array` sem `#[DataCollectionOf]` — **resolveu-se a favor do caminho barato na Task 2**: o transformer atravessa, e o Step 6 alternativo do plano não foi aplicado, porque aplicá-lo por precaução trocaria mecanismo medido por palpite. **Doze tasks, executor dividido por decisão do João (D12): `codex` nas 1, 2 e 11 (`backend/**`), `claude` no resto** — e `frontend/src/shared/types/generated.ts` deliberadamente **fora** dos `paths_autorizados` do Codex, porque a garantia mais barata contra a lei §5.3 é o executor do backend não alcançar o arquivo. **O sandbox do Codex não alcança `/var/run/docker.sock`**, então ele parou nos Steps de PHPUnit marcando `blocked` em vez de alegar verde; escalar a permissão foi **recusado** e o arranjo virou Codex autora o diff, Claude mede. **Três desvios de ambiente foram medidos, não deduzidos, e cada um custou um mecanismo:** o stack `lotus-*` do main tree servia `:8080` com a branch do BD-6, então `php artisan test` mediria a outra árvore (resolvido com `docker compose up -d --no-deps app`, sem derrubar nada); o `db:seed` da Task 11 precisava de MySQL/MinIO reais, que `--no-deps` não dá (resolvido com `docker network connect lotus_default fix-frontend-app-1`, reuso deliberado do banco de dev); e o `VITE_API_URL` apontava para o backend do main tree, **que é a causa raiz de a foto nunca ter aparecido em duas execuções seguidas** — todo `photo_url` chegava `undefined` porque aquele backend não tem os DTOs desta branch. **Uma correção de rumo do João inverteu o bloco no meio:** a frase "em turma, certificados e detalhes da turma não exibe foto de redatores, alunos" tinha sido lida como pedido de remoção e era **relato de defeito**. A extensão anterior foi desfeita, a foto passou a entrar em **todos** os sítios que chamam `IdentityCell` e no header. Nasceram `EnrollmentData::photo_url`, `EmissionPanelEnrollmentData::student_photo_url`, `CertificateData::aluno_photo_url` e `SessionUserData::photo_url`. No `CertificateData` a foto é **viva e deliberadamente fora do snapshot**: a decisão de auditoria do `9e3a68e` ("snapshot não se ilustra com dado mutável") foi revertida por instrução explícita do João **para a listagem** — PDF e rota pública do QR seguem só-snapshot. `IdentityCell` **não** entrou no header, também por decisão dele contra a leitura literal do pedido: o trigger do `UserMenu` colapsa o texto por `sr-only` abaixo de `sm` (UI-04 de 2026-08-12) e a célula não tem gancho para isolar só o texto — usá-la ali regrediria o UI-04, então ficou o `AppAvatar` com a foto encaixada. **Review ALTO risco em duas lentes** (Claude + Codex read-only sobre `0a1439f..840edf0`), **9 achados; 7 aprovados e corrigidos, 2 rejeitados pelo João**. Os dois 🔴 eram a mesma classe de defeito — texto afirmando o que o código não faz. **Q-6** rendeu o `CertificateQueryBuilder` (`LISTING = ['enrollment.student.user']`), com guarda de runtime em `CertificateEagerLoadTest` **vista vermelha** antes de valer. **Q-4:** a regex do `SignedUrlPropertyReadTest` varria `->photo_url` e os três campos novos escapavam pelo prefixo — a guarda existia e não cobria o que o bloco criou. **Um achado do Codex foi medido e reprovou como ele o escreveu:** o `DemoPhotosSeeder` "sem guarda de ambiente" já é barrado por `--force` do `ConfirmableTrait`, então virou 🟢 de higiene. Os dois rejeitados são edição do João à mão depois do plano e viraram **P-41** (`min-w-0` ausente, `truncate` inerte) e **P-42** (grafia diverge do D1) — nasceram como P-38/P-39 e foram **renumeradas no fechamento**, porque a reorg de docs da `main` (PR #51) chegou primeiro e já usava esses IDs. **A `main` foi mergeada na branch ANTES do fechamento**, por decisão do João: ela andara 24 commits, e a reorg apagou `docs/pendencias.md` e desceu `progress.md` para `historico/` — fechar antes escreveria em arquivo morto. Cinco conflitos, três deles previstos por este bloco na promoção paralela; `useCommercialClients` ficou com a forma `useLoadState` do BD-6 e o Grupo B por cima. **O gatilho da P-34 venceu aqui** (o bloco tocou o shell no `UserMenu`) e **não** foi absorvido: as 3 classes vivem em `Sidebar/` e a reorg já as agrupou no **BD-11**. **Gate pós-merge, reproduzido e não herdado:** backend **595 passed / 5 skipped (2162 asserções)**; `pnpm lint` exit 0; `pnpm build` exit 0; `pnpm test` **36 arquivos / 186 testes**; `typescript:transform` reexecutado não move `generated.ts`; Pint `passed` nos 15 PHP do bloco; zero `.gitkeep` órfão. **Critério de aceite provado por request real contra a API desta branch** (`:8081`, sessão Sanctum): `/api/me` devolve `photo_url` assinada e o GET da própria URL dá **200 `image/png` 1.877.301 b**; `client_photo_url` em 3 de 6 turmas, `redatores[0].photo_url` em 5 de 6, `EnrollmentData::photo_url` em 27 de 55 matrículas, `student_photo_url` em 7 de 15 do painel de emissão, `aluno_photo_url` em 3 de 5 certificados — o preenchimento parcial é o "um sim, um não" do `DemoPhotosSeeder`, e o resto cai nas iniciais, que é o ramo correto. Os **16 call sites** de `IdentityCell` passam `image`, conferidos um a um. **A checagem visual foi feita pelo João, não por `/lotus-ui-review`** — a skill tem `disable-model-invocation: true` e a porta 5173 esteve ocupada pelo stack do main tree durante toda a execução; o bloco fecha com a prova de pixel vinda dele, não de artefato de skill. | Plano `plans/archive/2026-08-14-celula-de-identidade.md` · spec `specs/archive/2026-08-14-celula-de-identidade-design.md` · Context Packet `context-packets/celula-de-identidade.md` (`partial`) · commits `abe976a`…`eb2b45c` + merge `9ed7351` · ledger `.superpowers/sdd/progress.md` |
| 2026-08-15 | Dashboard · contrato backend e agregações read-only (Sprint 5, bloco A) | Entregue | **Primeiro bloco da Sprint 5 (`backlog.md:39`), promovido explicitamente pelo João** com o estado em `idle`; o gate do `/planejar-bloco` reprovou pelo motivo de sempre (argumento era título de seção) e duas decisões dele o fecharam: o slug `dashboard-backend-agregacoes` e a **rota `context_required`**, que o backlog exige para a Sprint 5. **Aqui a fonte externa EXISTE declarada — o oposto dos BDs:** o Context Packet do Codex (read-only) recuperou o Drive (`dashboard-escopo-funcional-analitico.md`) e as oito tasks EAP 8.4.0–8.4.7 do Notion **por ID de página**, aplicando a lição das 12 falsas divergências de 2026-07-30; achou que os corpos das EAP **8.4.0 e 8.4.7 estão trocados entre si** e reconciliou pelo Drive, com base declarada. **Entrega:** domínio `App\Domains\Dashboard` read-only puro — sem Model, migration, tabela ou Policy —, 21 DTOs, sete classes de consulta, `GET /api/dashboard/metricas` e RBAC/ownership. **Nove decisões na spec** (D1–D6 escolhidas pelo João com o custo declarado; D7–D9 derivadas): sem tempos de ciclo nem ranking de redatores, filtro só de período, janelas 7d/30d, **dois DTOs raiz num endpoint só** (vazamento vira erro de tipo, não bug de runtime), certificado revogado **não** devolve a matrícula a "a emitir" (D6), zero permissão nova, regra de domínio reusada de `TurmaHabilitacaoService`/`BudgetSummaryService`, agregação por query sem cache. **Três medições entraram na spec, não em memória:** o catálogo RBAC real (35 permissões, 3 segregadas) sustenta a D7; `certificates` **não tem coluna de data de emissão**, e foi isso que expôs a ambiguidade do revogado que virou a D6; e o funil do Drive §3.4 tem sete rótulos com dois sobrepostos, então a spec fixa **seis baldes exclusivos**. **Execução MESCLADA claude/codex por instrução do João:** oito tasks, sete commits, **Tasks 2, 3 e 4 no Codex** via `lotus-execute-block` (sem acesso ao socket do Docker — "Codex escreve, eu rodo"), cada diff lido contra o plano e os `paths_autorizados` conferidos antes de aceitar; Tasks 1, 5, 6, 7 e 8 no claude. **O pre-flight scan achou uma contradição real entre spec e plano e mudou o contrato antes da primeira linha:** a spec §4.2 dizia que `AgendaTurmaData` era compartilhado **e** que o payload do Redator não continha cliente "por construção do tipo" — mas o plano dera `?string $client_name` ao DTO compartilhado, então o campo proibido chegava ao Redator pelo tipo. O João escolheu o split (`RedatorAgendaTurmaData`/`RedatorAgendaData`), e a Task 1 foi de 19 para 21 DTOs. **Review ALTO risco pelo gate binário** (regenera `generated.ts`, eixo central é RBAC/ownership), **duas lentes, 9 achados, todos aprovados e corrigidos**; quatro coincidiram sem que o prompt do Codex citasse achado nenhum do Claude, e **um achado do Codex foi recusado com razão escrita** (o `RedatorLoadQuery` contar carga em PHP não viola a D9 — uma query para a equipe inteira em vez do N+1 por linha). **Três achados foram medidos por sonda, não deduzidos:** o **Q-1** (papel sem `identity.*` recebia `redatores: null` e ainda assim ganhava alerta `redator_document_expired` com `redator_id` dentro — nasce o `IdentityMetricsQuery`), o **Q-4** (`$course->delete()` fazia a linha sumir do ranking enquanto a série a mantinha — `withTrashed()` na resolução de nome) e o **Q-2**, único achado só do Codex e verificado antes de aceito: `?period_end=2020-01-01` devolvia **200** com a janela invertida, onde a spec §4.1 promete 422. **Gate de fechamento REMEDIDO contra a API real, não herdado** — as nove correções entraram depois do e2e da Task 8 e mexeram em gate, contrato de filtro e escopo de certificado. Quatro atores, sessão Sanctum cookie+CSRF: **admin** com todo KPI conferido por SQL independente (turmas 4/0/3, `conclusoes_por_confirmar` 1, cotação 1 / `250.0000` UF, `certificados_a_emitir` 9) e a coerência interna fechando sozinha (`conclusoes_por_confirmar` = as turmas `turma_ready_for_conclusion` do funil; `alertas` = os `overdue` da agenda, que é a D8 viva); **três papéis-sonda criados pela API real** (`POST /api/roles` + `POST /api/users`), cada um cego a um módulo — sem comercial, `cotacoes` sai `null`, a série de UF sai `null`, **cada linha de ranking sai com `uf_aprovada: null`** e a rede de string no corpo bruto mostra `uf` só como **nome de chave seguido de null**; sem `identity.user.view`, `redatores` sai `null` (Q-1); sem `operation.turma.view`, os **quatro KPIs de turma saem `null` e não 0** (Q-3); **redator** ativado de forma temporária e reversível, com 6 chaves, nenhuma de admin, turmas ⊆ {1,4,5,6} conferidas em SQL, nenhuma turma alheia, e **zero ocorrência** das strings `uf`, `cotac`, `client`, `quote`, `budget`, `series` e `ranking` no corpo. **Q-2 provado nos dois sentidos:** só `period_end` antigo e só `period_start` futuro devolvem **422 `application/problem+json`**; sem sessão, 401 no mesmo formato. **Mutação zero foi medida, não afirmada:** 17 tabelas contadas antes e depois de 8 GETs (quatro formas de query × dois atores), `diff` vazio — `audits` e `login_logs` inclusive. **Restauração conferida:** o redator voltou byte a byte (`is_active`, hash e `updated_at`, com o MD5 do conjunto de usuários batendo com o capturado antes) e as sondas saíram, devolvendo `users`/`roles`/`role_has_permissions`/`model_has_roles` a 79/3/70/5. Placar: backend **628 passed, 5 skipped (2384 assertions)** contra o baseline **591**; `pnpm lint` exit 0 e `pnpm build` verde; Pint `passed` nos 18 `.php` do bloco; `typescript:transform` reexecutado **sem diff novo** — a regeneração é idempotente e o diff é exatamente os 4 campos de `AdminKpisData` que o Q-3 anulou (lei §5.3); zero órfão nas 34 classes do domínio. **O que o fechamento achou e corrigiu:** a tabela de gates da spec §4.2 estava **mais estreita que o código** — media-se ao vivo que um papel com identity e comercial mas sem `operation.turma.view` recebe `redatores`, `rankings` e `pipeline` nulos, porque a regra real (documentada no docblock do `AdminDashboardAssembler`) é "uma seção exige TODOS os gates dos módulos de que ela lê". A §4.2 foi completada **antes** de arquivar, e não retro-editada depois: a fase de correção deste mesmo bloco já a emendara para `kpis` e `alertas` e parara no meio — o precedente P-27 protege história de bloco **fechado**, e este fechava agora. **O que o gate NÃO provou, sem maquiagem:** a D6 não é distinguível no banco de dev (a matrícula do certificado revogado tem também um emitido, então as duas definições de "a emitir" dão 9) e o **Q-4**, o **Q-7** e o **Q-8** não têm caminho vivo lá (zero curso arquivado, nenhum cliente arquivado com cotação, um só tipo de documento de relator, zero documento vencido) — os quatro valem pelos testes, cada um com a guarda vista reprovar; e **nenhuma tela foi vista**, que é o bloco B. Nasceram a **P-43** (`der-fisico.md` chama `certificates` de "planejada" em quatro sítios, e a tabela existe desde a Sprint 4 — a §10 da spec já a previa) e a **P-44** (onze usuários de sonda de gates antigos vivem no banco de dev, e dois deles **aparecem na carga do dashboard** como "E2E Gate Redator 1/2" — nasceram como P-41/P-42 e foram renumeradas no merge com a `main`, que já usava esses IDs pelo fechamento da `celula-de-identidade`); a **P-26** cumpriu a sprint de rastro e saiu de `encerradas.md`. Três não-regressões declaradas no review foram para o `backlog.md` com o diagnóstico escrito: nenhum redator autentica em produção (o dashboard do redator está provado e hoje inalcançável), turma concluída com zero matrículas cai em `fully_issued`, e o `DomainDependencyTest` só detecta aresta usada-e-não-declarada. | `plans/archive/2026-08-14-dashboard-backend-agregacoes.md` · `specs/archive/2026-08-14-dashboard-backend-agregacoes-design.md` · packet `context-packets/2026-08-14-dashboard-backend-agregacoes.md` · commits `8c53f60`…`cdccb12` (execução) + `59b4f4d` (correções) · ledger `.superpowers/sdd/progress.md` |
| 2026-08-15 | Identity · Meu Perfil — backend self-service (Sprint 6, bloco 1 de 2) | Entregue | Primeiro bloco da Sprint 6, promovido explicitamente pelo João em 2026-08-14 com rota `context_required` (packet do Codex `ready` — a primeira vez que a fonte canônica externa EXISTE e foi recuperada por file ID: Drive `meu-perfil-escopo-funcional.md`) e worktree `fix-frontend` em paralelo com o `dashboard-backend-agregacoes` — **quarta exceção declarada à invariante, a primeira backend × backend: o gatilho da P-03 venceu**, registrado no `state.md` com a mitigação medida (`fix-frontend-app-1` montando esta árvore). **Entrega:** contrato próprio de perfil em `/api/profile` (D4 — `/api/me` e `SessionUserData` intocados): `GET` com bloco de redator (4 tipos documentais com `DocumentValidityStatus` calculado no backend, Drive §5 vencendo o comentário do código; `cursos_habilitados` via aresta já na allowlist), `PUT` de nome/telefone com os sete campos administrativos recusados por `missing` (422 nomeando o campo, inclusive presente-mas-vazio), foto própria sem gate administrativo (D7, reuso de `UserPhotoService`), troca da própria senha em `ChangeOwnPasswordAction` transacional com purge das outras sessões (D3), documentos self-service reusando `StoreRedatorDocumentAction` com REUF administrativo (D5/RN-09 — 422 nomeando campo, nunca 403), e `generated.ts` regenerado aditivo. Oito tasks sequenciais em `executing-plans`, executor `claude`, commit por task, vermelho medido antes do verde. **Duas armadilhas de teste novas, mesma raiz** (`RequestGuard` do Sanctum cacheado por processo de teste, sintomas opostos na Task 3 e na 6), isoladas por instrumentação em três camadas e registradas nos comentários de `ProfileReadTest`/`ProfilePasswordTest`. **Review ALTO em duas lentes** (Claude + Codex read-only): 5 achados Q-1..Q-5, todos aprovados pelo João e corrigidos **com prova por mutação** (stash contra o `prohibited` antigo: 3 failed; mutação no purge e no `$auditInclude` reprovando); um downgrade declarado no Q-3. **Fechamento provado e2e contra a API real** — stack próprio da worktree, sessão Sanctum cookie+CSRF, redator demo mutado e restaurado: GET com o contrato completo, PUT permitido 200, `email` e `roles: []` recusados 422 nomeando, REUF 422 / CV 201, foto 204, senha 204 **matando a outra sessão (401) e preservando a corrente (200)**. Placar: backend **647 passed / 5 skipped (2302 asserções)** contra baseline 592/5; `pnpm lint` e `pnpm build` verdes; Pint `passed` nos 22 `.php` do bloco; `typescript:transform` **sem drift**. **Um acidente do próprio fechamento, declarado:** o Pint chegou a rodar sem argumento (lista vazia por erro de cwd) e reformatou 37 arquivos alheios — revertido por stash antes de qualquer commit, árvore conferida limpa; a lição é que a proibição do CLAUDE.md §6 falha silenciosamente quando a expansão do shell devolve vazio. Nasce **D-15** (unificar `DIAS_AVISO`/`EXPIRY_WINDOW_DAYS`, travado no merge do dashboard). O bloco 2 (`meu-perfil-frontend`) segue no backlog, **não promovido**. | `plans/archive/2026-08-14-meu-perfil-backend-self-service.md` · `specs/archive/2026-08-14-meu-perfil-backend-self-service-design.md` · packet `context-packets/2026-08-14-meu-perfil-backend-self-service.md` · commits `3499439`…`d0430d0` + fechamento · ledger `.superpowers/sdd/progress.md` |
| 2026-08-16 | Dashboard · central de controle operacional — frontend (Sprint 5, bloco B1) | Entregue | **Bloco restante da Sprint 5, promovido explicitamente pelo João** com o estado em `idle`, pela rota `context_required` que o backlog exige — e o gate do `/planejar-bloco` reprovou pela décima vez o mesmo erro: o argumento era título de seção (`## Sprint 5 · Dashboard`), não slug. **Main tree por escolha dele:** o bloco é frontend e o gatilho da P-03 (dois blocos de *backend*) não vencia, então worktree era admissível e ele preferiu a árvore principal. **A decisão que reconfigurou o bloco foi a D1 da spec: o bloco B virou dois.** O contrato do bloco A expõe 14 seções de UI e entregá-las num plano só levaria o gate visual cansado ao fim; o corte é por pergunta respondida — o **B1** responde *"o que tenho para fazer agora"* (`kpis`, `pendencias`, `alertas`, `agenda`, `pipeline`) e o **B2** (`dashboard-frontend-analitico-e-redator`, nascido no backlog neste fechamento) responde *"como a operação evoluiu"*. **O corte se pagou duas vezes, e as duas por medição feita antes do desenho:** não existe chart lib no projeto (`package.json` sem `chart.js`, peer obrigatório do `Chart` do PrimeReact, e sem wrapper em `shared/ui`), e as 5 seções do B1 são exatamente as que não precisam de gráfico; e o filtro de período caiu junto, porque a D3 da spec do bloco A já dizia que estado operacional ignora o período — com séries e rankings no B2, o B1 não tem o que filtrar, e o hook nasceu **com o parâmetro e sem UI** (D5) para o B2 ligar a tela sem mexer no cache. **Uma premissa do Context Packet foi confirmada por medição em vez de aceita:** ele atribuía ao Drive a proibição de `features/dashboard`, e o repositório manda no mesmo sentido por outro caminho (`estrutura-monolito.md:100` reserva `app/pages/` para "página que NÃO é de domínio" e a lei §5.6 proíbe feature importar feature, o que um Dashboard que lê de três módulos violaria) — Drive e repositório convergem, não havia divergência a reconciliar. **Três achados de terreno mudaram o desenho, nenhum no packet:** `useLoadState` não serve (assinatura `UseQueryResult<T[]>`, query de lista, e o dashboard é objeto único com seções anuláveis — a política de estado ficou no `useDashboard`); `formatUf` vivia em `features/commercial/lib/` e subiu para `shared/lib` pelo ADR-18, com **5 sítios de import e não 4** (`DataStep.tsx` consome `parseUfInput` e ficara de fora da contagem — é o caminho de **escrita** do valor da cotação, o mais caro dos cinco); e dois alertas não têm rota de detalhe, então os CTAs levam à listagem sem seleção (D8), porque ancorar seria decidir o FUT-2. **11 tasks, uma por commit.** Três achados durante a execução, todos consertados no commit da própria task: `mockClear()` em `beforeEach` produzindo falha fantasma de rejeição não tratada (isolado empiricamente, 3/3, e resolvido adotando contagem relativa — a convenção que `useValidationPage.test.tsx` já usava); narrowing de TS perdido através do closure em `act(() => result.current.retry())`, que estava assim no código literal do plano e foi pego pelo `pnpm build`, não pelo `pnpm test`; e `react-refresh/only-export-components` sobre um helper puro, seguido o precedente do `AppToast.tsx`. **Review BAIXO risco pelo gate binário e medido** (`git diff main...HEAD -- backend/` e `-- generated.ts` vazios; o único toque em dinheiro é o `git mv` **byte-idêntico** de `uf.ts`, `R100` no diffstat), **e a segunda lente do Codex foi acionada mesmo assim, por decisão do João** — a divergência por alcance que a spec §9 deixara em aberto. **7 achados do Codex: 3 coincidiram com os meus, 2 novos e verificados, 2 descartados com razão registrada**, sem uma única divergência de julgamento entre as lentes. **O achado que só o Codex viu é o mais grave e foi verificado no backend antes de aceito:** `nenhumaSecaoLegivel` ignorava alertas liberados por `identity.user.view`; medido em `AdminDashboardAssembler.php:56-62,86,157`, essa permissão alimenta os alertas de documento de relator e **não liga KPI, pipeline nem agenda**, então um papel só com ela recebia tudo `null` e uma lista de alertas **cheia**, e a tela anunciava "nenhum módulo visível" escondendo alerta autorizado de peso de RN-09. O bloco A mandou `null` para a tela não mentir sobre o banco; aqui ela mentia na direção oposta. Ganhou teste com o vermelho visto antes do verde, e a spec foi **emendada onde o código passou a divergir dela** (§4 e §6), em vez de a divergência ficar para o `/auditar-docs` achar depois. **Gate de fechamento (2026-08-16), medido e não herdado:** `GET /api/dashboard/metricas` contra a API real devolve as 5 seções com dado do seed (6 KPIs, 7 pendências, 3 alertas, agenda de 4 janelas, funil de 6 estágios); **gate `null` provado ao vivo com papel-sonda** criado e removido por API (`sonda-q4`, só `identity.user.view` → todas as seções `null`, `users=0 roles=0` de resíduo); zero mutação (`turmas=6 quotes=9 budgets=8 certificates=5 enrollments=56 files=22`, idênticos ao snapshot da execução); catraca de cor provada nos dois sentidos (verde no HEAD, reprovando em `17:23` com `text-slate-400` injetado, sonda revertida); front `pnpm lint` exit 0, `pnpm build` verde, **38 arquivos / 205 testes**; Pint e `typescript:transform` **N/A por escopo medido**, não suposto. **A metade não reproduzível está declarada:** o payload "tudo `null` + alertas cheio" — o caso Q-4 exato — não tem gatilho vivo no seed (os 3 alertas são `turma_overdue`, e nenhum documento de relator está dentro do horizonte de 30 dias), então segue provado pelo 7º teste de `useDashboard`. **A suíte do backend reprovou 12 testes de sessão no gate, e a causa é externa ao bloco:** `TestCase.php:18` lê `FRONTEND_URL` cru e o `.env` virou lista separada por vírgula, então o `Referer` não bate com `sanctum.stateful` e o `StartSession` nunca roda — 12 failed / 672 passed com o `.env` como está, **684 passed / 5 skipped com `FRONTEND_URL` de origem única**. Nasceu a **P-45**; o fix pertence ao commit que fechar o multi-origin (o `config/cors.php` com `explode` está no working tree do João), não a um bloco de frontend. **Fecha a P-34** — a catraca `COR_HARDCODED` entra em `src/app/**` sem `ignores`, com os 3 sítios do shell convertidos para `--shell-ink`/`--shell-ink-muted` (não `--text-color`: a sidebar é navy fixa nos dois temas e a tinta do tema claro seria texto escuro sobre navy) —, **deixa o BD-11 só com a D-03**, e constata que **a trava de merge da D-15 caiu** (`DashboardWindows` está na `main`), sem agrupá-la. **Nasce a D-18** (o `description` de pendências e alertas é string fixa em espanhol nos quatro produtores; o front não pode traduzir porque em `turma_docs_incomplete` a string carrega a lista de documentos faltantes — fecha junto da D-07, pelo mesmo motivo). **O que NÃO ficou provado, sem maquiagem:** os passos visuais (3 locales × 2 temas, 5 sítios de UF) **não foram re-rodados neste gate**, e `f38585e` tocou 3 componentes de render (`DashboardItemRow`, `KpiRow`, `PipelineFunnel`) **depois** do commit da revisão visual `3273cbf` — a tela revisada não é exatamente a entregue; e o `/lotus-ui-review` demonstravelmente rodou (UI-01 a UI-05 com medidas em pixel) mas **não deixou artefato** em `docs/superpowers/audits/`. | Plano `plans/archive/2026-08-15-dashboard-frontend-central-controle.md` · spec `specs/archive/2026-08-15-dashboard-frontend-central-controle-design.md` · packet `context-packets/2026-08-15-dashboard-frontend-central-controle.md` · commits `1a56207`…`0afdb55` |
| 2026-08-17 | Identity · Meu Perfil — frontend (Sprint 6, bloco 2 de 2) | Entregue | **Bloco 2 de 2 da Sprint 6**, promovido explicitamente pelo João com esta árvore em `idle`. O gate do `/planejar-bloco` reprovou por motivo **novo** na décima segunda promoção: não era título de seção (o argumento trazia o slug), era **divergência de estado medida entre duas árvores** — `idle` aqui, `context_required` de OUTRO work item no main tree, 23 minutos à frente e numa branch que não chegara à `main`. Nenhum arquivo tocado antes da decisão dele. **Quinta exceção declarada à invariante de um `active_work_item`, e a primeira frontend × frontend:** a P-03 não dispara (o gatilho dela é *backend*), mas a colisão real era o shell `src/app/` — medida em quatro pontos antes de escrever, com `AppRouter.tsx` como colisão certa e textual, `features/` e `generated.ts` em zero por mecanismo. **Context Packet do Codex read-only** `ready`, 8 key facts (o teto), 802 palavras contra 1.200, três hashes de proveniência remedidos byte a byte; e o revisor **corrigiu a minha medição da frente paralela pela segunda vez** (ela avançou de `context_required` a `ready_for_planning` nos nove minutos entre as duas leituras). A suspeita de lacuna nas EAP foi levantada e **derrubada por medição**: das 9 páginas de Meu Perfil, as 3 de frontend (8.5.4/5/9) foram todas consumidas; as outras 6 são `Camada: Backend`. **Spec com 5 decisões:** duas colunas com corte por **mutabilidade** (cortar por seção deixava o Admin com uma coluna que só segura foto), `useResourceState` como arquivo novo em vez de generalizar `useLoadState` (aquele existe pela política *falhou vs. veio vazia* de uma LISTA, e recurso único não vem vazio), e `ProfileDocumentSlot` como **irmão** do slot administrativo, não reuso — são duas fontes de verdade para a mesma pergunta, e o front recalcula numa e não na outra. **11 tasks, uma por commit.** A Task 1 pagou um bug de tipo que o plano não previa (`Partial<UseQueryResult<…>>['error']` colapsa e perde o `null`), isolado em repro mínimo fora do projeto. **DoD end-to-end 6/6 no Admin e 6/6 no Redator**, e o passo 4 do plano estava errado: derrubar o nginx não alcança o `AppErrorState` (o `GET /api/me` morre junto e o shell redireciona para `/login`) — o ramo se prova com falha **isolada** de `/api/profile`, e `networkMode: 'online'` **pausa** a query em offline de browser em vez de falhar. Nessa medição nasceu um defeito **de raiz compartilhada**: sob 5xx de corpo vazio o aviso não aparecia, porque o `??` do interceptor só pega `null`/`undefined` e `''` virava a rejeição — alcança **todas** as listas pelo `useLoadState`, não só o perfil. **UI review do João:** 7 achados, 2 classe C, 8 commits de correção, e o alcance em `shared/ui` declarado sítio a sítio. **A rule que o bloco tinha de emendar foi emendada** (`frontend-fsliced.md` afirmava que status documental se calcula no front — verdade só no DTO administrativo), e tocar o arquivo disparou o gatilho **literal** da **P-38**, encerrada no mesmo passo com o corte **medido pelo runner**: 13 testes de componente, 9 montam wrapper PrimeReact. **Review de sprint em duas rodadas, alto risco, duas lentes.** Rodada 1: 6 achados, **zero violação de lei**, e dois achados do Codex **derrubados por medição** (o `void refetch()` é o idiom vigente do irmão `useLoadState`; o `created_at` no idioma do navegador já era a **D-18** registrada). O Q-4 mediu a premissa contra o TanStack **instalado** e isso **derrubou a alternativa sugerida no próprio achado**: `mutationObserver.mutate` faz `removeObserver` na anterior, então um `Set` por `onMutate`/`onSettled` deixaria o primeiro slot travado para sempre — `mutateAsync` por chamada resolve. Rodada 2: **duas das minhas próprias correções saíram com defeito**, as duas achadas pela segunda lente e verificadas no código antes de aceitas — o Q-6 escreveu a regra de domínio **ao contrário** (a D5 do bloco 1 diz que `valid_until` **segue aceito**; quem barra o REUF é o `Rule::in(selfServiceValues())`, por TIPO) e ainda **copiou** para um segundo arquivo um comentário já errado, que é a lição 13 na regra que decide habilitação de turma; e o Q-2 **não tinha guarda** — o caso do fallback nunca olhava o `detail` e passaria verde numa reversão. O Q-9 fechou a lacuna do `AppPassword`, que acumulara **quatro** achados de largura sem nenhum teste. As duas guardas novas foram **vistas reprovar** contra o código antigo. **Fechamento provado no navegador, não deduzido:** 26/26 verificações por CDP contra a API real, nos dois papéis e em es-CL, cada uma das seis correções exercitada no ramo certo — os 5 campos `disabled` com o PUT **pausado no interceptor**, o `detail` do fallback em es-CL, a mutation irmã zerada com o DELETE em voo, os dois uploads concorrentes com o **segundo terminando primeiro** e o primeiro **não** reabilitando, e o multipart sem `valid_until` com 201 nos dois. Três falhas intermediárias foram do harness, não do produto, e a última era a mesma causa nas três: header CORS com `*`, que em modo credenciado é lido **literalmente** e derrubava o preflight, jogando tudo no ramo de rede — que já era traduzido antes do Q-2, e por isso a prova passava pelo motivo errado. Gate: lint 0, build 0, suíte **42 arquivos / 213 testes**; `generated.ts` intocado nos 34 commits, **zero** arquivo PHP (Pint não se aplica), zero órfão, nenhum import de `primereact` ou cross-feature em `identity/**`, 581 chaves i18n com 0 divergência nos três locales. **Suíte de backend 684 passed / 5 skipped**, medida — as 12 falhas de login do primeiro run são do `.env` local (`FRONTEND_URL` virou lista com vírgula e o `TestCase` a injeta inteira num único `Referer`), preexistentes na `main` e alheias a um bloco que não tocou backend. **Fica aberto:** a `main` avançou 21 commits (PR #55 mergeou a frente paralela), então a branch está atrás e **9 arquivos foram tocados pelas duas frentes** — `AppRouter.tsx`, os 3 locales e 5 docs de `superpowers/`, inclusive as pendências, onde a `main` já encerrou a **P-34**. O merge é mecânico, mas é trabalho a fazer. | `plans/archive/2026-08-15-meu-perfil-frontend.md` · `specs/archive/2026-08-15-meu-perfil-frontend-design.md` · `context-packets/2026-08-15-meu-perfil-frontend.md` · 34 commits (`5ff2e7e`..`e103178`) · UI review em `.artifacts/ui-review/2026-08-16-1213-perfil/` |
| 2026-08-17 | Dashboard · revisão de UI e passe de correção (fora de bloco) | Entregue | **Não é bloco promovido** — o João invocou `/lotus-ui-review dashboard` (passo dele, `disable-model-invocation: true`) e, lido o relatório, mandou cruzar os achados com a skill `frontend-design` e corrigir. Estado ficou em `idle` do começo ao fim, sem spec e sem plano, por decisão dele. **A revisão não era redundante, e o fechamento do B1 já dizia por quê:** `f38585e` tocou `DashboardItemRow`, `KpiRow` e `PipelineFunnel` **depois** do commit da revisão visual `3273cbf` sem re-rodar os passos visuais do DoD. Medido agora, as cinco correções de 2026-08-16 se sustentam. **6 achados B, 0 C, todos corrigidos, com número antes e depois:** agenda com uma janela deixava 351,5px vazios enquanto o nome do curso truncava em 142px para 255px de texto (passa a uma coluna de 703px, caixa de 493px, sem truncar); card de KPI media 212px com 75–95px de área morta e empurrava as duas listas para y=622 de 688px úteis em 1024 e para 1274px de um scroll de 3210 em 390 (passa a 92px, morto uniforme de 15px, listas em 406px e 698px de 2500) — o que contradizia a tese escrita no docblock da própria página; os seis números ficavam em duas linhas de base, 81px e 101px, conforme a locale quebrasse o rótulo (passam a 47px nos seis); o número em tom de aviso media **2,86:1** no tema claro, abaixo até do 3,0:1 de texto grande, e os rótulos secundários sobre card tingido mediam 4,28–4,52:1 (passam a 10,35 e 4,76:1); a árvore de cabeçalhos saltava `h1`→`h3` (ganha duas faixas `h2`); e o primeiro focável dentro de `main` era o de índice 11 (passa a 1, com Enter pondo o foco em `main#contenido`). **A tese de desenho que amarra o passe é uma só: cor de sinal vive em traço e marca, texto fica em contraste cheio** — o tom sai do número, onde precisava de 4,5:1 e entregava 2,86, e vira trilho de 3px, onde 3:1 basta e entrega 5,2–5,8. **A causa da UI-04 estava no token, não no Dashboard:** `color-mix(hue 70%, --text-color)` só fechava contraste no escuro, e elevar a participação do corpo fecharia às custas do matiz (40% de amarelo sobre slate resolve para oliva). Entram cinco tokens `--tone-*-ink` que trocam o **degrau da rampa** por tema, que é a mecânica que o `--brand-ink` já usava — e o que iguala os quatro no claro é a razão medida, não o índice do degrau (info-700, success-800, warning-800, danger-700). Guarda: `tests/tone-ink.test.ts`, 18 asserções, com o controle que reprova a fórmula antiga. **Três achados apareceram ao corrigir e não estavam no relatório:** a faixa do `AppCardHeader` media 80px para 24px de texto em **todo card da aplicação** (o `h3` sem `m-0`); a barra do funil reprovava o 3:1 de elemento gráfico contra o próprio trilho nos dois temas (2,33 e 2,77:1 — precisou trocar barra E trilho, porque o 3:1 se cobra contra a cor adjacente); e `QuoteRow`/`DocumentTypeCard` pintavam texto com `var(--red-500)` cru a 3,52:1, que é o caso da **P-36**. **Alcance fora da superfície revisada, declarado:** as quatro tintas mudam de valor em ~20 sítios, o `AppCardHeader` encurta nos 8 consumidores, `variant="stat"` também é do `BudgetStatCard` (tela de orçamento conferida no navegador) e o link de salto é do shell. **Nasce a P-46** — a causa raiz da UI-02 que o passe NÃO fechou: sem Preflight, toda tag de bloco herda margem do agente do usuário, e o reset escopado que resolveria a classe inteira mexe no espaçamento de todas as telas de uma vez. **O que continua não provado, sem maquiagem:** os estados `loading`, `error`/`staleError`, `empty` das três seções, funil todo-zero, `unauthorized`, `unsupported` e os alertas `certificate_*`/`redator_document_*` — alcançá-los exige mutação de dado ou interceptação de rota, que a skill proíbe. Gate: `pnpm lint` exit 0, `pnpm build` verde, **39 arquivos / 223 testes**; zero mutação; `backend/config/cors.php` (WIP do João) fora de todo `git add`. | `audits/2026-08-17-lotus-ui-review-dashboard.md` · branch `fix/dashboard-revisao-visual-2026-08-17` de `main@18bf487` · commits `abff4be`…`2fc0bd8` · sem spec, plano ou packet (não é bloco) |
