# Fase 5: escrita habilitada somente para execução delegada via lotus-execute-block,
# dentro dos paths autorizados pelo plano ativo.

approval_policy = "never"
sandbox_mode = "workspace-write"
web_search = "indexed"